[package]
name = "himmelcad-document"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "HimmelCAD canonical document, project, and durable publication foundation."

[dependencies]
himmelcad-model = { path = "../himmelcad-model" }
himmelcad-process = { path = "../himmelcad-process" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
glam.workspace = true
sha2.workspace = true
hex.workspace = true
anyhow.workspace = true
zip.workspace = true
tracing.workspace = true

# File locking for the native project store; the WASM viewer never opens a project store.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
fs2 = "0.4.3"

[lints]
workspace = true
