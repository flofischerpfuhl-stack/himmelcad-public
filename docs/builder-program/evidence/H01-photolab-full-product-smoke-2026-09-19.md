# H01 — PhotoLab full product smoke on the release sidecar at HEAD (2026-09-19)

Run by the architect as a capped user unit (MemoryMax 18 G, CPUQuota 600 %, Nice 10, no core dumps), no Codex involved: `.build/photolab-runtime/run-h01.sh` → `cargo build --release -p himmelcad-sidecar --bins` (11 m 46 s) → `node scripts/photolab-e2e.mjs --source …/260706_Sulzberg_SUMA_UrGel/01_Photos --max-images 24 --smoke --profile fast --products depth,dense,dem,ortho,mesh,splat --dem-surface dsm --sidecar target/photolab/release/himmelcad-sidecar`.

Result: exit 0; wall 23 m 24 s; peak RSS 7.82 GB; all 12 stages `completed`; products sparse, dense, depth ×2, mesh (surface3d, resource storage), dem, orthomosaic, gaussianSplat — every one `provenanceStatus: complete`; jobs alignPhotos, buildDepthMaps, buildDensePointCloud, buildDem, buildMesh, buildOrthomosaic, buildGaussianSplat all `completed` (memory records note `unboundedStage` observations on depth/dense/mesh/splat stages — informational). Six product-import packages published with ready records (sparse, dense, mesh, dem, orthomosaic, gaussianSplat) under `.build/photolab-e2e/h01-full-smoke/photolab-e2e.hcad/.photolab/product-import-packages/` — the first real-data run of the PL-I1 publishers. Logs: `.build/logs/h01-build.log`, `.build/logs/h01-smoke.log`; result: `.build/photolab-e2e/h01-full-smoke/result.json`; HEAD in `.build/photolab-evidence/h01/head.txt`.
