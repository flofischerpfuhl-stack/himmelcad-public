# WIN-20 — work area on D: and Vega 8 rendering diagnosis (DESKTOP-BNB2PBA), 2026-09-19

Remote Codex run 18:01–19:35; full report, per-launch JSON diagnostics and CDP screenshots on the host under `.build\win-20\` (now on D: via junction).

**Part A — PASS.** `target`, `.build` and the Agisoft datasets of both clones live under `D:\himmelcad-work\` behind directory junctions; `git status` unchanged; C: gained 10.7 GiB immediately (40 GiB free at hand-off), D: 578 GiB free.

**Part B — reproducible defect boundary, no switch-only fix.** Host: Vega 8 (`VEN_1002&DEV_15D8`), AMD driver `31.0.12027.9001` dated **2023-03-30**, Electron 43.1.0 / Chromium 150. In every launch variant the out-of-process ANGLE GPU process exits at least once with code 34 (intermittently `0xC0000005`); Chromium then demotes compositing to software (`gpu_compositing = disabled_software`, WebGL/WebGPU `enabled_readback`). Builder keeps running: the viewer selects WebGPU on the physical AMD adapter and a small dataset orbits at 16.7 ms p50 / 17.6 ms p95 — usable, but presentation goes through readback. Variants tested: default, `--disable-features=Vulkan`, `--use-angle=d3d11`, `d3d11on12`, `gl` (no viable canvas), `--disable-gpu-sandbox`, `--disable-direct-composition`, occlusion off, WebGPU off (switch ineffective). The native fault inside Chromium/ANGLE cannot be named from exit codes alone.

Findings for the product: (1) the renderer chip says "Hardware rendering" while Chromium reports software compositing/readback — the status must use the effective feature status plus the viewer device (`apps/builder/renderer/src/App.tsx:342-359`); (2) V-08 has no pre-surface backend override (added for DEV in T-01; a diagnostic setting is still missing); (3) candidates to remove the crash: a current AMD driver on the host (the installed one is 2.5 years old — owner action), and/or an Electron revision that passes this matrix. Acceptance for either: zero GPU-process exits across repeated cold launches, `gpu_compositing` hardware, WebGL2 not readback, dataset orbit sample.
