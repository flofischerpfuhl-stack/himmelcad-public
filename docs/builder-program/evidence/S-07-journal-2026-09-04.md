# S-07 — journal group commit and durability indicator

Date: 2026-09-04

## Outcome

Builder ordinary canonical transactions now stage without an fsync on the request path and cross a grouped durability boundary after a tunable interval (`HCAD_JOURNAL_FLUSH_INTERVAL_MS`, default 50 ms). The sidecar reports visible and durable generations separately. A failed flush leaves the tail queued, rejects later canonical edits, and clears only after a successful retry.

The renderer shows only the acknowledged states `Stored`, `Storing…`, or `Not stored — <reason>`. The failure toast names that changes remain queued and exposes `Retry`. Ctrl+S and the existing ribbon Save position dispatch `project.flush`; that operation first retries/drains pending work, appends a manual `hcad.snapshot-marker@1` marker, and returns only after the marker is durable. Successful open adds and durably flushes one automatic `Session start` marker before returning the document snapshot.

## Changed surfaces

- `crates/himmelcad-sidecar/src/canonical_project_store.rs` — queued ordinary transactions, group fsync, truthful durability status, failure lockout/retry, recovered-tail count, and store/crash/performance tests.
- `crates/himmelcad-sidecar/src/canonical_app_runtime.rs` — ordinary document dispatch uses the queued path; open/session marker, manual snapshot create/list, flush/status, and close-time drain.
- `crates/himmelcad-sidecar/src/main.rs` — Builder canonical RPC routing only. Touched functions: `handle`, `handle_canonical_app_rpc`, and new `schedule_canonical_group_flush`. PhotoLab `handle_project_rpc`, jobs, save, archive, and journal handlers were not edited.
- `apps/builder/renderer/src/project.ts`, `durabilityPolling.ts`, `App.tsx`, and `ribbon.ts` — typed acknowledgement adapter, 25 ms non-overlapping poll, status/toast/retry UI, recovery report, Save/Ctrl+S routing.
- `packages/@himmelcad/ui/src/DurabilityIndicator.tsx`, its CSS module and export; gallery and semantic/a11y fixtures — 12 px text, 6 px stored/error dot, shared 12 px Spinner for pending state, and the gallery section `Durability indicator` with all three states.
- `schemas/automation/himmelcad-automation-v1.schema.json` and generated command/Python SDK outputs — added `project.flush`; the admitted `snapshot.create` and `snapshot.list` P11 rows remain additive and now have sidecar implementations.

`packages/@himmelcad/data/src/index.ts` was not changed. The S-01 snapshot/local-history admissions remain additive and local-history streams remain independent of document durability; S-07 does not merge their Ctrl+Z or corruption scope.

## Concurrency and recovery

- Canonical mutation acceptance remains serialized by the existing canonical runtime mutex. Multiple accepted ordinary commands inside the interval share one flush; redundant timer wakes are harmless.
- Synchronous import/publication paths drain pending ordinary commands first. A prior durability failure rejects them rather than allowing an unjournaled or falsely acknowledged state.
- The ready marker is synchronized before journal publication. Reopen completes a staged tail, counts it, and the renderer reports the recovery. A malformed/truncated journal still fails open; it is never skipped.
- Normal close flushes before releasing the project lock. A failed close-time flush keeps the project open and returns `closed: false`.
- Ordinary journal flush is bounded and intentionally not a cancellable long job. Heavy import/archive work stays on its existing background-job lifecycle.

## Gates and tests

### G-FP-P5

- Simulated 2 s drag (120 preview frames): **0** journal writes during preview; **1** queued append at gesture end. `simulated_two_second_drag_journals_only_at_gesture_end` passed.
- Gesture end → durable acknowledgement, 20 samples on this machine: **65.889112 ms p95** including the configured 50 ms group window; budget ≤ 100 ms. Passed.
- Durability acknowledgement → renderer indicator callback, 20 samples on this machine: **24.199 ms p95** through the production 25 ms polling helper; budget ≤ 50 ms. Passed. This measures delivery to the React state callback, not display scan-out.
- Injected flush failure: explicit failed state, later edit rejection, repaired-path Retry, durable reopen. Passed.
- Crash-tail simulation: store dropped between queue and group fsync, reopened tail replayed and `recoveredTailCount == 1`. Passed.
- Snapshot marker round-trip: automatic session marker and manual UI marker retain the exact `hcad.snapshot-marker@1` payload and origins. Passed.

### Required commands

- `CARGO_TARGET_DIR=target/builder cargo test -p himmelcad-core` — **passed**: 217 unit, 1 integration, 0 doc-test failures.
- `pnpm --filter @himmelcad/app test` — **passed**: 39/39.
- `pnpm --filter @himmelcad/builder typecheck` — **passed**.
- `pnpm --filter @himmelcad/photolab typecheck` — **passed**, including PhotoLab English UI check.

### Additional proportional checks

- `CARGO_TARGET_DIR=target/builder cargo test -p himmelcad-sidecar canonical_project_store::tests:: --lib` — **passed**: 17/17.
- `CARGO_TARGET_DIR=target/builder cargo test -p himmelcad-sidecar canonical_app_runtime::tests:: --lib` — **passed**: 7/7.
- `pnpm --filter @himmelcad/builder test` — **passed**: 7/7; measured indicator callback p95 above.
- `pnpm --filter @himmelcad/ui test` — **passed**: 32/32, including axe fixtures.
- `python3 -m unittest discover -s sdk/python/tests -v` — **passed**: 12/12; generated SDK current.
- `CARGO_TARGET_DIR=target/builder cargo check -p himmelcad-sidecar` — **passed** with one pre-existing dead-code warning for `ProjectRuntime::product_export_source`.
- `git diff --check` on the S-07 surfaces — **passed**.

## Not verified / follow-up

- No manual screenshot comparison was run in both themes; the deterministic gallery and axe fixtures were verified.
- The measurement uses the local test filesystem and renderer callback scheduling, not an Electron compositor frame timestamp or a power-cut rig.
- The existing File/Project program still needs its full Save split-menu and Builder Save As archive-copy flow. S-07 preserves the Save button position and establishes the flush/snapshot substrate; it does not add a second, partial archive implementation beside `project_archive.rs` or bypass FP-D20's shared job lifecycle.
- Snapshot rename/restore/delete UI and the FP-D21 local-history actions remain later file/project consumers. Their storage namespaces were not changed by S-07.

## Architect acceptance (G17, 2026-09-05)

Reviewed `gallery/shots/dark/durability-indicator.png`: Stored (success dot), Storing… (12 px spinner), Not stored — reason + Retry link, per the S-07 brief. Accepted; the Save split-menu/FP-D20 Save As archive job is carried to P-01.
