Domain: **Agent (embedded AI assistant)**. Output file: docs/builder-program/specs/agent/agent.md (~450-700 lines). Also read docs/adr/0024-automation-sdk-and-agent-trust-boundary.md. X3 (agent parity) is this domain's foundation.

Ground thoroughly in the existing implementation — it is substantial: packages/@himmelcad/agent/ (drivers.ts harness discovery, transport.ts, normalize.ts, timeline virtualization, AgentChatPanel.tsx, ManagedAgentChat.tsx, ManagedAutomationApproval.tsx approval gate, providerCredentials.ts, systemPrompt.ts — 5 lines pointing at /workspace/SDK.md and /workspace/SKILLS.md), packages/@himmelcad/automation-host/electron.cjs (SKILLS.md is generated as four hard-coded bullet lines — the placeholder your spec replaces; check the host's method allowlist proving io/registration are blocked at the boundary), sdk/python/ generated client, runtime/python/himmelcad_host.py, crates/himmelcad-sidecar/src/automation_runtime.rs (paginated queries, bulk leases, confirmation grants; io.*/registration.* not exposed).

Scope: (1) the SKILL SYSTEM replacing the placeholder — authoring (markdown skill files with frontmatter; built-in vs project scopes), discovery, on-demand loading so the agent pulls information instead of context spam (owner requirement: docs structured so the agent fetches what it needs, never preloaded); (2) the documentation surface: generated command/query reference from the automation schema, per-domain capability docs derived from the builder-program specs, help/search discoverability commands in the SDK; (3) exposing io.* (probe/import/export) and registration.* to automation under the ADR 0024 trust boundary (which operations need confirmation grants — import commits? exports to disk?) — the import-formats spec references the same grant model, cite ADR 0024 as the shared source; (4) chat UX completion: persistent workspace island (ui-platform UIP-D14 Escape exemption — cite), session persistence/resume across restarts, entity references in chat (click entity -> mention), agent-initiated selection/view changes visible to the user (X3 both ways: journal attribution, console lines); (5) multi-harness settings (credential UI exists). Boundary: the automation protocol schema is shared infrastructure — extend via decision records, don't redesign.

Full workflow narratives required: "user asks the agent to restore the last viewing box" (P1's motivating case — walk the whole path: skill/doc lookup -> query -> command -> approval if needed -> visible result); skill authoring; io exposure with grants. Verification: an agent-runnable end-to-end test against a scripted harness.
