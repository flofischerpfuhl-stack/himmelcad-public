WIN-23b — re-verify the ADR 0032 split on Windows: two product sidecars build, list their routes, and package. Host DESKTOP-BNB2PBA, clone C:\Users\flori\source\HimmelCAD (its `target` and `.build` are junctions to D:\himmelcad-work). Rules: never reset/stash/delete tracked or owner files; owner's website/ untouched; no commit/push; logs under .build\win-23b\; hard stop 2.5 hours; no GUI launches (the PC may be locked); keep C: free space above 10 GB.

Context: on `main` (commit 8e77b9a or later; WIN-23 failed on a missing `tracing` dependency in `himmelcad-document`, fixed since, and ADR 0032 is complete incl. the hardware-profile step) the single `himmelcad-sidecar` was split into `crates/himmelcad-builder-sidecar` (binary `himmelcad-builder-sidecar`, Builder + shared routes) and `crates/himmelcad-photolab-sidecar` (binary `himmelcad-photolab-sidecar`, PhotoLab + shared routes; also the PhotoLab-only `himmelcad-portable-mvs`), over the shared library `crates/himmelcad-sidecar`. Packaged apps keep the installed name `himmelcad-sidecar.exe`. Evidence of the Linux side: `docs\builder-program\evidence\SPLIT-S6B-2026-09-24.md` (section "Windows follow-up").

Steps
1. `git pull --ff-only origin main`; record the commit. `pnpm install --frozen-lockfile` if the lockfile changed.
2. MSVC build: `cargo build --release -p himmelcad-builder-sidecar -p himmelcad-photolab-sidecar --bins -j 4`. Record time, warnings count, and the three .exe paths/sizes (builder sidecar, photolab sidecar, portable MVS).
3. Routes: run each exe with `--list-routes`, count entries and compare with `crates\himmelcad-sidecar\tests\golden\route-inventory.json` filtered by product (Builder = shared + builder = 81; PhotoLab = shared + photolab = 115). Any difference is a failure.
4. Tests: `cargo test -p himmelcad-sidecar -p himmelcad-builder-sidecar -p himmelcad-photolab-sidecar -j 4 -- --test-threads=2` (includes the frozen protocol corpus replay; report every failure verbatim, and whether it also fails on the previous commit 5935a8b if you have time — use a separate `git worktree` under D:\himmelcad-work, never stash).
5. Packaging (no install, no GUI): run the repo's Windows packaging scripts for both apps as far as they go without signing (`pnpm --filter @himmelcad/builder build:win` and the PhotoLab equivalent in `apps\photolab\package.json`, or `pnpm photolab:audit:release:win` if that is the documented gate). Then inspect the unpacked output (`dist\win-unpacked\resources\` or equivalent): each app must contain `himmelcad-sidecar.exe` that is its OWN product binary (compare size/hash with step 2), PhotoLab additionally `himmelcad-portable-mvs.exe` and `libunwind.dll` if the config lists it, Builder must NOT contain portable MVS.
6. Headless start: start each packaged `resources\himmelcad-sidecar.exe` directly from a console, send one JSON-RPC line `{"jsonrpc":"2.0","id":1,"method":"ping","params":{}}` on stdin, record the response line, then close stdin and confirm the process exits within 10 s.

Report `.build\win-23b\WIN-23b-report.md`: commit, build times, exe sizes, route counts vs golden, test results, packaging results per app with resource listings, ping/exit results, anything that failed with exact output, C:/D: free space before/after. Print the report in full at the end.
