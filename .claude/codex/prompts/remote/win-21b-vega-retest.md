WIN-21b — after the AMD driver update and reboot: re-run the WIN-20 Part B acceptance on this PC. Same rules as WIN-20 (logs under .build\win-21b\; no commit/push; hard stop 90 minutes). Preflight: confirm the new driver version (`Get-CimInstance Win32_VideoController`) against D:\himmelcad-work\build\win-21\WIN-21-partA.md, and that an interactive desktop session exists (`query user`); if nobody is logged in, GUI launches cannot present — report "NO INTERACTIVE SESSION" and stop. Then `git pull --ff-only origin main`, rebuild what is stale, and repeat ONLY these launches of Builder (dev), five cold launches each, ≤ 3 min per launch: default flags; `--use-angle=d3d11`. Acceptance per WIN-20: zero GPU-process exits across the cold launches, `gpu_compositing` not `disabled_software`, an independent WebGL2 context not `enabled_readback`, physical AMD renderer, and an orbit frame-time sample on the mid-size dataset used in WIN-20; also record what Builder's renderer chip says versus the effective Chromium status. Report .build\win-21b\WIN-21b-report.md with the before/after table against WIN-20; print it in full at the end.
