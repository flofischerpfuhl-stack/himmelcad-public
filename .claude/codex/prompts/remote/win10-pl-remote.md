WIN-10-PL-REMOTE — the 16 GB gate run, third attempt (PhotoLab lane). Host DESKTOP-BNB2PBA. You start in C:\Users\flori\source. Rules as WIN-09-PL (never touch C:\Users\flori\source\HimmelCAD or website/; hard stop 3 hours; one session; cargo -j 3, CARGO_TARGET_DIR=target\win in your clone; NEVER copy a repository tree). Read `.build\win-09\WIN-09-report.md` in your clone first: the typed preflight is already proven (do NOT repeat Run 1); Run 2 was stopped at LightGlue 14/24 only because free space on C: fell to 4.85 GiB against a 6 GiB gate that was too strict for this PC. New disk rule: stop only if C: free space falls below 2.0 GiB; before starting, delete `.build\win-09\gate` and `.build\win-07\gate` in your clone (old attempts, ~4 GB) and record C: free space.

Setup: `cd C:\Users\flori\source\HimmelCAD-photolab && git fetch origin && git checkout main && git pull --ff-only origin main` (record HEAD). If `target\win\release\himmelcad-sidecar.exe` exists and was built from the current HEAD (compare `git rev-parse HEAD` with the HEAD recorded in WIN-09-report.md; if HEAD moved, rebuild: `cargo build --release -p himmelcad-sidecar --bins`, ~20 min). PotreeConverter at `vendor\potreeconverter\win32-x64\PotreeConverter.exe` (present).

The run: the exact WIN-09 Run 2 command with output `.build\win-10\gate` (`node scripts\photolab-e2e.mjs --source "<absolute photos path in the main clone>" --output .build\win-10\gate --max-images 24 --profile fast --target-epsg 31468 --target-vertical-epsg 7837 --sidecar target\win\release\himmelcad-sidecar.exe`, `HIMMELCAD_PHOTOLAB_FORCE_EXTRACTION_TILES=2x1`, `HIMMELCAD_COLMAP_EXECUTABLE`/`HIMMELCAD_PROJ_ROOT` as before), memory sampler to `.build\win-10\memory.csv` and a free-space sampler every 30 s to `.build\win-10\disk.csv`. Expected: 24/24 aligned AND the sparse product `complete/available` (potree@2) in result.json; job record with `toolchain` and the memory record. Stop only on the 2 GiB disk rule, a Windows kill, or a stage stalled > 100 min.

Evidence: `.build\win-10\WIN-10-report.md` (HEAD, free space before/after and its minimum, duration per stage, peak working set, products with provenance, the job memory record and `toolchain` verbatim) copied to C:\Users\flori\source\codex-last-win10-pl.md. Commit nothing (bundle only if a Windows-only fix was unavoidable: branch photolab/win10-pl, `git bundle create C:\Users\flori\source\win10-pl.bundle main..HEAD`). Final summary: gate result with products, disk minimum, memory peak, duration, and anything you had to decide yourself.
