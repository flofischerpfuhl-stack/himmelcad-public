WIN-05 — PhotoLab on Windows: the smallest complete workflow (import → align → sparse product) with the staged workers. Host DESKTOP-BNB2PBA, main clone C:\Users\flori\source\HimmelCAD (rules as before: never reset/stash/delete; owner's website/ untouched; logs under .build\win-05\; hard stop 2.5 hours; no commit/push). Read .build\win-03\WIN-03-report.md first (dev sidecar path fix, PROJ root env, import smoke result, Vega 8 findings) and .build\win-02\WIN-02-report.md §"Inputs missing".

Inputs now on this PC: Sulzberg photos under `photolab\Agisoft Exampleprojects\260706_Sulzberg_SUMA_UrGel\01_Photos\`; staged runtime `.build\photolab-runtime\win32-x64\workers\{colmap,geo,dedode}` (COLMAP cross-built 2026-09-09 as the audited 12-file closure — use exactly that `colmap.exe`, never a PATH COLMAP). Steps: (1) `git fetch origin && git checkout main && git pull --ff-only origin main`; record HEAD (the WIN-03 sidecar-path fix may still be on branch `win/photolab-dev-sidecar-path` — if so, check that branch out for this run and say so). (2) Verify the workers: run `colmap.exe -h` from the staged path (must start; record the version line and any missing-DLL error verbatim), `projinfo.exe`/`cct.exe` from workers\geo. (3) Start the sidecar with the env from WIN-03 (`HIMMELCAD_PROJ_ROOT`, `HIMMELCAD_SIDECAR_BIN` if used) plus whatever env the sidecar uses to locate the COLMAP worker in development (search `crates/himmelcad-sidecar/src` for the worker discovery: `workers\colmap`, `HIMMELCAD_COLMAP_ROOT` or similar — use the documented env, do not patch code); prove the startup probes and that COLMAP is discovered (sidecar log line). (4) Run the Linux-documented smoke through `scripts\photolab-e2e.mjs` on Windows: `--source <photos> --max-images 24 --profile fast --target-epsg 31468 --target-vertical-epsg 7837 --output .build\photolab-e2e\win-05` up to the sparse product (stop before dense/depth if the profile allows; CPU-only, 4 cores — expect 30–60 min); record wall time per stage and peak private bytes of colmap.exe (PowerShell `Get-Process` sampling every 10 s into a log); if the e2e cannot run on Windows (path separators, process groups, shell quoting), record the exact failing line and fix ONLY the e2e script/tooling in a minimal Windows-safe way (no product code), noting every changed line. (5) Open the produced project in PhotoLab (dev, with the Vega 8 launch flag WIN-03 found working, if any) and screenshot the Jobs tab and the sparse product row; if the viewer still cannot create a surface, screenshot the error and continue. (6) Report .build\win-05\WIN-05-report.md: step table, stage timings, peak memory, exact defects with file:line, what is still missing for the full Windows release gate; print it in full at the end. Commit nothing; if you changed the e2e script, `git diff > .build\win-05\e2e-windows.patch` and mention it.
