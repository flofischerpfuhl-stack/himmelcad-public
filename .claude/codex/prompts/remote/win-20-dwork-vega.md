WIN-20 — make the Windows PC a usable test host: work area on D:, then Builder on the Vega 8 with hardware rendering. Host DESKTOP-BNB2PBA, main clone C:\Users\flori\source\HimmelCAD (rules: never reset/stash/delete tracked or owner files; owner's website/ untouched; logs under .build\win-20\; no commit/push; hard stop 2.5 hours). Facts: C: has only ~6 GB free, D: (HDD) has ~640 GB free.

Part A — work area on D: (do first): create D:\himmelcad-work\{target,build,datasets}. Move the clone's `target` directory content to D:\himmelcad-work\target and replace `C:\…\HimmelCAD\target` by a directory junction (`cmd /c mklink /J`), same for `.build` → D:\himmelcad-work\build (move existing content, keep every file), and `photolab\Agisoft Exampleprojects` → D:\himmelcad-work\datasets\agisoft (junction). Do the same for the second clone C:\Users\flori\source\HimmelCAD-photolab if it has a target/.build. Verify with `dir` that junctions resolve, `git status` is unchanged, and report C:/D: free space before/after. Persist `CARGO_TARGET_DIR` is NOT needed (the junction covers it).

Part B — Vega 8 is a product defect, not a host limitation (owner: HimmelCAD must run well on weak hardware; slower is fine, crashing or software rendering is not). Read .build\win-03\WIN-03-report.md (GPU process `exit_code=34` with default flags; `--use-angle=d3d11` one crash then software; swiftshader works but slow) and `docs/builder-program/evidence/V-08-gpu-fallback-2026-09-10.md` after `git pull --ff-only origin main`. Diagnose with evidence, no guessing: GPU driver version/date (`Get-CimInstance Win32_VideoController`), Electron/Chromium version, `--enable-logging --v=1` GPU-process log around the crash, `app.getGPUFeatureStatus()`/`getGPUInfo('complete')`, and a matrix of launches of Builder (dev) each ≤ 3 min: default; `--disable-features=Vulkan`; `--use-angle=d3d11`; `--use-angle=d3d11on12`; `--use-angle=gl`; `--disable-gpu-sandbox`; `--disable-direct-composition`; `--disable-features=CalculateNativeWinOcclusion`; WebGPU disabled at the app level (force the viewer's WebGL2 backend if V-08 exposes a setting/env) — for each: does the GPU process survive, which backend does the viewer select, is `gpu_compositing`/`webgl2` hardware, and the orbit frame time on a mid-size dataset present in the repo/.build (or the Sulzberg sparse/dense product from .build\win-16 if present; do not download anything). Identify the single root cause if the evidence allows (e.g. WebGPU/Dawn D3D12 adapter creation on this driver, the GPU sandbox, a specific feature) and the minimal product-side change that makes Builder start in HARDWARE rendering on this machine without flags from the user (e.g. V-08 ladder must try WebGL2-hardware before WebGPU on adapters matching a crash signature, or app.commandLine switches set in main.ts for win32 + this vendor/driver class). Do NOT implement product code in this run; write the exact proposed change (file, lines, switches) and the evidence. Report .build\win-20\WIN-20-report.md (Part A table; Part B matrix table, root cause, proposal, screenshots via CDP); print it in full at the end.
