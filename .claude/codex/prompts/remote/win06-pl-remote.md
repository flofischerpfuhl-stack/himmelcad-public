WIN-06-PL-REMOTE — disk preflight through the OS API, executed on the Windows PC (second clone). Host DESKTOP-BNB2PBA. You start in C:\Users\flori\source. Rules: never touch C:\Users\flori\source\HimmelCAD (another lane's clone) or website/; hard stop 90 minutes; keep every cargo command at `-j 2` and CARGO_TARGET_DIR=target\win inside your clone; NEVER copy a repository tree. This clone cannot push; you commit on a branch and produce a bundle as told below.

Setup: `cd C:\Users\flori\source\HimmelCAD-photolab && git fetch origin && git checkout main && git pull --ff-only origin main` (record HEAD), then `git checkout -b photolab/win06-pl`. Logs under .build\win06\ inside your clone.

PACKAGE: WP-WIN-06-PL — the disk preflight must measure free space through the OS API, never by parsing tool output (blocks WIN-05, the first PhotoLab alignment on Windows). Read only: the code named below.

Defect (Builder WIN-05, 2026-09-10 00:34, DESKTOP-BNB2PBA): `photolab.jobs.startAlignment` is refused with `-32042 / insufficientDisk` because `system_available_bytes` in crates/himmelcad-sidecar/src/job_runtime.rs (~line 2470–2540) shells out — `df` on unix (parsing the last numeric line as kilobytes) and `fsutil volume diskfree` on Windows (taking the largest digit run of a line) — and the Windows output is localized ("Bytes frei", thousands separators), yielding "784 bytes free" on a volume with ~25 GB free. Parsing localized tool output is not a measurement.

Change (sidecar only): replace both branches with one cross-platform implementation using `fs2::available_space(path)` (fs2 0.4.3 is already a dependency: statvfs on unix, GetDiskFreeSpaceExW on Windows), resolving the deepest existing ancestor of `path` first (the target directory may not exist yet at preflight — keep whatever ancestor walk the current code does); remove the `df`/`fsutil` code and every helper only they used. Record the measured value: the `InsufficientDisk` error and the admission/preflight record that carries the free-space check must state `available_bytes` and `required_bytes` (both already exist? — if only one does, add the other, additive) so a refusal is auditable. Keep the existing refusal semantics and thresholds unchanged.
Tests (verbatim counts): job_runtime — `system_available_bytes` on a temp dir returns > 0 and equals `fs2::available_space` of the same path; on a non-existent child of a temp dir it measures the parent; the German-locale `fsutil` sample test (if one exists) is removed with the parser; an `InsufficientDisk` refusal fixture asserts both byte fields. All existing tests keep passing: `cargo test -p himmelcad-sidecar --lib`, `cargo test -p himmelcad-sidecar --bin himmelcad-sidecar`, `cargo check -p himmelcad-sidecar --tests --bins`.

Files you may edit: crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/main.rs (only if the refusal payload needs the second byte field), docs/implementation-plans/2026-09-photolab-release-polish.md (a "WP-WIN-06-PL" paragraph under the Windows section or after WP-B4). Nothing else; no new dependencies. Verify with CARGO_TARGET_DIR=target\win only; additionally run the new tests on THIS Windows host and quote `fs2::available_space` of C:\Users\flori\source in the summary (the WIN-05 refusal claimed 784 bytes; ~25 GB is expected). Commit on branch photolab/win06-pl with explicit paths (message "fix(photolab): measure free space through the OS API (WIN-06-PL)"), then `git bundle create C:\Users\flori\source\win06-pl.bundle main..photolab/win06-pl` and write the report to C:\Users\flori\source\codex-last-win06-pl.md; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under .build\win06\ inside your clone and delete it before finishing. Final summary: the ancestor-resolution rule, the byte fields in the refusal, test counts, and anything you had to decide yourself.
