WIN-09-PL-REMOTE — the 16 GB gate run, second attempt: publication tool present, typed preflight in place (PhotoLab lane). Host DESKTOP-BNB2PBA. You start in C:\Users\flori\source. Rules exactly as WIN-07-PL (never touch C:\Users\flori\source\HimmelCAD or website/; hard stop 3 hours; cargo -j 3, CARGO_TARGET_DIR=target\win in your clone; NEVER copy a repository tree; keep ≥ 6 GB free on C:). Read `.build\win-07\WIN-07-report.md` in your clone first (previous attempt: everything up to sparse export succeeded in 120 min; publication failed on a missing PotreeConverter.exe).

Setup: `cd C:\Users\flori\source\HimmelCAD-photolab && git fetch origin && git checkout main && git pull --ff-only origin main` (HEAD must include commit 7db8ba1 = WP-WIN-08-PL, the typed toolchain preflight). The architect placed `vendor\potreeconverter\win32-x64\PotreeConverter.exe` (+ LICENSE) into this clone — verify it exists and runs (`PotreeConverter.exe --help` or `--version`; record the output). Rebuild release: `cargo build --release -p himmelcad-sidecar --bins`.

Run 1 (preflight proof, seconds): start the sidecar alignment as in WIN-07 but with `HIMMELCAD_POTREE_CONVERTER` pointing at a NON-existent path — expected: the typed refusal `workerToolchainMissing` naming PotreeConverter and the expected paths, no worker started (quote the JSON). Then unset that override.
Run 2 (the gate): the exact WIN-07 command (`node scripts\photolab-e2e.mjs --source "<absolute photos path in the main clone>" --output .build\win-09\gate --max-images 24 --profile fast --target-epsg 31468 --target-vertical-epsg 7837 --sidecar target\win\release\himmelcad-sidecar.exe`, `HIMMELCAD_PHOTOLAB_FORCE_EXTRACTION_TILES=2x1`, `HIMMELCAD_COLMAP_EXECUTABLE`/`HIMMELCAD_PROJ_ROOT` as in WIN-07) with the memory sampler to `.build\win-09\memory.csv`. Expected: 24/24 aligned AND the sparse product published `complete/available` (potree@2) — read `result.json` products; job record with `toolchain` resolved paths and the memory record. If the run stalls > 100 min in one stage or Windows kills it, stop and report.

Evidence: `.build\win-09\WIN-09-report.md` (HEAD, PotreeConverter version/sha256, Run 1 refusal JSON, Run 2 duration, peak working set, products with provenance, the job memory record and `toolchain` verbatim) copied to C:\Users\flori\source\codex-last-win09-pl.md. Commit nothing unless a Windows-only fix is unavoidable (then branch photolab/win09-pl + `git bundle create C:\Users\flori\source\win09-pl.bundle main..HEAD`). Final summary: preflight proof, gate result with products, memory peak, duration, and anything you had to decide yourself.
