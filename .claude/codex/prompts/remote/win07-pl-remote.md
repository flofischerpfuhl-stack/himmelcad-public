WIN-07-PL-REMOTE — the 16 GB gate run: PhotoLab alignment on the Windows PC with the memory envelope (owner D12/S23 gate; PhotoLab lane). Host DESKTOP-BNB2PBA. You start in C:\Users\flori\source. Rules: never touch C:\Users\flori\source\HimmelCAD (another lane's clone) or website/; hard stop 3 hours; one Codex session; cargo `-j 3`, CARGO_TARGET_DIR=target\win inside your clone; NEVER copy a repository tree; keep ≥ 6 GB free on C: (check before the build and before the run; stop and report if below). This clone cannot push; produce the evidence as files and a bundle as told below.

Setup: `cd C:\Users\flori\source\HimmelCAD-photolab && git fetch origin && git checkout main && git pull --ff-only origin main` (record HEAD; must include commit 7f1b833 or later). Build the sidecar and portable MVS in release: `cargo build --release -p himmelcad-sidecar --bins` (target\win\release\himmelcad-sidecar.exe, himmelcad-portable-mvs.exe). Runtime inputs already on this PC (from WIN-03/WIN-05, do not modify): Sulzberg photos under `photolab\Agisoft Exampleprojects\260706_Sulzberg_SUMA_UrGel\01_Photos\` (in the MAIN clone — reference them by absolute path, read-only), staged runtime `.build\photolab-runtime\win32-x64\` in the main clone with `workers\colmap\colmap.exe`, `workers\geo` (PROJ/GDAL), `workers\dedode`; set `HIMMELCAD_COLMAP_EXECUTABLE` and `HIMMELCAD_PROJ_ROOT` exactly as WIN-05 did (read `.build\win-05\WIN-05-report.md` in the main clone for the values).

The run (the gate): `node scripts\photolab-e2e.mjs --source "<absolute photos path>" --output .build\win-07\gate --max-images 24 --profile fast --target-epsg 31468 --target-vertical-epsg 7837 --sidecar target\win\release\himmelcad-sidecar.exe` with `HIMMELCAD_PHOTOLAB_FORCE_EXTRACTION_TILES=2x1` (same as the Linux A7c smoke) — alignment only, no depth. This machine has 16 GB RAM: the plan must size extraction/matching for its usable envelope; on Windows there is no cgroup scope (record what `workerMemoryLimitMode` the job record shows — expected `rlimitAs`-less/none on Windows; note it, do not fake a cap). Watch memory with a PowerShell sampler (`Get-Process colmap | Select WorkingSet64`, every 10 s, to `.build\win-07\memory.csv`). Expected: alignment completes 24/24 with the memory record showing the plan (`matchingReplanned`, `attempts`), or a typed refusal — never a machine OOM. If Windows kills or the run stalls > 90 min, stop it and report.

Evidence: write `.build\win-07\WIN-07-report.md` (HEAD, build times, env values, the job memory record verbatim from `.build\win-07\gate\photolab-e2e.hcad\.photolab\jobs\records\*.json` for alignPhotos, peak working set from memory.csv, alignment duration, result.json summary) and copy it to C:\Users\flori\source\codex-last-win07-pl.md. Also create `git bundle create C:\Users\flori\source\win07-pl.bundle main..HEAD` only if you committed anything (you should not need to commit; if a Windows-only fix was unavoidable, commit on branch photolab/win07-pl and bundle). Final summary: the memory record, peak working set, duration, and anything you had to decide yourself.
