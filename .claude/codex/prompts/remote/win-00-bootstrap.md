You are Codex running on the Windows PC DESKTOP-BNB2PBA, prompted remotely by the Himmel:CAD architect session. This machine is the project's Windows test and measurement host. Work only under C:\himmelcad (create it). Report in English, tersely, with exact commands and outputs.

Task 1 — inventory: OS build, CPU, RAM, GPU(s) with driver versions (`dxdiag /t`, `nvidia-smi` if present), disk free; which of these exist and their versions: git, node, pnpm, rustc/cargo (and the MSVC toolchain: `cl.exe` via the Visual Studio Build Tools / "Desktop development with C++"), python3, Google Chrome, 7-Zip. Install what is missing with `winget install` (Git.Git, OpenJS.NodeJS.LTS = Node 22, pnpm via `corepack enable` + `corepack prepare pnpm@latest --activate`, Rustlang.Rustup then `rustup default stable-msvc`, Microsoft.VisualStudio.2022.BuildTools with the C++ workload, Python.Python.3.12, Google.Chrome). Say exactly what you installed.

Task 2 — clone and verify: `git clone git@gitlab.com:florian-fischer-group/himmelcad.git C:\himmelcad` (if SSH to GitLab is not set up on this machine, generate an ed25519 key, print the public key in your report so the owner can add it, and stop this task there); then `pnpm install`, `pnpm typecheck`, `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, then `cargo check --workspace --all-targets` with `CARGO_TARGET_DIR=C:\himmelcad\target\win`. Record wall times and every Windows-specific failure verbatim (path separators, CRLF, linker, missing tools, long paths — enable `git config core.longpaths true` and Windows long paths if needed). Do not fix product code; fix only environment issues, and list each product-code failure as a finding with file:line.

Task 3 — write the evidence: `docs\builder-program\evidence\WIN-00-bootstrap-<date>.md` in the clone (inventory, installed tools, wall times, findings), commit it on a branch `win-bootstrap` and push (if push rights exist; otherwise leave it committed locally and say so).

Constraints: no product-code changes; no GUI-dependent steps in this task (the screen may be locked); budget: one pass; if a step needs owner action (SSH key, VS Build Tools license prompt), say precisely what.
