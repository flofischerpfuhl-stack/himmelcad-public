WIN-02 — PhotoLab Windows smoke on this PC (DESKTOP-BNB2PBA), in the existing clone C:\Users\flori\source\HimmelCAD (rules as WIN-01: never reset/stash/delete; the owner's website/ files stay untouched; logs under .build\win-02\; hard stop 3 hours; do not commit or push). Preconditions from WIN-01's report (.build\win-01\WIN-01-report.md): the sidecar built and the app tests passed; if WIN-01 reported blocking Windows defects, stop and report "BLOCKED BY WIN-01: <items>".

Steps: (1) `git pull --ff-only origin main`; record HEAD. (2) Find how PhotoLab's e2e runs on Linux: `scripts/photolab-e2e.mjs` and `.build/photolab-runtime/run-dem-smokes.sh` in the repo history/docs (`docs/builder-program/PHOTOLAB-HANDOFF.md`, `docs/photolab-*`), and the portable toolchain the sidecar expects (COLMAP, the portable MVS binary `himmelcad-portable`); determine what exists for Windows in the repo (`libs/`, `scripts/`, `apps/photolab/electron`) and what the e2e needs to run CPU-only (this PC has no CUDA: Vega 8 iGPU, 4 cores, 16 GB). (3) Obtain the 24-image Sulzberg example dataset the Linux smokes use (`photolab/Agisoft Exampleprojects/260706_Sulzberg_SUMA`) — it is NOT in git: check whether it exists on this PC (search C:\Users\flori for "Sulzberg"); if not, do not download anything — report "DATASET MISSING on Windows" and continue with the remaining steps on a synthetic or repo-contained fixture if the e2e offers one (`--source` options, fixtures under apps/photolab/test). (4) Build what is needed: `cargo build --release -p himmelcad-sidecar --bins` (CARGO_TARGET_DIR=target\win, reuse WIN-01's), and the PhotoLab Electron app (`pnpm --filter @himmelcad/photolab build` or the documented script). (5) Run the smallest complete PhotoLab workflow that the platform allows: import → align (feature extraction + matching CPU-only) → sparse product; if COLMAP/portable MVS are not available for Windows in the repo, record exactly which binaries are missing and stop that step. (6) Electron smoke of PhotoLab: start it, take a screenshot (.build\win-02\photolab-window.png), open the project from step 5 if it exists, screenshot the Jobs tab, stop the app. (7) Report .build\win-02\WIN-02-report.md: step table, Windows-specific defects with exact error text and file:line, the list of binaries/datasets missing for a full Windows PhotoLab gate, and a bounded proposal for closing each gap; print the report in full to stdout at the end.
