WIN-01 — HimmelCAD Windows build and smoke on this PC (DESKTOP-BNB2PBA). You work in the existing clone C:\Users\flori\source\HimmelCAD (origin = GitLab over HTTPS, credentials stored in Git Credential Manager). Rules: never reset, stash, checkout --force or delete anything in this clone; the owner's uncommitted files under website/ stay exactly as they are; never copy the repository; do not push unless a step below says so; keep every command's full output in C:\Users\flori\source\HimmelCAD\.build\win-01\<step>.log (create the folder).

Steps, in order, each bounded (use `timeout`-like limits via PowerShell jobs if needed; hard stop 2 hours): (1) `git fetch origin` and `git pull --ff-only origin main` (if it refuses because of the website/ files, do NOT touch them — report and continue on the current commit); record `git rev-parse HEAD`. (2) Toolchain check: `node -v`, `pnpm -v`, `python --version`, `cargo --version`, `rustc --version`, `cl` from the VS Build Tools (run `"C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe" -latest -property installationPath` and load vcvars64 in the shell before cargo). (3) `pnpm install --frozen-lockfile` (if the lockfile is incompatible with Windows-only optional deps, record the exact error; try `pnpm install` once more without the flag and record what changed — do not commit lockfile changes). (4) `pnpm typecheck` at the root; record the pass/fail summary and the first 30 errors if any. (5) Rust: `cargo check -p himmelcad-sidecar --tests --bins` with CARGO_TARGET_DIR=C:\Users\flori\source\HimmelCAD\target\win (note free disk before/after: `Get-PSDrive C`), then `cargo build --release -p himmelcad-sidecar --bins` if the check passed; record Windows-specific compile errors (path handling, symlinks, `std::os::unix` uses, process groups, file locking). (6) wasm: run the project's wasm staging script as documented in package.json / scripts (`pnpm --filter @himmelcad/viewer …` or `scripts/build-wasm*`), record whether `wasm-pack`/`wasm-bindgen` are present and what fails. (7) Tests: `pnpm --filter @himmelcad/app test`, `pnpm --filter @himmelcad/viewer test`, `pnpm --filter @himmelcad/builder test`; record the pass/fail counts. (8) Electron smoke: `pnpm --filter @himmelcad/builder dev` (or the documented start script) for up to 3 minutes; take a screenshot of the Builder window with PowerShell (System.Drawing CopyFromScreen) to .build\win-01\builder-window.png; if a small LAS/LAZ fixture exists in the repo (search under packages/**/test/fixtures, apps/**/test, .build), import it through File ▸ Import and take a second screenshot; stop the app. (9) Report: write C:\Users\flori\source\HimmelCAD\.build\win-01\WIN-01-report.md with a table (step · result · time · log path) and the list of Windows-specific defects with exact error text and file:line; finish by printing that report in full to stdout (this is what comes back over the channel). Do not commit; do not push.
