WIN-21 — update the AMD graphics driver on this PC (owner approved 2026-09-19) and re-test Builder rendering. Host DESKTOP-BNB2PBA, clone C:\Users\flori\source\HimmelCAD (work area on D: via junctions). Rules: never reset/stash/delete tracked or owner files; logs and downloads under D:\himmelcad-work\build\win-21\ ; no commit/push; hard stop 2 hours. Read .build\win-20\WIN-20-report.md first (Vega 8 `VEN_1002&DEV_15D8`, driver 31.0.12027.9001 of 2023-03-30, Electron 43.1.0: GPU process exit 34 in every variant → software compositing/readback).

Part A — driver: (1) record the current driver (`Get-CimInstance Win32_VideoController`, `pnputil /enum-drivers` for the display class) and create a restore point if System Restore is enabled (`Checkpoint-Computer`; if it is disabled, say so and continue — the previous driver package stays in the driver store, note its published name for rollback via Device Manager / `pnputil`). (2) Check elevation (`whoami /groups` for High Mandatory Level / admin); if this SSH session is not elevated and cannot elevate non-interactively, STOP Part A and report exactly what the owner must do (one command/installer path) — do not try UAC tricks. (3) Obtain the current official AMD Software: Adrenalin Edition package for "AMD Ryzen 3 PRO 3200G with Radeon Vega Graphics" (Windows 10 64-bit) from amd.com only (the download needs a `Referer: https://www.amd.com/` header); verify the Authenticode signature (`Get-AuthenticodeSignature`: Status Valid, signer Advanced Micro Devices) and record version, size, SHA-256; if amd.com cannot be fetched non-interactively, fall back to the driver offered by Windows Update for this device (`UsoClient`/PSWindowsUpdate is NOT to be installed; use `pnputil /scan-devices` + the built-in update API only if available) or stop and report the exact download URL for the owner. (4) Install silently, driver only where the installer allows it (`Setup.exe -install` / documented silent switches; no optional bloat if selectable); capture the installer log. (5) Write D:\himmelcad-work\build\win-21\WIN-21-partA.md with before/after driver versions and rollback instructions, PRINT it to stdout, then reboot with `shutdown /r /t 60 /c "HimmelCAD WIN-21 driver update"` and end the session (the reboot will cut SSH — that is expected; the architect runs Part B as a separate brief once the PC is back and a user is logged in).
Do NOT run Part B in this session.
