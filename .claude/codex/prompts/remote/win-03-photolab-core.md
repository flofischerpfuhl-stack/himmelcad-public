WIN-03 — PhotoLab core on Windows: staged runtime, dev sidecar path, import smoke. Host DESKTOP-BNB2PBA, clone C:\Users\flori\source\HimmelCAD (rules as before: never reset/stash/delete; owner's website/ untouched; logs under .build\win-03\; hard stop 2 hours; commit only as told below). Read .build\win-02\WIN-02-report.md first.

Inputs now present on this PC (transferred from the Linux host, do not modify): the Sulzberg photo source under `photolab\Agisoft Exampleprojects\260706_Sulzberg_SUMA_UrGel\01_Photos\` and the staged runtime `.build\photolab-runtime\win32-x64\` with `workers\geo` (PROJ/GDAL) and `workers\dedode`; `workers\colmap` is still missing (Windows COLMAP worker cross-build pending on the Linux side — do not substitute a PATH COLMAP).

Steps: (1) `git fetch origin && git checkout main && git pull --ff-only origin main` (the fsync fix is merged in main now; record HEAD). (2) Fix the PhotoLab-owned Windows defect: `apps/photolab/electron/sidecar.ts` development path must be platform-aware (`himmelcad-sidecar.exe` on win32) and accept an explicit override env (name it `HIMMELCAD_SIDECAR_BIN`, documented in the file); add the Windows Electron test WIN-02 proposed (creates only `target\debug\himmelcad-sidecar.exe`, asserts selection). Check whether Builder's `apps/builder/electron` has the same pattern and fix it identically if so. (3) Build the dev sidecar (`cargo build -p himmelcad-sidecar --bins`, CARGO_TARGET_DIR=target\win — or point `HIMMELCAD_SIDECAR_BIN` at `target\win\release\himmelcad-sidecar.exe`). (4) Start the sidecar directly with `HIMMELCAD_PROJ_ROOT=<repo>\.build\photolab-runtime\win32-x64\workers\geo` (check main.rs:10933-10948 for the exact expected layout; if the staged layout differs, say exactly which path it expects and adapt the env, not the runtime) and prove both startup probes pass; record the exact command. (5) Start PhotoLab (dev) with those env vars; verify the status bar no longer says `Core unavailable`; create a project and import the Sulzberg images (`--max-images 24` equivalent through the UI or the e2e `scripts/photolab-e2e.mjs` up to the image-commit stage, stopping before alignment); screenshots: shell, imported images, Jobs tab; then run the e2e alignment step once to record the exact typed reason it refuses without the COLMAP worker (must be a typed reason, not a crash). (6) Viewer on Vega 8: capture Chromium GPU diagnostics (`chrome://gpu` equivalent via CDP `SystemInfo.getInfo`, Electron `app.getGPUFeatureStatus()`), and try exactly two documented flags in order — `--use-angle=d3d11` and `--use-gl=angle --use-angle=swiftshader` — via the Electron launch (do not change product code for this; report which one yields a WebGL2 surface and its frame time); this is evidence for the Builder-lane package V-08. (7) Tests: `pnpm --filter @himmelcad/photolab test`, `pnpm --filter @himmelcad/photolab typecheck`, `pnpm --filter @himmelcad/builder test`. (8) Commit ONLY the sidecar-path fix and its tests (explicit paths) on branch `win/photolab-dev-sidecar-path`; do not push (credentials are absent in this session) — the Linux host pulls it as a bundle: run `git bundle create .build\win-03\dev-sidecar.bundle main..win/photolab-dev-sidecar-path`. (9) Report .build\win-03\WIN-03-report.md (step table, exact env/commands, defects, the COLMAP refusal text, GPU findings) and print it in full at the end.
