WIN-01b — fix the two Windows defects WIN-01 found and finish the Builder smoke, on this PC in C:\Users\flori\source\HimmelCAD (rules as WIN-01: never reset/stash/delete; owner's website/ files untouched; logs under .build\win-01b\; hard stop 2 hours). Read .build\win-01\WIN-01-report.md first.

(1) Toolchain: install the wasm-bindgen CLI at exactly the version pinned in Cargo.lock (`cargo install wasm-bindgen-cli --version <that version> --locked`) and, if the staging script calls it, wasm-pack (`cargo install wasm-pack --locked`); rerun the wasm staging step from WIN-01 and record the result. (2) Code fix, Windows-safe durability: `apps/builder/electron/projectLifecycle.ts:93` opens a file with 'r' and calls `file.sync()` — Windows returns EPERM for fsync on a read-only handle; and the startup path fsyncs a directory, which Windows rejects (unhandled rejection before window creation — see .build\win-01\08f-builder-fallback.log for the exact stack). Find the shared durability helper these paths use (search for `sync()`/`fsync`/`fdatasync` under apps/builder/electron and packages/@himmelcad, and the S-07 durability records in docs/builder-program/specs/file-project/file-project.md) and make it platform-correct: fsync files through a writable handle (open 'r+') or after write only; directory fsync is best-effort and skipped on win32 (`process.platform === 'win32'`) with EPERM/EISDIR/ENOTSUP tolerated — the durability contract still holds because NTFS commits directory metadata on file flush; keep POSIX behaviour unchanged. Add a unit test that runs on both platforms (mock the platform for the skip path). (3) Run `pnpm --filter @himmelcad/builder test`, `pnpm --filter @himmelcad/app test`, `pnpm typecheck`; then the Electron smoke: start Builder (`pnpm --filter @himmelcad/builder dev` or the documented start), wait for the window, screenshot to .build\win-01b\builder-window.png; if a small LAS/LAZ fixture exists in the repo, import it through File ▸ Import and screenshot; stop the app. (4) Commit ONLY your code/test changes (explicit paths; never the website/ files, never .build) on a new branch `win/fsync-durability` and `git push -u origin win/fsync-durability` (HTTPS credentials are stored). (5) Report .build\win-01b\WIN-01b-report.md (step table, defects fixed with file:line, remaining Windows defects) and print it in full to stdout at the end.
