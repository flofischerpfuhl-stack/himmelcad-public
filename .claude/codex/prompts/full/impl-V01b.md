Work package V-01b — make Builder start and the viewer baseline measure on this machine. Read `.claude/codex/prompts/_impl_brief.md`, `scripts/perf/viewer-baseline.mjs` (+ README), `.build/perf/viewer-baseline-2026-09-06.json` (blocker: "Builder renderer page was not attached to the CDP browser within 120 seconds"), `docs/builder-program/evidence/D-02-scan-import-view-2026-09-05.md` (same symptom: "Electron exposed CDP but created no renderer target within 120 seconds"), the existing CDP benchmark the README mentions (find it under scripts/ or apps/builder), `apps/builder/package.json` scripts (`dev`, `dev:electron` = `tsc -b tsconfig.electron.json && wait-on http://localhost:5173 && node ../../scripts/run-electron.mjs .`), `scripts/run-electron.mjs`, `apps/builder/electron/main.ts`.

Step 1 — is Builder itself healthy? Start Builder the normal way (`pnpm --filter @himmelcad/builder dev`, DISPLAY=:0 is a real X session with the NVIDIA GPU; do not use xvfb or software GL), wait for the window, take a screenshot of the actual window (use `import -window root` / `xwd` / `gnome-screenshot` or Electron's own capturePage via the existing CDP/automation path), and collect main-process and renderer console errors. If the renderer crashes or never mounts (a white/blank window, an uncaught error from the recent substrate landings S-03…S-08, P-01, D-02 is the prime suspect), fix the root cause in Builder/app code with a test, and record it as the finding — this is the highest-value outcome of this package. Keep the screenshot at `.build/perf/builder-window-2026-09-06.png` for the architect's G17 look.
Step 2 — make the harness attach: find why no renderer page appears to the CDP client (target filter/URL pattern, timing of `--remote-debugging-port`, the `tsc -b` step's duration exceeding the 120 s wait, the page being created before the client connects, a second Electron instance, the `window.__hcadBuilderKernel` flag not being set because the kernel failed WebGPU adapter acquisition). Fix the harness (or Builder) at the cause; keep the software-adapter rejection.
Step 3 — measure once: run `node scripts/perf/viewer-baseline.mjs --date 2026-09-06 --metadata .build/perf/viewer-baseline-datasets/PW_GHT_251215_Orscholz_Deponie-1-1-ff05d6cffc61/metadata.json`; report p50/p95/p99 per scenario, points drawn, reason codes, adapter, and the machine load during the run (`uptime`) — other lanes and a PhotoLab diagnostic may be running; state that honestly; do not fabricate quiet conditions.

Gates: Builder starts and shows its shell (screenshot); harness attaches and writes a non-blocked report; `pnpm --filter @himmelcad/builder typecheck`, `--filter @himmelcad/viewer test`. Evidence `docs/builder-program/evidence/V-01b-harness-2026-09-06.md` (+ append the numbers to the viewer dossier §5). Concurrency: S-08b, S-10, V-03 and 0.5-01 are editing app/ui/viewer/render files — restrict your edits to the harness scripts, `scripts/run-electron.mjs`, and a minimal root-cause fix if Builder itself is broken; announce that fix at the top of the evidence. Budget: high effort, one pass.
