SPLIT STEP 7 — one hardware profile module (Rust + TypeScript) and a truthful rendering status. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English.

Read ONLY: `docs/adr/0032-module-architecture-and-crs-declaration.md` (§3 Hardware profile); `docs/ARCHITECTURE.md` § Module map; `docs/builder-program/evidence/SPLIT-PLAN-2026-09-23.md` § "Hardware profile"; `docs/builder-program/evidence/WIN-21-22-vega8-driver-and-isolation-2026-09-20.md`; `docs/builder-program/evidence/SPLIT-WASM-FIX-2026-09-24.md` (WASM closure rules); then the code you change.

TODAY (verify): hardware decisions live in `himmelcad-render::{hardware_policy (VRAM/RAM/core budgets, device classes, quality governor), gpu_calibration}`; `himmelcad-photolab-sidecar::hardware_runtime` (/proc or WMIC RAM/cores, AVX2, nvidia-smi, vulkaninfo); hardware DTO/planning parts of `photolab_models` in the photogrammetry domain; job memory policy (usable RAM, concurrency) in command/photogrammetry; Electron: `apps/builder/electron/rendererFallback.ts` (persisted WebGPU → WebGL2 → SwiftShader recovery), GPU switches in `apps/builder/electron/main.ts` (d3d11 on Windows, unsafe WebGPU on Linux dev) and `apps/photolab/electron/main.ts`; `KernelViewerSession` backend retries in `@himmelcad/viewer`. The Builder chip "Hardware rendering" (`apps/builder/renderer/src/App.tsx` ~4946) is inferred from "not persisted software" and was proven untruthful on the Vega 8 (Chromium ran with software compositing/readback while the chip said hardware).

GOAL (no change to budgets, thresholds or backend order — only where the decision lives, plus the truthful status)
1. New Rust crate `crates/himmelcad-hardware-profile` (layer foundation, WASM-safe, no wgpu/tokio/fs2): hardware identity/inventory types, capability and calibration observation types, render and compute budget derivation (moved verbatim from `hardware_policy` and the photogrammetry/command memory planning), deterministic quirk resolution. Native probing (/proc, WMIC, nvidia-smi, vulkaninfo, AVX2) lives in a `cfg(not(target_arch = "wasm32"))` module of that crate or a small native companion crate — choose and justify. `himmelcad-render` keeps only the calibration benchmark runner and consumes budgets from the profile; domain/command code consumes compute budgets from it.
2. New TypeScript package `packages/@himmelcad/hardware-profile` (layer foundation, no Electron import): pure functions that take injected facts (OS, `app.getGPUFeatureStatus()`, `app.getGPUInfo('basic')`, persisted fallback state, the viewer's reported backend/adapter incl. `isFallbackAdapter`) and return (a) the Chromium launch switches and (b) the rendering status. Both Electron hosts call it; `rendererFallback.ts` logic moves there (Electron-specific persistence stays in the app, behind an injected store).
3. Quirk rules: ONE source of truth, e.g. `packages/@himmelcad/hardware-profile/quirks-v1.json` with a schema: `{id, priority, match:{os, vendorId, deviceId, driverMin, driverMax, backend, sessionType, electronRange}, actions:{disableBackends, forceAngle, renderBudgetScale, computeBudgetScale}, reason, expires}`; stable ids, deterministic priority, conflict rejection, bounds, no free-form GPU-name matching. If Rust also needs the rules, generate a Rust table from the JSON with a `--check` drift gate (like `scripts/generate-command-table.mjs`) — never `include!`/`include_str!` across crates. Ship it with zero active rules unless an existing code path already encodes a device-specific decision — then express that decision as a rule and remove the inline check.
4. Truthful status: states Initializing, WebGPU (hardware), WebGL2 (hardware), Software (SwiftShader/readback), Unavailable. "Hardware" only when the selected adapter reports a non-fallback adapter AND Chromium's feature status reports hardware compositing/WebGL/WebGPU as enabled (not `*_software`/`disabled_software`/`enabled_readback`). Wire the Builder chip to it (same position and styling; label/tooltip text from the status; English). PhotoLab: expose the same status where it already shows renderer state, or report where it would go — no new UI surface.
5. `scripts/module-layers.json`: both new modules as foundation; the checker's display-closure rule must stay green (render may depend on the profile crate).

HARD RULES
- No change to any numeric budget, threshold, backend order, retry count or quality-governor behaviour (move verbatim; prove with the moved tests). Tests move verbatim; Rust `#[test]`/`#[tokio::test]` count stays 1,237 plus tests you add (list them).
- Rejected constructs as before (cross-crate `include!`, domain `macro_export`, `package =` renames, `extern crate self as`, blanket foundation re-exports from domain crates, duplicate types).
- NEVER `git stash`, `git reset`, `git checkout -- <path>`. Never revert, delete or edit files you did not change yourself (another agent may be writing `docs/ui-redesign/`). Rust only with `CARGO_TARGET_DIR=/media/oem/ZusatzSSD1/himmelcad-target/split` and `-j 4`. Scratch only under `.build/split-s7/`, delete at the end. No UI on `DISPLAY=:0` (use `scripts/ui-test-display.sh` for any literal UI check). Do not commit or push. Do not run `bindings:generate`.

GATES (verbatim)
- `cargo check --workspace --all-targets -j 4`; `cargo fmt --all --check`
- `cargo check -p himmelcad-wasm -p himmelcad-render -p himmelcad-hardware-profile --target wasm32-unknown-unknown -j 4`
- `PATH="$PWD/.build/llvm-mingw/llvm-mingw-20260407-ucrt-ubuntu-22.04-x86_64/bin:$PATH" cargo check --target x86_64-pc-windows-gnullvm -p himmelcad-builder-sidecar -p himmelcad-photolab-sidecar --all-targets -j 4`
- `cargo test -p himmelcad-hardware-profile -p himmelcad-render -p himmelcad-domain-photogrammetry -p himmelcad-command -j 4 -- --test-threads=2`; `cargo test -p himmelcad-builder-sidecar -p himmelcad-photolab-sidecar -p himmelcad-sidecar -j 4 -- --test-threads=2`
- `pnpm --filter @himmelcad/hardware-profile test` (new unit tests: switch derivation per OS/fallback state, status derivation incl. the Vega 8 facts from WIN-21/22 → "Software", quirk schema validation and priority/conflict cases)
- `pnpm --filter @himmelcad/builder typecheck`; `pnpm --filter @himmelcad/builder test`; `pnpm --filter @himmelcad/photolab typecheck`; `pnpm --filter @himmelcad/photolab test`; `pnpm --filter @himmelcad/viewer test`; `pnpm --filter @himmelcad/viewer test:browser-kernel-webgl2` (headless)
- `pnpm check:modules`; `node --test scripts/check-module-dependencies.test.mjs`
- Literal UI: start Builder via `scripts/ui-test-display.sh`, screenshot the status chip, record its state and the `app.getGPUFeatureStatus()` facts it was derived from.

EVIDENCE `docs/builder-program/evidence/SPLIT-S7-2026-09-24.md`: moved items (from → to, lines), profile API, quirk schema, status derivation table, what each host passes in, screenshot path, test counts, gates verbatim, what must be verified on Windows/Vega 8 (brief text for the architect), what was NOT verified, measured time.
