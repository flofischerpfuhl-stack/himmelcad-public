Work package PL-B1b (Builder-side items PhotoLab needs) — Windows-safe durable sync in Builder-owned sidecar sites, and DEM residency in the Builder viewer. Read `.claude/codex/prompts/_impl_brief.md`, PHOTOLAB-HANDOFF.md entries of 2026-09-11 06:15 and 06:25 (root cause: read-only open + `sync_all` fails on Windows with os error 5; PhotoLab landed `durable_fs::sync_file`/`sync_dir` in a111627 and replaced its nine sites; Builder-owned sites still affected: `crates/himmelcad-sidecar/src/canonical_project_store.rs:574, :585, :2375` (ready/journal sync) and `crates/himmelcad-sidecar/src/brush_runtime.rs:1191`), the `durable_fs` module as landed, and for DEM residency: `docs/builder-program/PHOTOLAB-G1C-MATRIX.md` DEM rows and `docs/builder-program/evidence/G1b-check-2-2026-09-10.md` (which DEM cells fail and why — the imported DEM prepared hierarchy is not resident/rendered in the Builder viewer), the G1b import code (`apps/builder/electron/productImportCatalog.ts`, `BuilderPhotoLabProductImportIsland.tsx`, the sidecar import runtime), the viewer bootstrap / residency path (the R-02d lane just added `hcad.pointcloud.height-grid@1` residency in the same area — read its evidence `R-02d-p0-spine-2026-09-11.md` and build on it, do not duplicate), and `viewer_raster_manifest.rs` (DEM prepared hierarchy layout, validity masks per tile).

Deliverable: (1) replace the four read-only fsync sites with `durable_fs::sync_file`/`sync_dir` (write-access open on Windows, dir sync no-op there), with a unit test per site's helper and the existing durability tests green; grep the whole Builder-owned sidecar/core for the same pattern (`File::open(...)` followed by `sync_all`) and fix any others, listing them; (2) DEM residency: an imported DEM product (DSM `product-2868864c…`, DTM `product-00e33bcd…`, read-only fixtures under `.build/photolab-e2e/…/product-import-packages/`) becomes a resident dataset in the Builder viewer — the prepared hierarchy streams, NoData holes render transparent per `dem_facts.nodata_policy`, pick returns elevations; verify through the literal UI against PhotoLab's oracle (`.build/photolab-g1c/{dsm,dtm}/oracle.json`: the 7 sample XY elevations ± 0.01 m; centre/corner cells are genuine holes) and fill the Builder DEM cells of the matrix (touch only Builder columns). Gates: `cargo test -p himmelcad-sidecar durable` and `-p himmelcad-sidecar canonical_project_store` once, `cargo check -p himmelcad-sidecar --tests --bins`, `pnpm --filter @himmelcad/builder test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme lint. Evidence `docs/builder-program/evidence/PL-B1b-durable-sync-dem-residency-2026-09-11.md` with the numeric DEM comparison table. Rules: never copy the repository or fixtures; each gate at most three times; two-hour budget; do not commit; do not edit PhotoLab-owned paths; cargo `-j 4` in target/builder. Budget: high effort, one pass.
