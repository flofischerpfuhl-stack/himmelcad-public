Work package 0.5-01 — Viewing box with lock/bake (S-09). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/viewing-box/*.md` in full (VB-D1–VB-D14: canonical viewing-box entity per P1, grips, keep-inside/remove-inside per X5, lock = bake a reduced resident dataset per P2, VB-D7 presented-frame cadence gate, VB-D8 mixed-scene lock parity, placement-revision bake), `docs/DECISION-DOCTRINE.md` P1/P2/P4/P5, `docs/builder-program/OWNER-DECISIONS.md` D-list entries on the box (canonical entity, real baking, agent restore), ADR 0031 item 3 (entity-referenced clips — the S-01 parser; S-08/S-08b ViewState v2), the landed pieces: S-03 arbiter (grip drags are a tool claim with the `Shared3DTarget` deviation set), S-04 selection, S-05 jobs (baking is a registered cancellable job), S-07 durability (one journal transaction at gesture end), V-02 frontier (a lock bakes a prepared dataset the frontier streams like native data), S-10 construction bar (typed extents), and the existing `KernelViewingBox.ts` / `KernelClipCapCoordinator.ts` in the viewer kernel plus Builder's viewing-box island.

Deliverable: the complete 0.5 viewing-box workflow through visible UI: create from selection / from typed extents / by dragging a box in the viewport; six face grips + corner grips with predictable resize (no jumping — grip stays under the cursor), rotation optional; keep-inside / remove-inside as the symmetric pair; named boxes as canonical entities (restore from tree, agent parity `view.box.*` rows in the single table); lock: bakes the reduced dataset as a registered job with progress and cancel, the locked scene then performs like native small data (VB-D8 parity), unlock restores the full dataset; box state journals once at gesture end (P5); Escape follows the ladder (rung 4 while dragging, rung 6/7 for the island).

Visual brief (G17): the box is drawn as a thin accent-outlined wireframe with 8 px square grips (support-blue when idle, orange when active), faces tinted 6 % accent only while hovering a face; the Viewing Box function panel (docked, detachable) shows Min/Max X/Y/Z NumberInputs with unit suffix, a "Lock" primary Button turning into "Unlock" with a progress row while baking, "Keep inside" / "Remove inside" as a two-button segmented pair, a "Name" field and "Save as entity"; the tree shows saved boxes under a "Viewing boxes" node with a box glyph. Gallery section "Viewing box panel" (idle, baking, locked) — regenerate shots serially.

Gates: VB-D7 — presented-frame-interval p95 ≤ 2× the class target during a grip drag over the 104 M-point prepared dataset (measure with the V-01 rings; run once with the machine idle and state the machine state); VB-D8 — locked-scene interaction ≤ 1.1× the cost of a natively small scene of the same point count (measure both); P5 — zero journal writes during a drag, one at completion; restore test — a named box restores exactly (extents, rotation, keep/remove mode) from the journal and through automation; cancel test — cancelling a bake leaves the unlocked scene intact and no orphan prepared data; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, `cargo test -p himmelcad-render` once (CARGO_TARGET_DIR exported, target/builder), registry rows + linter green. Evidence `docs/builder-program/evidence/0.5-01-viewing-box-2026-09-06.md`. Concurrency: S-10 edits the tree/bottom bar/construction bar (consume its API, do not edit); V-03 edits the render scheduler/governor (consume, do not edit). Budget: high effort, one pass.
