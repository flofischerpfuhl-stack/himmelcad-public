Work package S-02b — corrections from the architect's visual review of the S-02 base controls. Read `docs/builder-program/evidence/S-02-visual-review-2026-09-04.md` (the five numbered findings are the complete scope), then the affected files: `packages/@himmelcad/ui/src/{Button,Dialog,Menu,Toast}.tsx`, `BaseControls.module.css`, `packages/@himmelcad/ui/test/a11yFixtures.tsx`, `packages/@himmelcad/ui/gallery/*`, and `docs/DESIGN-SYSTEM.md` "Visual language" (tokens only; accent blue sparingly; status colours only for success/warning/error).

Do exactly the five required changes. Visual brief for the results (G17):
- Button variants: primary = accent fill, white text; secondary = surface fill with 1 px border token, foreground text; quiet = no fill, no border, foreground text, hover shows surface; danger = error token fill, white text. Height 28 px, padding 0 12 px, radius token, font UI 13 px medium. Hover = one surface step lighter (dark) / darker (light); focus-visible = 2 px accent ring with 2 px offset; disabled = 45 % opacity, no hover change; loading = spinner 14 px left of label, label stays.
- Dialog: 420 px wide, surface token, 1 px border token, title row 44 px with title 15 px semibold left and a 28 px close button right, body 16 px padding, footer 12 px padding right-aligned, buttons 8 px apart, Cancel (secondary) left of Delete (danger). Backdrop = the existing modal scrim token.
- Menu: min-width 180 px, items 28 px tall, 8 px horizontal padding, one highlighted item at a time using the hover surface token; focused item shows the focus ring inset 2 px; disabled item 45 % opacity; separator token between groups.
- Toast: 360 px wide, 4 px left accent bar coloured by kind (info = accent, success/warning/error = status tokens), body 13 px, close button 24 px on the right, optional action link before the close button.

Gates: update fixtures so the gallery shows every variant/kind/state as the review demands; add the "at most one focused menu item" test and the "hover row differs from default row" pixel check in `gallery/shoot.mjs` (fail if identical); regenerate `pnpm --filter @himmelcad/ui gallery:shots`; `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/ui typecheck`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` (if PhotoLab fails only inside apps/photolab/renderer/src/jobsChip*, that is the other lane's in-flight work — report it, do not touch it). Append a "Corrections applied" section with the new shot paths to the review file. No other component changes. Budget: medium effort, one pass.
