Work package V-07b — the 3D→2D→3D transition tail (1.8 ms over target). Read `.claude/codex/prompts/_impl_brief.md`, `docs/builder-program/evidence/V-07-frame-tail-2026-09-09.md` (after V-07 the motion paths pass at 19–21 ms p95, the transition is at 35.2 ms median-of-five p95 against 33.4 ms; the worst-frame table for the transition path), `docs/builder-program/specs/view/viewer-core-addendum.md` VC-D10 (transition contract: cursor-anchored blend, settle-then-commit, prewarm) and §1.1/§3, the V-06 evidence, `packages/@himmelcad/viewer/src/kernel/{KernelCameraController.ts,KernelNavigationController.ts,KernelStreamingDriver.ts}` (target-LOD prewarm lanes from V-07), `crates/himmelcad-render/src/camera.rs`, `scripts/perf/viewer-baseline.mjs`.

Deliverable: diagnose the transition's remaining tail with the V-01 rings (which frames of the blend exceed budget: the first frames after the projection switch? the prewarm completing mid-blend? plan-depth re-sort? text re-layout?) and fix within the contract — e.g. complete the prewarm before the blend starts (wait up to one budget window, then start anyway), amortise the plan-order re-sort across frames, keep the 2D text ordering incremental, avoid re-allocating the depth attachment on projection change — without shortening or simplifying the animation. Gate: median-of-five p95 for 3D→2D→3D ≤ 33.4 ms on the idle machine (`uptime` < 2 over 5 min; wait up to 20 min; else NOT IDLE) with the other four paths unchanged (re-measure them; they must stay ≤ 33.4 ms); `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`; `cargo test -p himmelcad-render` once if the crate changes, wasm staged, PhotoLab visual harness dark (`--no-a11y --no-compare-baselines`, zero GPU-device errors). Evidence `docs/builder-program/evidence/V-07b-transition-tail-2026-09-09.md` and the dossier §5 row. Rules: never copy the repository or datasets; each gate at most three times; 90-minute budget; do not commit; do not edit PhotoLab-owned paths. Budget: high effort, one pass.
