Work package D-02 — scan import and view subset for Release 0.5. Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/import-formats/import-formats.md` IF-D1–IF-D3, IF-D5–IF-D8, IF-D11, IF-D13, IF-D14 (registered import jobs, placement resolution, source truth retention, arrival policy), `docs/builder-program/specs/pointcloud/pointcloud.md` PC-D1–PC-D4 (cloud entity, classes, display), PC-D11 (display properties), `docs/builder-program/specs/view/view-domain.md` VD-D1/VD-D8 (fluid view, display layers), `docs/builder-program/MASTER-PLAN.md` §6 M-0.5 (order 1: "import → fluid view + HUD"), the S-05 job registry (imports are registered jobs with phases and cancel), S-07 durability, S-08 ViewState (if landed — check `docs/builder-program/evidence/S-08-*.md`), V-02 frontier (if landed — check `evidence/V-02-*.md`; otherwise build on ADR 0003 streaming as is), the existing importers (`crates/himmelcad-io`, sidecar `import_registration_runtime.rs`, Builder import island) and the real fixture list (find LAS/LAZ/E57 fixtures in the repo; the 104 M-point LAS under `libs/polyshapev01/dist/` is the extreme member).

Deliverable: the 0.5 import-to-view path end to end through visible UI: drag-and-drop or File ▸ Import of LAS/LAZ/E57 → registered job with real phases (read header, convert/prepare, register dataset, first frame) and cancel at every phase boundary (no partial canonical entity on cancel) → placement resolution visible (CRS/offset shown, IF-D5; no invented coordinates) → cloud entity in the tree with classes and display properties (point size, color mode, class visibility — P9 states) → fluid view with the HUD (S-08) and the frontier (V-02) → journal head durable (S-07). Source truth retained: the raw file is referenced, never rewritten; prepared data is content-addressed. P11 rows `import.open/import.cancel/pointcloud.display.set` as needed.

Visual brief (G17): import arrival = the S-05 chip and island rows ("Import scan_01.laz · Reading header 3 %", "Preparing hierarchy 42 %", "Registering", "First frame") with cancel; placement = an inline row in the import island (not a modal): "CRS: EPSG:25832 · offset 0 / 0 / 0 · source units m" with a "Change…" secondary Button that opens the existing import registration island; the tree node shows a cloud glyph, name, point count in muted mono ("103.7 M"); the Properties tab for a cloud shows Display (point size Slider 1–8 px, color mode Select: RGB / intensity / classification / elevation, class list with P9 tri-state Checkboxes). Gallery section "Import surfaces" with the island rows and the cloud Properties; regenerate shots serially.

Gates: `G-IF-1` — three real fixtures (small LAS, an E57, the 104 M-point LAS) import through the UI path with progress and land as cloud entities; `G-IF-2` — cancel at each phase leaves no entity, no orphan prepared data (or quarantined per B5-style rules), and the job reports cancelled; `G-IF-3` — reopen after import: the cloud reappears at the same placement from the journal + CAS, no re-import; extreme member: the 104 M-point import stays cancellable within 250 ms at every boundary and the UI stays responsive (presented-frame p95 ≤ 2× target during import, measured with V-01 rings on this machine — report numbers, machine state stated); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; sidecar tests for the import runtime (`cargo test -p himmelcad-sidecar import` in target/builder, once). Shared-substrate notice: `crates/himmelcad-io` and the sidecar are shared with PhotoLab — additive changes, list touched functions in the evidence. Evidence `docs/builder-program/evidence/D-02-scan-import-view-2026-09-05.md`. Concurrency: V-02 edits the render crate streaming/selector; do not edit those files — consume their API. Budget: high effort, one pass.
