IMPLEMENTER BRIEF (replaces the long reading list; read ONLY what is named here). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Build with CARGO_TARGET_DIR=target/builder; never touch target/ or target/photolab; do not commit; do not reformat unrelated code.

Rules that bind every slice: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists/blocks (gestures journal once at gesture end; appends async); P4 tools act on the visible set; P8 undo is domain-scoped (Ctrl+Z = document only); P11 automation/console rows come from the one generated command table; design-system copy never claims unverified state; no office convention hard-coded (P7). Shared-substrate files (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, schemas, sdk, scripts/verification) are announced by the architect before you edit — edit only files named in your package.

Definition of done (MASTER-PLAN §9.3, condensed): the user flow works end to end (discovery, entry, typed/pointer parity, cancel, close, recovery); canonical behavior matches the owning records; passive consumers traced; every continuous/long interaction has its named runnable gate with a measured result; required TEST-TIERS gates pass (changed tier at minimum; state capabilities you lack); an evidence file under docs/builder-program/evidence/<package>-<date>.md records what changed (files), tests run with verbatim results, gate status, and what was NOT verified. Spec wrong? Message the finding in your final report; never diverge silently.

Your package's owning records, gates, and files are listed below this brief. Read those spec sections only (by decision id), not whole specs.

PACKAGE: I-07 "deterministic registry linter" (MASTER-PLAN D8/§0a; new tooling, no cargo). Read only: docs/builder-program/README.md (registry rules), docs/builder-program/REGISTRY.md (structure: §1 row tables per spec with columns Id | Tab · group | Access | Surface | Perf | Automation | Spec link | Status; §2 shortcut map; §4.1 headline checks), and the catalog tables + decision-record headings of two specs as samples (docs/builder-program/specs/view/viewing-box.md, docs/builder-program/specs/draw/draw.md).

Build scripts/registry-lint.mjs (node, no dependencies beyond the repo's existing devDependencies; ESM like the other scripts): parse every spec under docs/builder-program/specs/*/*.md (excluding *review* and *criteria* files) for (a) catalog table rows whose first cell is a backticked function id, (b) decision-record ids matching /\b[A-Z]{2,4}-D\d+\b/ defined as bold headings (**XX-Dn**) and cited elsewhere, and parse REGISTRY.md §1 rows. Checks, each printed as PASS/FAIL with file:line: duplicate function ids across specs; function ids present in a spec catalog but absent from REGISTRY.md and vice versa; decision ids cited in any spec/registry that are defined nowhere (dangling); spec status line vs registry §4.5 status table mismatch; shortcut keys claimed by more than one owner in registry §2 (same key, different owner, no "scoped" marker). Exit non-zero on any FAIL; `--json` prints a machine-readable report; `--fix-registry-status` is NOT implemented (read-only tool). Add node --test unit tests with small fixture strings for each check. Add a package.json script `registry:lint` at the root. Run it on the real repo, record the real output (expect some FAILs — that is information, do not "fix" specs), and write docs/builder-program/evidence/I-07-registry-lint-2026-09-02.md with the run output, what the tool proves and what it cannot (semantic contradictions still need review), and gate status (G-INFRA-REGISTRY-LINT = the tool runs on the corpus and its self-tests pass). Final message: files, real-run findings summary, not verified.
