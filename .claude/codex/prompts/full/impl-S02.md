Work package S-02 — shared theme and base controls (minimum substrate for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md` first, then in `docs/builder-program/specs/ui-platform/ui-platform.md`: §1 component inventory, decision records UIP-D7 (including the 2026-09-02 revision note), UIP-D12, UIP-D14, and §3.6 gesture map; in `docs/builder-program/specs/select-edit/select-edit.md`: SE-D18; `docs/DESIGN-SYSTEM.md` "Shared controls" and "Input consistency"; `packages/@himmelcad/ui/src/Select.tsx` (the in-repo pattern to extend), `packages/@himmelcad/ui/src/escapeLadder.ts` (landed UIP-D14 dispatcher — consume it, do not reimplement), `packages/@himmelcad/theme/src/*`.

Scope (UIP-D12 build order, all in `packages/@himmelcad/ui` and `packages/@himmelcad/theme`, tokens only, semantic HTML, keyboard + screen-reader behavior):
1. `Menu` / `ContextMenu` (WAI-ARIA menu pattern: roving focus, ArrowUp/Down, Home/End, typeahead optional, Escape registers UIP-D14 rung 3 via `registerEscapeRung('menu', …)`, closes on outside click and on item activation; submenu optional — not required for 0.5).
2. `Button` (variants primary/secondary/quiet/danger, sizes, `aria-pressed` for toggles, loading state with `Spinner`).
3. `NumberInput` — the ONE commit/revert implementation (DESIGN-SYSTEM "Input consistency": Escape reverts to the last committed value and keeps focus, the revert must not be committed by the resulting blur — use `escapeFreeTextProps`/`revertEscapeField`/`consumeEscapeBlurCommitSuppression` from escapeLadder.ts; Enter commits; blur commits unless suppressed; unit suffix; step/precision; invalid state with message).
4. `Toast` (+ region with `aria-live=polite`, error `assertive`, auto-dismiss tunable, action slot) and `Spinner` (`role=status`, delayed appearance ≥ 300 ms so instant actions never flash — DESIGN-SYSTEM "Progress").
5. `Tooltip` (hover + focus, delay, `aria-describedby`, never on disabled-only content).
6. `Slider` (`role=slider`, keyboard steps, value text).
7. `Dialog` (modal, focus trap, initial focus, Escape registers rung 5 'modal' and traps, restores focus on close).
8. `ProgressBar` promoted from ImportChat to a top-level export (keep ImportChat working).
9. z-layer token scale in `@himmelcad/theme` replacing the hardcoded 10050/1000 pair (`Select.tsx:100–111`, `FloatingTaskIsland.module.css:1–9`); migrate those two usages.
10. SE-D18 axis tokens in `@himmelcad/theme`: `--hc-axis-x/y/z`, `--hc-axis-hover-outline`, `--hc-axis-active-outline`, explicit light and dark values, ≥ 3:1 contrast against the theme's viewport background (record the measured ratios in the evidence).
11. Ribbon tablist accessibility (queued finding): `Ribbon.tsx` implements the WAI-ARIA tabs pattern — roving tabindex (active 0, others −1), ArrowLeft/ArrowRight move and activate, Home/End; register the arrow keys in the §3.6 gesture map only if that map is a code artifact (if it is spec-only, note it in the evidence for the architect).
12. Builder obligation from the UIP-D7 revision: enable `closeFunctionTabs`/`onCloseFunction` on Builder's FunctionPanel usage (`apps/builder/...`), wired to `closeFunction` in `useLayoutStore.ts`. Do NOT edit `FunctionPanel.tsx` itself — the PhotoLab lane owns a pending fix there.

Out of scope: any other component migration (UIP-D12: raw controls migrate as their surfaces are touched), apps/photolab, apps/weltview, Rust, tsconfigs (I-03b is separate — if you find the tree mid-migration, run package-level `pnpm --filter … typecheck` instead of the root script).

Gates (`G-UIP-1` component suite subset): each component has a unit/interaction test (`packages/@himmelcad/ui/test/*.test.tsx`) covering keyboard behavior and ARIA roles; axe-core pass on a story/fixture page per component if the existing a11y audit script (`scripts/lib/a11y-audit.mjs`) can be pointed at it — otherwise a jsdom axe check in tests; `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/theme test` (if exists), `--filter @himmelcad/builder typecheck` and `--filter @himmelcad/photolab typecheck` pass; no new raw browser controls; no one-off colors (grep for `#` hex literals in the new CSS modules must be empty). Evidence file `docs/builder-program/evidence/S-02-base-controls-2026-09-02.md`: component list with file paths, test counts, the a11y result per component, the axis-token contrast ratios, and any UIP-D12 deviation with a decision record. Budget: medium effort, one pass; if a component cannot meet its gate, leave it out cleanly and say which.
