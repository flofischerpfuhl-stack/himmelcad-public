Work package S-05 — jobs substrate (minimum for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md` first, then `docs/builder-program/specs/ui-platform/ui-platform.md` decision records UIP-D10, UIP-D11 and the §2.4 progress/cancel section, `docs/DESIGN-SYSTEM.md` "Progress, cancellation, and feedback", `docs/FUNCTION-CONTRACT.md` D1 (performance classes, the >1 s rule: loading state + cancel), and the existing code: Builder's import island background path, the sidecar `progressKey` lines, `apps/builder/electron/*` main-process files, PhotoLab's job chip/registry (`apps/photolab/renderer/src/jobsChip*.ts` — READ ONLY, the other lane is editing it; do not modify PhotoLab), `packages/@himmelcad/ui` shared controls (ProgressBar, Toast, Spinner from S-02).

Deliverable: the job registry of record in Builder's Electron main process per UIP-D10, with a renderer mirror over the bridge that rehydrates on mount (chip, progress, cancel reappear and work after a renderer reload), sidecar progressKey mapping, needs-input jobs, the registration threshold + debounce (X6 tunables with values: register when expected or elapsed > 1 s; chip appears after 300 ms; a job finishing < 1 s after registration completes with a toast and no chip flicker), UIP-D11 cancellation contract (declared cancellable or a named reason; cancel between bounded units; "cancelling at next safe boundary" state), the surface chain status-bar chip → jobs island → toasts → console, and P11 rows `jobs.list/get/cancel/respond` with the S-01 envelope. Put the shared, product-neutral parts (registry types, mirror protocol, chip/island components) in `packages/@himmelcad/app` and `packages/@himmelcad/ui` so PhotoLab can adopt them later without a rewrite — but do not wire PhotoLab now.

Visual brief (G17): status-bar chip = 24 px tall pill in the StatusBar right slot: 14 px spinner or a 60×4 px ProgressBar, then label (UI 12 px, e.g. "Importing scan_01.laz · 42 %"), then a count badge when > 1 job ("+2"); click opens the jobs island. Jobs island = detached-capable island 360 px wide, title "Jobs", one row per job: 20 px status glyph, label, phase text muted, right-aligned ProgressBar 120×4 px or "waiting for input", cancel icon-button 24 px (disabled with tooltip naming the reason when not cancellable), needs-input rows show a primary "Respond" Button; completed jobs stay listed 30 s (tunable) then collapse into "3 finished" with a clear action. Toast per completion/failure/cancel using the S-02 Toast kinds; console lines per DESIGN-SYSTEM (start, completion, duration, degraded fallback, failure). Provide a gallery section "Jobs surfaces" with chip states (single, multi, needs-input) and the island (three rows: running, waiting, failed) and regenerate the shots.

Gates: `G-UIP-JOBS` — main-process registry unit tests (register/progress/complete/cancel/needs-input/threshold/debounce); reload test — an Electron-level or mocked-bridge test proving a running job's chip and cancel reappear after renderer remount; three-real-import gate — import three real fixtures back-to-back with a renderer reload in between, progress and cancel work (reuse the existing Builder e2e harness if present; otherwise a bridge-level integration test and say so); cancel never publishes partial canonical results (test with the import path's cancel); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` (a failure only inside apps/photolab/renderer/src/jobsChip* is the other lane's in-flight work — report, do not touch). Evidence `docs/builder-program/evidence/S-05-jobs-2026-09-04.md` with file:line per decision, tunables with values, test names, gallery shot paths.

Concurrency: S-02b edits Button/Dialog/Menu/Toast and their fixtures; S-04 edits the selection store and Properties — do not touch those. Budget: medium effort, one pass.
