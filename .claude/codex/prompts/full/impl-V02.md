Work package V-02 — prepared visible frontier (Viewer Core). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/view/viewer-core-addendum.md` VC-D2 (sampled working representations backed by raw authority), VC-D3 (hard budgets, fair datasets), VC-D8 (async decode/residency), §1.2 continuity rules, §4 gates `G-VC-REAL-100M` and the V-02 row in §5; `docs/builder-program/dossiers/viewer-performance-2026-09-04.md` §2 (technique inventory), §4.1/§4.2 (what the kernel has and lacks, with file:line), §5 (the prepared 104 M-point Potree dataset under `.build/perf/viewer-baseline-datasets/`); `docs/adr/0003-pointcloud-streaming.md`; `docs/DECISION-DOCTRINE.md` X2, P2, P4; the V-01 evidence (`docs/builder-program/evidence/V-01-measure-2026-09-04.md`, measurement rings and reason codes you must feed); and the code: `crates/himmelcad-render/src/{streaming.rs,tile_selector.rs,residency.rs,scheduler.rs,decode_limits.rs,streaming_decode_artifact.rs}`, `packages/@himmelcad/viewer/src/kernel/{KernelStreamingDriver.ts,KernelPotreeDatasetAdmission.ts,KernelViewerSession.ts}`, the Potree converter path used by `scripts/perf/viewer-baseline.mjs`.

Deliverable (VC-D2/VC-D3/VC-D8): (1) a generalized bake metadata contract for prepared point datasets — per-node screen-space-error, point spacing, sample statistics, station preservation (a station-based scan keeps its station id per node so station views stay possible), content hash, and a compatibility path for existing ADR 0003 Potree 2 hierarchies (they load unchanged; missing fields are computed lazily and cached, never guessed); (2) frontier selection under hard budgets: per-frame point/byte/draw budgets by hardware class (values from the addendum §1.1, tunable), visibility-ordered traversal (front-to-back, projected-error priority from the viewport center outward), a resident coarse fallback that is never evicted while its children are missing (§1.2 continuity), fair admission across multiple datasets; (3) decode/upload debt accounting feeding V-01's reason codes (`budget:points`, `budget:bytes`, `decode:backlog`, `upload:backlog`) so the HUD explains why density is what it is; (4) the real-fixture loader path so `scripts/perf/viewer-baseline.mjs` streams the prepared 104 M-point dataset through this frontier.

Gates: `G-VC-REAL-100M` — the prepared 104 M-point dataset loads through the new frontier in Builder; with the machine otherwise idle, an orbit over it keeps the selected frontier within budget every frame (assert from the V-01 rings: zero frames over the point budget, reason codes present) and no blank-tile frames (continuity test: children replace parents only when all needed children are resident — unit test on the selector with a synthetic hierarchy); Potree compatibility test (existing fixtures load with identical node sets before/after); fairness test (two datasets share the budget proportionally to projected error, not arrival order); `cargo test -p himmelcad-render` in target/builder (CARGO_TARGET_DIR is exported — never change it; run cargo only for this crate and only once the design is settled; the machine is shared with two other Codex lanes and a PhotoLab sidecar diagnostic), `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, wasm staging. Do NOT run the timed baseline yourself (the architect runs it on a quiet machine); instead print the frontier statistics of one orbit into the evidence. Evidence `docs/builder-program/evidence/V-02-frontier-2026-09-05.md`: decisions in doctrine format, budgets with values and class, before/after node-set diffs, test names. Registry: no new user-facing rows expected; if a display property becomes user-visible, add it under `owner: view-domain`. Concurrency: S-08 edits ViewState/HUD in `packages/@himmelcad/app` and Builder's viewport component; P-01 edits Builder project paths and the sidecar project store — do not touch those. Budget: high effort, one pass.
