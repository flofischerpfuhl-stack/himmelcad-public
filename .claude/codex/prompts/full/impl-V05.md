Work package V-05 — point/splat quality tiers and the renderer payloads the 0.5 UI still draws as DOM overlays (Viewer Core). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/view/viewer-core-addendum.md` VC-D6 (adaptive, bounded point quality subordinate to interaction), VC-D9 (compatible batching preserves entity semantics), §4 gates `G-VC-LOD-CONTINUITY` and the V-05 row in §5, the dossier §2 (adaptive point size from LOD spacing, EDL, splats) and §4.2 (shader gaps with file:line), V-02/V-03 evidence (frontier metadata incl. per-node spacing; lane budgets; governor tiers), the S-10 evidence (selection visual policy — direction arrows, point squares, support-role geometry, labels are DOM overlays pending V-05), the 0.5-08 evidence (measurement label chips and anchor squares as DOM overlays pending V-05), `docs/DESIGN-SYSTEM.md` tokens (selection orange, support blue), and the shaders `crates/himmelcad-render/src/shaders/mixed.wgsl` + `gpu_frame.rs` draw paths.

Deliverable: (1) adaptive point diameter per node from the V-02 spacing metadata (screen-space size = spacing × pixels-per-meter × k, clamped 1–8 px, tunable) with a per-cloud user offset (the D-02 point-size Slider becomes a per-entity multiplier — implement the per-entity renderer API that D-02 noted as missing), (2) EDL (eye-dome lighting) as a governor-tiered effect (tier off during motion on class I, on at rest), (3) bounded splats for the Gaussian-splat cloud kind if the provider exists (else leave a typed seam and say so), (4) the vector/overlay payloads: line direction glyph at the end of a selected polyline, selection point squares, support-role points/lines in support-blue, anchor squares and label chips (text quads via the existing text pipeline) so S-10's selection visuals and 0.5-08's measurement graphics move from DOM overlays into the frame (protected lane 2/3 per V-03), with the DOM fallback removed once the renderer path passes its tests; (5) effect telemetry into the V-01 rings (reason codes `effect:edl`, `quality:tier`).

Gates: `G-VC-LOD-CONTINUITY` — replacing a node's LOD changes the represented surface by ≤ 2 px and ≤ 12 % of viewport pixels per frame (§1.2; image-difference test on the fixture scene), no popping beyond threshold across an orbit; point-size identity test (adaptive diameter deterministic per spacing/zoom); per-entity size test (two clouds render with different multipliers in one frame); overlay payload tests: glyph/square/label positions match the DOM overlay positions they replace (pixel tolerance 1 px on a fixture scene), selection visual policy tests from S-10 and measurement tests from 0.5-08 keep passing with the renderer path; `cargo test -p himmelcad-render` once in target/builder (CARGO_TARGET_DIR exported; check `free -g` first); `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; wasm staging. Do not run the timed baseline (architect). Evidence `docs/builder-program/evidence/V-05-quality-tiers-payloads-2026-09-08.md` with doctrine-format decisions, tunables, and before/after shots from the gallery viewport mock or the harness screenshot path. Concurrency: 0.5-01 (viewing box kernel/island), 0.5-02a (fence path), 0.5-03 (sampling) — consume their APIs, never edit their files; in the render crate you own shaders, gpu_frame draw paths and the point pipeline; do not touch scheduler.rs/hardware_policy.rs (V-03 landed — consume the tier API). Budget: high effort, one pass.


Resume notice (2026-09-08 23:00): the previous run of this brief died at ~22:50 when the root filesystem hit 0 bytes (another lane copied 47 GB into /tmp; fixed). Its partial edits are in the working tree (typecheck green, no truncated files found). Run `git status --short` and `git diff --stat` first, keep what is sound, and finish the deliverable; do not start over. Additional gate since then: the theme package has `pnpm --filter @himmelcad/theme test` (token lint) — text colour must use the *-fg status tokens (your panel CSS currently uses `color: var(--hc-error)` for text; fix it). Run each gate at most three times; hard budget two hours; never copy the repository or datasets anywhere.
