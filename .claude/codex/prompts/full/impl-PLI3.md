Work package PL-I3 (PhotoLab, architect takeover) — close WP-G2: the PhotoLab console adapter consumes the generated command table. Lane rules: CARGO_TARGET_DIR=target/photolab, cargo -j 4; never copy the repository or datasets; never touch DISPLAY=:0 (UI only via scripts/ui-test-display.sh); no COLMAP/ALIKED/MVS runs; do not commit; English sentence-case UI copy (node scripts/check-photolab-english-ui.mjs); each gate at most three times. Read `docs/builder-program/evidence/PL-I2-photolab-automation-sdk-2026-09-19.md` (WP-G2 remains partial "because the PhotoLab console adapter is still hand-listed"), the WP-G2 section and acceptance text in `docs/implementation-plans/2026-09-photolab-release-polish.md`, `docs/photolab-automation-command-rows.md`, `packages/@himmelcad/app/src/generated/commandTable.ts` + `scripts/generate-command-table.mjs`, the PhotoLab console adapter in `apps/photolab/renderer/src` (find the hand-listed command map), Builder's console adapter as the reference for table consumption, and `docs/DECISION-DOCTRINE.md` P11. Deliverable: the PhotoLab console derives its commands (names, arguments, help text, job-or-transaction mapping, cancel routes) from the generated table filtered by `products: photolab`; the hand list is deleted; `help` lists exactly the table rows; a test fails when a PhotoLab row has no console exposure or the console knows a command the table lacks; console behaviour for existing commands is unchanged (snapshot of `help` before/after in the evidence; differences must be only additions of rows that were missing). Visible-UI proof through the private display: open the PhotoLab console, run `help` and two commands (one query, one cancellable job start + cancel on a tiny synthetic project), CDP screenshots under `.build/pl-i3/`. Gates: pnpm --filter @himmelcad/photolab typecheck + test, --filter @himmelcad/app test, G-1 coverage test, English UI check. Update the WP-G2 checklist line and status row. Evidence `docs/builder-program/evidence/PL-I3-photolab-console-table-2026-09-19.md`. Budget: high effort, one pass, 90 minutes.
