Work package R-02d — the five P0 items of the third review, plus the end-to-end replay, in one run. Read `.claude/codex/prompts/_impl_brief.md` (definition of done: literal Builder UI on the road scan, screenshots), then `docs/builder-program/evidence/R-01c-demanding-user-review-2026-09-10.md` in full (§2 steps and screenshots under `.build/r01c/`, §3 diff, §4–5 findings, §7 list items 1–5) and `R-02c-spine-fixes-2026-09-10.md` (what works, where the replay stopped: DGM publication "canonical representation contract is invalid", height grid not resident in the viewer bootstrap, fence clicks admit zero vertices), the specs the findings cite (file-project FP-D* for New/Open isolation, draw DR-D* for boundary close / breakline finish atomicity, mesh-terrain MT-D* for the surface publication contract and staged providers, pointcloud PC-D* for the fence, view VB-D* for the box lifecycle), and the code paths named in the evidence.

Fix, each with the reviewer's reproduction as acceptance on the road scan through the literal UI: (P0-1) **New / project replacement isolation** — prior project's boxes, selection and residency never leak into the new project; add a one-process visible gate: New → road import → draw → save/reopen in one Builder process. (P0-2) **First publication visibly real** — after import commit + Frame All / Zoom to, the road renders immediately without restart; presented-frame evidence (non-background pixels within 2 s of Frame All) and bounded idle GPU work. (P0-3) **Draw publication all-or-none** — exact Boundary Close and Breakline Finish store valid geometry once, or keep a noncanonical draft with a typed recovery; restart reopens either state cleanly. (P0-4) **DGM staged-provider publication** — root-cause "canonical representation contract is invalid" on Create for the checked road subset; the surface must create, reopen, edit and undo; on failure a usable checked draft remains; include the height-grid residency in the viewer bootstrap (`hcad.pointcloud.height-grid@1`) so the rasterize output renders and is accepted as a DGM source. (P0-5) **Fence picks** — viewport clicks in the fence tool must admit vertices (arbiter/pick path); accepted/rejected picks visible; closed polygon/rectangle fences drive Keep/Remove.

Then the end-to-end replay from the reviewer's start state, scan-first and boundary-first: named LAS → visible box lock → ground → fence segment → sample/raster → boundary/breakline → checked/fixed/edited DGM → LandXML/DXF export with a reopened-file verification; every step screenshot under `.build/r02d/`; where a spine still stops, record the exact step and reason. Gates: `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme + registry lint, `cargo test -p himmelcad-core mesh_surface` and `-p himmelcad-sidecar mesh` once, `cargo check -p himmelcad-sidecar --tests --bins` before you report. Evidence `docs/builder-program/evidence/R-02d-p0-spine-2026-09-11.md` (finding → root cause → fix → test → screenshot; spine ledger). Rules: never copy the repository or datasets; each gate at most three times; three-hour budget; do not commit; do not edit PhotoLab-owned paths; cargo `-j 4` in target/builder; memory cap applies. Budget: high effort, one pass.
