Task: small fix to scripts/registry-lint.mjs (node ESM, tests in the same dir per I-07). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. Do not commit. Read scripts/registry-lint.mjs, its test file, docs/builder-program/evidence/I-07-registry-lint-2026-09-02.md, and these heading samples: docs/builder-program/specs/civil/civil.md line 612 (`### 5.1 Gesture reconciliation (CIV-D1)`), docs/builder-program/specs/mesh-terrain/mesh-terrain.md line 1226 (`### 10.1 MT-D25 — …`) and line 1319 (`**MT-D25 — …**`).

Problem: the dangling-decision-id check only treats `**XX-Dn …**` bold headings as definitions, so decision records defined as markdown headings (`### … (XX-Dn)`, `### … XX-Dn — …`, `#### XX-Dn`) are reported as dangling (false positives for every CIV-D* id). Fix: a decision id counts as defined if it appears in a bold run at line start OR in any markdown heading line (`#`-prefixed) in the same spec directory tree, in either `XX-Dn —` or `(XX-Dn)` form; keep citations elsewhere as citations. Add unit tests for the three heading forms. Then add a `--summary` flag printing one line per check with the count of findings, and make the default output group findings by check with a count header. Re-run on the corpus, update the evidence file with the new real output (counts per check), and run prettier on the evidence file. Final message: counts per check before/after the fix.
