Work package S-10 (with S-B2-P9/G5–G11) — the Release 0.5 interaction subset. Read `.claude/codex/prompts/_impl_brief.md`, then: `docs/DECISION-DOCTRINE.md` P9 (interaction state per node: hidden / reference / editable / inert; parents propagate, mixed shows grey; global toggles never destroy per-element choices), `docs/DESIGN-SYSTEM.md` "Visual language" (selection orange with line direction and anchor-only highlight for symbol points; support geometry blue), `docs/builder-program/OWNER-STATEMENTS-2026-09-02.md` S2/S3/S5 (Trimble-Access-like selection look, bottom bar, tri-state layers with Ctrl+A/Ctrl+click) and generators G5–G11, `docs/builder-program/specs/select-edit/select-edit.md` SE-D18 (axis tokens — landed in S-02), SE-D19 (stable segment locator — schema landed in S-01, store hooks in S-04), the selection-visual and support-role records, `docs/builder-program/specs/draw/draw.md` DR-D18 (support role), DR-D21 (point acquisition), the construction input bar records (tri-modal input C1: click / constrain / type; Tab traverses the bar, Up/Down cycles candidates only with a live indicator), `docs/builder-program/specs/ui-platform/ui-platform.md` UIP-D16, the bottom-bar records (support-points toggle, explode polylines, 3D/2.5D/2D, selectable element kinds, labels), the landed S-03 arbiter, S-04 selection store, S-08 display stream (partial — check the S-08 evidence for what exists; do not depend on unlanded S-08b parts, but leave clean seams), and the viewer kernel's selection/highlight rendering path.

Deliverable, only what the 0.5 tools (viewing box, ground extraction, sampling, line drawing, DGM create/edit, measurement basics) consume:
1. P9 tree/layer states: the EntityTree and Layers panel expose the four-state model with tri-state checkboxes (P9 propagation, mixed grey), Ctrl+A / Ctrl+click on a checkbox (all / toggle-one), global toggles as defaults that never overwrite per-element choices; the visible/reference/editable set feeds picking and snapping (P4).
2. Selection visuals in the viewport: selected geometry orange with direction indication on lines (arrow glyph at the end, size tunable), orange square markers on selected points, anchor-only highlight for symbol-bearing points, blue support geometry (defining points/lines of higher-order entities, helper points) shown when the bottom-bar "support points" toggle is on; hover restyle only for pickable classes (UIP-D15).
3. Segments: SE-D19 curve-subentity references drive segment picking (click on a polyline segment selects the segment as a stable locator; "explode polylines" bottom-bar mode makes segments the pickable unit); remap-or-prune on source edits.
4. Tri-modal input bar (C1): the shared construction bar component (docked bottom of the viewport, detachable per DESIGN-SYSTEM) with fields declared by the armed tool; click / constrain (direction + angle snap + length) / typed absolute values are equivalent; Tab/Shift+Tab traverse fields without touching the viewport cursor; Enter commits, Escape reverts a field (S-02 NumberInput semantics); the S-03 arbiter routes typing and Tab to it while a coordinate tool is armed; polar values (direction, distance, Δz) shown live.
5. Bottom bar: the existing bottom bar rebuilt per S3 — support-points toggle, explode-polylines toggle, 3D / 2.5D / 2D segmented control (landed in S-05b viewport chrome — reuse), selectable element kinds (a Menu with Checkboxes per kind), labels toggle; every toggle is a display-stream action (P8) with its own undo, never the document journal.

Visual brief (G17): bottom bar 28 px tall on the viewport's lower edge, left cluster = the three toggles as icon buttons 24 px with tooltips and `aria-pressed`, center = the 3D/2.5D/2D segmented control, right = "Kinds ▾" Menu button and the labels toggle; construction bar = a 32 px docked strip above the bottom bar with fields "X", "Y", "Z" / "Dir °", "Dist m", "Δz m" as NumberInputs with unit suffix, the active field with the accent ring, a mono "1 of N" indicator slot at its right end; tree tri-state checkbox = the S-02 Checkbox in indeterminate style (grey dash). Gallery sections "Bottom bar", "Construction bar", "Tree states", "Selection visuals" (a viewport mock with an orange polyline with direction arrow, orange point square, blue support points) — regenerate shots serially.

Gates: `G-B2-P9-TREE` (propagation, mixed, Ctrl+A/Ctrl+click, global-toggle non-destruction, visible-set feeds picking), `G-B2-SELECTION-VISUAL` (token-only colors, direction glyph present, support geometry only with the toggle, hover restyle never on clouds), `G-B2-SEGMENTS` (segment pick → locator; remap after a vertex edit; prune after a deletion with a typed reason), `G-B2-INPUT` (click/constrain/type equivalence to 1e-6 m on a fixed fixture; Tab never cycles candidates; Up/Down cycles only with the indicator; Escape reverts the field, second Escape cancels the tool through the ladder), `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` (PhotoLab must keep today's behavior — no bar unless a tool arms it), registry rows + linter green. Evidence `docs/builder-program/evidence/S-10-interaction-subset-2026-09-06.md`. Concurrency: S-08b edits ViewState/bookmarks/display stream internals and the HUD — coordinate through the display-stream API only, do not edit its files; V-03 edits the render scheduler — do not touch the render crate. Budget: high effort, one pass.
