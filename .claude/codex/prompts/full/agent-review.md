You are performing an adversarial specification review for the Himmel:CAD Builder completion program. Repo root: /home/oem/Dokumente/003_Projekte/10_himmelcad. Documentation language: English.

FIRST read /home/oem/Dokumente/003_Projekte/10_himmelcad/.claude/agents/demanding-user.md and fully adopt that persona, procedure, and output format (numbered findings most-severe-first with severity / contract question / objection / proposed resolution; then (a) convincingly answered questions, (b) executed vs read, (c) owner-decision items with target count ZERO per the escalation protocol; then a 'system feedback' note on whether any contract question or doctrine axiom failed to do its job).

Then read the CURRENT normative rules: docs/FUNCTION-CONTRACT.md (all rules — evidence precedes spec, dossier-wide absence checks, code claims file:line with stubs-not-existing, per-dossier-row catalog dispositions, extreme-class-member, input-gesture arbitration, E1 in-repo artifact, E2 passive-consumer enumeration, A3 verified sibling semantics, C4 restore scope), docs/DECISION-DOCTRINE.md (X1-X7, precedents P1-P6), docs/builder-program/README.md (registry rules), docs/builder-program/OWNER-DECISIONS.md, docs/DESIGN-SYSTEM.md, docs/builder-program/REGISTRY.md (obligations registered to this domain — verify each is honored). Read the gold-standard spec docs/builder-program/specs/view/viewing-box.md and at least two prior review files under docs/builder-program/specs/*/ to calibrate on known failure patterns.

Static review: read any repo code for grounding (verify the spec's file:line citations — a wrong or stub citation is a finding), do not run builds or the app; web research allowed. Verify every A2 dossier citation against the dossier text. Check cross-domain consistency against the sibling specs the target cites (cite-and-revise rule: a spec must not re-disposition a capability another spec owns). Hold the zero-escalation standard: derive resolutions from the axioms/precedents/design system; report derived decisions as vetoable decisions, not questions.

Write the full review to the output file named below (this is repo-resident evidence; use the document-class header "Document class: report/verification evidence"). Final message: the review's headline counts (blockers/major/minor/idea), the top three findings in one line each, owner-decision count, and system feedback.

Review target: docs/builder-program/specs/agent/agent.md (embedded AI agent: skill system with on-demand loading, documentation surface, io.*/registration.* exposure under ADR 0024 grants, chat UX as persistent workspace island, session persistence, entity references, agent attribution, multi-harness settings). Output file: docs/builder-program/specs/agent/agent-spec-review-2026-09-02.md. Also read docs/adr/0024-automation-sdk-and-agent-trust-boundary.md and ground against packages/@himmelcad/agent/src/, packages/@himmelcad/automation-host/electron.cjs, crates/himmelcad-sidecar/src/automation_runtime.rs, sdk/python/ (verify file:line claims; a stub or placeholder cited as existing is a finding).

Sibling specs to check: specs/ui-platform/ui-platform.md (UIP-D14 Escape exemption for persistent islands, UIP-D10 jobs), specs/view/viewing-box.md (the P1 "restore the last viewing box" path — verify the walked path uses commands that spec actually defines), specs/file-project/file-project.md (io export grants, FP-D5 loss disclosure via automation), specs/import-formats/import-formats.md IF it exists yet (being written concurrently; the agent spec cites IF-D12 — if absent, report as pending cross-reference, not a blocker), specs/select-edit/select-edit.md and specs/measure-inspect (automation parity claims).

Extra attention as the owner's own daily use case (he wants to tell the agent "restore the last clipping box" and have it work without context spam): (1) walk the restore workflow literally — which skill file gets loaded, how many tokens, what query/command sequence, where a confirmation grant would trigger, what the user sees in journal/console; (2) the skill system's authoring format and discovery — is it genuinely on-demand or is the index itself context spam at scale (200 skills)? (3) trust boundary: import commits and exports need grants — but does the spec make routine safe operations (queries, view changes, bookmarks) grant-free, or does it nag? ADR 0024 line-level check; (4) session persistence across restarts and provider-credential handling (security: never journaled, never in .hcadx); (5) X3 both ways — agent actions attributed and undoable by the user; can the user undo an agent's 40-command batch as one step? (6) the documentation surface: is per-domain capability doc derived from builder-program specs specified as a build artifact with a staleness gate, or hand-written (drift)?
