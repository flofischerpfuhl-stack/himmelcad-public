Work package 0.5-04 — draw line/polyline with snapping and the construction input bar (Draw D-01 subset). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/draw/draw.md` §1–§3 and DR-D1–DR-D8 (line/polyline creation, live preview, snapping, closing, boundary polygons), DR-D16, DR-D18 (support role), DR-D21 (point acquisition provenance — admitted in S-01), the construction-bar and tri-modal input records (owner statements S-batch-2: two clicks / one click + direction + angle snap + length / absolute height or slope; live preview; polar values in the panel; Tab switches fields without moving the cursor; Up/Down candidate cycling only with a live indicator), `docs/builder-program/MASTER-PLAN.md` §5 row 19 (gates `G-DR-INPUT`, `G-B2-INPUT`, `G-B2-GESTURE-C1`; real scanned-street breakline and closed-boundary flow with click/constrain/type equivalence), the landed S-03 arbiter, S-10 construction bar (`InteractionBars.tsx`, `constructionInput.ts` — extend, do not fork), S-04 selection/candidates, S-08 ViewState, 0.5-08 measurement tool (the arming/snapping/commit pattern to reuse), V-05 renderer payloads (selection orange with direction arrow, support-blue helper geometry — use the renderer path, no DOM overlays), the existing polyline entity and snapping kinds in the viewer kernel, and the prepared 104 M-point fixture (a scanned road: draw a breakline along a curb).

Deliverable through visible UI: Draw ▸ Line / Polyline / Boundary polygon: arm through the arbiter; first vertex by click (snapped: point, cloud point via candidate cycling, line end/mid, intersection, perpendicular — the existing snap kinds plus a cloud-point snap), subsequent vertices by click, by constraint (direction with angle snap at 0/45/90° and typed length; absolute Z or slope %), or by typed absolute X/Y/Z in the construction bar (Tab traverses fields, never moves the cursor; Enter commits; Escape reverts the field, second Escape cancels the tool via the ladder); live preview of the segment with polar readout (direction, distance, Δz) in the bar; Enter/double-click ends a polyline, clicking the first vertex closes a boundary; each commit is one journal transaction (undo removes the last vertex, then the entity); point-acquisition provenance stored per vertex (DR-D21); the new polyline carries support-role geometry (defining points) rendered support-blue when the bottom-bar toggle is on; role choice "breakline" / "boundary" / "plain" in the panel feeds the Mesh source roles (0.5-05). P11 rows `draw.line/polyline/boundary` + `draw.vertex.add/constrain/type/undo` with the S-01 envelope; automation parity (typed vertex lists).

Visual brief (G17): the docked "Draw" panel shows the role Select, a snap-kind toggle row (icon buttons 24 px with `aria-pressed`), the live polar readout in mono 14 px, and a vertex list (mono, index · X Y Z · acquisition glyph: pick / typed / constrained); construction bar per S-10 with the active field ring and "1 of N" indicator; preview segment = accent 1.5 px with a 6 px rubber-band vertex square; committed polyline = selection orange with the direction arrow while selected (V-05 payload). Gallery section "Draw panel" (idle, drawing with readout, closed boundary); serial regeneration.

Gates: `G-DR-INPUT` and `G-B2-INPUT` — click / constrain / type produce identical coordinates to 1e-6 m on a fixture; `G-B2-GESTURE-C1` — Tab never cycles candidates, Up/Down only with the indicator, Escape ladder order; real-data flow: a breakline along the curb of the scanned road and a closed boundary around a fixture area, both journaled, undone and redone, reopened from the journal; snapping latency ≤ 4 ms p95 (measure with the V-01 rings, state machine load); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, theme lint, registry rows `owner: draw` + linter green. Evidence `docs/builder-program/evidence/0.5-04-draw-lines-2026-09-09.md`. Rules: never copy the repository or datasets; run each gate at most three times; two-hour budget; do not commit. Concurrency: 0.5-05 edits the Mesh domain (DGM window) and V-06 edits the camera controller — consume, do not edit their files. Budget: high effort, one pass.
