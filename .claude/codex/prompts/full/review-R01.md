Work package R-01 — demanding-user workflow review of the Release 0.5 spine through visible UI on real data. Read `.claude/agents/demanding-user.md` (you play this persona), `.claude/codex/prompts/_review_common.md`, `docs/builder-program/MASTER-PLAN.md` §0a (Release 0.5 "DGM aus Scan": import → fluid view + HUD → viewing box with lock → ground extraction → sampling/rasterize → lines with snapping (breaklines, boundary, tri-modal input) → DGM window with error fixing → DGM editing (region smoothing, downsampling) → DXF/LandXML export + measurement basics) and §6 M-0.5 ("usable at starter level" = the owner and 2–3 pilot offices complete the full workflow end to end through visible UI on real scan datasets without implementation assistance), the owner's daily-work statement (90 % of the owner's RealWorks time: lines, ground extraction, DGM create, DGM edit), the landed evidence files under `docs/builder-program/evidence/` for 0.5-01…0.5-08, 0.5-02a, D-02, P-01, S-*, V-05, V-06, and the specs they cite.

Method: drive Builder as a user, not as a developer — start Builder (`pnpm --filter @himmelcad/builder dev` on DISPLAY=:0; the V-01b harness path shows how to attach via CDP for screenshots), import the real road scan `libs/polyshapev01/dist/PW_GHT_251215_Orscholz_Deponie-1-1.las` (103.7 M points; the prepared hierarchy exists under `.build/perf/viewer-baseline-datasets/`), and complete the whole spine once, in order, using only the ribbon, panels, viewport, bottom bar, construction bar, context menus and keyboard — no console, no automation, no code. At every step take a screenshot (`.build/r01/<step>.png`) and record: what you tried, what happened, the exact copy shown, time to complete, and every moment you had to guess, wait without feedback, undo, or read a doc. Compare each step against the specification's promised workflow and against what a Trimble RealWorks / RIB Civil user would expect (the dossiers under `docs/builder-program/dossiers/`). Then run the same spine a second time as a pilot-office user with a different intent (boundary-first: draw the site boundary, box-lock, extract ground, DGM, export LandXML).

Report, in `docs/builder-program/evidence/R-01-demanding-user-review-2026-09-09.md`: (1) a step table (step · works through UI? yes/partial/no · time · screenshot · friction); (2) findings as the persona would raise them — blockers (spine cannot be completed through UI), defects (wrong/missing behavior vs spec), friction (guessing, hidden state, missing feedback, copy that lies), and the three things a RealWorks user would miss most — each with severity, the spec record it violates or the spec gap, and a proposed resolution (fix in code / fix the spec / accept); (3) the honest verdict against the M-0.5 "usable at starter level" definition; (4) a prioritized fix list for R-02 (bounded, one line per item). Machine: check `uptime`/`free -g` first and record them; if another heavy process runs (PhotoLab e2e, Q-01), stop and report NOT IDLE — this review measures the product, not the machine, but a loaded machine falsifies the feel. Do not change product code; do not commit. Budget: high effort, one pass, four-hour hard stop.
