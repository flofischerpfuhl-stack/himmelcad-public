Work package 0.5-05 — DGM creation window (Mesh/Terrain D-05 creation/check subset). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/mesh-terrain/mesh-terrain.md` MT-D1–MT-D12 (surface creation from clouds/points/lines, source roles boundary/breakline/form-line/exclusion, draft rules, error list with fixes, auto boundary + 2D crop polyline, one atomic publish), MT-D17 (resource/cancel budgets for 500 M logical points), MT-D25/MT-D26 (recipe, mean-height grid source), the owner statements on the surface-creation workflow (thin cloud, roles, errors, rule exclusions, automatic boundary, popup with error fixing — "surface creation in its own popup with error fixing"), `docs/builder-program/MASTER-PLAN.md` §5 row 20 (gates `G-B2-MESH-DRAFT-RULES`, `G-MT-1`, `G-MT-3`, `G-MT-5`, `G-B2-MESH-RECOVERY`: error list/fixes, constrained breaklines/boundary, one atomic publish, cancel/restart, real DGM flow), ADR 0031 item 6 (recipe + source roles — S-01), the landed 0.5-02 (ground cloud), 0.5-03 (grid_source role), 0.5-02a, S-05 jobs, S-07 durability, V-02 (prepared mesh datasets stream natively), the existing TIN/mesh code in `crates/himmelcad-core` / `himmelcad-io` (LandXML, Brandenburg fixture) and the Mesh entity in the kernel; note 0.5-04 (draw lines) is in flight — consume the existing polyline entity and the source-role component, do not edit draw files.

Deliverable through visible UI: Mesh ▸ Create surface opens the DGM window (a focused task surface per DESIGN-SYSTEM: modal-class island, detachable): source list with roles (ground cloud / sampled cloud / grid as points, polylines as breakline/boundary/form-line/exclusion; each source row shows count and role Select), draft rules (max edge length, thin-cloud spacing, exclusion rules, auto boundary with 2D crop polyline), a "Check" step producing the error list (crossing breaklines, zero-length edges, vertices outside boundary, duplicate points, invented Z per MT rule) where each error has a fix action (drop / snap / split / exclude) applied to the draft, then "Create" as one atomic journaled publish of a TIN mesh entity with recipe (P10) and source roles, baked as a prepared dataset (V-02); cancel/restart at every phase (`G-B2-MESH-RECOVERY`: kill mid-bake → no partial entity, restart resumes from the checkpoint); typed result (triangles, area, Z range, residual vs source points). P11 rows `mesh.create.draft/check/fix/publish` with the S-01 envelope; automation parity.

Visual brief (G17): DGM window 720 × 560 px island with three columns — Sources (rows with glyph, name, count, role Select), Rules (NumberInputs with units, Checkboxes), Check results (error list rows: severity glyph, message, "Fix ▾" Menu with the applicable fixes; a mono summary "12 errors · 9 fixable"); footer: "Check" (secondary), "Create surface" (primary, disabled until the check passes or all errors are fixed/excluded), "Cancel"; during creation the S-05 progress row with phases (triangulate, constrain, validate, bake) and Cancel. Gallery section "DGM window" (sources, errors with fix menu open, creating); serial regeneration.

Gates: `G-B2-MESH-DRAFT-RULES` (thin cloud + roles + exclusions produce the documented TIN on a fixture; each fix action changes exactly the listed errors), `G-MT-1`/`G-MT-3`/`G-MT-5` as the spec defines them (Brandenburg LandXML import → create → compare), `G-B2-MESH-RECOVERY` (kill/restart), real DGM flow: the 0.5-02 ground cloud of the 104 M-point road (sampled via 0.5-03) plus a fixture breakline → DGM in bounded time and memory (state numbers; MT-D17 budget), `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, theme lint, `cargo test -p himmelcad-core mesh` and `-p himmelcad-sidecar mesh` once each in target/builder, registry rows `owner: mesh-terrain` + linter green. Evidence `docs/builder-program/evidence/0.5-05-dgm-window-2026-09-09.md`. Rules: never copy the repository or datasets; each gate at most three times; two-hour budget; do not commit. Budget: high effort, one pass.
