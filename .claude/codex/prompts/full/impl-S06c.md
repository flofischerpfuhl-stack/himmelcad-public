S-06c — shared-substrate regression from S-06, reported by the PhotoLab lane (blocker for its release gate 7). Read `.claude/codex/prompts/_impl_brief.md`, `docs/DECISION-DOCTRINE.md` P11, `docs/builder-program/specs/ui-platform/ui-platform.md` UIP-D5/UIP-D6, `docs/builder-program/evidence/S-06-command-surfaces-2026-09-04.md`, `docs/photolab-automation-command-rows.md` (PhotoLab's P11 rows), the generated command table source (`scripts/generate-command-table.mjs` and its schema/JSON input), `packages/@himmelcad/app/src/commands.ts`, `packages/@himmelcad/ui/src/EntityTree.tsx` (+ `EntityCommandMenu`/`CommandSurfaces.tsx`), and READ ONLY `apps/photolab/renderer/src/*` where `onContextAction`/`handleTreeContextAction` is used (do not edit apps/photolab).

Regression: since S-06 the shared EntityTree renders the command-table menu and its onExecute mapping forwards only rename/hide/show/export/properties/zoom_to; PhotoLab's tree actions 'remove' ("Remove from project…", confirmation dialog "Remove image?") and 'showGcpImages' ("Images containing this GCP") vanished from the UI.

Architect decision (P11, UIP-D6 — one command table, no product-private menu, no ad-hoc `additionalCommands` prop): (1) the generated command table gains product rows with `surface: contextMenu` and an entity-kind predicate — `photolab.images.remove` (kind CameraImage, multi-select allowed, label "Remove from project…", ends in the host's confirmation) and `photolab.gcp.images` (kind GroundControlPoint, label "Images containing this GCP"); rows carry `owner: photolab` and are filtered by the host product id so Builder never shows them; (2) EntityTree/EntityCommandMenu dispatch generically: every menu entry invokes `onExecute(commandId, { entityIds, kind })`; the built-in handling of rename/hide/show/export/properties/zoom_to stays, and any command the tree does not handle itself is forwarded unchanged to the host's `onContextAction(commandId, entityIds)` (keep that prop name and signature so PhotoLab adapts with a one-line mapping from the new ids to its existing handlers); (3) menu entries are derived from the table filtered by product id + entity kind + selection cardinality, in the table's declared order and groups.

Gates: table generation check (`scripts/generate-command-table.mjs --check`) green with the new rows; unit tests: PhotoLab rows appear for a CameraImage node under product 'photolab' and never under 'builder'; unknown ids are forwarded to onContextAction with the selected ids; existing Builder menu content unchanged (snapshot test of the polyline menu); `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; regenerate the gallery "Command surfaces" shots serially (add one row: PhotoLab image node menu). Append "S-06c" to the S-06 evidence with the exact id → label mapping PhotoLab must wire. Do not touch apps/photolab, the render crate or viewer kernel (V-02 in flight) or Builder import paths (D-02 in flight). Budget: medium effort, one focused pass.
