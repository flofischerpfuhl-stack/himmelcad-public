Work package Q-01 — qualification lane: the open timing and real-data gates of the landed 0.5 slices, measured on an idle machine. Read `.claude/codex/prompts/_impl_brief.md`, then the "open" items in these evidence files: `docs/builder-program/evidence/0.5-01-viewing-box-2026-09-06.md` (VB-D7 presented-frame cadence during grip drag, VB-D8 locked-scene parity, the one-shot `scripts/benchmark-builder-viewing-box.mjs`), `0.5-02a-segmentation-2026-09-08.md` (`G-RW-SEGMENT`: 104 M-point segment with interaction inside the presented-frame gate; ≤ 1.1× native-small comparison), `0.5-04-draw-lines-2026-09-09.md` (snap latency ≤ 4 ms p95 via `window.__hcadDrawSnapLatency()`, curb trace on the road fixture), `0.5-05-dgm-window-2026-09-09.md` (`G-MT-3` benchmark target and `G-MT-5`/MT-D17 scale script are ABSENT — create the minimal versions the spec names, then run them; Brandenburg NOT RUN unless a fixture exists), `V-05-quality-tiers-payloads-2026-09-08.md` (LOD continuity ≤ 2 px / ≤ 12 % on the real fixture; EDL cost), `V-06-camera-continuum-2026-09-09.md` (`G-VC-TRANSITION` p95 vs class target), plus `scripts/perf/viewer-baseline.mjs` (class-I baseline via `HIMMELCAD_GPU=nvidia` on the Quadro) and `docs/builder-program/specs/view/viewer-core-addendum.md` §1.1 (class targets) and §3 (protocol).

Procedure, strictly in this order: (1) verify the machine is idle — `uptime` load average < 2.0 over 5 min, no other `codex exec`, no `himmelcad-sidecar` e2e, `free -g` ≥ 20 GB; if not idle, wait up to 30 minutes polling every 60 s, then stop and report "NOT IDLE" with the offending processes — never measure under contention; (2) run each measurement exactly once, sequentially, never in parallel, with the machine state (`uptime`, adapter, present source) recorded next to every number; (3) compare against the gate targets and write PASS / FAIL / NOT RUN per gate with the numbers — no tuning, no code changes except creating the two absent measurement scripts named above (they are evidence tooling, not product code) and fixing harness-only defects; a FAIL is a result, not a task; (4) append a "Q-01 qualification" section to each affected evidence file and a summary table to `docs/builder-program/evidence/Q-01-qualification-2026-09-09.md`, and update the viewer dossier §5 baseline table.

Gates to produce numbers for: viewer baseline (orbit, pan, zoom, fly-through, 3D→2D→3D p50/p95/p99 on the 104 M-point fixture, class I target 33.4 ms motion / 25 ms rest), VB-D7, VB-D8, G-RW-SEGMENT (+ ≤ 1.1× native comparison), draw snap latency, G-VC-TRANSITION, LOD continuity, EDL cost delta, G-MT-3 (1 M sampled points / 500 constraints timing), G-MT-5 scale budget (as far as the fixture allows; 500 M logical points only if a fixture exists), Brandenburg (only if a fixture exists). Never copy the repository or datasets; do not commit; budget: high effort, one pass, hard stop after three hours.
