Task: CONFORMANCE CHECK (read-only; write one report file). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English.

Compare docs/adr/0030-photolab-product-import-package-and-provenance.md against the authoritative spec text in docs/builder-program/specs/import-formats/import-formats.md, section "PhotoLab product datasets — 2026-09-02", decision records IF-D19–IF-D25 (especially IF-D19 registration + provenance states and IF-D22 the versioned ProductImportPackageManifestV1 / ProductLineageV1 shapes, package_sha256 canonicalization, ready-record atomicity, fail-closed compatibility, complete/partial/unknown states, adopted vs deferred formats). The ADR must be a cite-and-adopt with ZERO design freedom: field names, types, optionality, enum values, canonicalization steps, atomicity rules, and the format adoption/deferral list must match verbatim or be strict restatements.

Write docs/adr/0030-conformance-check-2026-09-02.md (document class: report) with: (1) a field-by-field table (spec name → ADR name → match / divergence); (2) every rule stated in IF-D19/IF-D22 and whether the ADR restates it exactly; (3) anything the ADR adds that the spec does not say (design freedom = defect) or omits; (4) whether the ADR's "Normative document changes proposed" texts (DATA-MODEL item-8 promotion, PROJECT-FORMAT paragraph) are consistent with the spec and with docs/DATA-MODEL.md pending item 8 as written by the architect; (5) verdict: conformant / conformant with listed edits / non-conformant. Do not edit the ADR or any spec. Final message: verdict + the divergence list.
