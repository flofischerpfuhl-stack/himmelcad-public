Task: small follow-up on scripts/registry-lint.mjs + a rule + five row edits. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. Do not commit. Read scripts/registry-lint.mjs (after I-07b), docs/builder-program/README.md "Rules", REGISTRY.md §1 header conventions, and the five duplicate rows the linter reports (run `node scripts/registry-lint.mjs --summary` and the duplicate section).

1. Convention: a catalog row in a spec that does NOT own the capability is an access-path/consumer row and must carry `owner: <spec-name>` at the start of its Spec-link cell (e.g. `owner: mesh-terrain; CIV-D5 access path`). Add this rule to docs/builder-program/README.md under "Rules" (one bullet, with the linter as enforcement).
2. Linter: rows whose Spec-link cell starts with `owner:` are consumer rows — excluded from duplicate-id and spec-vs-registry definition checks, but checked that the named owner spec actually catalogs the id (new check `consumer-rows-point-to-owner`). Unit tests.
3. Apply the convention to the five duplicates by editing the NON-owning spec's row (owners: mesh-terrain owns mesh.create-surface and derived.recipe-manage; measure-inspect owns inspect.point_info; ui-platform owns document.history; import-formats owns file.import per REGISTRY §1). Minimal edits; run prettier on touched specs.
4. Re-run the linter; update docs/builder-program/evidence/I-07-registry-lint-2026-09-02.md with the new counts. Final message: counts per check.
