S-06e — G17 finding on the void quick surface after S-06d/S-08b (`packages/@himmelcad/ui/gallery/shots/dark/command-surfaces.png`, row "void quick surface"): the entries are now "Clear selection, Paste in place, Frame all, Perspective, Capture view, Restore bookmark, Top" — the S-08b bookmark rows displaced the Front and Right presets under the 7-entry cap, and the group order changed (selection/edit before view). Read `docs/builder-program/specs/ui-platform/ui-platform.md` UIP-D13 (quick surface: frame, view presets, place-here, clear-selection-when-any; entry cap tunable), `packages/@himmelcad/app/src/commands.ts` (S-06d ordering/products), the generated table, and the S-06 evidence.

Architect decision: the quick surface keeps UIP-D13's content and order — group 1 view: Frame all, Top, Front, Right, Perspective; group 2 selection: Clear selection (only when any); group 3 edit: Paste in place (only when admissible). Bookmark capture/restore are NOT quick-surface rows (UIP-D13 does not list them; they live in the View ribbon group, the entity menu for bookmark entities, and the console/automation) — set their `surfaces` accordingly. The cap (tunable, 7) applies after ordering, so presets are never displaced by later rows; a table lint asserts the quick-surface row set and order equals the UIP-D13 list. Update the gallery fixture and snapshot, regenerate serially, append "S-06e" to the S-06 evidence. Gates: `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/builder typecheck` (if it fails only in BuilderKernelViewport.tsx from the in-flight viewing-box lane, report it), `--filter @himmelcad/photolab typecheck`. Budget: medium effort, one focused pass.
