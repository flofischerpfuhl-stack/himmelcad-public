Work package I-03b — TypeScript project references, second attempt. Read `.claude/codex/prompts/_impl_brief.md` first, then `docs/builder-program/evidence/I-03-ts-project-references-2026-09-02.md` (the first attempt) and the architect correction below.

Architect correction to the first attempt's diagnosis: the types `EntityId`, `SnapResult`, `SnapTargetMask`, `GeometryPrimitiveRef`, `GeometryTargetRef`, `SnapKind`, `SnapSource` ARE exported from `packages/@himmelcad/data/src/index.ts` (EntityId line 9, SnapTargetMask ~1360, SnapResult ~1365; verify the others with grep). The TS2305 the first run saw is therefore not a missing-export defect but declaration-redirect resolution: under `composite`, the `@himmelcad/data` specifier resolved to something other than `src/index.ts` (check `tsconfig.base.json` paths lines 39–41 — note the `@himmelcad/data/*` wildcard alias and `@himmelcad/data/canonical` — against the declaration output layout and `packages/@himmelcad/data/package.json` `main`/`exports`, which point at `./src/index.ts`). Find the actual resolution path with `tsc --traceResolution` limited to one importing file before changing anything, and record it in the evidence file.

Goal (unchanged from I-03): `pnpm typecheck` runs as one `tsc -b` graph over packages → apps with incremental build info, so a warm no-change typecheck of the whole workspace is fast and per-package diagnostics are identical to today's. Acceptance gate `G-INFRA-TSC`: (1) cold and warm full-workspace typecheck times measured and recorded; warm no-change ≤ 25 % of cold; (2) a deliberately injected type error in one package produces the identical diagnostic under the old and new configuration (probe removed afterwards); (3) `pnpm --filter @himmelcad/photolab typecheck`, `pnpm --filter @himmelcad/builder typecheck`, and `pnpm --filter @himmelcad/weltview typecheck` all pass; (4) `pnpm --filter @himmelcad/app test` and `--filter @himmelcad/ui test` pass; (5) no runtime source changes. Emitted declarations go to a git-ignored directory (e.g. `.tsbuild/`), never next to sources; add the ignore entry.

Allowed edits: `tsconfig.base.json`, every `tsconfig*.json`, `package.json` scripts (root and workspaces), `.gitignore`, the evidence file. If the import in `apps/photolab/renderer/src/App.tsx` of `../../electron/projectLifecycle.ts` needs a project reference or include to resolve under composite, add that reference/include — do not move code. If, after tracing resolution, the only correct fix does require exporting an additional symbol from `@himmelcad/data` or another package, add that export (types only) and list it in the evidence; do not stop as blocked for that.

If the change cannot meet the gate, revert completely (as the first run did), update the evidence file with the traced resolution and the exact failing configuration, and say BLOCKED with the reason. Do not leave a half-migrated tree. CARGO_TARGET_DIR is already exported; do not run cargo. Budget: medium effort, one migration pass, no exploratory rewrites.
