SPLIT STEP 6b — two product sidecar binaries and packaging. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English.

Read ONLY: `docs/adr/0032-module-architecture-and-crs-declaration.md`; `docs/builder-program/evidence/SPLIT-PLAN-2026-09-23.md` ("Dispatch, products and wire compatibility", step 6); `docs/builder-program/evidence/SPLIT-S6A-2026-09-24.md` incl. "Architect review"; the rejected-construct lists in `SPLIT-S5B-…`/`SPLIT-S5C-…` reviews; then the files you change.

GOAL
1. Distribute `crates/himmelcad-sidecar/src/routes/mod.rs` (~9.5k lines of mixed helpers) by product set: helpers used only by PhotoLab routes go next to the PhotoLab route modules, Builder-only helpers next to Builder modules, genuinely shared helpers stay shared. Where a helper is domain logic rather than wire adaptation, move it into the owning domain crate.
2. Make the sidecar library product-selectable WITHOUT Cargo feature unification leaks: the shared host (transport, registry, shared routes) stays in `himmelcad-sidecar` (lib); add two binary crates `crates/himmelcad-builder-sidecar` and `crates/himmelcad-photolab-sidecar` that each call the host with their route sets. Route modules for a product live in a crate that only that product's binary depends on (e.g. `himmelcad-sidecar-builder-routes` / `himmelcad-sidecar-photolab-routes` library crates, or move them into the binary crates) — choose the smallest structure and justify it. Acceptance: `cargo tree -p himmelcad-builder-sidecar -e normal` contains NO `himmelcad-domain-photogrammetry`; `cargo tree -p himmelcad-photolab-sidecar -e normal` contains no Builder-only domain crate that PhotoLab routes do not call (list what each binary links and why).
3. Route sets: Builder binary = shared + builder routes; PhotoLab binary = shared + photolab routes. Each binary's `--list-routes` must equal the golden filtered by product (`shared` + its product). Extend `frozen_protocol_routes.rs` to check both binaries and to replay each fixture against the binary whose set contains the route; all fixtures byte-exact.
4. Binary names: `himmelcad-builder-sidecar` and `himmelcad-photolab-sidecar` in Cargo; packaged/installed name stays `himmelcad-sidecar[.exe]` inside each app so Electron code and user installs are unchanged. Keep `crates/himmelcad-sidecar` building its old `himmelcad-sidecar` binary with ALL sets for one transition step ONLY if a consumer cannot be switched now — prefer switching every consumer; list any leftover.
5. Portable MVS binary moves with PhotoLab (into the PhotoLab binary crate or its own crate) — PhotoLab-only.
6. Update every consumer: both app `package.json` scripts, the four `electron-builder.*.yml`, `.gitlab-ci.yml`, sidecar path lookups/tests in `apps/*/electron`, `scripts/run-cargo.mjs` users, `scripts/build-photolab-windows-rust.mjs`, `scripts/stage-photolab-runtime.mjs`, `scripts/check-photolab-release-inventory.mjs`, `scripts/photolab-e2e.mjs` and other smoke/e2e scripts that name `target/*/himmelcad-sidecar` or `himmelcad-portable-mvs`, `scripts/verification/**`. Find them with `rg -n 'himmelcad-sidecar|himmelcad-portable-mvs|himmelcad_sidecar' --glob '!target' --glob '!node_modules' --glob '!docs/**'` and list each change.
7. `scripts/module-layers.json`: new crates classified (binaries = product layer); checker green.

REJECTED CONSTRUCTS (checker enforces most): cross-crate `include!`; `#[macro_export]` in domain crates; macros expanding into another crate; `package =` renames; `extern crate self as`; blanket foundation re-exports from domain crates; duplicate type definitions; Cargo features that switch product route sets inside one crate (feature unification would leak PhotoLab into Builder).

HARD RULES
- No schema, serde, default, error, algorithm or wire change; progress/cancel/drain unchanged. Tests move verbatim; Rust test count 1,237 plus the tests you add (list them).
- NEVER `git stash`, `git reset`, `git checkout -- <path>`; HEAD comparisons only via `git worktree add .build/split-s6b/head HEAD` with `CARGO_TARGET_DIR=/media/oem/ZusatzSSD1/himmelcad-target/split-head`; remove both afterwards.
- Rust only with `CARGO_TARGET_DIR=/media/oem/ZusatzSSD1/himmelcad-target/split` and `-j 4`. Check `df -h / /media/oem/ZusatzSSD1` before heavy steps; never let `/` drop below 20 GB (the release inventory stage writes under `.build/` — if it would, skip it and say so). Never copy the repo or datasets; scratch only under `.build/split-s6b/`, delete at the end. Do not commit or push. Do not run `bindings:generate`. No Windows execution (the architect dispatches a Windows check afterwards) — but keep Windows paths/names in scripts correct and name what must be verified there.

GATES (verbatim)
- `cargo check --workspace --all-targets -j 4`; `cargo fmt --all --check`
- `cargo build -p himmelcad-builder-sidecar -p himmelcad-photolab-sidecar --bins -j 4`; both `--list-routes` outputs compared with the golden
- `cargo tree -p himmelcad-builder-sidecar -e normal | grep himmelcad-` and the same for PhotoLab (quote)
- `cargo test -p himmelcad-sidecar -p himmelcad-builder-sidecar -p himmelcad-photolab-sidecar -j 4 -- --test-threads=2` incl. `frozen_protocol_routes`
- `pnpm check:modules`; `node --test scripts/check-module-dependencies.test.mjs`; `pnpm --filter @himmelcad/builder test`; `pnpm --filter @himmelcad/builder typecheck`; `pnpm --filter @himmelcad/photolab test`; `pnpm photolab:test:e2e-contracts`; `pnpm photolab:test:project-files`; the DXF registration smoke with its canonical fixture against the Builder binary
- `pnpm photolab:audit:release:linux` only if `/` has ≥ 30 GB free before it starts; otherwise state it was skipped.

EVIDENCE `docs/builder-program/evidence/SPLIT-S6B-2026-09-24.md`: crate structure and why, helper distribution summary, what each binary links, route-set checks, every consumer file changed, what must be verified on Windows, test counts, gates verbatim, what was NOT verified, measured wall-clock time.
