Work package T-01 — invisible UI test display on the Linux laptop (owner rule 2026-09-19: agents must NEVER open windows on, send input to, or take focus from the owner's desktop DISPLAY=:0 again). Read `.claude/codex/prompts/_impl_brief.md`, `scripts/run-electron.mjs` (launch flags, `--ozone-platform=x11`), the V-01b evidence (how Builder is driven through CDP and screenshots are taken), `scripts/perf/viewer-baseline.mjs` (it already renders on the Quadro M2200 without a visible window: hardware WebGL2 via ANGLE/Vulkan, present source `raf-render-complete`), and `docs/builder-program/evidence/V-08-gpu-fallback-2026-09-10.md` (backend ladder and the renderer chip that names the active backend).

Deliverable: `scripts/ui-test-display.sh` (+ a short section in `docs/TEST-TIERS.md` or the nearest test doc): starts a private virtual X server (Xvfb, 1920×1080×24, display number chosen free ≥ :90, auth file under `.build/ui-test-display/`), exports DISPLAY for the child only, launches Builder or PhotoLab (dev) inside it with a CDP port and an isolated `--user-data-dir` under `.build/ui-test-display/<run>/profile`, with GPU rendering on the NVIDIA card although the X server is virtual — try in this order and keep the first that yields a HARDWARE backend: (a) `--use-angle=vulkan --enable-features=Vulkan` (offscreen Vulkan needs no GLX), (b) `--use-gl=egl` with `__EGL_VENDOR_LIBRARY_FILENAMES`/`__NV_PRIME_RENDER_OFFLOAD=1`, (c) software as a labelled last resort. Prove the result: `chrome://gpu`-equivalent via CDP `SystemInfo.getInfo` / `app.getGPUFeatureStatus()`, the Builder renderer chip/HUD backend, and a frame-time sample on the prepared 104 M-point road dataset (orbit p50/p95 from the V-01 rings) compared with the Q-01/V-07 numbers on the real display (19–21 ms p95) — state the delta. Screenshots come from CDP `Page.captureScreenshot` (never from the desktop). The script must: refuse to run if DISPLAY resolves to :0 for the child; run the child under `systemd-run --user --scope -p MemoryMax=${UI_TEST_MEM:-12G} -p MemorySwapMax=0 -p LimitCORE=0 -p CPUQuota=400% --nice=10`; clean up Xvfb and the app on exit/timeout (trap), leaving no process behind; print the CDP URL and the display it used. Then update `.claude/codex/prompts/_impl_brief.md` and `.claude/codex/prompts/full/review-R01.md`: replace every instruction to use DISPLAY=:0 with "use scripts/ui-test-display.sh; DISPLAY=:0 is forbidden". Self-test: start Builder through the script, open the default project, import nothing, take one CDP screenshot to `.build/ui-test-display/selftest.png`, confirm with `xdotool`/`wmctrl` on :0 (if installed) or `xwininfo -root -tree -display :0` that NO HimmelCAD window exists on :0 during the run, stop, confirm zero leftover processes. Evidence `docs/builder-program/evidence/T-01-invisible-ui-test-display-2026-09-19.md`. Rules: you yourself must not touch DISPLAY=:0 in this run; never copy the repository or datasets; do not commit; 75-minute budget.
