Work package G1b-check — literal-UI verification of the PhotoLab product import (G1b, landed 437789f) against `docs/builder-program/PHOTOLAB-G1C-MATRIX.md`. Read `.claude/codex/prompts/_impl_brief.md`, the matrix (rows, the four checks identity / render / pick / snap, pass criteria, fixture paths), `docs/builder-program/evidence/G1b-photolab-product-import-2026-09-09.md` (what landed; the "Remaining unverified" list), the V-01b evidence for how to drive Builder through CDP on DISPLAY=:0 and take screenshots, and the fixtures (read-only) under `.build/photolab-e2e/g1a3-dsm-smoke/photolab-e2e.hcad/.photolab/product-import-packages/`.

Do, through the literal UI (ribbon → Import… → PhotoLab product dataset → chooser → Import), into a fresh Builder project under `.build/g1b-check/`: (1) sparse potree@2, (2) DEM prepared hierarchy, (3) dense potree@2 (1.24 GB; record copy time and peak RSS of the sidecar and renderer, machine load stated); for each: the chooser row (screenshot), the job progress, the rendered result (screenshot at the same camera as PhotoLab's reference if the matrix names one), the Lineage group (screenshot), a pick and a snap on the product with coordinates compared to PhotoLab's pick within product resolution (the matrix's criterion), undo through Edit ▸ Undo (entity gone, CAS blob kept), redo, re-import (idempotent message). Then fill every G1b cell of the matrix for these three rows with PASS / FAIL / N/A + evidence path; rows for orthomosaic, mesh and gaussianSplat are N/A (no fixture) — say so. Record the G17 gallery section name for the chooser/importing/lineage states and check the dark/light captures against the brief's visual bullet. Machine: state `uptime`/`free -g`; refuse to run the dense import if free memory < 8 GB or another heavy process runs. Write `docs/builder-program/evidence/G1b-check-2026-09-09.md` and append the filled cells to the matrix file's Builder column only (PhotoLab owns the rest of that file — touch nothing else in it). No product-code changes; if something fails, record it as a finding with the exact reproduction. Do not commit. Budget: high effort, one pass, 90-minute hard stop.
