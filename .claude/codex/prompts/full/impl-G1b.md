Work package G1b — Builder imports PhotoLab product packages (Import-Formats IF-D19–IF-D25, "PhotoLab product datasets — 2026-09-02"). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/import-formats/import-formats.md` §"PhotoLab product datasets" (IF-D19–IF-D25: the `hcad.product-import-package-manifest@1` profile, the `hcad.product-import-package-ready@1` ready record written last, Import-vs-Attach, lineage identity, readiness/admission, the `import.photolab-product(s)` command rows and the generated `io.import.product_dataset.*` rows), `docs/photolab-automation-command-rows.md` (the P11 rows PhotoLab supplies — consume the generated table, do not retype), `docs/builder-program/COORDINATION.md` §"P11 command rows from PhotoLab" and §"Workflow adoption", the landed PhotoLab side: `crates/himmelcad-core/src/product_import_package.rs` (manifest/ready/lineage types, `computed_package_sha256`), `crates/himmelcad-io/src/product_import_package.rs` (reader), `crates/himmelcad-sidecar/src/viewer_raster_manifest.rs` (DEM prepared-hierarchy manifests with `dem_facts` + validity bitset, G1a-3), `packages/@himmelcad/data/src/index.ts` (`ProductLineageIdentityV1`, `ProductPublicationReasonCode`), the Builder import path (S-06d product-aware command table, `io.import.plan/execute` rows, D-02 display properties, V-02 prepared datasets stream natively, 0.5-02 ground/DEM entities), and the read-only fixtures from the DSM smoke: `.build/photolab-e2e/g1a3-dsm-smoke/photolab-e2e.hcad/.photolab/product-import-packages/` with `product-75db7ea1…` = dense potree@2 (1.24 GB), `product-9ad8b322…` = sparse potree@2 (480 kB), `product-e288ad8f…` = DEM himmelcad-prepared-hierarchy@1 (22.9 MB, dem_facts + validity bitset); each has manifest.json + ready.json. Never copy or modify the fixtures; never copy the repository.

Deliverable through visible UI: ribbon **Import… → PhotoLab product dataset** opens the chooser (a local PhotoLab project or a package directory; lists the packages found with product kind, producer, counts, size, readiness state; packages without a ready record or with a manifest hash mismatch are shown as not importable with the reason); **Import** (copy into the project's dataset store, CAS-addressed) vs **Attach** (reference in place, marked as external) per IF-D19–IF-D25; registration as a registered cancellable job (`io.import.product_dataset.*` generated rows; S-05 phases: verify manifest/sha256 → verify ready record → stage/copy → register dataset → create entity), producing canonical entities: sparse/dense potree@2 → point-cloud entities streaming through the V-02 native path with the D-02 display properties; DEM prepared hierarchy → the elevation-surface/grid entity with `dem_facts` (cell size, validity ratio, NoData policy) shown in the properties and the validity bitset honoured by the renderer as NoData; lineage identity (`ProductLineageIdentityV1`: source project, processing set, mask scope, tool identities) stored on the entity's provenance and shown in a "Lineage" properties group; re-import of the same package sha256 is idempotent (no duplicate entity, message "already imported as …"); a package that changed on disk after attach is detected (hash) and flagged. P11: `import.photolab-product` / `import.photolab-products` with the S-01 envelope; console line; Python SDK parity via the generated table. Registry rows `owner: import-formats`, linter green.

Visual brief (G17): chooser island 640 px: path row with Choose…, package table (kind glyph · product · producer · counts · size · readiness Badge: Ready / Not ready · reason), Import/Attach segmented control with a one-line explanation of the difference, footer Import (primary) · Cancel; job progress row per S-05; "Lineage" properties group in mono (project · processing set · mask scope · tool ids). Gallery section "PhotoLab product import" (chooser with one not-ready row, importing, lineage group); serial regeneration.

Gates: all three fixtures import through the UI into a fresh Builder project and render (screenshot each: dense cloud with D-02 defaults, sparse cloud, DEM with NoData transparent) — record counts, times and peak RSS (state the machine load; 1.24 GB dense = copy time bounded, streaming after registration); idempotent re-import; tampered-package detection (test on a temp copy under `.build/g1b/`, ≤ 2 MB, of the sparse package only); Attach vs Import both journaled and undoable (undo removes the entity, keeps the CAS blob per S-07); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, `--filter @himmelcad/photolab test` (the PhotoLab renderer consumes the same data types), theme lint, `cargo test -p himmelcad-io product_import` once and `cargo check -p himmelcad-sidecar --tests --bins` before you report. Evidence `docs/builder-program/evidence/G1b-photolab-product-import-2026-09-09.md`, plus a one-paragraph note for PhotoLab in the evidence ("what Builder shows for each product"). Rules: each gate at most three times; two-hour budget; do not commit; do not edit PhotoLab-owned paths (`apps/photolab`, `crates/himmelcad-core/src/photolab_*`, `crates/himmelcad-sidecar/src/{colmap,job,mvs,process_group,project}_runtime.rs`); if the spec or the PhotoLab manifest disagree, record the finding — do not diverge. Budget: high effort, one pass.


Addendum (2026-09-09 07:10, arrived after the first run started — applies to the G1b matrix-check pass): the result is measured against `docs/builder-program/PHOTOLAB-G1C-MATRIX.md` (rows = admitted kinds sparse, dense, dem, orthomosaic, mesh, gaussianSplat × {potree@2, himmelcad-prepared-hierarchy@1}; checks identity, render, pick, snap with pass criteria and fixture paths). Expectations: idempotent re-import (no second entity); Import and Attach register the same lineage; the DEM validity bitset produces NoData holes (never zero heights), exactly per `dem_facts.nodata_policy`; pick/snap agree with PhotoLab's pick within product resolution; depth-map packages are refused with the typed reason. Report every ambiguity in manifest.json/ready.json as a finding for the G1c matrix.
