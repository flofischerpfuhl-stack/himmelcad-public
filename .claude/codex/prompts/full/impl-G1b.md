Work package G1b — Builder registers and opens PhotoLab product import packages (PhotoLab release gate 8; ADR 0030 chain "PhotoLab publishes → Builder registers → WeltView reads"). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/adr/0030-photolab-product-import-package-and-provenance.md` (rev 6), `docs/builder-program/specs/import-formats/import-formats.md` IF-D26–IF-D34 (product packages, lineage, ready records, DEM facts, splat and merged-alignment rows), `docs/DATA-MODEL.md` "Immutable resources" (PhotoLab paragraph), `docs/PROJECT-FORMAT.md` product package paragraph, PhotoLab's landed producer side: commits 3c6f4d0 (WP-G1a-2: `hcad.product-import-package-ready@1` ready records, `package_sha256` last, ids `product-<sha256>`, `manifest.json` + `ready.json` at the package root) and c7bb505 (WP-G1a-3: DEM facts, `view/validity.bin` bitset, `validityReference`, `interpolation: bilinear`, `PreparedElevationHierarchyArtifact.validity_resource`), the shared types in `packages/@himmelcad/data/src/index.ts` (`ProductImportPackageReadyRecordV1`, `PhotoLabProductPublicationRecordV1`, `PhotoLabDemFactsV1`, `ProductLineageIdentityV1`…), `crates/himmelcad-core/src/product_import_package.rs`, `crates/himmelcad-sidecar/src/viewer_raster_manifest.rs`, and the D-02 import path (`docs/builder-program/evidence/D-02-scan-import-view-2026-09-05.md`, sidecar `import_registration_runtime.rs`, Builder import island). Find PhotoLab's real published packages under `.build/` (the G1a-2/G1a-3 smokes publish sparse/dense potree@2, tiled meshes, DEM packages) and use them as fixtures; if none exist, generate them with PhotoLab's documented fast smoke and say so.

Deliverable: Builder registers a product import package (drop / File ▸ Import / automation `import.open` on a package directory or its `ready.json`) as a registered job: validate `ready.json` + `manifest.json` (schema version, `package_sha256` recomputed over the manifest, lineage identity, reason codes → typed refusal with the reason, never a partial entity), register immutable content-addressed resources without copying (IF-D26/ADR 0030: reference by hash; the package is authority), create the canonical entities per row (sparse/dense cloud → pointcloud entity streaming the prepared potree@2 hierarchy through V-02's frontier; tiled mesh → mesh entity; DEM → elevation grid entity with validity bitset and bilinear interpolation honored by the raster provider; splat row per IF-D31; merged-alignment row per IF-D32), attach the provenance/lineage record to each entity (visible in Properties under "Provenance": product, PhotoLab project id, publication time, package hash), and journal one transaction per package. Re-registering the same package hash is idempotent (no duplicates); a newer publication of the same lineage offers "Update" with old+new retention per IF-D33/D34.

Visual brief (G17): the import island row for a package shows "PhotoLab product · <product name>" with the package's product glyphs; tree nodes carry a small provenance badge (glyph + tooltip "Published by PhotoLab 2026-09-06 17:52 · sha 3c6f…"); Properties "Provenance" group in mono 12 px. Gallery section "Product import" (island row, tree badge, provenance group); serial regeneration.

Gates: `G-IF-PD-1`…`G-IF-PD-6` as the import-formats spec defines them (registration, refusal reasons, idempotence, lineage update, DEM facts honored, WeltView-readable references untouched); a real fixture of each row type registers and renders (screenshot of Builder with a registered DEM + dense cloud via the V-01b harness path, machine state stated); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, `cargo test -p himmelcad-core product` and `-p himmelcad-sidecar import` once each in target/builder (CARGO_TARGET_DIR exported; check `free -g` first; PhotoLab smokes may be running). Registry rows for the product import capability under `owner: import-formats`; linter green. Evidence `docs/builder-program/evidence/G1b-product-registration-2026-09-08.md` incl. the exact identity/render/pick/snap behavior per row type that PhotoLab's G1c matrix needs. Shared-substrate notice: additive edits only in `crates/himmelcad-io`, sidecar, `data/src/index.ts`; list touched functions. Concurrency: 0.5-01 edits viewing-box files; S-06d edits the command table/menus; S-07b edits snapshots — consume, do not edit. Budget: high effort, one pass.
