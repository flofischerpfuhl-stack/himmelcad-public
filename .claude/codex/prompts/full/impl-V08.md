Work package V-08 — viewer fallback on GPU-process failure (Windows Vega 8 finding). Read `.claude/codex/prompts/_impl_brief.md`, `docs/builder-program/evidence/WIN-02-photolab-windows-smoke-2026-09-09.md` and `WIN-03-photolab-core-windows-2026-09-09.md` (Vega 8 / driver 31.0.12027.9001: with default flags the Chromium GPU process exits with `exit_code=34` three times and the shared viewer reports `Failed to create surface for any enabled backend … webgl2 not available`; with `--use-angle=d3d11` one crash then readback/software mode, WebGPU selected; with `--use-gl=angle --use-angle=swiftshader` no crash, WebGL2 selected, slow), `docs/builder-program/specs/view/viewer-core-addendum.md` (VC-D* backend selection, class targets, the V-01 telemetry), the viewer kernel `packages/@himmelcad/viewer/src/kernel/{KernelViewerSession.ts,WgpuKernelViewer.ts}` (surface creation, backend selection), the Electron launchers `apps/builder/electron/main.ts` and `apps/photolab/electron/main.ts` (PhotoLab-owned: name the lines it needs, do not edit), `scripts/run-electron.mjs`, and the design system (status copy for a degraded state).

Deliverable: (1) the viewer session survives a GPU-process loss: on `child-process-gone`/`gpu-process-crashed` (Electron main) or a failed surface creation (kernel), the session retries once on the current backend, then falls back in order WebGPU → WebGL2 (ANGLE D3D11 on win32) → WebGL2 software (ANGLE SwiftShader) — the software step requires a relaunch with the flag on Electron ≥ 30 (`--use-gl=angle --use-angle=swiftshader`, plus `--enable-unsafe-swiftshader` only if the Electron version demands it — check the installed version), so the main process persists the decision (per machine, in the Builder settings store) and relaunches the window once with a visible reason; (2) a visible degraded state: the bottom-bar renderer chip shows "Software rendering" (warning-fg token) with a tooltip naming the GPU/driver and the reason, the HUD reports the backend, and the V-01 telemetry records `backend.fallback { from, to, reason }`; (3) a way back: View ▸ Renderer ▸ "Try hardware rendering again" clears the persisted decision and relaunches; (4) tests: kernel unit tests for the fallback ladder (mock surface creation failures), an Electron main-process test for the persisted decision and relaunch guard (never loop: at most one relaunch per session), PhotoLab typecheck/tests (the shared kernel change must not alter PhotoLab behaviour; PhotoLab adopts the launcher lines in its own lane — write the exact lines into the evidence). Gates: `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme lint, and the PhotoLab visual harness dark (`--no-a11y --no-compare-baselines`, zero GPU-device errors) if the kernel changed; registry rows under `owner: view-domain`. Evidence `docs/builder-program/evidence/V-08-gpu-fallback-2026-09-10.md`. Rules: never copy the repository or datasets; each gate at most three times; two-hour budget; do not commit; do not edit PhotoLab-owned paths; another lane drives the Builder UI on DISPLAY=:0 — do not start Builder windows on that display (use the harness's headless Chrome). Budget: high effort, one pass.
