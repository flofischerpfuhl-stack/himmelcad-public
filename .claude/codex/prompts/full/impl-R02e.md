Work package R-02e — the three stops left after R-02d. Read `.claude/codex/prompts/_impl_brief.md` (definition of done: literal Builder UI on the road scan, screenshots), `docs/builder-program/evidence/R-02d-p0-spine-2026-09-11.md` (spine ledger: LandXML stops for missing explicit units; idle GPU ~422 % GPU-process CPU at rest with no backlog; the segmentation progress flood that interrupted the strict New gate), the specs: file-project FP-D5/FP-D6 and the 0.5-07 export evidence (units/CRS preflight for LandXML — the export island must expose a units control with the project default and a typed refusal when unset), viewer-core addendum §1.1 (at-rest budget ≤ 25 ms p95 and the governor's rest state: no continuous re-render without input or streaming work; V-07's durability dedupe as the pattern), S-05 job progress rules (progress events coalesced to ≤ 10 Hz per job, never per point batch).

Fix, each with the reviewer's reproduction as acceptance: (1) **LandXML units control** — the export island gains a Units field (Select: metre default from the project CRS; feet/US-feet where the CRS says so) with the plan preflight showing the chosen unit; export succeeds on the road DGM; re-import verifies the unit; (2) **rest-state governor** — after publication and camera settle, the presenting loop must stop re-rendering (render on invalidation only: input, streaming completion, entity change); measure GPU-process CPU at rest with the HUD/V-01 rings before and after (target: near-idle within 2 s of settle; state the number); no visual regression on the PhotoLab harness dark run; (3) **progress coalescing** — segmentation (and any job that emits per-batch progress) publishes at most 10 events/s per job, phase changes immediately; the strict one-process New → import → draw → save/reopen gate from R-02d must now pass uninterrupted. Then replay the boundary-first spine to LandXML with a reopened-file verification; screenshots under `.build/r02e/`. Gates: `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme + registry lint, PhotoLab visual harness dark (`--no-a11y --no-compare-baselines`, zero GPU-device errors) if the kernel changed, `cargo check -p himmelcad-sidecar --tests --bins`. Evidence `docs/builder-program/evidence/R-02e-rest-units-progress-2026-09-11.md`. Rules: never copy the repository or datasets; each gate at most three times; two-hour budget; do not commit; do not edit PhotoLab-owned paths; cargo `-j 4` in target/builder. Budget: high effort, one pass.
