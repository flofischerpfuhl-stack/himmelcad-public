Work package R-02 — the P0 fixes from the demanding-user review R-01 (Release 0.5 failed the "usable at starter level" gate). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/evidence/R-01-demanding-user-review-2026-09-09.md` in full (findings, screenshots under `.build/r01/`, §5 priority list), the specs the findings cite (`docs/builder-program/specs/file-project/file-project.md` FP-D1–FP-D6 import/new/open, `docs/builder-program/specs/import-formats/import-formats.md` IF-D1–IF-D18 LAS/LAZ/E57 import, `docs/builder-program/specs/draw/draw.md` DR-D* boundary + default layer, the canonical model's layer records and tombstone rules in `crates/himmelcad-core`), and the landed code: the ribbon (`apps/builder/renderer/src/ribbon.ts`, `App.tsx`), the import path (`packages/@himmelcad/app/src/clients.ts` `io.import.plan/execute`, the S-06d product-aware command table, `apps/builder/electron/main.ts` file dialogs), the G1b product-import island (437789f/6784213 — its ribbon entry replaced or hid the general import route: find out which), project create/open (`apps/builder/renderer/src/project.ts`, `canonical_app_runtime.rs`, `canonical_project_store.rs`, V-07's `durabilityPolling.ts`), and the draw/boundary path (0.5-04: `PointcloudSamplingPanel`? no — the Draw panel and `drawing.ts`; the `default-layer` duplicate/tombstone error text in the sidecar).

Fix, with a regression test each and through visible UI: (P0-1) **general scan import reachability** — File ▸ Import… opens the OS file picker for LAS/LAZ/E57 (and the other registered readers) as the primary route; "PhotoLab product dataset" is a secondary entry (submenu or second row), never the only one; drag-and-drop of a LAS onto the viewport imports; the import job appears immediately in the Jobs list with needs-input surfaced (CRS/units) per IF-D*; a packaged visible-UI test imports the road scan `libs/polyshapev01/dist/PW_GHT_251215_Orscholz_Deponie-1-1.las` (or its prepared hierarchy if the test budget requires) and asserts the entity appears; (P0-2) **New / project replacement never blanks the renderer** — reproduce with the R-01 steps (screenshots `.build/r01/`), root-cause (viewer session torn down without re-attach? durability polling isolation from V-07? project id swap?), fix so New/Open swaps the project atomically with the viewer alive, failures are visible with a typed reason and a recovery action, and add create → reopen → restart coverage; (P0-3) **default-layer lifecycle** — the boundary tool fails with a canonical `default-layer` duplicate/tombstone error on new/reopened projects: root-cause (default layer created twice? tombstoned on reopen? journal replay order?), fix in the canonical model/journal so a three-vertex typed boundary, cancel/restart, save/reopen and a second drawing work without errors; regression tests at the core/sidecar level and one visible-UI test. Then the bounded P2 items if time remains (≤ 30 min): (P2-9) construction-bar overlap, ribbon label collision, Console header, clipped panel copy, stale HUD samples, missing HUD close; (P2-10) typed measurement Enter commits once or shows a specific validation reason, retained through save/reopen.

Gates: the three P0 scenarios recorded as visible-UI runs (V-01b CDP route on DISPLAY=:0) with screenshots under `.build/r02/`; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test` (the ribbon/import table is shared), theme lint, registry lint, `cargo test -p himmelcad-core layer` and `-p himmelcad-sidecar project` once, `cargo check -p himmelcad-sidecar --tests --bins` before you report. Evidence `docs/builder-program/evidence/R-02-p0-fixes-2026-09-09.md` with a table finding → root cause → fix → test → screenshot. Rules: never copy the repository or datasets; each gate at most three times; 2.5-hour budget; do not commit; do not edit PhotoLab-owned paths; a G1a-3b lane runs in parallel (PhotoLab DEM publisher) — do not touch `viewer_raster_manifest.rs` or the PhotoLab job code. Budget: high effort, one pass.
