Work package 0.5-02 — ground extraction (Pointcloud, ground-only subset of D-RW-03/D-03). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/pointcloud/pointcloud.md` PC-D1–PC-D7 (cloud entity, classes, class visibility, fences, edited revisions), the ground-extraction records (§10.2 versioned progressive filter `hcad.pointcloud.ground-progressive@1`, PC-D7 "cloud, not an inferred DGM", typed result with residuals, the Pointcloud→Mesh hand-off), `docs/builder-program/MASTER-PLAN.md` §5 row 17 (gates: outdoor-ground assertions within `G-RW-EXTRACT-GROUND-FLOOR`, P4, typed UI/automation parity, deterministic class/extract results, ground-cloud→DGM hand-off), `docs/DECISION-DOCTRINE.md` P2/P4/P10, ADR 0031 item 6 (derived recipe + source roles — the extracted ground cloud is a derived entity with a recipe), the landed D-02 import path and cloud Properties (class list), S-05 jobs, S-04/S-10 selection and P9 visible set, V-02 frontier (the extracted cloud is baked as a prepared dataset and streams like native data, P2), the sidecar's existing ground classification (`crates/himmelcad-sidecar/src/ground_classification.rs` from PhotoLab's A4 — SMRF; reuse the algorithm core, do not fork it), and the 104 M-point LAS fixture prepared under `.build/perf/viewer-baseline-datasets/`.

Deliverable through visible UI: Pointcloud ▸ Extract ground on the selected cloud (scoped by the visible set / active viewing box per P4) opens a docked function panel with the SMRF parameters (cell size, slope, window, threshold — defaults per the spec, tunable, with a "Terrain type" preset Select: flat / rolling / steep), runs as a registered cancellable job with real phases (grid, filter, classify, bake) and a live preview (classified points tinted during the job at coarse LOD), produces (a) class 2 assignment on the source cloud's edited revision (undoable, one journal transaction) and (b) a derived "ground" cloud entity with recipe (sources by id+revision+parameters, P10) baked as a prepared dataset; deterministic results (same input+parameters → identical class set, tested by hash); typed result summary in the panel (points classified, ratio, residual stats) and console; P11 rows `pointcloud.ground.extract/preview/cancel` with the S-01 envelope; hand-off: the ground cloud is selectable as a DGM source (Mesh) via its recipe/source-role admission.

Visual brief (G17): function panel per DESIGN-SYSTEM (docked right, detachable) titled "Extract ground": preset Select, four NumberInputs with units, a "Preview" quiet Button and "Extract" primary Button; during the job the S-05 progress row with phase text and Cancel; result group in mono 12 px ("Ground points 61.2 M (59 %) · residual σ 0.04 m"); the preview tints ground points in a distinct token color (success-tone, 60 % alpha) and non-ground unchanged. Gallery section "Extract ground panel" (idle, running, done); serial regeneration.

Gates: deterministic-hash test on the small LAS fixture; the 104 M-point LAS extracts end to end with cancel at each phase (no partial entity), memory bounded (state peak RSS; check `free -g` first — other lanes and PhotoLab smokes share the machine), and the derived cloud streams through the frontier at native cost (V-02 statistics, non-timed); P4 scope test (points outside the active viewing box are neither classified nor extracted); undo restores the source revision and removes the derived entity; automation parity round trip; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, `cargo test -p himmelcad-sidecar ground` once in target/builder (CARGO_TARGET_DIR exported). Registry rows under `owner: pointcloud`; linter green. Evidence `docs/builder-program/evidence/0.5-02-ground-extraction-2026-09-08.md`. Shared-substrate notice: `ground_classification.rs` is PhotoLab-authored — additive refactor only (extract a shared core function; keep their DEM job byte-identical, prove with their existing tests). Concurrency: 0.5-01 edits viewing-box files; G1b edits import registration; S-07b snapshots; do not edit those. Budget: high effort, one pass.
