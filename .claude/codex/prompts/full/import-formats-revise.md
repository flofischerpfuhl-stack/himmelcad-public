Task: REVISE an existing specification per its adversarial review. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Do not rewrite from scratch; rework in place.

Read: the spec named below, its review file named below (every finding), docs/FUNCTION-CONTRACT.md (current), docs/DECISION-DOCTRINE.md (X1-X7, P1-P6), docs/builder-program/README.md (registry cite-and-revise rule), docs/builder-program/OWNER-DECISIONS.md, and every sibling spec the review references.

Requirements: address EVERY finding — each with a spec change or an explicit deferral reason — in a disposition table appended to the spec (finding id → disposition → spec section/decision id). Blockers and majors must be resolved in the spec text, not only in the table. Update decision records in doctrine format (Decision/Derivation/Rejected/Tunable); add new records where the review's resolution is a consequential choice. Keep the zero-owner-question standard: every review resolution is doctrine-derivable — if you believe one is not, apply the escalation protocol in writing and show the dissolution. Cross-spec resolutions the review assigns to OTHER specs: do not edit those files — record the request in your final message; where the review resolves a contradiction in another spec's favor, adopt it here and cite that spec's record. Dossiers may gain sourced evidence/absence line items per doctrine rule 2. Only modify your own spec (and its criteria sibling if any). Final message: findings resolved vs deferred (counts), new/changed decision ids, cross-spec requests.

Spec: docs/builder-program/specs/import-formats/import-formats.md
Review: docs/builder-program/specs/import-formats/import-formats-spec-review-2026-09-02.md (3 blockers, 10 majors, 3 minors)

Notes from the architect: (a) blocker 1 (changed-source update): specify the three-way model concretely — source-old / source-new / canonical-edits-since-import keyed by stable provider keys; per entity: unchanged / updated in place / conflict (edited locally AND changed in source → explicit user resolution list in the registration island, never silent); passive-consumer effect matrix per E2 (locked viewing-box bakes via VB-D3 revision keys, draped rasters, measurements associativity, plan captures stale lifecycle, applied specifications, layer membership, attached-project references) — one rule per consumer, cite each sibling record; (b) blocker 2 (exact undo of 80 GB): apply the new contract C4 retention rule — physical retention roots (old object-store datasets stay pinned while reachable from the undo horizon or any snapshot), peak disk = old + new during the operation (stated, X2 spends disk), release rule, interaction with file-project FP-D16 maintenance (cite), plus the option "import as new entity instead" when disk budget is insufficient (refuse-to-start-without-budget per D1); (c) blocker 3 (apply-to-similar): define "similar" mechanically — same provider, same declared CRS/units source, same option-schema version, header-derived attributes within the preset's declared tolerance — and the review step (a per-file summary row the user confirms in one action; deviations flagged and excluded); (d) adopt the agent spec's registration.* bounds as the shared record per cite-and-revise (agent IF-D12 reconciliation is queued in the reconciliation pass — state your side).
