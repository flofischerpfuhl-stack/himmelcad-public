Work package R-02b — the P0 blockers that survive R-02, measured only by the reviewer's flow. Read `.claude/codex/prompts/_impl_brief.md` (note the new definition of done: the literal Builder UI flow on the named real dataset, screenshots, before you report), then `docs/builder-program/evidence/R-01b-demanding-user-review-2026-09-09.md` in full (§2 executed steps with screenshots under `.build/r01b/`, §3 diff table, §4 new findings N-01…N-07, §6 list), `R-02-p0-fixes-2026-09-09.md` (what R-02 changed and how it was tested — its tests passed, the reviewer's flow still fails: find why the two differ), the specs the findings cite, and the R-01b project states under `.build/r01b/` (the persisted projects the reviewer used: reuse them — "legacy/persisted project" is the failing case).

Fix, each with the reviewer's exact reproduction as the acceptance test: (P0-1) **New / project replacement regressed** — reproduce the exact blank-shell failure from R-01b step 03 (`.build/r01b/03-ne…png`): no typed failure, old project not preserved, target not created; root-cause why R-02's fix did not hold in this flow (auto-opened `r01-review-pass1` project? path with spaces? the Windows fsync merge f3241da changing the durability path? the R-02 code path only covering Open, not New?), fix, and prove it on the reviewer's start state (auto-open of the last project, then New). (P0-2) **Imported road scan invisible and unselectable (N-01, N-02)** — the 103.7 M-point LAS publishes into the tree but the viewport shows nothing after removing the old clip, Frame All and Zoom to; the HUD reports ~0.1 M points; the tree row cannot enter selection (`aria-selected=false`, Selected: 0, empty Properties). Root-cause both (prepared hierarchy not bound to the entity? the Viewing Box of the persisted project still clipping? entity kind not selectable in the tree adapter? first-frame gate never satisfied?), fix, and prove with the reviewer's steps plus a pixel proof (non-background pixels in the viewport) and a selection proof (row selected, Properties filled). (P0-3) **`default-layer` on legacy/persisted projects and atomic exact entry (B-03, N-04)** — `EntityId("default-layer") already exists or is tombstoned` on measurement and boundary in the reopened R-01 project; and exact boundary input commits a partial vertex when focus leaves X before Y/Z are typed. Fix the canonical bootstrap for projects persisted before R-02 (migration/repair on open: one default layer, never a second create; tombstone restoration idempotent) and make typed entry atomic (commit only on Enter with all three fields valid; Tab moves focus without committing — S-10 rule). (P0-4) **Viewing Box Lock stuck at 0 % (N-03)** — a bounded 10 m box lock stays at "Preparing resident dataset 0 %" > 70 s on the 104 M cloud; find whether the bake never starts (job not dispatched? waiting on a lock? progress never published?) or runs without progress; fix so the phases report and a small box locks in bounded time; cancel must return within 2 s.

Then rerun BOTH R-01b spines yourself, scan-first and boundary-first, through the literal UI on the road LAS from the reviewer's start state, as far as they go; every step screenshot under `.build/r02b/`; where a spine still stops, record the exact step and reason — do not claim more than the screenshots show. Gates: the four reproductions as visible-UI runs; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme + registry lint, `cargo test -p himmelcad-core layer` and `-p himmelcad-sidecar project` once, `cargo check -p himmelcad-sidecar --tests --bins` before you report. Evidence `docs/builder-program/evidence/R-02b-p0-fixes-2026-09-09.md` (finding → root cause → why R-02's test missed it → fix → test → screenshot). Rules: never copy the repository or datasets; each gate at most three times; three-hour budget; do not commit; do not edit PhotoLab-owned paths; a PhotoLab COLMAP smoke and a C++ cross-build run in parallel — keep cargo at `-j 4`. Budget: high effort, one pass.
