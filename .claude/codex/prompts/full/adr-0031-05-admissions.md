Task: draft ADR 0031 — "Release 0.5 data-model admissions bundle" — as a Proposed ADR in `docs/adr/0031-release-0-5-data-model-admissions.md`. Documentation only; touch no code or schema files.

Read first: `.claude/codex/prompts/_common.md`, `docs/DECISION-DOCTRINE.md` (axioms X1–X7, precedents P1–P11, decision-record format), `docs/DATA-MODEL.md` (section "Pending data-model admissions" 1–9 and the "Immutable resources" paragraph), `docs/builder-program/REGISTRY.md` §4.4 (items 1–12), `docs/builder-program/MASTER-PLAN.md` §0a (Release 0.5 scope table) and §6 "M-0.5", and an existing accepted ADR for the house format (e.g. `docs/adr/0019-*.md` and `docs/adr/0030-photolab-product-import-package-and-provenance.md` for the cite-and-adopt style with verbatim blockquotes).

Purpose: work package S-01 may implement only admissions covered by an ADR. The owner is away; this ADR is a vetoable derived record (MASTER-PLAN §9.7). It must admit exactly the subset of the §4.4 pending admissions that the 0.5 "DGM aus Scan" path requires, and explicitly defer the rest with the reason.

Architect's candidate partition (verify each against the 0.5 path; you may move an item across the line, but every move needs a decision record):
- ADMIT for 0.5: item 6 (`hcad.derived-recipe@1` + `hcad.mesh-source-roles@1` — DGM creation/editing recipes, P10); item 7 (point-acquisition provenance, support-role component, offset/parallel recipe profile — draw lines with tri-modal input and blue support geometry; check whether the offset/parallel profile is really needed in 0.5 or belongs to 1.0 split/trim/parallel); item 12 (local histories: selection/display/camera streams — P8 undo domains used by the 0.5 bottom-bar subset); item 5 (snapshot markers — P-01 save/recovery); the 0.5 subset of item 3 (ViewState v2: entity-referenced clips for the viewing box, VD-D8 two-layer display state; NOT pinned Plan viewports); a minimal profile of item 1 (`hcad.measurement@1` — saved measurement geometry with exact anchors for "measurement basics").
- DEFER: item 2 (edit-lock), 4 (Plan root), 9 (journal actor metadata / agent batches), 10 (civil bundle), 11 (stable segment locator), 8 already promoted to ADR 0030.

For every admitted item the ADR states: the schema id and version, the owning spec and decision ids (cite verbatim from the spec — copy the decision text as a blockquote, do not paraphrase), the migration rule (new component/entity types are additive; existing `.hcap`/journal files must open unchanged — say how), the automation exposure (P11: which command/query rows expose it), and the acceptance gate the implementation must pass (name it `G-S01-<item>`). For every deferred item: one sentence why 0.5 does not need it and which milestone will admit it.

Also include: a "Consequences" section (what S-01 may now implement, what stays forbidden), a "Vetoable decisions" list at the end (each with Decision/Derivation/Rejected/Tunable per the doctrine format) so the owner can veto line by line, and status `Proposed (architect-derived, owner acceptance pending; implementation of admitted items authorized under MASTER-PLAN §9.7 as vetoable substrate work)`.

Then add the ADR to `docs/README.md`'s ADR index if such a list exists, and append a one-line entry under REGISTRY.md §4.4 stating which items ADR 0031 covers. Run `npx prettier --write` on the files you touched. Report: the admit/defer table, any item you moved across the line and why, and the file paths. Budget: one pass; no spec edits.
