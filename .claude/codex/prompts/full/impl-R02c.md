Work package R-02c — the two spine stops left after R-02b, plus the P1 prerequisites the reviewer named. Read `.claude/codex/prompts/_impl_brief.md` (definition of done: literal Builder UI on the road scan, screenshots), `docs/builder-program/evidence/R-02b-p0-fixes-2026-09-09.md` (what now works and its spine ledger: scan-first reaches Extract Ground, boundary-first stores the polygon), `R-01b-demanding-user-review-2026-09-09.md` §6 items 5–7, the specs: `docs/builder-program/specs/pointcloud/pointcloud.md` (ground extraction PC-D*, 0.5-02 evidence `0.5-02-ground-extraction-2026-09-08.md` — SMRF runs on the 104 M cloud with sampled capture; the job contract S-05), `docs/builder-program/specs/view/viewing-box*.md` or the viewing-box records in the view spec (VB-D*: "From selection" derives the box from the selection's resident bounds), the S-01 source-role admission, and the code: `apps/builder/renderer/src/viewingBoxBake.ts`, the ground-extraction panel/job wiring in App.tsx and `crates/himmelcad-sidecar/src/pointcloud_ground.rs`, the resident-dataset capture path used by both (R-02b fixed the Viewing Box capture; ground extraction uses a sibling path that still stalls).

Fix, each with the reviewer's reproduction as acceptance on the road scan through the literal UI: (1) **Extract Ground capture stays at 0 % on the real LAS and cancel takes 225 s** — root-cause (same residency fallback R-02b fixed for the box? a full-resolution capture instead of the sampled capture the 0.5-02 evidence used? progress never published? cancel not checked inside the capture loop?), fix so the capture reports phases and progress within 2 s, runs on the 104 M cloud in bounded time (state the number; 0.5-02 measured minutes, not hours), and cancel returns in ≤ 2 s (S-05 rule); (2) **Viewing Box → From selection with a boundary polygon selected reports "no resident bounds"** — a polyline/boundary has canonical geometry but no resident dataset; From selection must derive the box from the selection's canonical bounds (polylines, boundaries, measurements) and only from resident bounds for clouds; fix + test; (3) **source predicates explicit (R-01b item 6)** — Pointcloud ribbon actions (Extract, Segment, Sample, Rasterize) with no selected cloud must be disabled with a tooltip reason ("Select one resident point cloud") or offer the single visible cloud as the implicit source with a visible chip; context-menu actions operate on their invoked entity; no silent clicks; (4) if time remains (≤ 30 min): View ▸ Color Mode / Background placeholders (R-01b item 8) — either wire the existing display-property controls or remove the empty surfaces and say so.

Then replay the scan-first spine from the reviewer's start state as far as it goes: import (already works) → box lock → extract ground → segment/sample/raster → boundary → DGM create → LandXML; screenshots under `.build/r02c/`; record every stop with the exact step and reason. Gates: `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme + registry lint, `cargo test -p himmelcad-sidecar pointcloud_ground` once, `cargo check -p himmelcad-sidecar --tests --bins` before you report. Evidence `docs/builder-program/evidence/R-02c-spine-fixes-2026-09-10.md`. Rules: never copy the repository or datasets; each gate at most three times; 2.5-hour budget; do not commit; do not edit PhotoLab-owned paths; cargo `-j 4` in target/builder. Budget: high effort, one pass.
