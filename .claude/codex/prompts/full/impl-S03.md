Work package S-03 — gesture/Escape arbiter (minimum substrate for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md` first, then `docs/builder-program/specs/ui-platform/ui-platform.md` §3.6 "The platform gesture map" (the arbitration baseline), decision records UIP-D2, UIP-D5, UIP-D8, UIP-D14, UIP-D15, §9.5 (`Shared3DTarget` deviation set), `docs/FUNCTION-CONTRACT.md` rules C1 (tri-modal input) and the gesture-arbitration rule, `docs/builder-program/specs/draw/draw.md` DR-D18/DR-D21 and the construction input bar records (Tab = focus/traverse the bar, Up/Down = candidate cycling only while a "1 of N" indicator is live), `packages/@himmelcad/ui/src/escapeLadder.ts` (landed UIP-D14 dispatcher — the Escape rung of the arbiter must register through it, never a second listener), and the viewer input code the gesture map cites (`packages/@himmelcad/viewer/src/kernel/KernelNavigationController.ts` around lines 381–440).

Deliverable: one platform gesture arbiter in `packages/@himmelcad/viewer` (or `@himmelcad/app` if the existing input plumbing lives there — follow the in-repo location of the navigation controller) that owns the idle meanings of §3.6 and lets exactly one armed tool claim the claimable rows:
- A registry of gesture rows (`registerGestureClaims(toolId, claims)` returning a release function): a tool may claim only rows marked claimable in §3.6; claiming a platform-owned row (LMB drag, RMB drag, wheel, RMB click) is rejected at registration with a typed error unless the claim carries a `deviationReason` (the `Shared3DTarget` set is the one pre-registered deviation). At most one armed tool; arming a second releases the first with a typed reason.
- Idle behavior exactly as the table: LMB click selects a pickable entity and is inert on void/bare cloud (UIP-D2/D15); double-click on entity is reserved (no-op); double-click on void clears selection; Ctrl+LMB toggles membership; RMB click opens the entity menu/quick surface (UIP-D5) via a callback; MMB click unassigned; Tab/Shift+Tab is ordinary focus traversal unless a coordinate tool is armed, then it focuses/traverses the tool's construction bar and never cycles candidates; Up/Down cycles the candidate set only while a live indicator is registered (`setCandidateIndicator(n, i)`), otherwise passes through; Escape goes through `registerEscapeRung('tool', …)` only for the armed tool; typing routes to registry shortcuts unless the armed tool declared numeric/text entry focus. Touch: tap/tap-again/tap-hold/double-tap map to their pointer equivalents (UIP-D2 touch-only deselect).
- The global recenter-on-Escape listener is removed (UIP-D8) if it still exists — find it, remove it, cite the file.
- Click-versus-drag threshold and double-click interval are tunables in one place (X6), documented with rationale.

Gates: `G-B2-GESTURE-C1` — a browser (Playwright, existing harness) or jsdom-level test that arms a stub coordinate tool and proves: Tab focuses the construction bar and never cycles; Up/Down cycles only while the indicator is live; Escape cancels the tool through the ladder and does not clear selection on the same press; a second Escape then clears selection; LMB click on void with the tool armed goes to the tool (vertex placement stub), on idle is inert. Collision gate: a unit test that tries to claim a platform-owned gesture without a reason and gets the typed rejection; two tools claiming the same row in the same state fail at the second registration. Camera regression: orbit/pan/wheel still work when a tool is armed (test through the existing navigation controller tests). `pnpm typecheck`, `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` pass. Evidence `docs/builder-program/evidence/S-03-gesture-arbiter-2026-09-02.md`: table of §3.6 rows → implementing code lines, tunables with values, the removed recenter listener citation, test names/results.

Out of scope: real tools (draw/viewing box wire in later), selection store changes beyond calling the existing select/toggle/clear API, apps/photolab, Rust. Shared substrate notice: PhotoLab also uses the viewer package — keep default behavior identical for PhotoLab (no tool armed ⇒ today's behavior), and run its typecheck. Budget: medium effort, one pass.


Resume notice: an earlier run of this exact brief was interrupted mid-work (process killed, no report). The tree may contain its partial files. Run `git status --short` first, inspect the partial work, keep what is sound, and finish the deliverable; do not start from scratch if the partial state is usable.
