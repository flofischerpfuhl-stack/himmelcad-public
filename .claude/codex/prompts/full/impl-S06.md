Work package S-06 — command surfaces (minimum substrate for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/ui-platform/ui-platform.md` UIP-D5 (entity context menu), UIP-D6 (single command registry), UIP-D13 (quick surface), UIP-D16 ("Select under cursor ▸" — S-04 exposes the candidate data), §2.3 and §7 copy tables; `docs/DECISION-DOCTRINE.md` P11 (one generated command table drives console, automation, SDK); `docs/DESIGN-SYSTEM.md` "Discoverability and contextual access"; the S-01 automation table and envelope, the S-04 selection store, the S-05 jobs rows, the S-02 Menu/ContextMenu components, `packages/@himmelcad/console` and the automation host.

Deliverable: the UIP-D6 runtime command registry in `@himmelcad/app` — one table of commands/queries (id, label, shortcut, enablement predicate, surfaces: ribbon | contextMenu | quickSurface | console | automation, owner spec) generated from the same source the P11 table uses (do not hand-maintain a second list; if the P11 table is JSON/Rust-generated, derive the runtime registry from it and extend the schema with the surface flags), a shortcut dispatcher honoring the registry (no ad-hoc key handlers; the S-03 arbiter's "typing → registry shortcuts" callback connects here), the entity context menu (UIP-D5) built on the S-02 ContextMenu with the registry's contextMenu entries filtered by selection, including "Select under cursor ▸" with the S-04 candidates, the void quick surface (UIP-D13: frame, view presets, clear selection when any, paste in place when admissible), and the console vocabulary derived from the registry (`help`, completion, and every command callable by id). Automation parity: automation calls resolve through the same registry entries (X3).

Visual brief (G17): context menu = S-02 Menu look (180 px min width, 28 px items, separators between groups: selection · edit · view · entity-specific), submenu "Select under cursor ▸" lists candidates as "<kind> · <name>" with the current one marked; quick surface = same menu at the pointer, max 7 entries (tunable), header row "Viewport" in mono uppercase muted; shortcuts shown right-aligned in mono muted inside menu items. Gallery section "Command surfaces" with: entity menu (polyline selected), void quick surface, and the submenu open; regenerate shots.

Gates: `G-UIP-CMD` — every registry entry reachable from at least one visible surface (test iterates the table); shortcut collision test (registry lint already checks the spec; add a runtime assertion); context menu content test per selection kind (point, polyline, mesh, cloud → cloud gets only deliberate-selection entries per UIP-D15); quick-surface cap; console `help` lists exactly the table; automation host round-trip for three commands; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/console test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`. Evidence `docs/builder-program/evidence/S-06-command-surfaces-2026-09-04.md`. Concurrency: S-05b edits JobsSurfaces/ProgressBar/viewport chrome in @himmelcad/ui; V-01 edits the render crate and viewer kernel — do not touch those. Budget: medium effort, one pass.
