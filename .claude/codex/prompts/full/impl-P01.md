Work package P-01 — save and recovery path (minimum substrate for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/file-project/file-project.md` FP-D1–FP-D8 (create/open/recent/close), FP-D11, FP-D19–FP-D22, `docs/builder-program/OWNER-DECISIONS.md` D1 and D5 (project-as-block is later; only note the seam), `docs/PROJECT-FORMAT.md` (`.hcad` project, `.hcadx` archive, planned fragment profile), the S-07 evidence (`docs/builder-program/evidence/S-07-journal-2026-09-04.md` — group commit, durability indicator, snapshot markers, the explicit gap: Save split-menu and FP-D20 Save As archive job), the S-05 job registry (Save As is a registered, cancellable job), and Builder's project code (`apps/builder/renderer/src/project.ts`, electron main/preload, sidecar `canonical_project_store.rs`).

Deliverable: the complete working open/save/flush/restart path for Builder: New / Open / Recent (main-process recent list, MRU 10, tunable) / Save (S-07 flush + snapshot) / Save As (FP-D20 archive job through the S-05 registry with phases and cancel; never a partial archive on cancel) / Close (D1 semantics: no "unsaved changes?" modal because the journal is durable; close waits for the pending flush with the S-07 indicator, offers Cancel while storing) / relaunch recovery (reopen the last project with the crash-tail report from S-07 as a toast + console line, never silently) / project replacement (S-04/S-08 local streams stored then unloaded, FP-D21). P11 rows `project.new/open/recent/save/save_as/close`.

Visual brief (G17): File tab of the ribbon (existing Ribbon component): New, Open, Recent (a Menu from S-02 listing MRU entries with path muted under the name, mono), Save, Save As…, then Import group; recovery toast = warning kind "Recovered 14 unsaved changes from <time>" with action "Show in console"; Save As progress via the S-05 chip/island ("Saving archive · 42 %", cancel allowed until the final rename). Gallery section "File surfaces" with the Recent menu and the recovery toast; regenerate shots serially.

Gates: `G-FP-1` — create → edit → close → reopen restores the exact journal head; `G-FP-2` — kill during Save As leaves no partial `.hcadx` (temp + atomic rename) and the job reports cancelled/failed; `G-FP-3` — three real imports followed by relaunch: chips rehydrate (S-05) and the project reopens at the same head; recent list survives restart; `cargo test -p himmelcad-sidecar` project-store tests in target/builder (CARGO_TARGET_DIR exported; run once at the end), `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`. Shared-substrate notice: sidecar `main.rs` and `canonical_project_store.rs` are shared with the PhotoLab lane — restrict edits to the Builder project routes, list touched functions in the evidence. Evidence `docs/builder-program/evidence/P-01-save-recovery-2026-09-05.md`. Concurrency: S-08 edits ViewState/HUD and the viewport — do not touch those. Budget: medium effort, one pass.
