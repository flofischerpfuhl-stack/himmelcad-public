Work package G1b-fix — repair the defects the literal-UI check found in the PhotoLab product import (G1b, 437789f). Read `.claude/codex/prompts/_impl_brief.md`, `docs/builder-program/evidence/G1b-check-2026-09-09.md` (the findings with screenshots/logs under `.build/g1b-check/`), `docs/builder-program/PHOTOLAB-G1C-MATRIX.md`, the G1b evidence, `docs/builder-program/specs/import-formats/import-formats.md` IF-D19–IF-D25, `docs/DECISION-DOCTRINE.md` P8 (journal/undo), the S-07 durability records, `crates/himmelcad-sidecar/src/{canonical_app_runtime.rs,canonical_project_store.rs,viewer_raster_manifest.rs}`, `crates/himmelcad-io/src/product_import_package.rs`, `apps/builder/electron/productImportCatalog.ts`, `apps/builder/renderer/src/BuilderPhotoLabProductImportIsland.tsx`, and the fixtures (read-only) under `.build/photolab-e2e/g1a3-dsm-smoke/…/product-import-packages/` and `…/g1a3-dtm-smoke/…` (DSM `product-e288ad8f…`, DTM `product-60af8cb2…`, sparse `product-9ad8b322…`, dense `product-75db7ea1…`).

Defects to fix, in this order, each with a regression test: (1) **DEM import rejected** — the sidecar answers `prepared dataset binding differs from canonical geometry` for both DSM and DTM packages (`.build/g1b-check/logs/dsm-import.json`): root-cause whether the importer builds the elevation-surface/grid canonical geometry (extent, cell size, origin, CRS, validity) from a different source than the prepared-hierarchy manifest binding expects, or the manifest's `dem_facts` disagree with the hierarchy header; fix on the Builder side if the importer is wrong; if the manifest is inconsistent, record the exact fields as a finding for PhotoLab and make the importer refuse with a typed, user-readable reason instead of an RPC error. The DEM must import, render with NoData holes per `dem_facts.nodata_policy`, and expose dem_facts in Lineage. (2) **Undo/redo are placeholders** — product import must be a journaled transaction (P8/S-07): undo removes the entity and dataset registration (CAS blob kept), redo restores it, both through Edit ▸ Undo/Redo and the journal reopen path. (3) **Sparse cloud snapping** returns no geometry snap at a visible feature (`sparse-line-snap.json`) while dense snaps; find why (candidate radius vs point spacing? sparse clouds have no prepared spacing? snap lane excludes small clouds?) and fix so the draw tool snaps to sparse points with the cloud-point snap kind. (4) The strict render/pick cells need PhotoLab's counterpart oracle — do not fake them; leave them "awaiting PhotoLab oracle" in the matrix but make sure the Builder pick coordinates are logged in a comparable form (world XYZ + product resolution) at `.build/g1b-check/logs/`. (5) The RPC error surfaced as a raw `Error invoking remote method 'sidecar:call': SidecarRpcError: …` modal — every import failure must be a typed reason in the island (S-01 envelope), never a raw RPC string.

Gates: re-run the literal-UI import of all four fixtures via the V-01b CDP route on DISPLAY=:0 with screenshots (chooser, result, Lineage; DEM with NoData holes visible), undo/redo screenshots, sparse snap log with a `Snap: Point` result; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme lint, `cargo test -p himmelcad-io product_import` and `-p himmelcad-sidecar product_import` once, `cargo check -p himmelcad-sidecar --tests --bins` before you report; update the Builder cells of the matrix with evidence pointers. Evidence `docs/builder-program/evidence/G1b-fix-2026-09-09.md`. Rules: never copy the repository or fixtures; each gate at most three times; two-hour budget; do not commit; do not edit PhotoLab-owned paths. Budget: high effort, one pass.
