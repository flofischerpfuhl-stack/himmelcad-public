Work package V-01 — viewer measurement authority. Read `.claude/codex/prompts/_impl_brief.md` first, then `docs/builder-program/specs/view/viewer-core-addendum.md` in full (VC-D1 is the authority for this slice; §1.1 hardware classes; §4 gate `G-VC-MEASURE`; §6 registry rows), `docs/builder-program/dossiers/viewer-performance-2026-09-04.md` §4.2 (the missing instrumentation, with file:line) and §5, `docs/builder-program/specs/view/view-domain.md` VD-D10 (presented-frame instrumentation, `view.diagnostics.sample`), `scripts/perf/viewer-baseline.mjs` and its README, and the cited code: `packages/@himmelcad/viewer/src/kernel/WgpuKernelViewer.ts` (telemetry observation ~903–933), `crates/himmelcad-render/src/{gpu_frame.rs,gpu_frame_timing.rs,hardware_policy.rs,scheduler.rs}`.

Deliverable (VC-D1): presented-frame timestamps and intervals (rAF-present pairing plus the GPU timestamp queries where the adapter supports them, with an honest `present_source` label), input-to-present latency with an input id, exact per-frame primitive counts (points, triangles, lines, text quads) and per-lane phase timers (protected lanes 1–3 vs cloud/mesh refinement per §1.3), budget/deadline reason codes from the governor, all in fixed-size rings; an immutable sample query `view.diagnostics.sample` (P11 row; S-01 envelope) returning p50/p95/p99/max over a window plus the last N frames; a HUD seam (a typed snapshot the S-08 HUD will render — no HUD UI in this slice beyond the existing telemetry window showing the new distribution). No renderer behavior change: this slice measures; it must not alter selection, budgets or draw order (a test proves frame output is byte-identical with instrumentation on/off for a fixed scene).

Then make the baseline real: adapt `scripts/perf/viewer-baseline.mjs` to read the new exact counts/intervals (replace the labelled proxies), run it on this machine (hardware WebGPU on the Quadro M2200, class W; the script rejects software adapters), and record the numbers in the dossier §5 and in a new `docs/builder-program/evidence/V-01-measure-2026-09-04.md`. Run the baseline ALONE — do not run cargo builds, pnpm tests or other heavy work concurrently with the measurement, and state the machine idle state in the evidence.

Gates: `G-VC-MEASURE` as the addendum defines it (interval/latency rings, exact counts, reason codes, sample query, on/off identity); `cargo test -p himmelcad-render` in target/builder (CARGO_TARGET_DIR is exported — never change it); `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` (a failure only inside apps/photolab/renderer/src/jobsChip* is the other lane's in-flight work — report, do not touch); wasm staging via the existing `stage-builder-viewer-wasm` path. Registry rows for `view.diagnostics.sample` if absent (owner: view-domain); linter green. Budget: medium effort, one pass; if GPU timestamp queries are unavailable on this adapter, say so and keep the rAF-present path as the measured source.
