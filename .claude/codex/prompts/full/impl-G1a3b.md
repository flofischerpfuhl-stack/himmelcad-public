Work package G1a-3b (PhotoLab lane, run by the architect while the PhotoLab session is offline) — tile-local validity masks in the DEM prepared-hierarchy publication. Read `.claude/codex/prompts/_impl_brief.md`, `docs/builder-program/evidence/G1b-fix-2026-09-09.md` §"PhotoLab publication defect" (every 512×512 tile of the DSM `product-e288ad8f…` and DTM `product-60af8cb2…` prepared hierarchies references the full 524,288-byte 2048×2048 validity mask instead of the 32,768-byte tile-local mask; exact manifest fields and hashes there), the G1a-3 evidence and brief (`.claude/codex/prompts/photolab/` and `docs/builder-program/evidence/` — G1a-3, DEM validity bitset resource + dem_facts), `crates/himmelcad-sidecar/src/viewer_raster_manifest.rs` (prepared-hierarchy manifests; the publish_* helpers), the DEM publisher in the PhotoLab job code (`crates/himmelcad-sidecar/src/dense_raster_prep.rs` / `photolab` job runtime — find the code that writes the validity bitset resource and the per-tile references), the Builder-side reader `crates/himmelcad-io/src/product_import_package.rs` and its new `invalid_package` check (G1b-fix), and the finished DSM/DTM smoke projects under `.build/photolab-e2e/g1a3-dsm-smoke/` and `g1a3-dtm-smoke/` (their e2e launch lines are in `.build/photolab-runtime/run-dem-smokes.sh`; the PhotoLab e2e supports `--reuse` so only the DEM publish stage reruns).

Deliverable: (1) the publisher writes one tile-local validity mask (32,768 bytes for 512×512 at 1 bit/cell, row-major, same bit order as today's full mask) per tile and references it from the tile record; the full-raster mask may stay as a separate resource if `dem_facts` cites it, but tile records must bind tile-local masks; (2) a unit test that publishes a synthetic 2048×2048 DEM with a known hole pattern and asserts every tile's mask size and content against the full mask; (3) rerun only the DEM publish stage for the DSM and DTM projects with `--reuse` (sidecar built once into `CARGO_TARGET_DIR=target/photolab`; record time and peak RSS), producing two new `complete` product-import packages; (4) prove them with the Builder reader: `cargo test -p himmelcad-io product_import` plus a small Rust or node check that opens each new package and validates every tile mask (no `invalid_package`); if the Builder is idle enough, a literal import through the Builder UI is a bonus, not required; (5) evidence `docs/builder-program/evidence/G1a-3b-validity-mask-2026-09-09.md` with the new package ids/paths (read-only for the Builder's matrix) and an entry for the G1c matrix rows DSM/DTM (identity re-check). Gates: `cargo test -p himmelcad-sidecar dem` and `-p himmelcad-sidecar viewer_raster` once, `cargo check -p himmelcad-sidecar --tests --bins`, `pnpm --filter @himmelcad/photolab typecheck` + `test`. Rules: never copy the repository or datasets; do not touch `.build/codex-partials/` (the PhotoLab session's saved A7c patches); each gate at most three times; two-hour budget; do not commit (the architect commits with explicit PhotoLab paths and records the takeover in PHOTOLAB-HANDOFF.md). Machine: an R-01 UI review runs in parallel — keep cargo at `-j 4` and do not run the full DSM smoke. Budget: high effort, one pass.
