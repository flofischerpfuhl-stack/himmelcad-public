S-06b — one G17 finding on the S-06 command surfaces. Read the "Architect review" section at the end of `docs/builder-program/evidence/S-06-command-surfaces-2026-09-04.md`, then `packages/@himmelcad/ui/src/Menu.tsx` (submenu handling) and the "Command surfaces" gallery fixture. Requirement: a submenu opens anchored at the parent item's right edge, top-aligned with that item (2 px overlap), and flips to the parent's left edge when it would clip the viewport; keyboard: ArrowRight opens and focuses the first submenu item, ArrowLeft closes back to the parent item; hover opens after 150 ms (tunable) and pointer travel toward the submenu keeps it open. Fix the component if it lacks this, fix the fixture so the open-submenu row shows the anchoring (the parent menu and the submenu side by side), add a test for the anchoring geometry (jsdom rect stub) and the keyboard pair, regenerate the gallery shots serially, and append "S-06b" with the shot path to the S-06 evidence. Gates: `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/ui typecheck`, `--filter @himmelcad/builder typecheck`. Do not touch other components or in-flight files (render crate, viewer kernel, sidecar, core). Budget: medium effort, one focused pass.
