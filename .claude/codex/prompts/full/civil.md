Domain: **Civil** (alignments, profiles, corridor surfaces, embankments). Output file: docs/builder-program/specs/civil/civil.md (~600-900 lines). This domain UN-DEFERS draw DR-D8's civil subsystem per owner decision D7.

Owner evidence (repo-resident): docs/builder-program/OWNER-STATEMENTS-2026-09-02.md S7 (embankments/pit surfaces), S8 (sections vs profiles, live-or-stale), S9 (best-fit alignments from edge polylines, gradients in profile, width bands → surface, slopes; table entry remains), S10 (surface from alignment with width bands), S11 (solids/rasters — mesh-terrain owns; cite). Doctrine P10 (live-or-stale) and P9/P8 apply; contract C1 tri-modal input applies to every civil construction (station/offset/height entry with absolute Z, ΔZ, slope %).

Dossier evidence: dossiers/rib-civil.md (§2.1 Klothoide, W3 element-wise axis construction, W4 gradient design with parabola rounding, §2.6/§2.7 DGM + QP-Generator cross sections), field-codes.md where relevant; for best-fit alignment from points/polylines, extend a dossier with sourced evidence FIRST (Civil 3D "create best fit alignment", published clothoid-regression approaches) per doctrine rule 2, then cite — never claim feasibility without a source.

Boundaries (architect-decided; cite-and-revise, never re-disposition): view-domain VD-D1 owns rigid section views (a section from a picked line with direction/depth, arrowed specification, rotated view — you may REQUEST the workflow addition there); mesh-terrain owns surface creation/editing/volumes and the surface-creation window (your corridor surfaces and pit surfaces are produced THROUGH its records: request the hand-off); draw owns clothoid/arc/line drafting primitives (DR-D8 un-deferred: cite DR-D6 multi-solution cycling, DR-D12 snapping); pointcloud owns cloud sampling; select-edit owns transforms.

Scope to specify at workflow level: (1) horizontal alignment: best-fit from one or two edge polylines (or picked points) with constraints (min radius, clothoid A bounds, tolerance, element count penalty) → a true line/arc/clothoid tuple as `hcad.alignment@1` (exists — cite entity_model.rs and landxml.rs import with file:line); classic element-wise construction and table editing as parity paths (X5, P7 for office conventions); (2) vertical alignment/gradient: profile view along the axis (P10: station/offset-defined geometry is LIVE; arbitrary projected geometry is stale-with-sync), best-fit to the cloud or drawn elements with parabola/arc rounding, table parity; (3) width bands: edge polylines promoted to "pretty" secondary axes with their own gradients; corridor surface emerges live as a derived parametric surface and is baked via mesh-terrain; (4) embankments/slopes: parametric derived entities (base edge/surface + angle + extent; transparent display; configurable extent), applied to areas from imported polygons with a common height, to corridor edges, and to BIM outer faces; "build surface from lower edge" = robust intersection of all selected surfaces and slopes into a pit surface (state the algorithm class, edge-case handling for coincident/overlapping slopes, and the failure modes — never silently wrong, X1); (5) profiles: long profile and cross profiles (QP) with the live-or-stale indicator and confirm/discard on view switch; (6) stations/chainage labelling, station+offset point construction (Achskleinpunkt), auto-layer per axis (draw idea from rib-civil §2.3). Everything with catalog rows, per-dossier-row dispositions, contract A1–E3, decision records, gates (named, agent-runnable; presented-frame-interval for live profile editing), E1 criteria, delta with file:line, zero owner questions. Do not edit other specs; cross-spec requests in the final message.
