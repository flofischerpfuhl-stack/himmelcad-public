Task: AMEND docs/builder-program/specs/import-formats/import-formats.md (in place) to close nine contract gaps found by the PhotoLab implementation of WP-G1a (doctrine rule 2: the spec is fixed first; the implementer then follows). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Zero design freedom left to the implementer afterwards: every gap gets exact field identifiers, primitive types, encodings, and enumerations.

Read: the spec's "PhotoLab product datasets — 2026-09-02" section (IF-D19–IF-D25), docs/adr/0030-photolab-product-import-package-and-provenance.md (rev 3), docs/DATA-MODEL.md "Immutable resources" PhotoLab paragraph, docs/DECISION-DOCTRINE.md (X1, P10, P11), the implementation as it exists uncommitted or committed in the tree: crates/himmelcad-core/src/canonical_json.rs, crates/himmelcad-core/src/product_import_package.rs, the products.list projection in crates/himmelcad-sidecar/src/main.rs and project_runtime.rs (verify shapes with file:line — where the implementation already chose a reasonable encoding, ratify it explicitly rather than inventing a different one, unless it violates X1), specs/pointcloud/pointcloud.md (Gaussian-splat arrival ownership row), specs/raster/raster.md (RA-D11 PlanGrid2D, DEM arrival), docs/TRANSFORMATIONS.md (CRS epoch representation).

Close each gap with a decision record (doctrine format) and the exact schema text:
1. ProductLineageV1 serialized shape: field identifiers, primitive types, identity-array shape (ordered arrays of {id, sha256} objects), optional-field encoding (absent vs null), algorithm/configuration/tool identities — so a site CAN reach `complete`.
2. `missingFieldIds` vocabulary = the ProductLineageV1 member identifiers (ratify), plus rules for nested/array members.
3. `reason_code` enumeration: ratify `needs_republish_recompute`, `needs_preparation`, `unsupported_format`, `no_package` and add any the spec needs; each with meaning and the user-visible copy rule.
4. Manifest id / ready-record schema id / publication generation: define `manifest_id` derivation (deterministic, content-derived or per-publication UUID — decide with X1 reasoning), `schema_id` for the ready record, and the publication-generation authority (ratify the candidate command sequence if sound) and id/role derivation rules.
5. DEM constraint: list the exact facts PhotoLab must freeze (RasterInterpolation, RasterConnectivity, NoData semantics, validity/connectivity resource) as the required fact list for the PhotoLab lane; until frozen, DEM packages are honestly absent — state that; do not relax the row.
6. Prepared Gaussian splats: authoritative ownership is the pointcloud spec's registry row (Pointcloud-owned arrival) — state that the ADR 0030 Decision 10 wording must follow the spec (cross-spec request), and the eligibility condition (after the data-model admission).
7. Add a disposition row for merged-alignment datasets (overlap / shared-control merges publishing sparse datasets): adopted under the same manifest with a lineage identity for the merge (alignment ids + hashes list).
8. Where ProductLineageV1 lives when no package is written: define residency (a lineage-only record in the publication record, same schema, `package: null`) so a durable V1 exists for every publication; the five-field legacy lineage relation is then defined (legacy = projection with `provenanceStatus: partial|unknown`).
9. Float prohibition vs FrozenCrsEndpoint epoch: epoch (and any f64 in manifests) is encoded as a canonical decimal string with a stated precision rule; packages never rewrite the model's epoch.
Update IF-D19/IF-D22 text, the disposition table, the delta (file:line to the implementation), verification (tests the PhotoLab lane must add), and a "cross-spec requests" list (ADR 0030 rev 4 by the PhotoLab session as cite-and-adopt; pointcloud/raster citations). Zero owner questions. Only modify import-formats.md; run prettier on it. Final message: the nine resolutions in one line each with record ids.
