Bounded repair (architect, 2026-09-11 14:40): the working tree holds the landed but uncommitted R-02c and V-08 work (see `docs/builder-program/evidence/R-02c-spine-fixes-2026-09-10.md` and `V-08-gpu-fallback-2026-09-10.md`). Two reds remain; fix ONLY these, no other behaviour change: (1) root `pnpm typecheck`: `apps/builder/renderer/src/App.tsx(4573,15): error TS2322: Type '(commandId: CommandInvocation["id"], entityIds: readonly EntityId[]) => void' is not assignable to type 'EntityTreeContextAction | LegacyEntityTreeContextAction'` — align the handler with the EntityTree prop type introduced in packages/@himmelcad/ui/src/EntityTree.tsx (V-08/R-02c changed it); (2) `pnpm --filter @himmelcad/builder test`: one failing test (the concurrent point-cloud test fails on `lucide-react` module resolution under the test tsconfig — fix the test/tsconfig resolution, not the product). Then run: root `pnpm typecheck`, `pnpm --filter @himmelcad/builder test`, `--filter @himmelcad/app test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/photolab typecheck`, `cargo check -p himmelcad-sidecar --tests --bins` (CARGO_TARGET_DIR=target/builder, -j 4). Report the verbatim counts. Never copy the repository; do not commit; 45-minute budget.
