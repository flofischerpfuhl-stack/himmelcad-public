Task: CONSOLIDATED CROSS-SPEC RECONCILIATION for the Himmel:CAD Builder completion program. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English.

Read: docs/builder-program/README.md (registry rules), docs/DECISION-DOCTRINE.md, docs/builder-program/REGISTRY.md (§4.2 findings F1-F14; F1 is closed), .claude/codex/out/cross-spec-needs.md (the queue of cross-spec requests reported by wave-2 authors and reviews), and every spec under docs/builder-program/specs/*/ plus their *-spec-review-*.md files.

Apply every queued cross-spec item in the OWNING spec (this task is the one exception to "only the author edits": you may edit any spec, but only for the queued reconciliation items — no re-design, no re-disposition, cite-and-revise only). For each item: make the minimal text/decision-record change, add a row to that spec's disposition table under a "cross-spec reconciliation 2026-09-02" heading, and ensure both sides of each reconciliation cite each other (e.g. raster RA-D5/RA-D7 ↔ mesh-terrain MT-D6; draw DR-D13 ↔ mesh-terrain MT-D12; agent ↔ import-formats IF-D12 registration.* bounds; select-edit SE-D1 ↔ file-project FP-D15; plan-editor PE-D6/PE-D10 ↔ view-domain VD-D12 / bim-specs BS-D14/15 / ui-platform UIP-D14; measure-inspect ↔ view-domain ribbon layout / draw wording / pointcloud PC-D10). Resolve REGISTRY F8 automation-leaf naming with ONE convention (recommend the schema's existing convention in schemas/automation/himmelcad-automation-v1.schema.json — verify) and apply it in every catalog. Then REBUILD docs/builder-program/REGISTRY.md from all specs (wave 1 + wave 2): all rows, shortcuts, gesture map, consistency report re-run (duplicate acts, contradictory guarantees, dangling decision ids, unowned capabilities, pending domains now closed). Also handle: docs/PROJECT-FORMAT.md `.hcadx` fragment profile stub section (select-edit request) and a "Data-model additions pending ADR" list (hcad.measurement@1, edit-lock component, ViewState v2, Plan root, snapshot markers) appended to REGISTRY.md §4 — do not write ADRs. Final message: items applied (by spec), items you could not apply and why, registry consistency report headline counts.

Round 3 instructions (2026-09-02, after owner batch 2): the queue file now also contains sections "From owner S13/S14", "From civil.md", "From owner S1 Tab question" (APPLIED/SUPERSEDED — verify Tab = input bar and ↑/↓ = candidate cycling everywhere, incl. civil.md and every gesture table), and "Dossier corrections" (Trimble Access 3-state Off/Visible/Selectable; blue selection — orange is owner taste per DESIGN-SYSTEM; no RealWorks single reticle) — sweep every spec for wrong attributions and fix citations to DESIGN-SYSTEM/owner statements or the corrected dossier subsections. Doctrine P8/P9/P10 and contract C1 were sharpened per OWNER-STATEMENTS-2026-09-02-GAP.md §12 — run the re-walk against the CURRENT text (P10 recipe lifecycle: every derived entity in every spec cites the one owning dependency record; sibling invalidation rows exist both ways). Include the new civil spec (revised today) in the registry: rows, gesture claims, shortcuts, named gates; cross-check GAP §6 inbound citations landed on both sides. Rebuild REGISTRY.md fully (wave 1 + wave 2 + civil + batch-2 amendments); rerun the standing checks; update §4.4 pending admissions (add station equations, circular vertical segments, slope-derivation components, support-role component, segment locator, local histories if the specs introduced them); set statuses per the README gate. Do NOT edit MASTER-PLAN.md (separate task). Final message: registry counts, statuses, items you could not apply, and the exact list of spec-level P10/G12 dependency records with their ids.

Addendum (2026-09-02 16:40): doctrine P11 exists (one generated command table for automation + console; no raw-RPC allowlists) — agent.md (AG records on automation exposure), ui-platform UIP-D6, import-formats (io.import.* incl. the new "register a PhotoLab product dataset" capability), and file-project (io export) must cite P11; the PhotoLab release-polish session implements against this wording, so do not paraphrase it. Also register the import-formats PhotoLab-dataset rows and their sibling citations (pointcloud/raster/mesh-terrain provenance fields).

Addendum (2026-09-02 18:00): import-formats gained IF-D19–D25 (PhotoLab product datasets, provenance manifest, P11 rows) — register them and apply the sibling citations listed under "From import-formats PhotoLab revision" in the queue. The three batch-2 revisions (ui-platform UIP-D23–26, draw DR-D21–22, mesh-terrain MT-D28–32) and civil CIV-D15–24 are final — include all. DATA-MODEL pending admissions were extended by the architect (items 6–9) — mirror them in REGISTRY §4.4, do not re-list differently.
