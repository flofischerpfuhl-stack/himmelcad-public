Task: research and specify the **Viewer Core program (V-00)** — how Himmel:CAD Builder's 3D view reaches "as fast and fluid as Trimble RealWorks, on every hardware class" for point clouds together with all other entity kinds, and a clean 3D ↔ 2D/2.5D transition. Documentation plus one measurement script; no renderer code changes in this run.

Read first: `.claude/codex/prompts/_common.md`, `docs/FUNCTION-CONTRACT.md` (D1 performance classes and extreme budgets), `docs/DECISION-DOCTRINE.md` (X1, X2, X4, X6; P2, P4), `docs/adr/0003-pointcloud-streaming.md`, `AGENTS.md` §2 (performance budgets), `docs/builder-program/specs/view/view-domain.md` (VD-D1 presented-frame instrumentation, VD-D8 two-layer display, the `browser-gpu` gates), `docs/builder-program/specs/pointcloud/pointcloud.md` (display density/LOD statements, PC-D5 volumes), `docs/builder-program/specs/viewing-box/*.md` (VB-D7/VB-D8 gates), `docs/builder-program/dossiers/realworks*.md` §4–5 (the "sampled working cloud backed by raw scans" pattern), `docs/builder-program/MASTER-PLAN.md` §7.1 and the M-RW row "Viewer beats TRW — a measured claim only (D-RW-07)". Then the code: `crates/himmelcad-render/src/{streaming.rs,tile_selector.rs,residency.rs,scheduler.rs,frame_graph.rs,gpu_frame.rs,gpu_frame_timing.rs,hardware_policy.rs,camera.rs,precision.rs}`, the Rust `RuntimeQualityGovernor` (grep for it), `packages/@himmelcad/viewer/src/kernel/{KernelStreamingDriver.ts,KernelCameraController.ts,KernelNavigationController.ts,WgpuKernelViewer.ts}`, and the shaders directory. Cite file:line for every claim about the current kernel (contract rule: stubs are not "existing").

Deliverables:
1. `docs/builder-program/dossiers/viewer-performance-2026-09-04.md` — evidence dossier: (a) how the reference products keep navigation fluid at 10⁸–10⁹ points — RealWorks (sampled working cloud + raw scans, station-based display), Potree/Potree 2 (octree LOD, point budget, adaptive point size, EDL, progressive refinement), CloudCompare, Leica Cyclone/TruView, Autodesk ReCap, Bentley/Pointools (visibility-ordered streaming), Cesium 3D Tiles — with sources (web search; cite URLs; mark anything unverified as such); (b) the technique inventory with what each buys and costs: hierarchical LOD + screen-space-error selection, hard point/triangle/byte budgets per frame, GPU-driven culling/indirect draw, progressive refinement at rest vs reduced density during motion, hold-last-frame/temporal reprojection during camera motion, adaptive point size / splatting / EDL / ambient occlusion, depth-based occlusion culling for indoor scans, precision (RTE / origin rebasing for georeferenced clouds), async decode and residency, multi-cloud and multi-entity batching (lines, areas, text, meshes, rasters, splats in the same frame), orthographic 2D/2.5D plan views (top-down with depth ordering, section-aware) and an animated 3D→2D camera transition (perspective→orthographic blend, up-vector lock, cursor-anchored zoom), hardware classes (integrated GPU / laptop Quadro-class / desktop) and how a budget governor adapts; (c) current-kernel assessment: what ADR 0003's vendored stack and the Rust governor already do (cite), what is missing, and where the frame time goes today.
2. `scripts/perf/viewer-baseline.mjs` (+ README lines): a repeatable measurement of the current kernel on this machine using the existing browser-gpu harness or Playwright path (check what `apps/builder` already has for `view.diagnostics.sample`/presented-frame gates and reuse it): fixed camera paths (orbit, pan, zoom, fly-through, 3D→2D switch) over the largest real fixture available in the repo/test data (find it; if none > 50 M points exists, say so and use the largest, and describe how to point the script at an external LAZ) — output presented-frame-interval p50/p95/p99, points drawn per frame, budget hits, decode backlog, GPU/CPU ms, into `.build/perf/viewer-baseline-<date>.json` + a short markdown summary. Run it once on this machine (NVIDIA Quadro M2200, 4 GB — a "laptop workstation" class, which is a good floor) and record the numbers in the dossier. If the harness cannot run headless here, record exactly why and what is needed.
3. `docs/builder-program/specs/view/viewer-core-addendum.md` — the program specification: user-visible guarantees (what "fluid" means per hardware class, in numbers: presented-frame p95 targets during motion and at rest, time-to-full-density after motion stops, maximum visual popping, cursor latency), the technique decisions as doctrine-format decision records (Decision/Derivation/Rejected/Tunable) VC-D1…, the TRW comparison protocol that D-RW-07 requires (same dataset, same camera path, same screen, frame-interval capture on both; how RealWorks' frames are measured externally, e.g. PresentMon/FrameView on Windows), the 3D↔2D transition contract (interaction state during the blend, what stays pickable, Escape/cancel), entity co-rendering rules (clouds never delay line/point/text interaction; UI overlays never wait for streaming), and the slice plan V-01…V-0n with dependencies on the existing queue (D-02 scan import/view subset, S-08 ViewState/HUD), each with an acceptance gate `G-VC-*`, estimated size, and the hardware classes it must pass on.
4. Registry: append rows for any new user-facing capability (e.g. `view.quality-governor` display properties, `view.transition-3d-2d`) to `docs/builder-program/REGISTRY.md` following its columns, with `owner: view-domain`; run `node scripts/registry-lint.mjs --summary` and leave it green.

Rules: reference default X4 — adopt what the references do unless an axiom conflicts; X2 — spend preprocessing (bake, precompute) to keep interaction fluid; X6 — pick numbers, mark tunable, cite evidence; no repo-external norms; cite-and-revise any spec decision you touch (VD-D*, PC-D*, VB-D7) instead of restating it. Do not modify existing spec files except appending a one-line pointer to the addendum under their catalog/decision sections. Budget: high effort, one pass; the dossier may be long, the addendum must be precise.
