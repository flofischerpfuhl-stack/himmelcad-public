Work package 0.5-08 — measurement basics (Measure/Inspect subset). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/measure-inspect/measure-inspect.md` MI-D1–MI-D14 (single point, 2D/3D distance, Δz; exact anchors; measurement plane; visible-precision rule; continuous update; saved measurements as canonical entities), ADR 0031 item 1 (the 0.5 measurement profile `hcad.measurement@1`: point, distance, height difference — S-01 landed the schema, readers and rows), `docs/builder-program/MASTER-PLAN.md` §5 row 23 (gates `G-MI-UNIT-MATH`, `G-MI-UNIT-ANCHOR`, `G-MI-COMMAND`, `G-MI-GESTURE`, `G-MI-VISIBLE-PRECISION`, `G-MI-CONTINUOUS`), the landed S-03 arbiter (a measurement tool arms and claims LMB click), S-10 construction bar (typed coordinates as the third input mode) and selection visuals, S-04 candidates (Up/Down cycling for ambiguous picks), the snapping in the viewer kernel (existing snap kinds), and the S-01 `MeasurementV1` types.

Deliverable through visible UI: Inspect ▸ Measure point / distance / height difference: arms a tool (arbiter claim), picks with snapping (existing snap kinds; the 1-of-N indicator for ambiguity), shows the live value while moving (continuous, no journal writes), commits on the second click into a saved measurement entity (`hcad.measurement@1`, one journal transaction, undoable), Δz uses the measurement plane rule (MI records); values displayed with the visible-precision rule (units from project settings, decimals per zoom, never more digits than the anchor precision supports); measurements listed in the tree under "Measurements", selectable, deletable, with Properties showing anchors and the metric; P11 rows `measure.point/distance/dz` + `measurement.list/delete` with the S-01 envelope; automation parity.

Visual brief (G17): measurement graphics in the viewport = a thin accent line between anchors with 6 px anchor squares (support-blue idle, orange selected), a label chip near the midpoint (mono 12 px, island-hi fill, border-subtle: "12.345 m" / "Δz 0.412 m"); the tool's docked panel shows the live readout in mono 14 px, the construction bar (S-10) exposes X/Y/Z for typed anchors; Escape cancels through the ladder (rung 4). Gallery section "Measurement tool" (live readout panel, label chip states); serial regeneration.

Gates: the six G-MI gates as the spec defines them (unit math against known coordinates to 1e-9 m, anchors exact, command parity, gesture through the arbiter incl. Up/Down cycling, visible precision, continuous update ≤ 1 frame behind the cursor — assert via V-01 rings on a fixture scene); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; registry rows under `owner: measure-inspect`; linter green. Evidence `docs/builder-program/evidence/0.5-08-measurement-basics-2026-09-08.md`. Renderer note: if the kernel lacks a payload for label chips/anchor squares, render them as DOM overlays anchored by projected positions this time and record the V-05 dependency; do not edit the render crate (V-03/V-05 territory). Concurrency: 0.5-01 viewing-box, 0.5-02 ground extraction, S-06d menus, S-07b snapshots — consume their APIs, do not edit their files. Budget: high effort, one pass.
