V-01c — put Builder's Electron renderer on the NVIDIA Quadro M2200 on this machine (Class W qualification needs it). Read `docs/builder-program/evidence/V-01b-harness-2026-09-06.md` (finding: the measured adapter was Intel HD Graphics 630 via WebGL2; NVIDIA PRIME initialization fails in Electron/ANGLE), `scripts/run-electron.mjs`, `apps/builder/electron/main.ts` (GPU/command-line switches), `scripts/perf/viewer-baseline.mjs`, and `crates/himmelcad-render/src/hardware_policy.rs` (adapter classification).

Investigate on the real display (DISPLAY=:0): `nvidia-smi`, `glxinfo -B` with and without `__NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia`, `vulkaninfo --summary` (is the NVIDIA ICD present?), Electron/Chromium switches (`--use-gl=egl`, `--use-angle=vulkan`, `--enable-features=Vulkan,DefaultANGLEVulkan`, `--enable-unsafe-webgpu`, `--ozone-platform=x11`, `--gpu-vendor-id`/`--gpu-device-id` hints), and chrome://gpu via CDP after each attempt; record which combination yields the Quadro as the active GPU for WebGPU (preferred) or WebGL2. If WebGPU on the Quadro works, prefer it (the kernel is wgpu-based; check which backend the kernel currently negotiates in Electron and why it fell to WebGL2). Make the working configuration the default for the baseline harness AND a documented opt-in for normal Builder start (env `HIMMELCAD_GPU=nvidia` → the switches; default unchanged for users), never a hard dependency. If no combination works, say so with the exact failures (driver 580, ANGLE, PRIME) and record that this laptop qualifies as Class I only; the Class W qualification then needs a real workstation (owner decision, recorded in the evidence).

Then, with the machine as quiet as you can get it (check `uptime`, `free -g`; do not start heavy work yourself; state the load honestly), run the baseline once on the best adapter you achieved and append the table to the dossier §5 with adapter, present source, and load. Gates: `pnpm --filter @himmelcad/builder typecheck`, `--filter @himmelcad/viewer test`; evidence `docs/builder-program/evidence/V-01c-nvidia-2026-09-08.md`. Do not edit render-crate scheduling or viewing-box files (other lanes). Budget: high effort, one pass.
