Task: second small refinement of scripts/registry-lint.mjs (node ESM; tests alongside). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. Do not commit. Read the script and its tests first, then run `node scripts/registry-lint.mjs --summary` and the dangling section.

Two remaining false-positive classes: (1) BS-D18–BS-D22 are defined in docs/builder-program/specs/bim-specs/bim-specs.md as `#### Decision record — BS-D18: …` headings (id followed by a colon) — extend the definition matcher so ANY markdown heading line containing an id counts, regardless of the delimiter after the id (`—`, `:`, `)`, end of line) (keep the rule: a definition is a heading line or a line-start bold run containing the id; a table cell or prose mention is a citation); (2) GAP-D* ids are defined in docs/builder-program/OWNER-STATEMENTS-2026-09-02-GAP.md, outside the specs tree — extend definition sources to every *.md directly under docs/builder-program/ (README, OWNER-*, REGISTRY, MASTER-PLAN, GAP, COORDINATION) while keeping catalog-row parsing limited to specs/. Add tests for both. Re-run, update docs/builder-program/evidence/I-07-registry-lint-2026-09-02.md counts (append a dated run), prettier the evidence file. Final message: counts per check before/after; list any dangling ids that remain and are therefore genuine.
