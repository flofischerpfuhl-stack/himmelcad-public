Work package V-03 — protected scheduler and explainable governor (Viewer Core). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/view/viewer-core-addendum.md` §1.3 (priority lanes 1–6, protected lanes), VC-D3 (hard budgets, protected lanes, fair datasets), VC-D4 (motion coarse / rest refines / bounded stale truth), VC-D11 (governor measured, hysteretic, explainable), §4 gates `G-VC-MIXED`, `G-VC-GOVERNOR`; `docs/builder-program/dossiers/viewer-performance-2026-09-04.md` §4.2 (governor limits today, with file:line) and §4.3; the V-01 evidence (rings, reason codes, phase timers) and V-02 evidence (frontier budgets, class policy, reason codes); the code: `crates/himmelcad-render/src/{scheduler.rs,frame_graph.rs,hardware_policy.rs,gpu_frame.rs,streaming.rs,tile_selector.rs,residency.rs}`, `packages/@himmelcad/viewer/src/kernel/{KernelStreamingDriver.ts,KernelViewerSession.ts,KernelFrameDiagnostics.ts}`.

Deliverable: (1) per-frame lane budgets: lanes 1–3 (camera/clip, cursor/tool/grips/selection/pick ids, canonical vectors/text/measurement graphics) are protected — their work is scheduled first every frame and never yields to cloud/mesh/raster refinement; lanes 4–6 receive the remainder with hard per-lane point/byte/draw/upload/decode caps; when protected work alone exceeds the class target the frame reports `protected_work_over_budget` (never silently drops protected content); (2) motion/rest policy (VC-D4): during camera motion the cloud/splat lanes run at the coarse tier with a resident fallback; at rest (250 ms after the last input) refinement resumes within 100 ms ordered from the viewport center and largest projected error outward; a reused cloud frame is labelled `reprojected` and lasts at most two presents or 50 ms; (3) the governor (VC-D11): measured hardware class I/W/D from the V-01 calibration seam (not core count), hysteretic tiering (enter a lower tier after N frames over target, leave after M frames under; N/M tunable), every tier change carries a reason code into the V-01 rings, and a query `view.quality.get` returns class, tier, targets, current reasons — the HUD "quality" field (S-08/S-08b) reads this; (4) mixed-benefit admission across clouds, meshes, rasters, splats with dataset fairness (V-02) under the lane caps.

Gates: `G-VC-MIXED` — a synthetic mixed scene (cloud + mesh + 5 000 lines + 500 labels) at 2× the class point budget keeps lane 1–3 work complete every frame while cloud density degrades (assert from the rings: zero frames with dropped protected primitives, reason codes present); `G-VC-GOVERNOR` — a forced-slow adapter fixture makes the governor tier down within N frames and back up within M after load is removed, with reasons; no oscillation over 600 frames of alternating load (hysteresis test); frame-output identity test: with the governor pinned to a tier, output is byte-identical to V-02's frontier for the same scene; `cargo test -p himmelcad-render` once in target/builder (CARGO_TARGET_DIR exported); `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, wasm staging. Do not run the timed baseline yourself (the architect runs it on a quiet machine); report the synthetic-scene statistics in the evidence `docs/builder-program/evidence/V-03-scheduler-governor-2026-09-06.md` with doctrine-format decisions and the tunables. Registry: `view.quality.get` row under `owner: view-domain`. Concurrency: 0.5-01 edits the viewing-box kernel and island; S-10 edits tree/bottom bar/construction bar; S-08b edits ViewState/HUD — consume their APIs, do not edit their files. Budget: high effort, one pass.
