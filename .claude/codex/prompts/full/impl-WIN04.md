Work package WIN-04 (PhotoLab lane, architect takeover) — the Windows COLMAP worker cross-build. Read `.claude/codex/prompts/_impl_brief.md`, then `scripts/build-colmap-worker-win-cross.sh` in full (vcpkg at the pinned commit, llvm-mingw 20260407-ucrt, COLMAP 4.1.0 at the pinned commit with `patches/colmap-4.1.0-no-copyleft.patch`, triplet `x64-mingw-static-himmelcad`, overlay ports, install into `vendor/colmap/win32-x64`), `vendor/colmap/win32-x64/VENDOR.json` (the expected `bin/` files with sizes and sha256 — the audited closure), `vendor/colmap/linux-x64/VENDOR.json` and the Linux worker layout under `.build/photolab-runtime/linux-x64/workers/colmap` (what a complete worker looks like), `scripts/stage-photolab-runtime.mjs` (`stageColmapRuntime('win32-x64')` — what it copies and verifies), and the state on disk: `vendor/colmap/win32-x64/{lib,include,share}` exist from an earlier attempt, `bin/` is EMPTY; `.build/colmap-worker/` and `.build/llvm-mingw/` may hold the previous build tree and toolchain cache.

Deliverable: a complete `vendor/colmap/win32-x64/bin/` (COLMAP worker executable(s) + the DLL/license closure VENDOR.json lists) produced by the script, then `node scripts/stage-photolab-runtime.mjs win32-x64` producing `.build/photolab-runtime/win32-x64/workers/colmap` that passes the script's own verification. Procedure: (1) run the build script as is with `HIMMELCAD_COLMAP_BUILD_ROOT=.build/colmap-worker` (reuse the cache; do not delete it), capturing the log under `.build/win-04/`; (2) if it fails, diagnose the exact stage (vcpkg port, CMake configure, link of `colmap.exe`, install step) and fix the build inputs only where the script/overlay/triplet is wrong for the current toolchain — never edit COLMAP sources beyond the pinned patch, never add new dependencies; (3) compare every produced `bin/` file against VENDOR.json: if sizes/hashes match, done; if the build is legitimately non-reproducible (timestamps, different toolchain minor), update VENDOR.json with the new sizes/sha256 and say so explicitly in the evidence (the reviewer decides whether the audit closure still holds — list every changed file); (4) stage for win32-x64 and run the staging verification; (5) evidence `docs/builder-program/evidence/WIN-04-colmap-worker-windows-2026-09-09.md` with the exact commands, timings, disk use, and the file table. Rules: never copy the repository or datasets; the vcpkg cache lives under `.build/colmap-worker` only; check `df -h /` before the build (need ≥ 25 GB free; abort and report otherwise) and after; keep cargo out of this (pure C++ cross-build); another lane runs a COLMAP CPU smoke in parallel — use `-j 4` for the build; do not commit (the architect commits VENDOR.json/script changes with explicit paths). Budget: high effort, one pass, three-hour hard stop; if the hard stop hits mid-build, leave the tree resumable and report the exact resume command.
