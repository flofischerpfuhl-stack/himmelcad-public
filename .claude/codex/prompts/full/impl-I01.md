Task: IMPLEMENT work package I-01 "Cargo resolver" of the Himmel:CAD Builder completion program (first autonomous implementation slice; Q1 Branch A active). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. Use CARGO_TARGET_DIR=target/builder for any cargo invocation (another session builds concurrently in its own lane — never touch target/ or target/photolab). Do not commit.

Read first: docs/builder-program/MASTER-PLAN.md §3 (iteration-speed package, I-01 definition and gate G-INFRA-CARGO), §9.3 (definition of done), docs/builder-program/COORDINATION.md (I-01 touches scripts/verification/** — shared substrate; the architect has announced it; edit only the files this package needs), docs/TEST-TIERS.md, docs/AGENTS.md principles, the build-env notes if present in docs (cargo lives in ~/.cargo/bin and is not always on PATH).

The defect (from the 2026-09-01 infra exploration, verify before changing): scripts/verification/planner.mjs spawns bare 'cargo' for rust.test:workspace, rust.fmt, rust.clippy, licenses.cargo-deny, automation.wire-rust, while every package.json goes through scripts/run-cargo.mjs precisely because cargo is not on PATH; scripts/verification/runner.mjs stops at first failure, so the local push gate dies before Rust tests/clippy/lint ever run (.build/verify/timings.json showed rust.test:workspace at 2 ms exit 1 on 2026-09-01). Fix: resolve cargo the same way run-cargo.mjs does (reuse its resolver — extract a shared helper if run-cargo.mjs has none — checking PATH, CARGO_HOME/bin, ~/.cargo/bin, rustup shims; clear error naming the searched locations when absent), apply it to every cargo/cargo-deny task the planner emits, keep behavior identical when cargo IS on PATH, and add a unit test for the resolver (node --test, in the existing test layout) plus a planner test asserting emitted cargo tasks use the resolved executable. Record before/after: capture the current .build/verify/timings.json summary (if present) as "before", run `pnpm verify:changed` (or the smallest verify tier that exercises the planner) as "after", and write the evidence to docs/builder-program/evidence/I-01-cargo-resolver-2026-09-02.md (document class: report — what changed, files, tests run and their results verbatim, what was NOT verified, gate G-INFRA-CARGO status). Respect P5/X1 in spirit: no behavior change beyond resolution; no reformatting of unrelated code. Final message: files changed, tests run + results, gate status, anything you could not verify.
