TAKEOVER PREAMBLE (architect, 2026-09-09 19:00): run the PhotoLab A7c smoke as described below. The A7c implementation landed in b9b092b; the exact command the implementer recorded is in `.claude/codex/out/impl-A7c.last.md` §"READY FOR SMOKE" (HIMMELCAD_PHOTOLAB_FORCE_EXTRACTION_TILES=2x1, Sulzberg 24 images, profile fast, target EPSG 31468 / vertical 7837, sidecar target/photolab/debug). Build the sidecar once in target/photolab first (`cargo build -p himmelcad-sidecar --bins`). Check `uptime` (load < 3 over 5 min; wait up to 15 min) and `free -g` (≥ 20 GB available) before the run; abort the run and report NOT IDLE otherwise. Peak-RSS per stage and wall time go into the evidence `docs/builder-program/evidence/A7c-smoke-2026-09-09.md`; do not commit; 90-minute budget.

IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-A7c — tiled ALIKED extraction end to end (owner D12/S23; unblocks the 16 GB gate run). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7", "WP-A7b" (status paragraph) and "WP-A7c", and the code named below.

What exists: WP-A7b landed the memory rule (a unit of work is checked against the whole usable envelope; stage shares govern worker counts only), the tile planner (`extraction_tiling { columns, rows, overlap_px }` in crates/himmelcad-sidecar/src/job_runtime.rs, X6: overlap 256 px, tile floor 1,024 px), the deterministic merge kernel in crates/himmelcad-sidecar/src/dedode_colmap_bridge.rs (shift tile coordinates, 2 px de-duplication keeping the higher score, global top-k), the typed job record `extractionTiled { tiles, overlapPx }` (photolab_jobs.rs, index.ts) and an interim admission REFUSAL in main.rs (~line 7161) when tiling would be required, because the curated COLMAP worker writes ALIKED keypoints/scores/descriptors only into its SQLite database (`features/aliked/database.db`, see colmap_runtime.rs ~1780/2192) and the sidecar has no SQLite reader.

Files you may edit: crates/himmelcad-sidecar/Cargo.toml (add `rusqlite` with the `bundled` feature — agreed with the Builder lane; sidecar-only, never himmelcad-core/io; the shared Cargo.lock change is expected), crates/himmelcad-sidecar/src/{colmap_runtime.rs,dedode_colmap_bridge.rs,job_runtime.rs}, a NEW crates/himmelcad-sidecar/src/colmap_feature_db.rs (+ `mod` line in lib.rs — one line, announced), crates/himmelcad-sidecar/src/main.rs ONLY the refusal block at ~7161 (replace refusal by the tiled path) and its call site; crates/himmelcad-sidecar/src/project_runtime.rs ONLY the `ColmapOutputSummary` initializers in tests (~17445, ~17682) if you add the summary field. Do not run e2e, real COLMAP, the visual harness or vite; a DSM/DTM smoke may still be running; unit tests only; `free -g` before tests allocating > 1 GB.

Implement:
1. `colmap_feature_db.rs`: read-only + write access to the COLMAP database schema used by the vendored COLMAP (inspect a real `database.db` produced by the unit-test fixtures or derive the schema from COLMAP's `database.cc`: tables `images`, `keypoints(image_id, rows, cols, data BLOB float32 x,y,scale,orientation or 6-col affine)`, `descriptors(image_id, rows, cols, data BLOB uint8 or float32 for ALIKED)`; verify the actual column layout the ALIKED extractor writes and assert it in a test — do not assume). Functions: `read_image_features(db, image_name) -> { keypoints: Vec<[f32; N]>, descriptors: Matrix, scores? }` (ALIKED scores: check whether COLMAP stores them; if not, use the keypoint order/size as the score proxy and STATE IT), `write_image_features(db, image_id, keypoints, descriptors)`; all typed errors; fixtures built with rusqlite in tests.
2. Tiled path in colmap_runtime.rs: when the memory plan carries `extraction_tiling`, for each image write the tiles (resized image split into columns × rows with overlap, temporary files under the scratch, deterministic names), run the ALIKED `feature_extractor` over the tile set into a scratch database with the same parameters (`max_num_features` per tile = the per-image budget so top-k after merge is meaningful), read the per-tile features back (item 1), merge with the A7b kernel (shift by tile origin; de-duplicate; global top-k = per-image budget), and write the merged features for the original image into the alignment database (item 1 write, or `feature_importer` if the write path proves unsafe — say which and why). Preserve cancellation (check between tiles), the per-image hash lineage, and the feature-store cache identity (tiling parameters become part of the cache key). Record `extractionTiled` in `ColmapOutputSummary` (additive, serde default) and thus in the alignment lineage.
3. main.rs: replace the interim refusal by the tiled plan; the refusal remains only when even the smallest allowed tile does not fit (then `ExtractionEdgeReduced` applies per A7b) or when the tiled path is unavailable for the selected feature store (SIFT is never tiled — it does not need it; DeDoDe: out of scope, refuse typed).
4. Determinism: same input → identical merged feature set and identical alignment database content hash (unit test on synthetic features; document that COLMAP's own extraction is deterministic on CPU).

Tests (verbatim counts): colmap_feature_db round-trip on a rusqlite-built fixture with the asserted schema; tiled path unit test with a fake extractor (the existing fake COLMAP rig in colmap_runtime tests) over a 2 × 1 tiling producing merged features with the expected count and coordinates; cancellation between tiles; cache-key includes tiling; `cargo test -p himmelcad-sidecar` lib + bins (the two Builder-lane failures are not yours; cancellation-timing tests may flake under load — rerun serially), `cargo test -p himmelcad-core`, `cargo fmt --all -- --check` on your files, `cargo build -p himmelcad-sidecar --bins` must succeed with the bundled SQLite (report the Cargo.lock diff size).

Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only. Final summary: the asserted database schema (columns, types), how scores are obtained, where the tiled path enters and leaves COLMAP, the lineage field, files changed with line references, verification counts, what was NOT verified (real COLMAP run — the PhotoLab session runs the 24-image smoke with forced tiling via an env override you must add, e.g. `HIMMELCAD_PHOTOLAB_FORCE_EXTRACTION_TILES=2x1`, documented), contract gaps by record id.
