S-06d — two shared-substrate findings from the PhotoLab lane on the S-06 command surfaces (capture: `apps/photolab/test/visual-baselines/1440x900/context-menu-product.png`, read only): F15b — the shared `exportableSelection` predicate hard-codes `!cloud`, so PhotoLab's product export (LAZ/E57/DEM/mesh packages) is unreachable from the entity tree, its only UI entry; F15c — PhotoLab's product context menu shows Builder-only rows (Restore bookmark, Isolate, Display properties). Read `docs/DECISION-DOCTRINE.md` P11, `docs/builder-program/specs/ui-platform/ui-platform.md` UIP-D5/UIP-D6/UIP-D15, the S-06/S-06c evidence, `packages/@himmelcad/app/src/commands.ts` (product/kind/cardinality filtering from S-06c), the generated command table source, `packages/@himmelcad/ui/src/{EntityTree,CommandSurfaces}.tsx`, and PhotoLab's P11 rows in `docs/photolab-automation-command-rows.md` + its `automationCommandRows` (read only; never edit apps/photolab).

Architect decision: enablement predicates are declared per row in the table, never hard-coded in the UI: (1) every row gets an explicit `products: ['builder'] | ['photolab'] | ['builder','photolab']` field (default: the owning spec's product; rows without an owner product are shared) — Builder-only rows (bookmark restore, isolate, display properties, viewing-box actions) declare `['builder']` and disappear from PhotoLab menus; (2) the export row's predicate becomes table-declared per product: Builder keeps UIP-D15's rule that clouds are not click-selectable but export of a deliberately selected cloud IS allowed (the `!cloud` exclusion was never a spec rule — cite UIP-D15: clouds are selected deliberately via tree/RMB, and export acts on the selection), PhotoLab declares clouds, DEMs and meshes exportable; (3) `exportableSelection` in the UI is removed in favor of the row predicate evaluated by the shared `commandsForSurface`; (4) a table lint asserts every row has `products` and that no Builder-only row leaks into the PhotoLab fixture menu (snapshot test for a PhotoLab product node and a Builder polyline node).

Gates: generated-table `--check`; app/ui tests incl. the two snapshots; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; gallery "Command surfaces" rows updated (PhotoLab product node menu now with Export…, without Builder-only rows); serial regeneration. Append "S-06d" to the S-06 evidence with the row→product table PhotoLab should verify. Concurrency: do not touch viewing-box, FunctionPanel, snapshot or import files (other lanes). Budget: medium effort, one focused pass.
