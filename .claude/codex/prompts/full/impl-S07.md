Work package S-07 — journal group commit and durability indicator (minimum substrate for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/file-project/file-project.md` FP-D19 (group commit), FP-D20, FP-D21 (local-history streams), FP-D5/FP-D11 as consumers, `docs/builder-program/OWNER-DECISIONS.md` D1 (journal persistence, visible Save control, P5 guarantees), `docs/DECISION-DOCTRINE.md` P5/P6, `docs/DESIGN-SYSTEM.md` "UI copy" (never claim saved state that is not verified), the current journal implementation (`crates/himmelcad-core` journal/persistence modules, the sidecar project runtime, `apps/builder/renderer/src/project.ts`), and the S-01 admissions (snapshot markers `hcad.snapshot-marker@1`, local histories).

Deliverable: asynchronous group commit of journal appends off the UI/render thread (P5: continuous gestures journal once at gesture end; appends batched with a bounded flush interval — X6 tunable, default 50 ms — and fsync policy per FP-D19), a durability acknowledgement path from the sidecar to the renderer, the "stored" indicator in the StatusBar reflecting true durability with bounded lag and an explicit failure state (copy per DESIGN-SYSTEM/D1: "Stored", "Storing…", "Not stored — <reason>" with a retry action), the visible Save control per D1/P6 (Ctrl+S = flush + snapshot marker via the S-01 schema, never a modal; Save As = archive path), P11 rows `project.flush/snapshot.create/snapshot.list`, and crash-recovery: on reopen, an unflushed tail is replayed or reported, never silently dropped.

Visual brief (G17): indicator = StatusBar left cluster, 12 px UI text with a 6 px dot (success token = stored, muted = storing with the S-02 Spinner 12 px, error token = failure) — no animation on stored; the Save ribbon button keeps its place (P6); a failure toast (error kind) carries "Retry". Gallery section "Durability indicator" with the three states.

Gates: `G-FP-P5` — zero journal writes during a 2 s simulated drag, exactly one at completion; gesture-end → durability ack ≤ 100 ms p95 and ack → indicator ≤ 50 ms p95 on this machine (report numbers); crash test — kill the sidecar mid-flush, reopen, tail recovered or reported; snapshot marker round-trip; `cargo test -p himmelcad-core` in target/builder (CARGO_TARGET_DIR exported; do not run the full workspace); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`. Shared-substrate notice: the sidecar `main.rs` is also edited by the PhotoLab lane (their jobs/save handlers) — restrict sidecar edits to the Builder project-runtime path and announce the touched functions in the evidence; `packages/@himmelcad/data/src/index.ts` additive only. Evidence `docs/builder-program/evidence/S-07-journal-2026-09-04.md`. Budget: medium effort, one pass.
