Work package S-04 — selection substrate (minimum for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md` first, then `docs/builder-program/specs/ui-platform/ui-platform.md` decision records UIP-D2, UIP-D3, UIP-D15, UIP-D16, UIP-D17, UIP-D18 and §7.8 (indicator copy), `docs/builder-program/specs/select-edit/select-edit.md` §1–§3 and SE-D1–SE-D6, SE-D18, SE-D19 (stable segment locator, admitted by ADR 0031 item 11 and implemented in S-01 — reuse `hcad.curve-subentity-ref@1`), `docs/DECISION-DOCTRINE.md` P8/P9, ADR 0031 item 12 (local histories — S-01 landed `hcad.local-history@1`; the selection stream is one of its three instances), and the landed S-03 arbiter (`packages/@himmelcad/viewer/src/kernel/PlatformGestureArbiter.ts`) whose idle click/ctrl+click/double-click callbacks you now connect to a real store.

Deliverable: one selection store of record for Builder (in `packages/@himmelcad/app`, consumed by `apps/builder` and exposed to automation), with:
- Set semantics per UIP-D2 (replace / toggle / clear / idempotent mouse click / touch tap-again), UIP-D15 (clouds/splats never click-selected; bounding-box halo treatment flag), UIP-D18 (prune on delete at journal-apply, survive hide, store-then-unload on project switch).
- P8 local history per UIP-D3 / ADR 0031 item 12: own undo/redo (not Ctrl+Z; the command ids `select.undo`/`select.redo` and their shortcuts per the registry), persisted independently via the S-01 local-history stream (`hcad.local-history@1` selection instance), corruption-scoped recovery.
- Candidate set + indicator state per UIP-D16: "N of M under cursor — Up/Down cycles" status text (exact copy from §7.8), Up/Down cycling through the arbiter's `setCandidateIndicator`, invalidation on every listed event, "Select under cursor ▸" context-menu contribution (register through the UIP-D6 command registry if it exists; otherwise expose the data and leave the menu row for S-06 with a note).
- Multi-select properties model per UIP-D17: a pure function `sharedPropertySet(selection) → {count, perKind, fields: {key, value | MIXED}}` plus `assignToAll(field, value)` producing one journaled command batch; the Properties tab renders count + Mixed markers using existing shared controls (NumberInput from S-02 for numeric fields).
- Automation parity (X3/P11): `select.get/set/toggle/clear/undo/redo/candidates` rows in the single command table with the S-01 envelope; the App's existing automation selection path becomes a wrapper over the store (no second store).

Visual brief (G17) for the two UI pieces: status-bar indicator = mono 12 px text in the existing StatusBar left slot, appears only while a candidate set is live, no animation; Properties multi-select header = "3 selected" in the tab header (UI 13 px medium) with a per-kind breakdown line below in muted text ("2 points · 1 polyline"), Mixed marker = the word "Mixed" in muted italic inside the field, replaced by the typed value on commit. Provide a gallery fixture for both in `packages/@himmelcad/ui/gallery` (do not edit S-02's fixtures; add a new section "Selection surfaces") and regenerate the shots.

Gates: `G-SE-CORE` — unit tests for every UIP-D2 row per modality, UIP-D15 exclusion, UIP-D18 prune/survive/switch; `G-SE-P4` — hidden entities are not selectable by click but stay in the set; `G-B2-HISTORY` selection instance — undo/redo across 1 000 steps in < 50 ms each, persisted and rehydrated, corrupted stream recovers to a valid baseline without touching the document; extreme-member test — a selection of 10⁵ ids toggles/clears in < 150 ms and the properties model is computed lazily; automation parity test — every row round-trips through the automation host; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` (if PhotoLab fails only inside apps/photolab/renderer/src/jobsChip*, that is the other lane's in-flight work — report, do not touch). Evidence `docs/builder-program/evidence/S-04-selection-2026-09-04.md` with file:line per decision, test names, timings, gallery shot paths.

Concurrency: S-02b edits Button/Dialog/Menu/Toast and their fixtures — do not touch those files. S-05 edits Electron main jobs and the jobs UI — do not touch job code. Budget: medium effort, one pass.
