Work package S-08 — ViewState v2 and the presented-frame HUD (minimum substrate for Release 0.5). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/view/view-domain.md` VD-D3 (ViewState), VD-D8 (two-layer display model), VD-D10 (instrumentation), VD-D13, VD-D14 (camera history), `docs/builder-program/specs/view/viewer-core-addendum.md` §1 and VC-D1/VC-D11 (HUD seam, explainable governor), `docs/adr/0031-release-0-5-data-model-admissions.md` item 3 (the 0.5 ViewState profile, already parsed by S-01 in `packages/@himmelcad/app/src/view.ts`) and item 12 (camera/display local-history streams; the selection stream is live since S-04 — reuse its persistence), the V-01 diagnostics contracts (`view.diagnostics.get/sample`, the typed HUD snapshot), `docs/DECISION-DOCTRINE.md` P8/P9, and the Builder viewport code (`apps/builder/renderer/src/BuilderKernelViewport.tsx`, viewer kernel session).

Deliverable: (a) ViewState v2 as the live state of record for Builder: entity-referenced clips (viewing box ids), VD-D8 display layers (global defaults + per-node overrides, P9 states), view presets (top/front/right/perspective — S-06 exposed the command rows; define the axis convention from the repo's existing camera code and cite it), bookmarks as canonical entities per P1 (`view.bookmark.create/list/restore` rows), automation `view.state get/set`; (b) display and camera local-history streams (P8): `view.display.undo/redo`, `view.camera.undo/redo` with their own persistence via the S-01 local-history schema, camera history journaling once at gesture end (P5); (c) the HUD: a viewport overlay reading the V-01 snapshot — presented-frame interval p50/p95 for the last 2 s, points drawn, budget reason, quality tier, residency backlog — toggled by a registry command `view.hud.toggle` (default off) and by the console; the existing telemetry window keeps working.

Visual brief (G17): HUD = top-left of the viewport, 8 px inset, mono 11 px, two lines on a translucent island-hi surface with border-subtle, no animation: line 1 "24.1 ms p95 · 16.4 ms p50 · 41.2 M pts", line 2 "quality W-2 · budget: gpu · backlog 3"; values right-aligned within fixed-width columns so digits do not jitter; the p95 turns warning-tone above the class target and error-tone above 2× target. View presets and bookmark actions appear in the ribbon View tab (existing Ribbon component) and in the S-06 quick surface (already listed). Gallery section "Viewport HUD" with the three tone states, plus "View presets" showing the ribbon group; regenerate shots serially.

Gates: `G-VD-STATE` — round trip of ViewState v2 through the S-01 parser and the journal; P8 tests: display toggle never enters the document journal, camera undo restores the previous pose, both streams rehydrate after reload; presented-frame HUD gate: HUD values equal `view.diagnostics.sample` for the same window (test with the V-01 fixture rings); HUD render cost: adding the HUD changes presented-frame p95 by ≤ 0.5 ms on the V-01 fixture scene (report the number); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; registry rows + linter green. Evidence `docs/builder-program/evidence/S-08-viewstate-hud-2026-09-05.md`. Concurrency: P-01 edits Builder's project open/save paths and the sidecar project store — do not touch those. Budget: medium effort, one pass.
