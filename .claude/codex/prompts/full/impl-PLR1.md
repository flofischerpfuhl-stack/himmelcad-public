Work package PL-R1 (PhotoLab lane, architect takeover) — steps 2 and 3 of "Path to PhotoLab release" in `docs/photolab-release-acceptance-status-2026-09-19.md`. Read that document (rows for the release inventories, the Python SDK / automation rows, R22), the PhotoLab lane rules (first 12 lines of `.claude/codex/prompts/photolab/a7c.md`: CARGO_TARGET_DIR=target/photolab, no repository/dataset copies), `scripts/stage-photolab-runtime.mjs`, `scripts/check-photolab-release-inventory.mjs`, `vendor/colmap/linux-x64/VENDOR.json`, `scripts/generate-automation-sdk.py`, `sdk/python/generator-manifest.json`, and `docs/builder-program/evidence/PL-B1a-potreeconverter-windows-2026-09-11.md` + `WIN-04-colmap-worker-windows-2026-09-09.md` (what is staged for win32-x64 and what is not: the Windows Geo source tree is missing on this laptop; `.build/photolab-runtime/win32-x64/workers/geo` was restored from the PC).

Deliverable: (1) Linux release inventory green: find why `check-photolab-release-inventory.mjs linux-x64` fails closed (the reconciliation says the staged Linux COLMAP subtree is absent), restage `linux-x64` with the audited script from the vendored/approved inputs only (no downloads beyond what the scripts pin; if an input is missing, name it exactly and stop that part), rerun the inventory, record counts. (2) Windows inventory: run `check-photolab-release-inventory.mjs win32-x64`, list every missing input with the script/step that produces it (cross-target `x86_64-pc-windows-gnullvm` sidecar + portable MVS via `scripts/build-photolab-windows-rust.mjs`, Geo/GDAL source, DeDoDe embedded Python, MSVC/LLVM-MinGW runtime closure); execute the ones that are pure builds on this laptop within `systemd-run --user --scope -p MemoryMax=12G -p MemorySwapMax=0` and `-j 4` (the Rust cross-build: yes; anything needing a multi-GB download: no — list it); rerun the inventory and record what remains. (3) Python SDK drift: regenerate via the documented generator, review the diff (it must only reflect command rows landed since the last manifest — R-02…R-02e, G1b, V-06, V-08), run the SDK tests to 14/14 and `pnpm --filter @himmelcad/app test` (generated-table freshness), and the automation-host suite (the one skip caused by the installed Codex version mismatch: report the versions, do not change the pin). (4) Update the status rows you closed in `docs/photolab-release-acceptance-status-2026-09-19.md` with evidence; evidence file `docs/builder-program/evidence/PL-R1-release-inputs-2026-09-19.md`. Rules: check `df -h /` first (≥ 15 GB; abort otherwise) and keep build trees under `.build/`; never touch DISPLAY=:0; do not commit; two-hour budget; each command at most twice. Budget: high effort, one pass.
