Work package PL-H5r (PhotoLab lane, architect takeover — no PhotoLab session is active) — reconcile the release acceptance checklist with executed evidence, and execute the cheap open items. Read `.claude/codex/prompts/photolab/a7c.md` lines 1–12 only for the PhotoLab lane rules (CARGO_TARGET_DIR=target/photolab, no repository/dataset copies, scratch under `.build/codex-scratch/`), then `docs/implementation-plans/2026-09-photolab-release-polish.md` §"Acceptance checklist" (45 unchecked items, every one "open — not executed") with each WP's own acceptance text earlier in that file, `docs/builder-program/PHOTOLAB-HANDOFF.md` in full (what was executed since 2026-09-02: smokes, A7 series, G1a/G1b/G1c, H1–H5, F3 audits, WIN-01…WIN-16), `docs/builder-program/PHOTOLAB-G1C-MATRIX.md`, every `docs/builder-program/evidence/*` and `docs/photolab-*` evidence file the handoff cites, `.build/photolab-evidence/`, and the H5 evidence ledger (`docs/photolab-evidence-ledger*.md` or the path the plan names).

Deliverable: (1) a table `docs/photolab-release-acceptance-status-2026-09-19.md`: one row per checklist item — status EXECUTED (evidence path + commit + date, and whether the evidence matches the item's exact acceptance text), PARTIAL (what is covered, what is missing, the exact command/run that closes it, estimated runtime, machine: laptop / Windows PC / needs a human), NOT EXECUTED (same closing info), or PARKED (cite the owner decision that parked it). Never mark EXECUTED on inference: the evidence must show the named run with its result. (2) For items closable by a test/command that runs < 15 min on this laptop within an 8 GB memory cap and needs no GUI human (unit/integration suites, fixture-based acceptance, `pnpm --filter @himmelcad/photolab test`, the G-1 coverage test, the filtered sidecar suites, the English UI check, release-inventory checks), run them now, once each, record verbatim counts, and mark them in the table. Do NOT run COLMAP/MVS smokes, the 135-image golden, or anything ALIKED — list those under "heavy runs" with the capped-unit command line (`systemd-run --user --unit=… -p MemoryMax=18G -p MemorySwapMax=0 -p LimitCORE=0`). (3) Update the checklist in the plan: tick only items whose evidence fully matches; append the evidence pointer to each ticked line. (4) A closing section "Path to PhotoLab release": the ordered list of remaining runs with machine, runtime, and which need the owner (hands-on passes, PC unlocked for GUI, installer decisions such as whether Windows is a supported release platform — WP-F4 — and whether WP-A6/E2/E4 stay parked). Do not commit. Two-hour budget; each command at most once (twice if the first failure was environmental). Budget: high effort, one pass.
