Work package PL-B1a (Builder-side item for the PhotoLab Windows release inventory) — PotreeConverter win32-x64 staging. Read `.claude/codex/prompts/_impl_brief.md`, `scripts/build-potreeconverter-windows.mjs` in full (the audited cross-build: PotreeConverter 2.1.1 at the pinned commit with the repository's patches, llvm-mingw 20260407 toolchain, static LASzip/C++ runtime closure), `vendor/potreeconverter/win32-x64/VENDOR.json` (expected `PotreeConverter.exe` sha256), `vendor/potreeconverter/linux-x64/` (a complete worker for comparison), `scripts/stage-photolab-runtime.mjs` (`stagePotreeConverter`/how win32-x64 consumes it), and `docs/builder-program/evidence/WIN-04-colmap-worker-windows-2026-09-09.md` (the sibling COLMAP cross-build: toolchain cache location `.build/llvm-mingw`, disk rules, VENDOR.json reproducibility handling). Deliverable: `vendor/potreeconverter/win32-x64/PotreeConverter.exe` (+ any DLL/license closure the VENDOR.json lists) produced by the script; compare against VENDOR.json — if the hash differs only by the PE link timestamp (same size, same imports), update VENDOR.json and say so; then `node scripts/stage-photolab-runtime.mjs win32-x64` must accept the PotreeConverter part (the geo source tree may still be missing on this machine — report that part as before, do not delete retained workers: check whether the script still removes `workers/geo` before validating; if it does, run only the PotreeConverter staging step or fix the script's order minimally and record the change). Evidence `docs/builder-program/evidence/PL-B1a-potreeconverter-windows-2026-09-11.md` (commands, timings, disk before/after, file table). Rules: build under `.build/potreeconverter-worker/` only, never copy the repository; `-j 4`; check `df -h /` first (≥ 15 GB free; abort otherwise); no cargo; do not commit; 90-minute budget; if the hard stop hits mid-build leave the tree resumable and report the resume command. Budget: high effort, one pass.
