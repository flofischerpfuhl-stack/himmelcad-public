Task: INCREMENTAL registry update (mechanical; no redesign; no LLM rebuild). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. Do not commit. Run `node scripts/registry-lint.mjs` (and `--summary`) first and read docs/builder-program/README.md "Rules" (registry rules incl. the `owner:` consumer-row convention).

Apply ONLY what the linter reports: (1) function ids present in a spec catalog but absent from docs/builder-program/REGISTRY.md → add the row to the matching §1 table by copying the spec's catalog row into the registry's column layout (Id | Tab · group | Access | Surface | Perf | Automation | Spec link | Status) — this covers registration-stations RS rows, mesh-terrain §11 and pointcloud batch-3 rows, import-formats IF-D19–D34 rows, civil CIV rows if missing; add a §1.14 table for registration-stations if none exists; (2) registry ids absent from any spec → check whether the spec renamed the id (search the spec for the closest id) and update the registry row to the current id, else mark the row "retired — not cataloged by any spec" in Status (never delete silently); (3) status mismatches → update REGISTRY §4.5 to list every spec with its CURRENT status line, and for specs whose only blocker was missing rows, set the spec's status line to "specified (registry 2026-09-02 incremental)"; (3b) the three remaining definition duplicates get owner marking per the README convention — `view.mode`: owner view-domain (ui-platform's bottom-strip row becomes `owner: view-domain`); `view.point-size`: owner pointcloud (per VD-D8 per-entity display + view-local multiplier; view-domain's row becomes `owner: pointcloud`); `view.station`: owner registration-stations (VD-D11 discharged; view-domain's row becomes `owner: registration-stations`) — edit only the non-owning spec's Spec-link cell; (4) genuine dangling decision ids → list them in the final message, do not edit specs; (5) re-run the linter until only genuine findings remain; append a dated "Incremental update 2026-09-02 late" note to REGISTRY §4.1 with before/after counts. Prettier on REGISTRY.md and any touched spec. Final message: rows added (by spec), rows retired/renamed, status changes, remaining findings.
