Work package 0.5-06 — DGM editing: region smoothing, then downsampling (Mesh/Terrain D-RW-05/06). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/mesh-terrain/mesh-terrain.md` MT-D13–MT-D24 (surface editing: region selection, smoothing = cut-out-and-fill with protected breaklines/boundaries, simplification/downsampling with certified error, error policy, outside-region identity), MT-D25 (recipe lifecycle — edits are derived generations with recipes, P10), MT-D27+, the owner statements (S-batch-2: "DGM bearbeiten incl. glätten (ausschneiden & füllen)"), `docs/builder-program/MASTER-PLAN.md` §5 row 21 (gates `G-RW-DGM-SMOOTH` then `G-RW-DGM-DOWNSAMPLE`: outside-region identity, protected breaklines/boundaries, certified error, deterministic bake, undo/cancel/restart), the landed 0.5-05 (DGM window, `mesh_surface.rs`, `mesh_surface_runtime.rs`, constrained TIN with `spade`, prepared triangle-mesh datasets, recipe/source roles), 0.5-02a (fence tool — reuse the fence volume for region selection on a mesh), S-05 jobs, S-07 durability, V-02.

Deliverable through visible UI: Mesh ▸ Edit surface: (1) region selection on the selected DGM by fence (polygon/rectangle via the 0.5-02a fence tool, projection-true) or by existing boundary polyline; (2) Smooth region: cut out the region's triangles and refill by constrained re-triangulation from a smoothed/thinned vertex set (Gaussian or median height filter with radius; breaklines and boundaries inside the region are protected constraints and never move; vertices outside the region are byte-identical after the operation — the outside-region identity gate), preview overlay before apply, apply = one journaled derived generation with recipe (P10; the source generation stays as authority, undo restores it); (3) Downsample: simplification with a certified maximum vertical error (tunable target, e.g. 0.02 m) reported per result and enforced (edge-collapse or vertex-decimation with error tracking), protected constraints kept, deterministic bake; (4) both as registered cancellable jobs with phases, checkpoints and restart (`G-B2-MESH-RECOVERY` pattern from 0.5-05); (5) typed result (vertices before/after, max/RMS error vs source, region area) in the panel and console; P11 rows `mesh.edit.region.select`, `mesh.edit.smooth`, `mesh.edit.downsample` with the S-01 envelope; automation parity with typed polygons.

Visual brief (G17): docked "Edit surface" panel: region source segmented control (Fence / Boundary polyline), region readout in mono ("Region 1 248 m² · 6 412 vertices"), Smooth group (filter Select Gaussian/Median, radius NumberInput m, "Preview" quiet, "Smooth" primary), Downsample group (target error NumberInput m, "Downsample" primary), result group in mono ("Vertices 61 204 → 18 731 · max error 0.019 m · RMS 0.006 m"), running state with phase + Cancel; viewport: region fill 8 % accent, protected constraints drawn support-blue, preview triangles at 50 % alpha (V-05 payloads). Gallery section "Edit surface panel" (region selected, preview, downsample result); serial regeneration.

Gates: `G-RW-DGM-SMOOTH` — outside-region identity (hash of outside vertices/triangles unchanged), protected constraints unchanged, deterministic result for the same input (hash), undo restores exactly; `G-RW-DGM-DOWNSAMPLE` — certified error ≤ target on every vertex (test measures against the source TIN), constraints kept, deterministic; recovery: kill mid-bake → no partial generation, restart resumes; real DGM flow on the 0.5-05 road DGM (region on the road surface; state timings and peak RSS; machine load stated); `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, theme lint, `cargo test -p himmelcad-core mesh_surface` and `-p himmelcad-sidecar mesh` once each in target/builder, `cargo check -p himmelcad-sidecar --tests --bins` before you report; registry rows `owner: mesh-terrain`, linter green. Evidence `docs/builder-program/evidence/0.5-06-dgm-editing-2026-09-09.md`. Rules: never copy the repository or datasets; each gate at most three times; two-hour budget; do not commit. Concurrency: 0.5-07 edits export wiring — consume, do not edit. Budget: high effort, one pass.
