Task: AMEND an existing specification (do not rewrite it). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. File: docs/builder-program/specs/file-project/file-project.md. Read it fully first, then: docs/builder-program/OWNER-DECISIONS.md (D1 — amended 2026-09-02 with the visible Save control and non-interruption guarantees), docs/DECISION-DOCTRINE.md (precedents P5 and P6, new), docs/builder-program/REGISTRY.md section 4.2 (findings F3, F7, F9), docs/builder-program/specs/ui-platform/ui-platform.md (UIP-D10 job registry).

Amendments, each recorded in the disposition table under a new "owner correction 2026-09-02 / registry reconciliation" heading, with decision records updated in doctrine format:
1. Save control (owner correction, D1 + P6): FP-D2 currently removes the Save button and binds Ctrl+S to an affirmation. Revise: a visible Save control exists in the File tab (catalog row `file.save`, Ctrl+S, automation `project.flush`) whose effect is (a) force the durability flush of pending group commits, (b) affirm "All changes stored · <time>" in the status bar/toast, never claiming unverified state (loud failure state stays); its split-button dropdown offers "Save snapshot…" (→ existing snapshot flow) and "Save As…" (→ existing archive copy). Update section 1.3 narrative, the shortcut table, B1/B2 answers, E1 criteria, and verification (test: Ctrl+S during a pending group commit completes the flush and the indicator reflects it; Ctrl+S with a failed journal shows the failure state, never "stored").
2. P5 non-interruption guarantees: add a decision record citing P5 with the named gates from D1 (commit-latency p95 budget on the interaction tier, zero journal writes during a scripted drag, indicator-lag budget — X6 tunable values), and the automatic "Session start" snapshot marker on open (section 1.4 + catalog).
3. F3: record the ui-platform-queued "apply answers to similar imports" item in FP-D14 (it is now owned by the import-formats spec — cite by name, do not specify it).
4. F7: cite UIP-D10 for long-running operations (export, pack, attach bake, maintenance) — chip/jobs island/cancel.
5. F9: claim Ctrl+Y as a redo alias alongside Ctrl+Shift+Z in the shortcut table.
Only modify file-project.md. Final message: what changed, by item.
