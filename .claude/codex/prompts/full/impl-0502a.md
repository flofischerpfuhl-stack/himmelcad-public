Work package 0.5-02a — fence segmentation (Pointcloud, owner statement S21: segmentation is part of Release 0.5). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/pointcloud/pointcloud.md` PC-D5 (projection-true fence volumes: prism / eye-apex frustum / box; rectangle, polygon, typed extents), PC-D6 (keep inside / remove inside as the X5 pair; edited revisions; undo restores the source), the fence gates `G-PC-A`…`G-PC-E` (find the fence subset) and `G-RW-SEGMENT` in `docs/builder-program/MASTER-PLAN.md` (M-RW "segmentation" row + the S21 insertion note), `docs/DECISION-DOCTRINE.md` P2 (segmenting out bakes a reduced resident dataset — must feel as fast as a locked box), P4 (acts on the visible set), P5, X5; the dossier `docs/builder-program/dossiers/realworks*.md` §4–5 (RealWorks segmentation: fence draw, in/out, sampled working cloud); landed pieces: S-03 arbiter (fence drawing is an armed tool claiming LMB), S-04 selection (clouds selected deliberately), S-05 jobs (a bake is a registered cancellable job), S-07 durability, S-10 construction bar (typed extents), V-02 frontier (the segmented result is a prepared dataset), 0.5-02 (derived cloud + recipe pattern), and the viewing-box kernel (`KernelViewingBox.ts`, `KernelClipCapCoordinator.ts`) for volume math to reuse — do not edit those files (0.5-01 in flight).

Deliverable through visible UI: Pointcloud ▸ Segment: draw a rectangle or polygon fence in the viewport (tool armed through the arbiter; Escape rung 4 cancels; typed extents via the construction bar), the fence becomes a projection-true volume (prism in orthographic views, eye-apex frustum in perspective) applied to the visible set; two symmetric actions "Keep inside" / "Remove inside" produce an edited revision of the cloud as one journal transaction (undoable; source truth retained), bake the reduced dataset as a registered cancellable job so the result streams like native small data (P2 parity test vs a natively small cloud), and record the recipe (P10). Repeated segmentation composes (edit chain on revisions). P11 rows `pointcloud.fence.begin/commit/cancel`, `pointcloud.segment.keep_inside/remove_inside` with the S-01 envelope; automation parity with a typed polygon.

Visual brief (G17): fence outline while drawing = accent 1.5 px dashed polyline with 6 px vertex squares (support-blue), closing edge highlighted on hover of the first vertex; after closing, the fence fills 8 % accent inside with the outline solid; the docked "Segment" panel shows the fence vertex count and area, two equal buttons "Keep inside" (primary) and "Remove inside" (danger-styled secondary is wrong — use secondary with the icon; destructive semantics are undoable so no danger tone), a "Clear fence" quiet Button, and during the bake the S-05 progress row with Cancel; the viewport shows the excluded points at 25 % alpha during preview before commit. Gallery section "Segment panel" (drawing, closed fence, baking); serial regeneration.

Gates: fence-subset of `G-PC-A`…`G-PC-E` (projection-true volume test at multiple depths in perspective; keep/remove symmetry; undo restores the source revision; P4 scope with a hidden class), `G-RW-SEGMENT` (segment 104 M points with cancel at every phase; interaction during the bake stays within the presented-frame gate — measure with V-01 rings, state machine load; the segmented cloud's interaction cost ≤ 1.1× a natively small cloud of the same count), `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, `cargo test -p himmelcad-sidecar segment` once in target/builder. Registry rows `owner: pointcloud`; linter green. Evidence `docs/builder-program/evidence/0.5-02a-segmentation-2026-09-08.md`. Concurrency: 0.5-01 (viewing box), 0.5-03 (sampling), V-05 (render crate) — consume, do not edit. Budget: high effort, one pass.


Resume notice (2026-09-08 23:00): the previous run of this brief died at ~22:50 when the root filesystem hit 0 bytes (another lane copied 47 GB into /tmp; fixed). Its partial edits are in the working tree (typecheck green, no truncated files found). Run `git status --short` and `git diff --stat` first, keep what is sound, and finish the deliverable; do not start over. Additional gate since then: the theme package has `pnpm --filter @himmelcad/theme test` (token lint) — text colour must use the *-fg status tokens (your panel CSS currently uses `color: var(--hc-error)` for text; fix it). Run each gate at most three times; hard budget two hours; never copy the repository or datasets anywhere.
