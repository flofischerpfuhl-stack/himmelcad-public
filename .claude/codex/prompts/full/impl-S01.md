Work package S-01 — Release 0.5 data-model admissions and migrations (minimum substrate). Read `.claude/codex/prompts/_impl_brief.md` first, then `docs/adr/0031-release-0-5-data-model-admissions.md` in full (it is the authority for this package: admitted items, schema ids/versions, the common compatibility rule, per-item migration and automation exposure, and the `G-S01-*` gates), `docs/DATA-MODEL.md`, `docs/PROJECT-FORMAT.md`, `docs/adr/0019-canonical-document-authority.md`, and the existing schema/generation pipeline (`schemas/`, `packages/@himmelcad/data/src/generated/`, `crates/himmelcad-core/src/` — find how existing `hcad.*` component schemas are declared, validated, generated into TS/Rust, and journaled; follow that pipeline exactly, do not invent a second one).

Scope: implement, for the admitted items only (1 basic measurement profile, 3 ViewState v2 0.5 profile, 5 snapshot markers, 6 derived recipe + mesh source roles, 7 point-acquisition + support-role components, 11 curve-subentity reference, 12 local histories), the schema definitions, validators, additive readers with the ADR's lazy-migration rules, generated TS and Rust contracts, persistence plumbing, and the generated command/query rows the ADR names (P11: rows go into the single command table, never an RPC allowlist). No producer UI, no domain workflow, no deferred item (edit-lock, Plan root, journal actor/batch, Civil bundle, offset/parallel producer, full measurement producer, Plan-pinned ViewState) — if you need one of those to make an admitted schema compile, stop and report instead of adding it.

Gates: every `G-S01-<item>` gate in the ADR as an executable test (name the test after the gate); old fixtures open unchanged (byte-identical journals, no eager write — assert with a hash before/after open); unknown-version fail-closed test; `.hcad`/`.hcadx` round trip retains every admitted record; `pnpm typecheck` (root, tsc -b graph), `pnpm --filter @himmelcad/data test`, `--filter @himmelcad/app test`, `--filter @himmelcad/photolab typecheck`; `cargo test -p himmelcad-core` (CARGO_TARGET_DIR is exported to target/builder — never change it; run cargo only for this crate and only once the TS side is done; the machine-wide compute lease `/tmp/himmelcad-photolab-compute.lock` may be held by another lane — that causes timing-test waits, not failures in your code). Evidence file `docs/builder-program/evidence/S-01-admissions-2026-09-02.md`: per item the files, the gate test names and results, and the exact command/query rows added; list any ADR ambiguity you had to resolve with a decision record (Decision/Derivation/Rejected/Tunable).

Shared-substrate notice: `packages/@himmelcad/data/src/index.ts` and `crates/himmelcad-sidecar` are also edited by the PhotoLab lane; do not touch the sidecar at all, and in `data/src/index.ts` make additive changes only, appended at the end of the relevant sections, never reordering or reformatting existing exports. Budget: medium effort, one pass; if an item cannot meet its gate, leave it out cleanly, keep the others, and say which.


Concurrency notice: a second Builder-lane run (S-02) is editing packages/@himmelcad/ui, packages/@himmelcad/theme and apps/builder at the same time. If the root `pnpm typecheck` fails only inside those packages, treat it as their transient state: gate on `pnpm --filter @himmelcad/data typecheck`, `--filter @himmelcad/app typecheck`, `--filter @himmelcad/photolab typecheck` instead and say so in the evidence. Never edit files under those three paths.
