S-05b — G17 corrections for the S-05 jobs surfaces plus two shared light-theme contrast defects reported by the PhotoLab lane, and the cross-product jobs-chip convergence decided by the architect. Read `docs/builder-program/evidence/S-05-jobs-2026-09-04.md`, `packages/@himmelcad/ui/src/JobsSurfaces.tsx` (+ its CSS), the S-02 Button, the gallery fixtures, and PhotoLab's chip for reference only: `apps/photolab/renderer/src/jobsChip.ts` (READ ONLY — never edit apps/photolab).

1. Defect: in `packages/@himmelcad/ui/gallery/shots/dark/jobs-surfaces.png` the primary "Respond" Button inside the jobs island renders with washed-out text (light blue on accent, reads like 45 % opacity) while the same Button variant renders correctly on the Button sheet. Find the cause in the island's CSS cascade (an inherited `color`/`opacity`/`font` on the row or a `button` reset in the island stylesheet) and fix it at the cause; add the same pixel-contrast check the S-02c dialog got (Respond text vs fill ≥ 4.5:1, both themes).
2. Unknown units: a job without known total units must never show "overall 0 %"; the row shows the phase text plus "in progress" and an indeterminate bar (the ProgressBar's indeterminate mode; add it if missing: 4 px track with a 30 % width sweeping segment, 1.2 s, no easing extremes). Fixture row for it.
3. Chip convergence (architect decision, UIP-D10 revision — one shared chip for Builder and PhotoLab): the shared chip is the S-05 component with H2's grammar: 20 px tall pill at the right end of the status bar after the panel toggles, UI 11 px, 1 px tone border (accent = running, warning = needs input, error = failed with error-tone text, success = completed), left slot = 14 px spinner or 48×3 px ProgressBar when a fraction is known, label grammar: "1 job running · Import scan_01.laz 42 %", "2 jobs running", "Needs input · Import scan_02.laz", "Cancelling…", "Job failed — Import scan_03.laz", "Job completed — Import scan_01.laz" (4 s linger, tunable), aria-label "Jobs: <label>", click toggles the jobs island. Replace the "+2" badge with the "n jobs running" grammar. Update fixtures (running single, running multi, needs input, cancelling, failed, completed) and shots.
4. Shared light-theme contrast defects (PhotoLab finding, shared components): the viewport toolbar's active segment ("3D" in the 3D/2.5D/2D segmented control) and the axis chip ("X — Y — Z —") lose almost all contrast in the light theme. Locate the shared components (grep `2.5D` and the axis chip in packages/@himmelcad/ui or viewer), fix with tokens only (active segment = accent outline pattern from DESIGN-SYSTEM, text fg-strong; chip = island-hi fill, border-default, fg-default), add both to the gallery (section "Viewport chrome") and a contrast check ≥ 4.5:1 in both themes.

Gates: `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/ui typecheck`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, regenerated gallery shots (serial run), the new contrast checks passing. Append "S-05b" to the S-05 evidence with causes and shot paths. Do not touch the render crate, the viewer kernel (V-01 in flight) or apps/photolab. Budget: medium effort, one pass.
