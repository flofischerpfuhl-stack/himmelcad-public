Work package G1b-check-2 — fill the Builder pick/snap/render cells of the PhotoLab G1c matrix against PhotoLab's numeric oracle. Read `.claude/codex/prompts/_impl_brief.md`, `docs/builder-program/PHOTOLAB-G1C-MATRIX.md` (rows, checks, pass criteria, the new "Oracle" section), the oracle files `.build/photolab-g1c/dsm/oracle.{json,md}` and `.build/photolab-g1c/dtm/oracle.{json,md}` (schema hcad.photolab-g1c-oracle@1: per package 7 deterministic sample points — index, XYZ in EPSG:31468 + 7837, rgb, class byte — with pick tolerance = mean nearest-neighbour spacing; per DEM package the elevation at those XY plus corner/centre cells with NoData flags, tolerance 0.01 m), the previous passes `docs/builder-program/evidence/G1b-check-2026-09-09.md` and `G1b-fix-2026-09-09.md` (how the literal-UI import and pick logging work; logs under `.build/g1b-check/logs/`), and the fixtures (read-only): sparse/dense under `.build/photolab-e2e/g1a3-dsm-smoke/photolab-e2e.hcad/.photolab/product-import-packages/` and the republished DSM `product-2868864c…` (same dir) and DTM `product-3d43364e…` (`…/g1a3-dtm-smoke/…`).

Do, through the literal Builder UI (V-01b CDP route on DISPLAY=:0) in a fresh project under `.build/g1b-check-2/`: import each of the four packages; for sparse and dense: pick at each of the 7 oracle sample XYZ (use the draw/measure pick with the cloud-point snap), log the picked world XYZ, compare against the oracle within its tolerance, and screenshot one representative pick; for DSM and DTM: read the elevation at the 7 sample XY and at the corner/centre cells through the UI pick (or the entity's grid query if the UI exposes it), compare within 0.01 m, and check that NoData cells are holes (no elevation, transparent) — note PhotoLab's open question: the DTM oracle shows NoData at the raster centre and interior corners while point samples have elevations (wide empty margin vs wrong validity mask) — report what Builder sees, do not guess; render cells: screenshot each product at the oracle's stated camera if one is given, else Frame All, and confirm non-background pixels. Fill the Builder cells of the matrix (pass/fail + evidence pointer per cell) — touch only Builder columns. Evidence `docs/builder-program/evidence/G1b-check-2-2026-09-10.md` with the numeric comparison table (sample index · oracle XYZ · Builder XYZ · delta · tolerance · result). No product-code changes; if a pick fails outside tolerance, record it as a finding with the exact numbers. Do not commit. Machine: check `uptime`; refuse the dense import if free memory < 8 GB. Budget: high effort, one pass, 90-minute hard stop.


Addendum (PhotoLab answer, 23:55): the DTM/DSM rasters are 2048×2048 at 0.05 m (102 × 102 m, origin 4375475.2/5281305.6) with STATISTICS_VALID_PERCENT 25.92 — the extent is the bounding box of a diagonal image strip, so the centre and the four interior corners are GENUINE holes. Expected: those oracle rows render as holes (no elevation, transparent); the 7 dense-point rows give the listed elevations ± 0.01 m.
