Task: AMEND an existing specification (rework in place, do not rewrite). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. File: docs/builder-program/specs/bim-specs/bim-specs.md.

Read first: docs/builder-program/OWNER-DECISIONS.md item D6 (owner statement + derived directions, INCLUDING the owner correction now recorded as doctrine P7), docs/DECISION-DOCTRINE.md (X1-X7, P1-P7 — P7 "office conventions are user data, not product rules" is binding here: specify the MECHANISM, an editable DEFAULT table, and table import/export — never a mandated code grammar), docs/FUNCTION-CONTRACT.md (current), docs/builder-program/README.md, the new evidence dossier docs/builder-program/dossiers/field-codes.md (cite its sections per A2; Trimble feature libraries, STRATIS *.SPZ/F9, card_1 coding, ALKIS/ATKIS/OKSTRA/ISYBAU code structures, role-based generation precedents), and the sibling specs you must cite-and-revise against: specs/draw/draw.md (DR-D4 exactly-one-layer, DR-D16 drafting-time styling), specs/import-formats/import-formats.md (XYZ/CSV workflow IF records, IFC identity), specs/ui-platform/ui-platform.md (surface rules, UIP-D14), specs/measure-inspect (no overlap), specs/select-edit (transforms of generated objects).

Amend bim-specs.md with, each as workflow narrative + contract answers + doctrine-format decision records + catalog rows + disposition rows under a "D6 owner statement 2026-09-02" heading:
1. **Specification codes as catalog keys**: every definition/type row has a code; the catalog declares its own code grammar (per-prefix digit layout, which segments encode kind/class/parameters, digit-encoded parameters OR attribute suffixes — both supported; free-text codes allowed if declared). Ship a DEFAULT specification table as editable project/user data (seeded from the field-codes dossier's German conventions where sourced; the owner's `0941100` is a default-table example). Catalog import/export (CSV/XML) so offices bring their own tables; versioning per catalog; collision policy per BS-D1's import preview.
2. **Specification shortcuts panel**: pinned set of specifications (unbounded, default 8 visible slots, X6), project-persisted and automation-visible (P1); clicking sets the one canonical "current specification" that Draw consumes (cite DR-D16/DR-D4: style applied and entity moved to the specification's layer immediately); surface choice per B3 (right-panel tab, detachable per ui-platform rules); reachability matrix; gesture claims reconciled with ui-platform §3.6; keyboard access (number keys? decide with X4 evidence or record absence).
3. **Role-based BIM generation**: a generative capability of D3 — point/line/area + role (from the specification) + parameters → canonical BIM object via one journaled command, with completion workflows: point + role "manhole center" → pick measured cover point → full 3D manhole (typed diameter/depth twins, C1); line + role "inner wall edge, thickness t, fall direction" → wall object; area + role "floor/ceiling" + room height → room. State validation (never invent geometry: missing parameters block generation with a named reason, X1), undo granularity, passive consumers (E2: layers, display, export/IFC, measurements, plan captures), P4 where applicable.
4. **Code-driven field import contract** (shared with import-formats — cite their CSV workflow record and record what you need from them): code column → catalog resolution (grammar of the selected catalog) → entity kind + specification + parameters → generation via item 3; unresolved codes land as plain points with a review list, never silently dropped.
5. **Sewer objects**: catalog rows for manhole and pipe run (Schacht, Haltung) as BIM object definitions with typed parameters; exchange: disposition ISYBAU XML (DWA-M 145-3 current, M 150 legacy) explicitly per the dossier; "easyBAU" recorded as unidentified — no compatibility claim (A2 silence rule); import-formats owns the format rows — request them.
Keep zero owner questions (the one open factual item — what "easyBAU" refers to — is recorded as an evidence gap, not a design question). Bring the spec up to the current contract (registry-complete catalog columns, extreme-class members, gesture arbitration, code claims file:line). Only modify bim-specs.md (dossier may gain sourced line items per doctrine rule 2). Final message: what changed by item, new decision ids, cross-spec requests.
