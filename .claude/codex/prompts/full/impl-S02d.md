S-02d — F16 from the PhotoLab lane: with three function tabs open, the shared `packages/@himmelcad/ui/src/FunctionPanel.tsx` closeable tablist overflows its panel width (capture: `apps/photolab/test/visual-baselines/1440x900/context-menu-product.png`, right panel). Read `docs/builder-program/specs/ui-platform/ui-platform.md` UIP-D7 (+ its 2026-09-04 revision), `FunctionPanel.tsx` + `.module.css`, `IslandTabs.tsx`, and the PhotoLab capture (read only; never edit apps/photolab).

Required behavior (architect, G17 brief): the tablist never wraps or overflows; tabs shrink to a minimum of 96 px with ellipsized labels (title attribute carries the full label); when even minimum-width tabs do not fit, the rightmost overflow moves into a "⋯" Menu button (S-02 Menu) at the end of the tablist listing the hidden tabs with their close affordance; the active tab is always visible (it is pulled out of the overflow when activated); Properties stays first and never overflows; keyboard: the roving tablist includes the overflow button as the last stop, ArrowRight from the last visible tab focuses it, Enter opens the menu; ARIA stays valid (tablist owns only tabs; the overflow button is outside the tablist container but inside the header row). Gallery: extend the FunctionPanel section with rows "3 tabs at 320 px", "5 tabs at 320 px (overflow)", "overflow menu open"; regenerate shots serially; add a pixel check that no tab text is clipped outside the panel bounds in those rows.

Gates: `pnpm --filter @himmelcad/ui test` (add overflow + keyboard tests), `--filter @himmelcad/ui typecheck`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`; the existing a11y fixture for FunctionPanel keeps zero axe findings. Append "S-02d" to `docs/builder-program/evidence/S-02-visual-review-2026-09-04.md` with the shot paths. Do not touch other components or in-flight files (viewing-box, render crate). Budget: medium effort, one focused pass.
