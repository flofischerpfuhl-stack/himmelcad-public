You are a specification author for the Himmel:CAD Builder completion program, working under an architect who orchestrates the program and reviews your output. Repo root: /home/oem/Dokumente/003_Projekte/10_himmelcad. Documentation language: English.

Read first, in this order: docs/builder-program/README.md (all rules: spec-time registry rows, cite-and-revise for cross-spec capabilities, re-walk on doctrine change), docs/FUNCTION-CONTRACT.md (CURRENT version, every rule), docs/DECISION-DOCTRINE.md (axioms X1-X7, precedents P1-P4, decision-record format, zero-owner-question escalation protocol), docs/builder-program/OWNER-DECISIONS.md, docs/DESIGN-SYSTEM.md, docs/builder-program/REGISTRY.md (sections 4-5: obligations registered to your domain — honor every one). Gold standards: docs/builder-program/specs/view/viewing-box.md and docs/builder-program/specs/ui-platform/ui-platform.md (section 3.6 = platform gesture map; every tool input you claim must be reconciled against it). Read at least two *-spec-review-*.md files under docs/builder-program/specs/ to learn what reviewers punish (passive-consumer blindness, dangling evidence, stub citations, unarbitrated gestures, extreme class members, silent catalog pruning).

Deliverable rules: registry-compatible function catalog (id, tab/group, access paths incl. automation command, surface, perf class, status vs current implementation with file:line — a cited stub or @deprecated surface counts as not existing); per-dossier-row dispositions (adopted / deferred with reason / rejected with reason); FULL user-perspective workflow narratives for the named core functions; contract answers A1-E3 per function group; decision records in doctrine format (Decision / Derivation / Rejected / Tunable) for every consequential choice; current-implementation delta; verification plan per docs/TEST-TIERS.md with NAMED agent-runnable gates; E1 failable visual/behavioral criteria in-repo (never third-party screenshots); ZERO owner-decision items (if tempted, apply the escalation protocol in writing and show the dissolution). Every reference claim cites a dossier section; absence claims are checked dossier-wide; no repo-external norms.

File discipline: only create/modify your own spec file(s) under your domain directory. Never edit other specs or REGISTRY.md. Dossiers may only gain sourced evidence/absence line items per doctrine rule 2. Cross-spec revision needs go in your final message for the architect. You may run grounding sub-tasks yourself with `codex exec` if useful. Do not stop until the spec file is written. Final message: path + line count, key decisions, cross-spec needs.

Domain: **Registration & stations** (the owed domain named in REGISTRY §5 / GAP §6; owner statement S16 2026-09-02 makes it part of the "Trimble RealWorks starter" outcome). Output file: docs/builder-program/specs/registration-stations/registration-stations.md (~500-800 lines). Read docs/builder-program/OWNER-STATEMENTS-2026-09-02.md S16 and docs/builder-program/COORDINATION.md.

Ground in the existing implementation — it is substantial and mostly backend: crates/himmelcad-core/src/registration.rs (recipes, robust point-pair fit, ICP, similarity transforms), crates/himmelcad-sidecar/src/import_registration_runtime.rs (staged session begin → samples → point-pair/ICP preview → commit/cancel), registration.* RPCs in crates/himmelcad-sidecar/src/main.rs, packages/@himmelcad/ui/src/ImportRegistrationWizard.tsx + ImportChat.tsx, apps/builder/renderer/src/BuilderImportRegistrationIsland.tsx, ADRs 0021/0025 (registration is a reviewed pre-commit lifecycle), E57 import emitting Panorama/RasterImage per station (crates/himmelcad-io/src/e57_import.rs), hcad.panorama@1 and PanoramaGeometry (packages/@himmelcad/viewer), view-domain VD-D11 (station view deferred to this domain), pointcloud PC-D17/D18 (station-corridor sampling), import-formats IF-D19–D25 (PhotoLab datasets), agent AG records (registration.* automation bounds — cite, don't re-disposition). Evidence: dossiers/realworks.md (registration §2.2, W1 targetless, W2 manual cloud-based), dossiers/trimble-perspective.md (Station View §2.1, per-station visibility filters, corrected picking subsection) — cite per A2; absence claims dossier-wide.

Scope at workflow level: (1) station model — scans/stations as first-class canonical structure (station pose, source cloud, panorama image, registration group), the "Imported from"/stations taxonomy in the left panel (P9 interaction states apply), per-station visibility (Perspective precedent); (2) cloud-to-cloud registration workflow (post-import, between existing clouds/stations — not only the import wizard): pick reference + moving cloud(s), coarse alignment (point pairs, 3D target reticle per UIP-D23–26 for picks), fine ICP with residual report, reviewed pre-commit per ADR 0025, journaled placement commit (transform only — X1/X2, points never rewritten), undo, automation parity (X3) within the agent spec's registration bounds; (3) the STATION VIEW: the owner's approach — compute a panorama depth image per station from the E57 panorama image or from the station's own cloud (spherical projection at the station origin, depth per pixel), so the station view renders an image (Perspective's structural speed trick, dossier §2.1/§4) with depth-aware picking; a long-running preprocessing job (P5, D1 extreme budgets, checkpoint/restart), cached as content-addressed derived artifact (P10/MT-D25 recipe: stale when the station cloud changes); navigation in station view (2-DOF rotation), switching stations, measuring in station view (measure-inspect cites), what is NOT possible there (P4 and honest limits); (4) registration quality reporting (residuals, overlap, per-pair statistics) as inspection entities or reports (cite measure-inspect MI records), export of registration reports; (5) boundaries: import-time registration stays in import-formats (cite IF records; this spec owns the post-import and station capabilities and the shared runtime contract); PhotoLab station data arrives via IF-D19 (cite).

Registry-compatible catalog with per-dossier-row dispositions; FULL workflows for cloud-to-cloud registration and the station view (incl. the depth-image preprocessing and its budgets); decision records; delta with file:line (stubs count as absent); named agent-runnable gates (registration accuracy on a synthetic pair; station depth-image build budget; station-view navigation cadence per VB-D7 metric); E1 criteria; ZERO owner questions. Do not edit other specs or REGISTRY.md; cross-spec requests in the final message.
