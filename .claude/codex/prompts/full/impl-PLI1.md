Work package PL-I1 (PhotoLab, architect takeover) — the missing product-import-package publishers, and a typed refusal for a missing COLMAP worker. Lane rules: PhotoLab lane — CARGO_TARGET_DIR=target/photolab, cargo -j 4; never copy the repository or datasets; scratch only under .build/codex-scratch/<pkg>/; never touch DISPLAY=:0 (UI only via scripts/ui-test-display.sh); no COLMAP/ALIKED/MVS smokes; do not commit; English sentence-case UI copy (node scripts/check-photolab-english-ui.mjs); each gate at most three times.
Read: docs/builder-program/PHOTOLAB-G1C-MATRIX.md (rows "orthomosaic — pending (no publisher yet)", "gaussian splat — pending (no publisher yet)", "merged point cloud — not a package kind yet", "dense mesh (tiled) — pending"), docs/builder-program/specs/import-formats/import-formats.md §"PhotoLab product datasets" (IF-D19–IF-D25; admitted kinds per core: sparse, dense, dem, orthomosaic, mesh, gaussianSplat × {potree@2, himmelcad-prepared-hierarchy@1}), crates/himmelcad-core/src/product_import_package.rs, crates/himmelcad-io/src/product_import_package.rs, the landed publishers for sparse/dense/DEM in crates/himmelcad-sidecar/src/project_runtime.rs + viewer_raster_manifest.rs (G1a-2/G1a-3/G1a-3b/3c/3d: ready-last publication, frozen lineage, tile-local validity masks, durable_fs sync), the orthomosaic/mesh/splat/merge job outputs in the sidecar (BuildOrthomosaic, dense mesh A3, splat, alignment merge), and WIN-03 evidence (missing COLMAP executable yields JSON-RPC -32000 with localized filesystem text).
Deliverable: (1) publishers that emit a complete hcad.product-import-package-manifest@1 package with ready record for: orthomosaic (prepared raster hierarchy; same tile/validity conventions as the DEM, RGB(A) payload, georeference facts), dense mesh (verify the A3 path publishes a package; finish it if not), gaussian splat (prepared hierarchy or the admitted container the core type names — follow the core contract, do not invent a format), and decide merged point cloud strictly from the spec: if IF-D19–25 do not admit it as a kind, document "not a release kind" in the matrix instead of inventing one; (2) each publisher: unit tests on synthetic fixtures (publish → reader accepts → hashes/lineage/ready-last/crash-order), reuse the existing helpers, no new dependencies; (3) the Builder reader accepts the new kinds (crates/himmelcad-io reader tests only — no Builder UI work); (4) typed refusal: when the COLMAP worker executable is missing/invalid, alignment admission answers a typed reason colmapWorkerMissing (domain error payload with the resolved path), never a raw -32000 with OS text; test. Gates: cargo test -p himmelcad-sidecar product_import and publish (filtered), cargo test -p himmelcad-io product_import, cargo check -p himmelcad-sidecar --tests --bins, pnpm --filter @himmelcad/photolab typecheck + test. Update the matrix rows' PhotoLab columns and docs/photolab-release-acceptance-status-2026-09-19.md rows you affect. Evidence docs/builder-program/evidence/PL-I1-product-publishers-2026-09-19.md. Budget: high effort, one pass, 2.5 hours.
