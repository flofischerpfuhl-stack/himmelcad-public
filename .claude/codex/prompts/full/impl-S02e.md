S-02e — F17 from the PhotoLab light-theme accessibility run: 250 serious colour-contrast findings, all from the plain status tokens used as small text on the light panel #eef0f3 — `--hc-info` (= `--hc-accent-base` #1597f2) 2.72:1, `--hc-success` #3d8b0a 3.75, `--hc-warning` #c67a12 2.96, `--hc-error` #d63b4a 4.01 (WCAG AA needs 4.5). The `*-fg` variants pass (success-fg 9.0, warning-fg 9.3, error-fg 11.9) but `--hc-info-fg` does not exist. Read `packages/@himmelcad/theme/src/tokens.css`, `packages/@himmelcad/console/src/*.module.css` (`.line .level` / `.msg` use the plain tokens — ~140 of the findings), `docs/DESIGN-SYSTEM.md` "Visual language" (status colours communicate only success/warning/error; tokens only), and the gallery's contrast checks in `packages/@himmelcad/ui/gallery/shoot.mjs`.

Required: (1) add `--hc-info-fg` to the theme (light ≈ #0b5fa8, ≥ 4.5:1 on #eef0f3 and on #f7f8fa; dark may stay #1597f2 which is 5.5:1 on #1a1c20) plus its documentation line in the tokens file header; (2) switch the console level and message colours to the `*-fg` tokens (info → `--hc-info-fg`), keep the plain tokens for fills/borders/bars only; (3) grep every shared package (`packages/@himmelcad/ui`, `console`, `app`, and `apps/builder`) for small-text usage of the plain `--hc-info/success/warning/error` tokens as `color:` and switch those to `*-fg` as well (list each change in the evidence); do not touch apps/photolab (their lane fixes their usages in parallel); (4) add a gallery section "Status text" showing the four levels as text on panel, island and island-hi surfaces in both themes with the ≥ 4.5:1 pixel-contrast check; (5) add a stylelint-style check (a node script in the theme package, wired into `pnpm --filter @himmelcad/theme test` or a new `lint:tokens`) that fails when a `color:` declaration in any shared CSS module references a plain status token. Gates: `pnpm --filter @himmelcad/theme test` (or lint:tokens), `--filter @himmelcad/console test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, gallery regenerated serially, the existing a11y fixtures keep zero axe findings. Append "S-02e" to `docs/builder-program/evidence/S-02-visual-review-2026-09-04.md`. Do not touch viewing-box, sampling, segmentation or render-crate files (lanes in flight). Budget: medium effort, one focused pass.
