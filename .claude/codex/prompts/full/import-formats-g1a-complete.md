Task: VERIFY-AND-COMPLETE (scoped, small). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. The previous run amending docs/builder-program/specs/import-formats/import-formats.md for nine WP-G1a contract gaps applied its patches but died before reporting. Read ONLY the spec section "PhotoLab product datasets — 2026-09-02" (from the heading at ~line 1313 to the end) and the gap list below; do not read other documents except docs/TRANSFORMATIONS.md if needed for gap 9.

Gaps that must each be resolved with exact schema text and a doctrine-format decision record (Decision/Derivation/Rejected/Tunable): 1 ProductLineageV1 serialized shape (field ids, primitive types, ordered identity arrays of {id, sha256}, optional-field encoding); 2 missingFieldIds vocabulary = ProductLineageV1 member ids incl. nested/array rules; 3 reason_code enumeration (needs_republish_recompute, needs_preparation, unsupported_format, no_package, + any needed, with copy rules); 4 manifest_id derivation, ready-record schema_id, publication-generation authority, id/role derivation; 5 DEM required fact list (RasterInterpolation, RasterConnectivity, NoData semantics, validity/connectivity resource) — row NOT relaxed; 6 prepared Gaussian splats: ownership authoritative = pointcloud spec's registry row, eligibility after admission, ADR 0030 Decision 10 to follow the spec; 7 merged-alignment datasets disposition row (same manifest; merge lineage identity = ordered alignment ids + hashes); 8 ProductLineageV1 residency without a package (lineage-only record in the publication record, `package: null`; legacy five-field = projection with partial|unknown); 9 float prohibition vs FrozenCrsEndpoint epoch — canonical DECIMAL STRING encoding with a stated precision rule for epoch and any f64 in manifests; packages never rewrite the model's epoch.

For each gap: quote the sentence(s) in the section that resolve it, or add the missing text minimally (gap 9 "decimal string" is currently absent — add it; check the others). Ensure a "WP-G1a contract gaps — 2026-09-02" disposition table lists all nine with their record ids. Run prettier on the file. Final message: for each gap 1–9 the resolving record id and a one-line summary — this list goes verbatim to the implementing PhotoLab session.
