Task: REVISE an existing specification per its adversarial review. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Do not rewrite from scratch; rework in place.

Read: the spec named below, its review file named below (every finding), docs/FUNCTION-CONTRACT.md (current), docs/DECISION-DOCTRINE.md (X1-X7, P1-P6), docs/builder-program/README.md (registry cite-and-revise rule), docs/builder-program/OWNER-DECISIONS.md, and every sibling spec the review references.

Requirements: address EVERY finding — each with a spec change or an explicit deferral reason — in a disposition table appended to the spec (finding id → disposition → spec section/decision id). Blockers and majors must be resolved in the spec text, not only in the table. Update decision records in doctrine format (Decision/Derivation/Rejected/Tunable); add new records where the review's resolution is a consequential choice. Keep the zero-owner-question standard: every review resolution is doctrine-derivable — if you believe one is not, apply the escalation protocol in writing and show the dissolution. Cross-spec resolutions the review assigns to OTHER specs: do not edit those files — record the request in your final message; where the review resolves a contradiction in another spec's favor, adopt it here and cite that spec's record. Dossiers may gain sourced evidence/absence line items per doctrine rule 2. Only modify your own spec (and its criteria sibling if any). Final message: findings resolved vs deferred (counts), new/changed decision ids, cross-spec requests.

Spec: docs/builder-program/specs/agent/agent.md
Review: docs/builder-program/specs/agent/agent-spec-review-2026-09-02.md (4 blockers, 10 majors, 2 minors; disposition "reject as specified")

Notes from the architect: (a) blocker 1 (automation can invoke `agent.approval.respond`): doctrine X3 and contract B1 were amended today — approval responses, confirmation grants, and credential handling are user-only surfaces absent from automation BY CONSTRUCTION (host allowlist, not policy); rewrite the affected records and the E2 consumer/trust table accordingly, and add a verification item that the automation host rejects those methods; (b) blocker 2 (IF-D12 contradiction): read docs/builder-program/specs/import-formats/import-formats.md IF-D12 and ADOPT its registration.* bounds — cite-and-revise, do not re-disposition; record the residual delta you need from import-formats as a cross-spec request; (c) blocker 3 (registry rows): the registry rebuild is a separate reconciliation task — make your catalog table registry-complete (all columns) so it can be merged verbatim; (d) blocker 4 (restore-last-viewing-box workflow): make it deterministic — name the bounded query (viewing_box entities ordered by journal generation, limit 1), the exact command per specs/view/viewing-box.md (verify its command names and reopen semantics; cite VB records), and an enforceable context ceiling for skill+doc loading (X6 tunable, e.g. token budget per skill load, index size cap) with a gate that fails when exceeded; (e) X3's new both-ways clause: agent batches presented as one action are user-undoable as one step — specify the batch boundary in the journal.
