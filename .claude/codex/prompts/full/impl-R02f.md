Work package R-02f — the pilot blockers of the fourth review, and native-dialog support for the invisible test lane. Read `.claude/codex/prompts/_impl_brief.md` (UI work ONLY through `scripts/ui-test-display.sh`; DISPLAY=:0 is forbidden), `docs/builder-program/evidence/R-01d-demanding-user-review-2026-09-19.md` in full (findings with pixel-precise descriptions, screenshots under `.build/r01d/`, the "blocks the pilot hand-over" list), `T-01-invisible-ui-test-display-2026-09-19.md`, `docs/DESIGN-SYSTEM.md` (FunctionPanel states, empty/placeholder copy rules, status copy), and the specs the findings cite (file-project FP-D* import/new/close, mesh-terrain MT-D1–MT-D12 source roles and draft rules: a boundary supplies no Z).

Fix, each with a regression test and a private-display UI proof: (1) **Native pickers in the test lane** — OS file dialogs are separate X windows that CDP cannot see or drive, so in R-01d "Import files…", New and Export looked dead. Add a DEV/test-only dialog responder in both Electron main processes' shared helper (Builder now; name the PhotoLab lines for its lane): when `HIMMELCAD_TEST_DIALOG_QUEUE=<json file>` is set (and only in unpackaged/dev builds), `showOpenDialog`/`showSaveDialog` pop the next queued response (`{kind:'open'|'save', filePaths|filePath, canceled}`) and log `dialog.responded`; without the variable behaviour is unchanged; `scripts/ui-test-display.sh` gains `--dialog-queue <file>` and a `dialog-push` helper; document it in TEST-TIERS. Unit test for the responder; never active in packaged builds (test that too). (2) **Import placeholder** — while the import picker is open (and after cancel) the Function panel shows "Parameters for file.import appear here once the function ships." (R-01d screenshot 03): a shipped function must never show the unshipped-function placeholder; per DESIGN-SYSTEM the panel shows the function's real state: title "Import", one line of `--hc-text-muted` body copy "Choose files to import…" with a secondary Button "Choose files…" (Button size md, 28 px) that reopens the picker, and after cancel returns to the previous panel tab; audit every ribbon command for the same placeholder and list which are genuinely unshipped (those stay disabled with a tooltip reason instead of opening a placeholder). (3) **Boundary-only DGM passes Check** — a source set without any Z-supplying source (only boundaries/exclusions) must fail Check with a typed error row "No elevation source — add a point cloud, grid or breakline with heights" (severity error glyph, Fix ▾ offers "Add source…"), Create stays disabled; remove the possibility of the durable 2-triangle surface; core + sidecar tests. (4) **Project Close leaks tool/UI state** — Close (and New/Open replacement) must end the active tool through the Escape ladder, clear the construction bar, function tabs, selection, HUD overlays and any armed placement; test + UI proof. Then replay the scan-first spine through the private display with the dialog queue (road LAS `libs/polyshapev01/dist/PW_GHT_251215_Orscholz_Deponie-1-1.las`) from a fresh profile to LandXML, screenshots under `.build/r02f/`, recording any stop exactly.

Gates: `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, theme + registry lint, `cargo test -p himmelcad-core mesh_surface` once, `cargo check -p himmelcad-sidecar --tests --bins`. Evidence `docs/builder-program/evidence/R-02f-pilot-blockers-2026-09-19.md`. Rules: never copy the repository or datasets; each gate at most three times; 2.5-hour budget; do not commit; do not edit PhotoLab-owned paths; cargo `-j 4` in target/builder (a PhotoLab lane builds in target/photolab in parallel). Budget: high effort, one pass.
