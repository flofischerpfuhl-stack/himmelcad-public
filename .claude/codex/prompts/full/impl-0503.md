Work package 0.5-03 — spatial sampling and mean-height rasterize (Pointcloud PC-D8/PC-D17, Mesh MT-D26). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/pointcloud/pointcloud.md` PC-D8 (spatial sampling: distance / grid / random with deterministic ties, source immutability) and PC-D17 (cloud-side grid mean-height sampling → prepared grid for Mesh), `docs/builder-program/specs/mesh-terrain/mesh-terrain.md` MT-D26 (mean-height grid as a DGM source), the batch-2 gate `G-B2-PC-MEAN-SAMPLE` in `docs/builder-program/MASTER-PLAN.md` §5 row 18 (mean-height grid, deterministic ties, bounded streaming, provenance, source immutability, real-data oracle), `docs/DECISION-DOCTRINE.md` P2/P4/P10, ADR 0031 item 6 (recipe + source roles), the landed 0.5-02 ground extraction (`docs/builder-program/evidence/0.5-02-ground-extraction-2026-09-08.md`: derived cloud, recipe, phases, panel pattern — reuse its job/panel/recipe scaffolding), V-02 frontier (prepared datasets), S-05 jobs, and the prepared 104 M-point fixture.

Deliverable through visible UI: Pointcloud ▸ Sample (distance / grid / random; typed parameters; scope = visible set / viewing box per P4) producing a derived sampled cloud with recipe (P10), baked as a prepared dataset (P2), deterministic (same input + parameters → identical point set, hash-tested; ties broken by a documented stable rule); Pointcloud ▸ Rasterize mean height (cell size, aggregation mean / min / max / count, empty-cell policy) producing a prepared height grid (`hcad.mesh-source-roles@1` role "grid source") consumable by the Mesh domain (0.5-05 reads it), streaming-bounded (state peak RSS and per-phase timings for the 104 M-point cloud), with provenance and immutability of the source; registered cancellable jobs with phases; P11 rows `pointcloud.sample`, `pointcloud.rasterize` with the S-01 envelope; automation parity.

Visual brief (G17): the two functions share the 0.5-02 panel pattern (docked, detachable): method Select at the top, NumberInputs with units (spacing m, cell size m, grid origin X/Y optional), aggregation Select for rasterize, a result group in mono ("Sampled 3.1 M of 103.7 M points · spacing 0.25 m" / "Grid 2 048 × 1 536 · 1 m · 4 % empty"), Preview (quiet) + primary action Button (accent fill — the 0.5-02 panel rendered its primary as outline; do it right here and note it), running state with phase + Cancel. Gallery section "Sample and rasterize panels" (idle, running, done for both); serial regeneration.

Gates: `G-B2-PC-MEAN-SAMPLE` as defined; determinism hashes on the small LAS; the 104 M-point cloud samples and rasterizes with cancel at every phase and no partial entity; memory bounded (peak RSS reported; check `free -g` before heavy steps — other lanes share the machine); derived entities stream at native cost (V-02 statistics, non-timed); undo removes the derived entity; automation parity; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, `cargo test -p himmelcad-sidecar sampl` once in target/builder (CARGO_TARGET_DIR exported). Registry rows `owner: pointcloud` / `owner: mesh-terrain` for the grid role; linter green. Evidence `docs/builder-program/evidence/0.5-03-sampling-rasterize-2026-09-08.md`. Concurrency: 0.5-01 edits viewing-box files, 0.5-02a edits the fence/segmentation path, V-05 edits the render crate — consume, do not edit. Budget: high effort, one pass.


Resume notice (2026-09-08 23:00): the previous run of this brief died at ~22:50 when the root filesystem hit 0 bytes (another lane copied 47 GB into /tmp; fixed). Its partial edits are in the working tree (typecheck green, no truncated files found). Run `git status --short` and `git diff --stat` first, keep what is sound, and finish the deliverable; do not start over. Additional gate since then: the theme package has `pnpm --filter @himmelcad/theme test` (token lint) — text colour must use the *-fg status tokens (your panel CSS currently uses `color: var(--hc-error)` for text; fix it). Run each gate at most three times; hard budget two hours; never copy the repository or datasets anywhere.
