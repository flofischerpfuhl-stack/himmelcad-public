Task: REVISE an existing specification per its adversarial review. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Do not rewrite from scratch; rework in place.

Read: the spec named below, its review file named below (every finding), docs/FUNCTION-CONTRACT.md (current), docs/DECISION-DOCTRINE.md (X1-X7, P1-P6), docs/builder-program/README.md (registry cite-and-revise rule), docs/builder-program/OWNER-DECISIONS.md, and every sibling spec the review references.

Requirements: address EVERY finding — each with a spec change or an explicit deferral reason — in a disposition table appended to the spec (finding id → disposition → spec section/decision id). Blockers and majors must be resolved in the spec text, not only in the table. Update decision records in doctrine format (Decision/Derivation/Rejected/Tunable); add new records where the review's resolution is a consequential choice. Keep the zero-owner-question standard: every review resolution is doctrine-derivable — if you believe one is not, apply the escalation protocol in writing and show the dissolution. Cross-spec resolutions the review assigns to OTHER specs: do not edit those files — record the request in your final message; where the review resolves a contradiction in another spec's favor, adopt it here and cite that spec's record. Dossiers may gain sourced evidence/absence line items per doctrine rule 2. Only modify your own spec (and its criteria sibling if any). Final message: findings resolved vs deferred (counts), new/changed decision ids, cross-spec requests.

Spec: docs/builder-program/specs/civil/civil.md
Review: docs/builder-program/specs/civil/civil-spec-review-2026-09-02.md (6 blockers, 8 majors, 2 minors)

Notes from the architect: (a) blocker 1: docs/DECISION-DOCTRINE.md P10 was EXTENDED today with the recipe lifecycle (linked by default; sources by id+revision+parameters; stale at gesture end; journaled regeneration — automatic under an X6 budget else explicit/batched; detachable with recipe kept as provenance; auto-detach with console note on source loss; DAG enforced at command time; regeneration errors reuse the creation error list; each owning spec names live budget, stale trigger, inverse mapping, and whether switch confirmation applies only to pending edits; drafts never modify sources) — apply it to every derived civil entity (best-fit axes, gradients, width-band axes, corridor manifests, slopes, pit manifests, profiles) with ONE civil dependency record that cites the mesh-terrain dependency record where mesh owns the surface side (that record is being added by the batch-2 amendment run — cite it as "mesh-terrain dependency record (batch-2 amendment)" if the id is not yet visible); (b) blocker 2: station equations — define the schema (station + equation index/region id so repeated or discontinuous stations are unique; back/ahead station pairs; display and typed-entry rules; automation representation) and mark it for the DATA-MODEL pending-admissions list; (c) blocker 3: the 10 km corridor cold build — apply contract D1's extreme-budget and restart/checkpoint rules: bounded, cancellable, progressively published (chunked by station range) with time-to-first-progress, cancel bound, peak memory/disk, completion definition (X6 tunables) and a named gate script to be created; (d) replace every viewport Tab candidate/solution-cycling claim with the settled rule: Tab = construction input bar, ↑/↓ = candidate cycling while the indicator is live (draw DR-D1, 2026-09-02); (e) A2 hygiene: cite dossier sections only where they exist; the tri-state/reticle/selection-colour claims were corrected today in trimble-perspective.md and realworks.md (Access: Off/Visible/Selectable, blue selection; no RealWorks single rotatable reticle) — do not attribute those to references; (f) receiving amendments in draw/view/mesh/pointcloud/select-edit are being applied concurrently — cite them as pending where their ids are not yet visible; the registry rebuild reconciles. Only modify civil.md (dossiers may gain sourced line items per doctrine rule 2).
