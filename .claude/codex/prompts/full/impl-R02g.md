Work package R-02g (Builder — testability only; owner rule 2026-09-19: no feature growth, no domain-behaviour changes). A stopped lane left partial, uncommitted work in the tree: packages/@himmelcad/app/src/testDialogResponder.ts + test, scripts/dialog-push.mjs, scripts/ui-test-display.sh (--dialog-queue), docs/TEST-TIERS.md, apps/builder/electron/main.ts, apps/builder/renderer/src/{App.tsx,project.ts,ribbon.ts,importFunctionPanelState.ts,projectInteractionLifecycle.ts}, apps/builder/test/{importFunctionPanelState,projectInteractionLifecycle,ribbonAvailability}.test.ts, apps/builder/tsconfig.test.json, packages/@himmelcad/app/src/index.ts. Its DGM-check changes were reverted by the architect and must NOT come back (do not touch mesh_surface.rs, mesh_surface_runtime.rs, DgmCreationWindow.tsx). Read `.claude/codex/prompts/_impl_brief.md` (UI only via scripts/ui-test-display.sh; DISPLAY=:0 forbidden) and items (1), (2), (4) of `.claude/codex/prompts/full/impl-R02f.md` — ignore its item (3) and both correction preambles. Finish exactly these three: (1) DEV/test-only dialog responder (HIMMELCAD_TEST_DIALOG_QUEUE; inactive in packaged builds, tested); (2) the Import function panel never shows the "once the function ships" placeholder for a shipped function (per DESIGN-SYSTEM: title "Import", one muted line "Choose files to import…", secondary Button "Choose files…", 28 px; after cancel the previous tab returns); audit all ribbon commands: genuinely unshipped ones are disabled with a tooltip reason, and produce a table of every ribbon command → shipped / disabled in the evidence; (4) Project Close/New/Open ends the active tool via the Escape ladder and clears construction bar, function tabs, selection, HUD overlays and armed placement. Then a crash/reachability sweep for the owner's own testing: through the private display with the dialog queue, from a fresh profile, invoke EVERY enabled ribbon command once with the road LAS imported (`libs/polyshapev01/dist/PW_GHT_251215_Orscholz_Deponie-1-1.las`) and once with an empty project; record per command: opens / errors on open / crashes / silent no-op, with the console error text; fix only crashes, unhandled errors and dead entries of already implemented functions (smallest possible fix); do not change workflows, copy beyond the broken state, or domain rules — list everything else as findings for the owner. Gates: pnpm --filter @himmelcad/app test, --filter @himmelcad/builder test, --filter @himmelcad/ui test, --filter @himmelcad/builder typecheck, --filter @himmelcad/photolab typecheck, theme + registry lint, cargo check -p himmelcad-sidecar --tests --bins (CARGO_TARGET_DIR=target/builder, -j 4). Evidence docs/builder-program/evidence/R-02g-testability-sweep-2026-09-19.md with the command table. Never copy the repository or datasets; do not commit; 2.5-hour budget. Budget: high effort, one pass.
