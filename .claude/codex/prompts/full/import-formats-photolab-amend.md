Task: AMEND docs/builder-program/specs/import-formats/import-formats.md (rework in place; do not rewrite). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English.

Read: the spec, docs/DECISION-DOCTRINE.md (X1–X7, P1–P11 — P11 is new: one generated command table for automation/console), docs/FUNCTION-CONTRACT.md (current), docs/builder-program/README.md, docs/ROADMAP.md (R1 gate: "PhotoLab outputs open through canonical contracts in Builder and WeltView"), docs/adr/0018-*, 0019-*, 0021-*, and VERIFY these code claims before relying on them (contract code-claim rule; a wrong citation is a defect): Builder import registration matches only formatId 'potree@2' (apps/builder/renderer/src/BuilderImportRegistrationIsland.tsx ~125/155, apps/builder/renderer/src/App.tsx ~1398, crates/himmelcad-sidecar/src/import_registration_runtime.rs ~371); PhotoLab publishes potreeV2 (sparse/dense), rasterPyramid (DEM/ortho), tiledMesh, mvsDepth, and Gaussian-splat datasets (crates/himmelcad-sidecar/src/project_runtime.rs publication paths); the shared render core decodes potree@2, mesh@1, himmelcad-prepared-hierarchy@1 (crates/himmelcad-render providers).

Add, under a "PhotoLab product datasets — 2026-09-02" heading with catalog rows, contract answers, doctrine-format decision records, delta with file:line, verification plan, and disposition rows: (1) a "register a PhotoLab product dataset" capability that dispositions EVERY prepared format the render core decodes (adopted / deferred with reason per format), reusing the existing registration lifecycle (ADR 0021/0025: reviewed pre-commit, no partial canonical results) and carrying product lineage as dataset provenance — alignment id + hash, GCP revision + snapshot hash, frozen CRS (never re-derived, X1) — as immutable provenance fields on the resulting canonical entities (pointcloud/raster/mesh/splat kinds); which sibling specs own the resulting entities (pointcloud, raster, mesh-terrain) and what they must cite (request list in your final message); (2) the automation exposure of this capability per P11 (io.import.* rows in the one command table; validate/status/cancel; grants per ADR 0024 for commits); (3) an honest statement that R1 gate 8 is unmet today and what closes it, WITHOUT specifying PhotoLab-side changes (the PhotoLab session owns those — reference their plan docs/implementation-plans/2026-09-photolab-release-polish.md Phase G / WP-G1 by path only). Zero owner questions. Only modify import-formats.md. Final message: formats dispositioned, new decision ids, cross-spec requests.
