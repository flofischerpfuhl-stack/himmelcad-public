S-02c — one visual defect from the architect's G17 review of the regenerated gallery. Read `docs/builder-program/evidence/S-02-visual-review-2026-09-04.md` first.

Symptom: in `packages/@himmelcad/ui/gallery/shots/light/dialog.png` the Dialog fixture's secondary "Cancel" button renders like a disabled control — grey fill (~#c8ccd2) with faint light-grey text, as if at 45 % opacity — in all three rows (default, Delete focus, close hover). In `shots/dark/dialog.png` the same Cancel renders normally, and in `shots/light/button.png` the secondary default variant renders correctly (white/island-hi fill, dark text, border). So the defect is specific to the secondary Button inside the Dialog in the light theme. Candidates to check, in order: the fixture (`packages/@himmelcad/ui/test/a11yFixtures.tsx` ~line 52 — is `disabled` or `aria-busy` set, or does the gallery's state simulation add a forced attribute to every button in the row?), the gallery renderer (`packages/@himmelcad/ui/gallery/Gallery.tsx` / `shoot.mjs` — does the light run inject a state class on all footer buttons?), the CSS cascade (`BaseControls.module.css` `.button:disabled:not([aria-busy='true'])`, `.dialogActions`, `.dialog { color }`; a `color-mix`/opacity rule that only resolves differently in light), and the Dialog's initial-focus logic (`Dialog.tsx` line ~34 selects footer buttons — does it toggle `disabled`/`inert` while focusing the least-destructive action?).

Fix the actual cause (not the symptom), regenerate `pnpm --filter @himmelcad/ui gallery:shots`, and add a check to `gallery/shoot.mjs`: for the Dialog section in both themes, the Cancel button's text-vs-fill contrast measured from the screenshot pixels is ≥ 4.5:1 (sample the darkest text pixel and the fill colour). Gates: `pnpm --filter @himmelcad/ui test`, `--filter @himmelcad/ui typecheck`. Append a "S-02c" line to the review file naming the cause. Do not touch any other component, and do not touch `JobsSurfaces.tsx` or Builder's App.tsx (other runs in flight). Budget: medium effort, one focused pass.
