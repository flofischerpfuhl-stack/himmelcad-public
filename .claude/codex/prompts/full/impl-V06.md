Work package V-06 — the 3D / 2.5D / 2D camera continuum (Viewer Core VC-D10). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/view/viewer-core-addendum.md` VC-D10 (one cancellable camera continuum: perspective ↔ top-down orthographic morph, interaction state during the blend, what stays pickable, retarget/cancel/Escape, plan depth/text ordering, camera-history settlement) and §4 gate `G-VC-TRANSITION`, the dossier §4.2 bullets on 2D/2.5D sharing one camera with mode committed before the animation settles, `docs/builder-program/specs/view/view-domain.md` VD-D8 (display model), VD-D14 (camera history), the owner statement S3 (3D/2.5D/2D control in the bottom bar; 2D = plan view with no Z on picks, 2.5D = top-down with Z retained), the landed S-08 (ViewState v2, camera history stream), S-03 arbiter, S-05b viewport chrome segmented control, V-03 lanes (protected lane 1 = camera), and the code `packages/@himmelcad/viewer/src/kernel/{KernelCameraController.ts,KernelNavigationController.ts}` plus `crates/himmelcad-render/src/camera.rs`.

Deliverable: a single camera state with a continuous projection parameter (perspective FOV → orthographic) and up-vector lock; switching 3D → 2.5D → 2D animates the morph (duration tunable, 250 ms default, cursor-anchored: the point under the cursor stays put), the semantic mode commits only when the animation settles (VC-D10), a new input during the blend retargets (no snap-back), Escape cancels back to the start pose through the ladder (rung 4 while animating); picking during the blend uses the interpolated projection (test); 2D omits Z on picks and orders plan content by draw order with text on top, 2.5D keeps Z; the camera history (S-08) records one settled pose per transition, never per frame (P5/P8); the bottom-bar segmented control (S-05b/S-10) and the view presets (S-08) drive the same continuum; automation `view.mode.set` with the transition parameters.

Gates: `G-VC-TRANSITION` — the 3D→2D→3D scenario in the V-01 harness stays within the class presented-frame target during the blend (measure with the rings; state machine load), zero camera-history entries mid-blend and exactly one after settling, retarget and Escape tests, interpolated-pick test, cursor-anchor test (≤ 1 px drift of the anchored point), 2D pick omits Z / 2.5D retains Z; `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` (PhotoLab uses the same viewer — its 3D/2.5D/2D buttons must keep working; run its renderer tests too: `pnpm --filter @himmelcad/photolab test` if present), `cargo test -p himmelcad-render camera` once in target/builder; WebGL2 harness gate (PhotoLab visual harness dark run, `--no-a11y --no-compare-baselines`, zero GPU-device errors). Registry rows `view.transition-3d-2d` (exists from V-00 — update status) under `owner: view-domain`; linter green. Evidence `docs/builder-program/evidence/V-06-camera-continuum-2026-09-09.md`. Rules: never copy the repository or datasets; each gate at most three times; two-hour budget; do not commit. Concurrency: 0.5-04 edits draw/construction-bar files, 0.5-05 the Mesh domain — consume, do not edit. Budget: high effort, one pass.
