Work package V-07 — frame-time tail on the real fixture (Viewer Core: presented-frame budget under streaming). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/evidence/Q-01-qualification-2026-09-09.md` (the measured FAILs: class-I p95 154–163 ms for orbit/pan/zoom/fly and 206 ms for 3D→2D→3D on the 103.7 M-point fixture while p50 is 26.6–28.4 ms — the tail, not the median, breaks the 33.4 ms target; machine state recorded), `docs/builder-program/specs/view/viewer-core-addendum.md` §1.1 (class targets, presented-frame definition), §3 (measurement protocol: five-run median, `raf-render-complete`), VC-D1–VC-D9 (fluid-view budget governor, streaming lanes, LOD swap rules, no blocking work on the presenting thread), the landed V-01 (rings/telemetry: `KernelFrameDiagnostics.ts`), V-02 (native streaming driver: `KernelStreamingDriver.ts`, hierarchy/tile uploads), V-03 (budget governor + lanes), V-05 (adaptive points/EDL tiers), V-06 (transition), `scripts/perf/viewer-baseline.mjs`, `crates/himmelcad-render/src/{gpu_frame.rs,resource_builder.rs,streaming*.rs}` and the wasm bridge, plus the prepared 104 M-point fixture under `.build/perf/viewer-baseline-datasets/`.

Deliverable, in two ordered steps. Step 1 — diagnosis with evidence before any change: instrument (using the existing V-01 rings; add ring fields only if missing) what happens in the > 33 ms frames during a captured orbit on the fixture: per-frame breakdown of CPU submit, GPU wait, tile/hierarchy decode, buffer upload sizes, LOD swap counts, JS GC pauses (PerformanceObserver longtask / `performance.measureUserAgentSpecificMemory` where available), wasm memory growth, and the governor's decisions; produce a table of the 20 worst frames with their dominant cause and a histogram; state the hypothesis with numbers (e.g. "uploads > 8 MB in one frame", "hierarchy decode on the presenting thread", "LOD swap batches", "GC after allocation bursts", "transition re-layout"). Step 2 — fix the dominant causes under the governor's contract (VC-D3: the presenting thread never waits on streaming; uploads are budgeted per frame; decode in workers; LOD swaps amortised; allocation reuse to avoid GC bursts; the transition pre-warms its target LOD before the blend starts — VC-D10), each fix with a before/after measurement on the same path. Also fix the protocol gap: `scripts/perf/viewer-baseline.mjs` must run five repetitions per path and report the median of p95 (plus each run) — the dossier §5 table gets the protocol-complete numbers. Do not lower the target, do not reduce point budgets or quality tiers to pass (that is the governor's runtime job, not a benchmark trick); if the class-I hardware cannot reach 33.4 ms p95 with all fixes, say so with the residual breakdown — that is a finding for the owner's machine-class decision.

Gates: five-run-median p95 for orbit/pan/zoom/fly/3D→2D→3D on the fixture on the idle machine (check `uptime` < 2 over 5 min; wait up to 20 min; else NOT IDLE) before and after, with the worst-frame tables; `pnpm --filter @himmelcad/viewer test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck` + `test`, `cargo test -p himmelcad-render` once in target/builder, wasm staged, PhotoLab visual harness dark (`--no-a11y --no-compare-baselines`, zero GPU-device errors) — the WebGL2 gate applies to any render/wasm change. Evidence `docs/builder-program/evidence/V-07-frame-tail-2026-09-09.md` and the dossier §5 update. Rules: never copy the repository or datasets; each gate at most three times; hard budget 2.5 hours; do not commit; do not edit PhotoLab-owned paths. Budget: high effort, one pass.
