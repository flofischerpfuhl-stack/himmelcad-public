Task: AMEND an existing spec (do not rewrite it). File: docs/builder-program/specs/pointcloud/pointcloud.md. Read it fully first, plus docs/builder-program/REGISTRY.md section 4.2 (findings F1, F4, F7, F12), docs/builder-program/specs/view/view-domain.md (decision record VD-D8, the two-layer display model), and docs/builder-program/specs/ui-platform/ui-platform.md (UIP-D10 job registry, UIP-D14 Escape ladder).

Amendments: (1) F1: cite VD-D8 in PC-D11, section 2.4, and section 3.5 — the per-entity canonical display model stands unchanged, but the View-tab color control is VD-D8's un-journaled view-level override with a "Follow entity display" default (not a journaled scene-wide accelerator, and not "removed"); revise the scene-wide recolor path accordingly (tree Ctrl+A + one Mixed edit remains the canonical path; the ribbon override sits above it). (2) F4: VD-D8 migrates view.opacity / view.exaggeration to Pointcloud canonical commands — claim them in your catalog (per-entity opacity fits your display section) or record the reconciliation explicitly. (3) F7: cite UIP-D10 for long-running compaction/bake jobs (chip, jobs island, cancel). (4) F12: state the Classes island's Escape-rung position against UIP-D14 explicitly. Update the disposition table with a "registry reconciliation" row set. Only modify pointcloud.md. Final message: what changed, by finding.
