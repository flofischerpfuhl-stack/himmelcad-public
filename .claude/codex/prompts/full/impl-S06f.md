S-06f — three small findings after the S-06d/e and 0.5-02/P-01 landings. (1) `packages/@himmelcad/console` test "console help vocabulary is exactly the generated registry table" fails since S-06d: the console (product builder) correctly no longer lists `photolab.images.remove` / `photolab.gcp.images`, but the test still expects the whole table — change the expectation to the table filtered by the console's product (and add the symmetric assertion that a PhotoLab-product console lists them), keeping "exactly" semantics. (2) `apps/builder/renderer/src/GroundExtractionPanel*`: the idle-state "Extract ground" action renders as a secondary (outline) Button; the brief specified the primary accent fill — fix and regenerate the "Extract ground panel" gallery rows. (3) P-01 gallery fixture "File surfaces": the recovery Toast overflows the section column so its close button is clipped — widen the fixture container (not the toast) and add the section to the pixel-bounds check. Gates: `pnpm --filter @himmelcad/console test`, `--filter @himmelcad/ui test`, `--filter @himmelcad/app test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, theme `lint:tokens`, gallery regenerated serially. Append notes to the S-06, 0.5-02 and P-01 evidence files. Do not touch viewing-box, sampling, segmentation or render-crate files (lanes in flight). Budget: medium effort, one focused pass.
