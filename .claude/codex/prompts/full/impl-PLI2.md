Work package PL-I2 (PhotoLab, architect takeover) — R22 implementation gap: public PhotoLab command methods in the generated clients and a brokered-grant smoke harness. Lane rules: PhotoLab lane — CARGO_TARGET_DIR=target/photolab, cargo -j 4; never copy the repository or datasets; scratch only under .build/codex-scratch/<pkg>/; never touch DISPLAY=:0 (UI only via scripts/ui-test-display.sh); no COLMAP/ALIKED/MVS smokes; do not commit; English sentence-case UI copy (node scripts/check-photolab-english-ui.mjs); each gate at most three times.
Read: docs/builder-program/evidence/PL-R1-release-inputs-2026-09-19.md (§R22 gap: "generated clients lack public PhotoLab command methods and no compliant brokered-grant sync/async smoke harness exists"), docs/photolab-automation-command-rows.md (the 72 documented rows, G-1 test), the plan section WP-G2 in docs/implementation-plans/2026-09-photolab-release-polish.md (acceptance text), scripts/generate-command-table.mjs, scripts/generate-automation-sdk.py, sdk/python (client.py, models.py, tests), packages/@himmelcad/automation-host (brokered grants, electron.cjs, provider-credentials), schemas/automation/himmelcad-automation-v1.schema.json.
Deliverable: (1) the generator emits public, typed sync and async Python methods for every PhotoLab command row that the command table marks as automation-exposed (products: photolab), with request/result models from the schema — generated only, no hand-written per-command code; drift check stays green; (2) a smoke harness scripts/smoke-photolab-automation.py (+ pnpm script) that starts the sidecar/automation host in the documented brokered-grant mode, obtains a grant, and drives a minimal real PhotoLab sequence without heavy compute: project create → image inspect/commit on a tiny synthetic image set generated in the scratch dir → jobs list → a cancellable job start + cancel → project close, once sync and once async, asserting typed results and typed errors (a refused command without a grant); runtime < 5 min, memory < 4 GB; (3) tests: SDK unit tests for the new methods, G-1 coverage test extended so every automation-exposed PhotoLab row has a generated method; (4) update the WP-G2 checklist line and the status table row with evidence. Gates: python SDK tests, pnpm --filter @himmelcad/app test (generated-table freshness), automation-host suite, pnpm --filter @himmelcad/photolab typecheck + test, the smoke itself twice (sync/async). Evidence docs/builder-program/evidence/PL-I2-photolab-automation-sdk-2026-09-19.md. Budget: high effort, one pass, 2.5 hours.
