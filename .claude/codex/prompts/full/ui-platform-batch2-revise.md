Task: REVISE an existing specification per its adversarial review. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Do not rewrite from scratch; rework in place.

Read: the spec named below, its review file named below (every finding), docs/FUNCTION-CONTRACT.md (current), docs/DECISION-DOCTRINE.md (X1-X7, P1-P6), docs/builder-program/README.md (registry cite-and-revise rule), docs/builder-program/OWNER-DECISIONS.md, and every sibling spec the review references.

Requirements: address EVERY finding — each with a spec change or an explicit deferral reason — in a disposition table appended to the spec (finding id → disposition → spec section/decision id). Blockers and majors must be resolved in the spec text, not only in the table. Update decision records in doctrine format (Decision/Derivation/Rejected/Tunable); add new records where the review's resolution is a consequential choice. Keep the zero-owner-question standard: every review resolution is doctrine-derivable — if you believe one is not, apply the escalation protocol in writing and show the dissolution. Cross-spec resolutions the review assigns to OTHER specs: do not edit those files — record the request in your final message; where the review resolves a contradiction in another spec's favor, adopt it here and cite that spec's record. Dossiers may gain sourced evidence/absence line items per doctrine rule 2. Only modify your own spec (and its criteria sibling if any). Final message: findings resolved vs deferred (counts), new/changed decision ids, cross-spec requests.

Spec: docs/builder-program/specs/ui-platform/ui-platform.md
Review: docs/builder-program/specs/ui-platform/ui-platform-batch2-review-2026-09-02.md (5 blockers, 7 majors, 2 minors) — SCOPED to the batch-2 amendments (UIP-D19–D22 and the S2/S3/S4/S5 additions); earlier dispositions stand.

Notes from the architect: (a) blocker 1 (capabilities absent from catalog/registry): make the spec's own catalog registry-complete for every batch-2 capability (global strip toggles, histories, node-state control, reticle, cursor vocabulary, selection modes); the REGISTRY rebuild is round 3 — status stays "drafted (registry pending)"; (b) blocker 2 (Tab): fix every remaining contradiction inside ui-platform.md to the settled rule (Tab = construction input bar focus/traversal, ↑/↓ = candidate cycling while the indicator is live); list remaining stale places elsewhere for round 3; (c) blocker 3 (reticle): give the shared 3D reticle a complete contract — gesture arbitration against §3.6 (which buttons rotate/translate it, how it coexists with orbit), lifecycle (arm/cancel/confirm; Escape rung), coordinate frame (world/local/view-aligned, typed transform twins per C1), provenance (an "estimated" acquisition state; never inferred authority — X1), and its consumers (draw point, viewing-box center, section plane); note RealWorks has no such single tool (realworks.md corrected subsection) — deviation with reason; (d) doctrine P9 was sharpened just now — permission state vs orthogonal display overlays; snapping capability-bound; Access precedent = three states as sourced, editable/inert are our extensions — re-walk UIP-D20/D21 against the CURRENT P9 text; (e) all other findings per the review, derivation-cited. Only modify ui-platform.md.
