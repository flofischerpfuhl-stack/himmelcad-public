Work package 0.5-07 — DXF/LandXML export UI wiring (File/Project FP-D5/FP-D6 subset). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/specs/file-project/file-project.md` FP-D5, FP-D6 (export reachability, plan/execute with plan-loss disclosure), `docs/builder-program/specs/mesh-terrain/mesh-terrain.md` §6 (DXF round trip, `G-MT-5` Brandenburg LandXML import → edit → export → re-import equality), `docs/DECISION-DOCTRINE.md` P11, `docs/builder-program/MASTER-PLAN.md` §5 row 22 (existing `io.export.plan/execute`; File export component/browser plan-loss parity), the existing exporters in `crates/himmelcad-io` (DXF, LandXML, IFC, GeoTIFF, splat) and their `io.export.plan/execute` rows, the S-06d product-aware command table (export row per product), the S-05 jobs (export is a registered cancellable job), the landed 0.5-05 DGM (TIN entity) and 0.5-06 (edited generations — if landed; else export the 0.5-05 product), 0.5-04 polylines (breaklines, boundaries).

Deliverable through visible UI: File ▸ Export… and the entity/tree context menu Export… (S-06d row) open the export island (docked, detachable): format Select (DXF, LandXML, IFC, GeoTIFF, splat — only formats applicable to the selection are enabled, with a tooltip naming why others are not), scope (selection / visible set / whole project — P4 visible set as default when nothing is selected), target path via the OS file dialog, and the **plan** step (`io.export.plan`): a table of what will be written and what is lost (plan-loss disclosure per FP-D6: entity kinds not representable, precision/CRS notes, recipe/provenance dropped) shown before execute; "Export" runs `io.export.execute` as a registered job with phases and cancel (no partial file: temp + atomic rename), success toast with "Show in folder"; console lines. P11 parity: the same plan/execute rows drive the console and Python SDK (already exist — verify and wire, do not duplicate).

Visual brief (G17): export island 480 px wide: format Select, scope segmented control (Selection / Visible / Project), path row (mono path + "Choose…" secondary), plan table (rows: entity kind · count · "written as …" · loss note in warning-fg when lossy), footer "Plan" (secondary) → "Export" (primary, enabled after the plan) · "Cancel"; running state with phase + progress + Cancel. Gallery section "Export island" (formats, plan with a lossy row, running); serial regeneration.

Gates: DXF round trip — export the road DGM + breaklines to DXF, re-import, compare geometry within tolerance (Mesh §6 rule); `G-MT-5` — if a Brandenburg LandXML fixture exists in the repo or `.build/`, run import → export → re-import equality; otherwise create a small synthetic LandXML fixture, run the same equality, and record the Brandenburg gate as NOT RUN with the reason; plan-loss parity — the plan table lists exactly what the executed file lacks (test with a project containing a measurement entity and a cloud — clouds are not DXF-representable → disclosed); cancel leaves no partial file; `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/builder test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, theme lint, `cargo test -p himmelcad-io export` once in target/builder, `cargo check -p himmelcad-sidecar --tests --bins` before you report; registry rows `owner: file-project` (+ `owner: import-formats` consumer rows), linter green. Evidence `docs/builder-program/evidence/0.5-07-export-wiring-2026-09-09.md`. Rules: never copy the repository or datasets; each gate at most three times; two-hour budget; do not commit. Concurrency: 0.5-06 edits the Mesh editing path — consume, do not edit. Budget: high effort, one pass.
