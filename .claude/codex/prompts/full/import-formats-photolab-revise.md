Task: REVISE an existing specification per its adversarial review. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. Do not rewrite from scratch; rework in place.

Read: the spec named below, its review file named below (every finding), docs/FUNCTION-CONTRACT.md (current), docs/DECISION-DOCTRINE.md (X1-X7, P1-P6), docs/builder-program/README.md (registry cite-and-revise rule), docs/builder-program/OWNER-DECISIONS.md, and every sibling spec the review references.

Requirements: address EVERY finding — each with a spec change or an explicit deferral reason — in a disposition table appended to the spec (finding id → disposition → spec section/decision id). Blockers and majors must be resolved in the spec text, not only in the table. Update decision records in doctrine format (Decision/Derivation/Rejected/Tunable); add new records where the review's resolution is a consequential choice. Keep the zero-owner-question standard: every review resolution is doctrine-derivable — if you believe one is not, apply the escalation protocol in writing and show the dissolution. Cross-spec resolutions the review assigns to OTHER specs: do not edit those files — record the request in your final message; where the review resolves a contradiction in another spec's favor, adopt it here and cite that spec's record. Dossiers may gain sourced evidence/absence line items per doctrine rule 2. Only modify your own spec (and its criteria sibling if any). Final message: findings resolved vs deferred (counts), new/changed decision ids, cross-spec requests.

Spec: docs/builder-program/specs/import-formats/import-formats.md
Review: docs/builder-program/specs/import-formats/import-formats-photolab-review-2026-09-02.md (4 blockers, 7 majors) — SCOPED to the "PhotoLab product datasets — 2026-09-02" amendment (IF-D19–D21); earlier dispositions stand.

Notes from the architect: (a) blocker 1 (historical provenance not reconstructible from current PhotoLab publication records): do not invent — define the provenance contract with an explicit "provenance: unknown/partial" state for legacy publications (X1), mandatory fields only for publications made after the contract exists, and a Builder-side registration rule that surfaces missing provenance visibly (never silently downgrades); the PhotoLab-side publication change is theirs — record the exact fields Builder requires as the contract they implement (their WP-G1) and reference their plan by path only; (b) blocker 2 (no versioned product-package/provenance schema): specify it as a pending DATA-MODEL/ADR admission with the schema shape (versioned manifest id, fields, hashes) — list it for the architect's DATA-MODEL pending-admissions section in your final message; (c) blocker 3 (orthomosaic vs Raster's PlanGrid2D semantics): cite-and-revise against raster RA-D11 (PlanGrid2D) — orthomosaics are georeferenced grids, not plan-only; state the arrival mapping per raster record, request the raster-side citation; (d) all majors per the review's resolutions (P10 recipe for re-published products via MT-D25, P11 command rows, bounded-work budgets per D1, ownership of resulting entities per pointcloud/raster/mesh records). Only modify import-formats.md.
