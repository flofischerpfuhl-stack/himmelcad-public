Work package S-08b — complete S-08 (ViewState v2 and HUD). Read `.claude/codex/prompts/_impl_brief.md`, then `docs/builder-program/evidence/S-08-viewstate-hud-2026-09-05.md` — its "Required remaining work" items 1–8 are the exact scope, plus the fixture fix noted under "Architect review" — then the original brief `.claude/codex/prompts/full/impl-S08.md` for the guarantees and visual brief, the S-04 selection history implementation (reuse its persistence for the display and camera streams), and the landed V-02 evidence (`docs/builder-program/evidence/V-02-*.md`) for the governor/class seam the HUD's "quality" field must read (never infer a tier; if V-02 exposes none, show "—" and record the gap).

Gates: everything the S-08 brief listed (`G-VD-STATE`, P8 stream tests, HUD = diagnostics sample equality, measured HUD overhead ≤ 0.5 ms p95 with the number reported), plus: canonical bookmarks round-trip through the journal and automation (`view.bookmark.create/list/restore`), viewing-box references validate revisions and reject stale refs atomically, global display toggles never edit canonical entities (test), reload/project-switch persistence exercised in a running Builder (Electron test or the existing e2e harness — state which). `pnpm --filter @himmelcad/app test`, `--filter @himmelcad/viewer test`, `--filter @himmelcad/builder typecheck`, `--filter @himmelcad/photolab typecheck`, registry rows + linter green, gallery shots regenerated serially. Append the results to the S-08 evidence file. Concurrency: D-02 edits import paths and the Builder import island; do not touch those. Budget: medium effort, one pass.
