Task: write docs/builder-program/MASTER-PLAN.md — the sequencing and execution plan of the Himmel:CAD Builder completion program. Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English. This is the document the owner reads last and the autonomous execution phase runs from.

Read first: docs/builder-program/README.md, docs/DECISION-DOCTRINE.md (X1-X7, P1-P6), docs/FUNCTION-CONTRACT.md, docs/builder-program/OWNER-DECISIONS.md (note Q1 status — if still pending, plan both branches explicitly), docs/CURRENT-DIRECTION.md, docs/ROADMAP.md (R2 requires milestones as user outcomes with integration and performance gates — never entity/toolbar counts), docs/TEST-TIERS.md, docs/builder-program/REGISTRY.md (current), ALL specs under docs/builder-program/specs/*/ (catalogs, decision records, implementation deltas, verification plans, cross-spec dispositions), all *-spec-review-*.md files (open findings), and .claude/codex/out/cross-spec-needs.md (pending reconciliations).

Required content:
1. Sequencing principles derived from doctrine: iteration-speed package FIRST (the broken verifier bare-`cargo` spawn, unconditional release wasm build on dev, tsc redundancy/project references, parallel runner, mold/sccache, dev profile — from the 2026-09-01 infra exploration; each as a task with a measurable before/after); then shared-platform substrate (ui-platform: gesture map, job registry in main process, selection model, base components; view-domain ViewState v2; select-edit gizmo) because every domain depends on it; then domains in dependency order derived from the specs' cross-references (build a dependency graph: which decision records consume which).
2. Milestones as USER OUTCOMES with integration + performance gates (named agent-runnable gates from the specs, presented-frame-interval metric per VB-D7, P5 persistence gates), never counts. Each milestone: outcome sentence, included specs/records, gates, owner-visible demo.
3. Both Q1 branches: (a) Builder implementation starts now in parallel to PhotoLab — iteration-speed + shared platform first (benefit PhotoLab too); (b) PhotoLab retains exclusive priority — what is safe to do meanwhile (planning-only maintenance, iteration-speed package as PhotoLab-serving work).
4. Autonomous execution protocol for after the owner's vacation: how an implementing agent picks the next task (registry status + milestone order), the definition of done (docs/CURRENT-DIRECTION.md completion discipline + the spec's verification plan + demanding-user implementation review), how contract/doctrine changes propagate (re-walk rule), how escalations are batched (doctrine protocol, target zero), where the owner reads (weekly digest of derived decisions to veto), and the rule that Claude/Codex roles are: architect orchestrates, Codex executes.
5. Open reconciliation queue: every item from cross-spec-needs.md and unresolved review findings, assigned to an owning spec, ordered.
6. Honest risk register: cross-spec ownership drift, data-model additions needing ADRs (hcad.measurement@1, edit-lock component, ViewState v2, Plan root), PhotoLab lifecycle divergence (FP-D14), rate limits/tooling.
Length ~400-700 lines, table-heavy. Zero owner questions beyond Q1 (already registered). Only create MASTER-PLAN.md. Final message: milestone list in one line each, first five tasks of the autonomous queue, and the risks you consider most likely to bite.
