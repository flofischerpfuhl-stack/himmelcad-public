IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-G1c-oracle — the PhotoLab-side numeric oracle for the G1c product matrix (R1 gate 8; docs/builder-program/PHOTOLAB-G1C-MATRIX.md "pick" and "identity" cells). Read only: PHOTOLAB-G1C-MATRIX.md, docs/builder-program/evidence/G1a-3b-validity-mask-2026-09-09.md (package layout), and the files named below.

Goal: a script the Builder lane can compare against without a PhotoLab UI: for every Available product package of a finished PhotoLab project it emits deterministic reference values in the project CRS — sample points for point clouds and elevations for DEMs — plus tolerances, so the Builder's pick/snap on its imported entity is checked numerically.

Deliverable `scripts/photolab-g1c-oracle.mjs` (+ helper `scripts/lib/photolab-ply.mjs`; package.json script `photolab:g1c:oracle`): usage `node scripts/photolab-g1c-oracle.mjs --project <path/to/photolab-e2e.hcad> --out <dir>`; no sidecar, pure file reads.
1. Discover packages under `<project>/.photolab/product-import-packages/product-*/` with `ready.json` present; read `manifest.json` (`lineage.payload.product_kind`, `normalized_format_id`, `package_sha256`, `dem_facts`), skip unsupported formats with a listed reason.
2. Point clouds (sparse/dense, potree@2): locate the source PLY the package was built from via the manifest/lineage dataset reference (dense: `<project>/datasets/mvs/<job>/output/dense.ply`, binary_little_endian, `double x y z`, uchar rgb, float confidence, float nx ny nz — parse the header generically, never assume the layout; sparse: find its PLY the same way). Stream the file (never load 45 M points into memory) and take 7 deterministic sample indices {0, n/8, n/4, n/2, 3n/4, 7n/8, n−1}; for each emit index, xyz (project CRS, full double precision), rgb, and for dense the class byte from `dense.classification.bin` at the same index (the current classification; also record which `dense.classification.<hash16>.bin` it equals). Tolerance for a Builder pick at that xyz: the product's mean nearest-neighbour spacing estimated from a 10,000-point deterministic stride sample (report the estimate).
3. DEM (himmelcad-prepared-hierarchy@1): locate the published raster (`<project>/datasets/raster/<job>/…cog.tif` via the manifest's dataset/artifact references) and evaluate the elevation at the XY of the 7 point samples of the dense cloud of the same project plus the raster's 4 corner-interior cells and centre, with `gdallocationinfo -valxy -geoloc` (GDAL is at /usr/bin); emit value, NoData flag (per `dem_facts.source_no_data` / the validity bitset semantics from the manifest), resolution, CRS, and the tolerance = half the vertical quantum of the raster's data type or 1 cm, whichever is larger. Record `dem_facts.surface` and `ground_classification` when present.
4. Output: `<out>/oracle.json` (schema id `hcad.photolab-g1c-oracle@1`, project fingerprint, generated-at, per package: id, sha256, kind, format, samples, tolerances) and `<out>/oracle.md` — one Markdown table per package ready to paste into the matrix as the PhotoLab counterpart. Deterministic: two runs on the same project produce byte-identical `oracle.json` except `generated_at` (test this).
5. Run it on the two finished projects `.build/photolab-e2e/g1a3-dsm-smoke/photolab-e2e.hcad` and `.build/photolab-e2e/g1a3-dtm-smoke/photolab-e2e.hcad` with `--out .build/photolab-g1c/<dsm|dtm>` (read-only on the projects; the dense PLY is 45 M points — streaming, expect a minute each) and quote the DEM table of the DTM project in the final summary.
Tests (verbatim counts): `scripts/lib/photolab-ply.test.mjs` — header parsing for the two layouts above plus a synthetic ascii-rejection case, streaming sample extraction on a synthetic 1,000-point binary PLY (exact xyz), and determinism of the sample-index function; `scripts/photolab-g1c-oracle.test.mjs` — a synthetic project fixture with one fake package + tiny PLY + 3×3 GeoTIFF built with `gdal_create`/`gdal_translate` → exact expected oracle.json. Run `node --test scripts`, `pnpm typecheck`, `node scripts/check-photolab-english-ui.mjs`.

Files you may edit: scripts/photolab-g1c-oracle.mjs, scripts/photolab-g1c-oracle.test.mjs, scripts/lib/photolab-ply.mjs, scripts/lib/photolab-ply.test.mjs, package.json (one script line), docs/builder-program/PHOTOLAB-G1C-MATRIX.md (an "Oracle" section: how to run, what the Builder compares, tolerances). Nothing else; no Rust; no sidecar. Do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/g1c-oracle/` and delete it before finishing. Final summary: the sample-index rule, the tolerance rules, the DTM DEM table, test counts, and anything you had to decide yourself.
