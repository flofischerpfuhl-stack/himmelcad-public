IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-G1a-3f — GDAL preparation failures must carry the tool's output, then find the intermittent DSM failure (Size S; PhotoLab lane). Read only: crates/himmelcad-sidecar/src/dense_raster_prep.rs, the DEM job code paths that call it (grep `DenseRasterPrepError::GdalFailed` / `dense_raster_prep::` in crates/himmelcad-sidecar/src/), `docs/builder-program/evidence/G1a-3b-validity-mask-2026-09-09.md` lines 50–60, and the logs named below.

Defect: the DSM republish on the finished project `.build/photolab-e2e/g1a3-dsm-smoke/photolab-e2e.hcad` (24 images, DEM only, `--reuse`) fails intermittently in the GDAL-backed dense-input preparation with `denseRasterPreparation: GDAL preparation command failed with exit code Some(1)` — twice now (architect 2026-09-09 first attempt, retry succeeded; architect's failure record `.build/photolab-e2e/g1a3-dsm-smoke/attempts/failed-2026-09-09T14-50-03-579Z.json`; and 2026-09-10 17:55, job `e2e-dem-1789055268959`, log `.build/logs/g1a3c-dsm-republish.log`). The DTM republishes on the sibling project succeeded every time. `DenseRasterPrepError::GdalFailed(Option<i32>)` discards the tool's stdout/stderr, so nobody can see why.

Change (sidecar only):
1. Every GDAL/OGR command spawned by dense_raster_prep.rs (and the DEM/ortho raster builders if they share the wrapper) captures stdout+stderr; on failure the error becomes `GdalFailed { command: String (program + args, paths shortened to the project-relative form), code: Option<i32>, stderr_tail: String (last 2 KiB) }`, the job failure message includes the first stderr line, and the job record stores the tail (additive field `workerLogTail` on the failure state or the existing log-tail mechanism the COLMAP path uses — reuse that if it exists). Typed, no new dependencies.
2. Test: a fake `gdal_*` executable (shell script in a temp dir put first on PATH, or the existing fake-tool rig) that exits 1 with a known stderr line → the error carries it verbatim.
3. Then reproduce: run the DSM republish yourself ONLY if `uptime` load < 4 and ≥ 12 GB free (`node scripts/photolab-e2e.mjs --source 'photolab/Agisoft Exampleprojects/260706_Sulzberg_SUMA_UrGel/01_Photos' --output .build/photolab-e2e/g1a3-dsm-smoke --reuse --max-images 24 --profile fast --products dem --dem-surface dsm --target-epsg 31468 --target-vertical-epsg 7837 --sidecar target/photolab/debug/himmelcad-sidecar`, ≈ 6–12 min, wrap it in `systemd-run --user --scope -p MemoryMax=12G -p MemorySwapMax=0 --` ); up to three attempts; for every failed attempt quote the captured GDAL stderr and the command; if it fails at least once, diagnose the cause from the stderr (candidates: temp-file collision between the dense-input FlatGeobuf/VRT of consecutive DEM jobs on the same project, an exhausted `/tmp` (the project's `tmp/` is on the root fs — check `df` in the failure window), a GDAL cache/threads setting, a race with the previous job's not-yet-deleted scratch) and fix it if it is in the sidecar; if all three succeed, say so and leave the capture in place. Do not change DEM semantics or output hashes: the successful run must produce a package with `dem_facts.surface = "dsm"` and no ground_classification (quote the package id).
Files you may edit: crates/himmelcad-sidecar/src/dense_raster_prep.rs, the DEM/ortho job code in crates/himmelcad-sidecar/src/project_runtime.rs and job_runtime.rs (failure plumbing only), crates/himmelcad-sidecar/src/main.rs ONLY if the failure message is assembled there (the Builder lane has uncommitted hunks in main.rs: touch nothing else in it), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-G1a-3f" paragraph). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/g1a3f/` and delete it before finishing. Final summary: the captured stderr of every failed attempt, the diagnosed cause and fix (or "three successes, cause not reproduced"), the package id, test counts, and anything you had to decide yourself.
