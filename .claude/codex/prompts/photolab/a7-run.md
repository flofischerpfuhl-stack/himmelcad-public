IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-A7 — Memory envelope as a product contract (owner D12; R1 gate 2 amendment). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "WP-A7" (Phase A, verbatim design 1–7 and footer), the tunables register in the same file, and the code named below. Do not read the rest of the plan.

Owner statements (verbatim, 2026-09-08): S22 "Why 64 GB? If Agisoft runs on weaker hardware, that is not acceptable for PhotoLab." S23 refinement: no fixed hardware class is a requirement — PhotoLab uses all memory the machine has and on smaller machines gets slower, not worse. Consequences (owner D12): the memory envelope is a product contract computed PER MACHINE at job start; the degradation order is fixed — TIME FIRST (fewer workers, sequential pair batches, tiling, streaming/spilling intermediates to disk) and QUALITY ONLY when a single unit of work cannot fit even sequentially (one full-resolution extraction of one image; one k×k attention pass — e.g. a 24k×24k pass at 2.3 GB fits a 16 GB machine, so the keypoint cap must NOT trigger there; it triggers only when one pass alone would not fit). The 32 GB / 16 GB figures are documented reference points for evidence, never a requirement.

Measured facts you must design against (2026-09-06/08, 21 MP DJI 5280×3956): ALIKED_N32 extraction at `--FeatureExtraction.max_image_size 8192` (image smaller than the cap, so full resolution) = 15.7 GB RSS for ONE COLMAP worker ≈ 750 B per actual pixel (fp32 activations). `colmap_feature_worker_threads(max_image_edge)` in crates/himmelcad-sidecar/src/main.rs budgets 160 B/px against the CAP (8192² = 67 MP), which is wrong per actual pixel by ~4.7× and only accidentally lands at 1 worker. ALIKED + LightGlue matching = 29.1 GB anon RSS at the kernel OOM kill (`colmap_aliked_matching_worker_threads` assumed 3 GB/worker × 4 physical cores; real ≈ 7 GB/worker) — cause: LightGlue attention ∝ keypoints² and `alignment_feature_budget` allows up to 24,000 keypoints/image for Quality Hybrid (one 24k×24k fp32 matrix = 2.3 GB per layer). Fast profile (2400 px, SIFT, 8 threads) = 1.9 GB total. Portable MVS depth on 24 images = 0.2 GB. Poisson mesh and splat: unmeasured.

Files you may edit: crates/himmelcad-sidecar/src/main.rs ONLY inside the alignment/job-start handlers and the thread-policy helpers (`colmap_feature_worker_threads`, `colmap_matching_worker_threads`, `colmap_aliked_matching_worker_threads`, `alignment_feature_budget`, `default_job_manager_config`) — no new RPC routes, no changes outside those functions and their call sites; crates/himmelcad-sidecar/src/{colmap_runtime.rs,job_runtime.rs,process_group.rs,project_runtime.rs,mvs_runtime.rs}; crates/himmelcad-core/src/photolab_jobs.rs (job record fields, additive with serde defaults); packages/@himmelcad/data/src/index.ts only for the additive job-status fields (announced); apps/photolab/renderer/src/{PhotolabBottomPanel.tsx,AlignmentProfilePanel.tsx,jobSurfaceItems.ts} for showing applied degradations (status text only, English sentence case). Do not touch golden-bin/runtime copies, `.build/**`, the compute lease, or run e2e/COLMAP/DeDoDe — a DSM/DTM smoke is running on this machine; unit tests only; `free -g` before any test that allocates more than 1 GB.

Implement (as specified in the plan section; report contract gaps rather than diverging):
1. Instrumentation: the process-group supervisor samples the peak RSS (sum over the group, plus the largest child) per job stage; the job record gains `memory: { envelopeBytes, stages: [{ stage, peakRssBytes, workers, parameters }] }` (additive, serde default), exposed by `photolab.jobs.status`/`jobs.list` and written into the report data. Unit test with a fake child that allocates a known buffer.
2. Budget per machine at job start: usable = available physical memory (existing hardware probe) − OS/UI reserve (X6 tunable, default 4 GB or 12.5 %, whichever is larger) − predicted peaks held by running jobs (the B4 admission accounting). Per-stage budgets are shares of usable (X6 tunables with rationale comments; append rows to the plan's tunables register). No fixed table; Agisoft's published memory guidance is quoted in the summary as the reference point.
3. Memory models: extraction bytes per image = BYTES_PER_ACTUAL_PIXEL (calibration constant 750 B/px, overridable by the measured value once a run recorded one) × pixels after resize to min(max_image_size, longest image edge), using the real image dimensions known at job start (`width_pixels`/`height_pixels`). Neural matching bytes per worker = attention term k² × 4 B × LAYERS (calibrate LAYERS so 24,000 keypoints ≈ 7 GB) + a fixed base; SIFT matching 256 MB/worker (measured). Time-first choices: extraction workers = clamp(floor(budget / per-image bytes), 1, logical cpus); matching workers likewise, pair batches sequential when workers = 1; MVS tiles already stream.
4. Quality degradation only per the unit-of-work rule: if ONE extraction of ONE image does not fit the stage budget, reduce the extractor edge to the largest that fits (multiple of 256) → `extractionEdgeReduced { from, to, budgetBytes }`; if ONE k×k attention pass does not fit, cap keypoints for the neural matcher at the largest k (multiple of 500) that fits → `matchingKeypointsCapped { from, to }`. Both typed, recorded in the job record, the alignment summary/lineage and the status text. Unit tests must show the 16 GB case runs 24k keypoints with 1 worker and NO cap, and the 32 GB case runs 2 workers.
5. Dense/mesh/splat: only instrumentation in this package (record peaks per stage); write the budget check as a warning-level typed observation, not a bound, and state in the summary what you could not bound without measurements.
6. Admission: extend the B4 disk preflight (`JobAdmission`) with a memory preflight: predicted peak after time-first choices vs usable; only a unit that cannot fit even sequentially triggers a quality degradation; a job that cannot run at all is refused with `InsufficientMemory { predictedBytes, availableBytes }` (typed, inline error in the panel like `InsufficientDisk`), never started.
7. Accuracy guard: not implementable without the golden metrics — write the hook (`degradations` on the run record) and a note in the summary; the PhotoLab session runs the golden comparison.

Tests (verbatim counts in the summary): unit tests for the two memory models at the measured points (15.7 GB at 21 MP; 24k keypoints → ≈ 7 GB/worker), the time-first choices (32 GB usable → 1 extraction worker, 2 matching workers, no cap; 16 GB → 1 worker each, sequential batches, NO cap; 8 GB → edge reduction and cap with the exact typed records), the preflight (refuse vs time-first vs quality), the preflight (refuse vs degrade), the record round-trip (serde defaults for old records), and the renderer status rendering of both degradations. Sidecar suites: `cargo test -p himmelcad-sidecar` (lib + bins; the two Builder-lane failures `automation_runtime::…destructive_approval…` and `canonical_project_store::…group_commit_machine_gate…` are not yours — report them verbatim), `cargo test -p himmelcad-core`, `pnpm --filter @himmelcad/photolab test`, `pnpm typecheck`, `cargo fmt --all -- --check`, prettier --check on touched TS, `node scripts/check-photolab-english-ui.mjs`.

Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only. Final summary: the two memory models with their constants, the exact degradation records, where the caps are applied in the COLMAP invocation, files changed with line references, verification counts, what was NOT verified (no real COLMAP run — the PhotoLab session measures the 135-image Quality Hybrid afterwards), and any contract gap by record id.
