IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-A7j — the DEM/orthomosaic preparation stages join the memory envelope (Size S/M; PhotoLab lane; owner D12/S23). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7", "WP-A7e", "WP-A7f", "WP-A7g", "WP-G1a-3g" (incl. the "Recalibration" paragraph), crates/himmelcad-sidecar/src/dense_raster_prep.rs, and the worker-scope code in crates/himmelcad-sidecar/src/colmap_runtime.rs (`worker_command`, `systemd_user_scope`, `configure_systemd_user_bus_environment`, `WorkerMemoryLimitPlan`).

Evidence (2026-09-10): the DEM job records its preparation stages (`ogr2ogr` CSV → FlatGeobuf, `gdal_grid`, pyramid/COG steps) as `unboundedStage` in the job memory record; measured on the 45.8 M-point Sulzberg cloud: ogr2ogr 2.7 GB RSS, gdal_grid 3.2 GB anon RSS (killed by a 12 GB unit memcg through page-cache pressure of the 10.5 GB scratch write; succeeded under 20 GB). The COLMAP stages, by contrast, run under per-worker cgroup scopes (A7f/A7g) with typed limit outcomes.

Change (sidecar only):
1. Plan: extend the job memory plan with a `rasterPreparation` stage model — per-point constants derived from the measurements (ogr2ogr ≈ 59 B/point, gdal_grid ≈ 70 B/point, both + 256 MiB base; document the derivation), applied to the actual dense point count at admission; the stage shares follow the existing rule (throughput only; quality changes only when a unit does not fit the whole usable envelope → for rasters: refuse typed `alignment needs more memory` analogue "DEM needs more memory than this machine has", never silently downsample).
2. Bound: spawn every GDAL/OGR preparation command through the same per-worker cgroup scope mechanism as COLMAP workers (`systemd-run --user --scope -p MemoryMax=<planned unit × 1.25, min 2 GiB> -p MemorySwapMax=0`, `prlimit --core=0` inside, bus environment passed to the cleared command; RLIMIT_AS×2 fallback when no user scope), with the same `workerMemoryLimit` typed outcome and `workerMemoryLimitHit` degradation; record `workerMemoryLimitMode`, `modelBytes`, `peakRssBytes` (sample the child like the COLMAP supervisor does) per preparation stage so they are no longer `unboundedStage`. Important: the scope cap bounds the tool's memory, and page cache of its own writes counts inside the scope — set the cap from the model plus the file-write working set (use 1.5 × the CSV+FGB scratch estimate share that the tool writes in that stage, or set `MemoryHigh` instead of `MemoryMax` for the write-heavy ogr2ogr step so reclaim throttles instead of killing — decide, justify in the summary, and test both paths' command lines).
3. Tests (verbatim counts): plan model at 45.8 M points ≈ 3.0 GB (ogr2ogr) / 3.5 GB (gdal_grid) ± 5 %; refusal when a unit exceeds the usable envelope; command-line tests for the scope wrapping of ogr2ogr and gdal_grid (fake-tool rig) incl. the fallback; the memory record of a fake DEM job shows the two stages with mode + modelBytes and no `unboundedStage` observation for them. All existing suites keep passing (`cargo test -p himmelcad-sidecar --lib`, `--bin himmelcad-sidecar`).
Files you may edit: crates/himmelcad-sidecar/src/dense_raster_prep.rs, crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/colmap_runtime.rs (only to expose the scope helpers for reuse — no behaviour change for COLMAP), crates/himmelcad-sidecar/src/project_runtime.rs (DEM job wiring), crates/himmelcad-sidecar/src/main.rs ONLY the DEM/ortho admission and record sites (the Builder lane has uncommitted hunks there: touch nothing else), crates/himmelcad-core/src/photolab_jobs.rs (additive record fields; then `pnpm bindings:generate`, include regenerated files), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-A7j" paragraph). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/a7j/` and delete it before finishing; unit tests only — no dataset runs and no systemd-run calls from your sandbox (it has no D-Bus; the architect runs the DEM republish afterwards). Final summary: the constants and derivation, the cap decision (MemoryMax vs MemoryHigh) with justification, the record fields, test counts, and anything you had to decide yourself.
