IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-G1c-oracle-mesh — extend the PhotoLab G1c oracle to mesh packages (Size S; follows WP-G1c-oracle). Read only: docs/builder-program/PHOTOLAB-G1C-MATRIX.md (rows + "Oracle" section), scripts/photolab-g1c-oracle.mjs, scripts/lib/photolab-ply.mjs, and the mesh package named below.

Context: the dense-mesh smoke published a textured DEM mesh package `.build/photolab-e2e/g1a3-mesh-smoke/photolab-e2e.hcad/.photolab/product-import-packages/product-33915de9…/` (read-only): `manifest.json` (lineage payload product_kind "mesh", dataset format himmelcad-prepared-hierarchy@1, content_kind gltf, root `dataset/kernel-manifest.json`), `dataset/tiles/` (21 glTF tiles), `dataset/textures/` (3), `dataset/section-topology.json`, `dataset/preparation.json`. The mesh is derived from the DEM raster of the same lineage (bilinear surface), so a Builder pick on the mesh at XY must return the DEM elevation within one cell's interpolation error.

Change (scripts only): in `scripts/photolab-g1c-oracle.mjs` add a `mesh` package handler that emits, per mesh package: (a) identity — package sha256, tile count, texture count, the kernel-manifest bounds (read the JSON, do not assume its schema: discover the bounds/extent keys and quote them; if the manifest carries a tile index with per-tile bounds, include tile count and total triangle count when present); (b) expected surface elevations at the same 7 dense-sample XYs + centre used for the DEM rows, taken from the DEM package of the same lineage in the same project (resolve via `lineage.payload` source alignment / processing set / the DEM entity id if the mesh manifest references it; otherwise the newest DEM package of the project) with tolerance = max(DEM tolerance, 0.5 × cell size × 2 = one cell, i.e. 0.05 m at 0.05 m resolution) and the NoData flag inherited from the DEM (a hole in the DEM must be a hole in the mesh unless `interpolateHoles` was true in the mesh configuration — read `preparation.json`/lineage for that flag and mark such rows "filled by interpolation, elevation not asserted"); (c) a plain vertex sample: parse the first tile's glTF/glb accessor for POSITION (binary glTF or .gltf+.bin — implement a minimal reader for the accessor/bufferView used, assert componentType FLOAT and VEC3, no external libraries) and emit its first, middle and last vertex in project CRS (apply the tile/node transform if the kernel manifest or the glTF node carries one; state the transform you applied). Output goes into the same `oracle.json` schema (bump nothing; add `kind: "mesh"` packages) and `oracle.md`. Deterministic; the existing tests keep passing; add `scripts/photolab-g1c-oracle-mesh.test.mjs` with a synthetic package (tiny glb with 3 vertices + a 3×3 DEM GeoTIFF + manifest) → exact expected rows. Run the oracle on the mesh project: `node scripts/photolab-g1c-oracle.mjs --project .build/photolab-e2e/g1a3-mesh-smoke/photolab-e2e.hcad --out .build/photolab-g1c/mesh` and quote the mesh table. `node --test scripts/*.test.mjs scripts/lib/*.test.mjs`, `pnpm typecheck`, English UI check.

Files you may edit: scripts/photolab-g1c-oracle.mjs, scripts/photolab-g1c-oracle-mesh.test.mjs, scripts/lib/photolab-gltf.mjs (new minimal reader) + its test, docs/builder-program/PHOTOLAB-G1C-MATRIX.md ONLY the "Oracle" section (the Builder holds uncommitted rows in that file — touch nothing else in it). Do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/g1c-mesh/` and delete it before finishing. Final summary: the kernel-manifest keys used, the transform handling, the mesh table, test counts, and anything you had to decide yourself.
