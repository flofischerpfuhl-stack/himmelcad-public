IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-A4 — Ground classification for a real DTM (Size L; R1 gates 1/4). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "### WP-A4 — Ground classification for a real DTM" (problem, design, acceptance criteria, footer — the inline Decision record is already taken: deterministic SMRF now, ML multi-class deferred); docs/photolab-metashape-reference-2026-09.md row "Depth maps / dense"; the code anchors named below. HEAD is d5d75a1 or later.

Problem today: DEM `surface: 'dtm'` is a class filter over classes the in-house dense cloud never carries (`crates/himmelcad-sidecar/src/main.rs` `elevation_tiles` passes `classification_field: None, accepted_classifications: []` for every tile; `raster_runtime.rs` ~928-970 applies the GDAL class filter only when a field is set), so DTM == DSM. The dense cloud reaches the DEM job as `dense.ply` (binary little-endian; x y z double, r g b uchar, reprojection_error float — `dense_raster_prep.rs` ~270) converted by `prepare_dense_vector` into a FlatGeobuf points layer with field `z`.

Files you may edit: NEW crates/himmelcad-sidecar/src/ground_classification.rs (+ `mod` line in lib.rs/main.rs as the crate already wires modules); crates/himmelcad-sidecar/src/dense_raster_prep.rs; crates/himmelcad-sidecar/src/raster_runtime.rs (only the DEM points-source plumbing); crates/himmelcad-sidecar/src/main.rs ONLY inside the DEM job assembly (`elevation_tiles` and the DEM start handler that builds the raster job — no new RPC routes, no allowlist change); crates/himmelcad-core/src/photolab_products.rs (DEM config/descriptor additions); crates/himmelcad-sidecar/src/pointcloud_export.rs (carry class 2 into LAS/LAZ point records); NOT packages/@himmelcad/data/src/index.ts (the Builder lane is editing it right now — if a shared type is unavoidable, keep the field renderer-local for now and name the exact line the reviewer must add there); apps/photolab/renderer/src/productConfiguration.ts and ProductPanel.tsx for the three parameters; apps/photolab/renderer/src/BatchConfiguratorPanel.tsx if the batch surface mirrors DEM config. Nothing else. Cargo with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only. NOTE: the Builder lane has an in-flight tsconfig/package.json migration (I-03b); if `pnpm --filter … typecheck` fails only inside tsconfig/composite plumbing, report it and fall back to `pnpm exec tsc --noEmit -p apps/photolab/tsconfig.json`.

Design (implement as specified; report contract gaps rather than diverging):
1. `ground_classification.rs`: SMRF (Pingel et al. 2013 simple morphological filter) as a pure, deterministic, cancellable function `classify_ground(points: &[Point3], params: &SmrfParams, cancellation: &CancellationToken, progress: impl FnMut(u64,u64)) -> Result<Vec<PointClass>, GroundClassificationError>` where `PointClass` is `Ground` (LAS class 2) or `Unclassified` (LAS class 1). Steps: (a) grid the minimum surface at `cell_size_m`; (b) fill empty cells by nearest-neighbour inpainting bounded to `max_window_m`; (c) progressive morphological opening with window radius growing linearly from one cell to `max_window_m`, elevation threshold per window = `slope * window_size_m` (+ `initial_distance_m` floor); cells removed at any scale are non-ground; (d) build the provisional DEM from surviving cells and classify each point ground if its height above the interpolated provisional surface is ≤ `initial_distance_m + slope * cell_size_m` (elevation scaling per Pingel). Determinism: fixed iteration order, no HashMap iteration in output-affecting paths, no floating-point reductions that depend on thread scheduling (single-threaded or chunked with fixed chunk boundaries and per-chunk merge in index order). Memory: the grid is dense but bounded — reject with a clear error when the grid would exceed `MAX_GRID_CELLS` (X6 tunable, 200 M cells, name it in a comment) and tell the user to raise the cell size.
2. Parameters on the DEM product config with surveying defaults (X6 tunables, each with a rationale comment): `cell_size_m = 1.0` (≈ 2–5× typical UAV GSD), `slope = 0.15` (15 % terrain slope tolerance), `max_window_m = 18.0` (large enough to remove typical buildings), `initial_distance_m = 0.5`. They apply only when `surface == 'dtm'`; DSM ignores them. Freeze them into the product's immutable config/descriptor so lineage (ADR 0012) records what produced the DTM.
3. Pipeline: after the dense cloud is loaded for a DTM job (before `prepare_dense_vector`), run `classify_ground`, add a `classification` uchar column to the FlatGeobuf layer (2 = ground, 1 = unclassified), and set `classification_field: Some("classification")`, `accepted_classifications: vec![2]` on every DTM tile; DSM tiles keep the current no-filter path. The classification array is also persisted next to the dense artifact as an immutable, content-addressed sidecar (`dense.classification.bin` — u8 per vertex in vertex order, plus its SHA-256 recorded in the product record) so the LAZ export (pointcloud_export.rs) writes LAS `classification` = that class per point when the artifact exists, and 0 (created, never classified) otherwise. Progress: the classification is a named stage of the DEM job (stage label "Classifying ground", units = points), cancellable between chunks; cancellation discards the candidate while the immutable dense input survives (E2).
4. UI: the DEM function panel shows the three parameters (typed number fields, DESIGN-SYSTEM "Input consistency": Escape reverts, blur commits) only when DTM is selected, with the sentence "Ground points are classified with a morphological filter (SMRF); DSM uses all points." Copy stays English sentence case.

Tests (verbatim counts in the summary): Rust unit tests on a synthetic scene — a tilted plane with Gaussian noise (σ = 3 cm) plus three boxes (2 m, 6 m, 12 m footprints, heights 3–10 m) and a 15 % slope ramp: ground recall ≥ 0.98 and precision ≥ 0.98 on plane points, every box roof point non-ground; two runs on the same input produce a byte-identical class array (assert SHA-256 equality); cancellation mid-run returns `Cancelled` within one chunk; the grid-size guard rejects an oversized request; `dense_raster_prep` writes and reads the classification column; pointcloud_export writes class 2 into LAS records (round-trip through the existing LAS reader/validator in tests). Renderer: a pure-helper test for the DTM parameter validation (positive finite numbers, slope in (0, 1]).

Verify: CARGO_TARGET_DIR=…/target/photolab cargo test -p himmelcad-sidecar (full crate) and -p himmelcad-core; cargo fmt --all -- --check; pnpm --filter @himmelcad/photolab test:renderer; typecheck; node scripts/check-photolab-english-ui.mjs; prettier --check on touched files. Do NOT run the e2e smoke yourself (the reviewer runs the DTM-vs-DSM inspection on the 24-image dataset). Final summary: algorithm parameters table (name, default, rationale), the precision/recall numbers measured, determinism hash evidence, files changed, verification counts, anything not verified, contract gaps (record id + sentence).
