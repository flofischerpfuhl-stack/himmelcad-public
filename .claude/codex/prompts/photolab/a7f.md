IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-A7f — self-bounding matcher: fix the cgroup probe and retry with fewer threads on a memory-limit kill (owner D12/S23; follows WP-A7e; blocks every ALIKED smoke). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7d", "WP-A7e", and the code named below.

Evidence (A7c smoke #3, 2026-09-10 00:20, unit MemoryMax=18G, 24 Sulzberg images, forced 2×1 tiling, 8,122 keypoints/image): the matcher was re-planned to 4 threads at 3.43 GB (model 13.7 GB) and reached anon-rss 18.77 GB after 64 min of pair matching before the UNIT's cgroup killed it — the per-worker cap never engaged: the job record says `workerMemoryLimitMode: rlimitAs`, limit 34.3 GB. Root cause in crates/himmelcad-sidecar/src/colmap_runtime.rs `systemd_user_scope()`: the probe runs `systemd-run --user --scope -p MemoryMax=1 -- /bin/true` — a one-byte limit kills /bin/true itself, so the probe always fails and the code falls back to RLIMIT_AS×2, which bounds nothing useful. (Verified: from inside a user unit on this machine, DBUS_SESSION_BUS_ADDRESS and XDG_RUNTIME_DIR are present and a nested `systemd-run --user --scope -p MemoryMax=64M -- /bin/true` succeeds.) Data points for the model: 8 threads → 26.4–28.8 GB (two kills), 4 threads → ≥ 18.8 GB (sampled at the kill) at the same 8,122 keypoints — not linear in threads; the model cannot be trusted to pick the thread count, so the matcher must bound itself.

Change (sidecar only):
1. Probe fix: `MemoryMax=64M` (and `MemorySwapMax=0`), keep the once-per-process cache; add a unit test for the probe command line; log the probe outcome once at info level.
2. Self-bounding matcher (colmap_runtime.rs + job_runtime.rs): the per-worker cgroup limit for the matching stage becomes the matching budget itself (half the usable envelope, i.e. the whole stage share — not unit × workers × 1.25, which trusts the model). Keep unit × workers × 1.25 only as the extraction limit. On a memory-limit kill of the matching worker (already detected as `workerMemoryLimit`): if threads > 1, halve the threads (round up), record a `PhotolabMemoryDegradation::MatchingThreadsHalved { from, to, observed_peak_bytes }`, delete the partial matching output the worker left (only what the retry would rebuild — read what COLMAP writes to the database for matches and clear that table/scope deterministically, never the features), and retry the stage once per halving until 1 thread; if the 1-thread attempt is killed, fail the job with the existing typed refusal `alignment needs more memory than this machine has for matching`. The retries count toward the job's cancellation checks (P5: cancel must interrupt a retry immediately). Each attempt's observed peak (from the sampler) goes into the stage record as `attempts: [{threads, peakRssBytes, outcome}]`.
3. Calibration feedback: after a successful matching stage, record `observedBytesPerThread = peakRss / threads` in the stage parameters and compare with the model in the memory record (`modelBytes` exists since A7e) — no automatic model change in this package; the plan doc gets the two incident data points plus this smoke's 4-thread point.
Tests (verbatim counts): colmap_runtime — probe command line; retry ladder with the fake COLMAP rig: a rig that "dies of memory" at 8 and 4 threads and succeeds at 2 yields two `MatchingThreadsHalved` degradations and a completed stage with `attempts` of length 3; a rig that dies at every thread count yields the typed refusal; cancellation during a retry returns the cancelled outcome within the rig's poll interval. job_runtime — the matching-stage limit equals the matching budget for a 29.1 GB usable machine (14.55 GB) regardless of the model. All existing tests keep passing (`cargo test -p himmelcad-sidecar --lib`, `--bin himmelcad-sidecar`).
No smoke in this package: end with "READY FOR SMOKE" (unit MemoryMax=18G as before; expected: mode cgroupScope, matching limit 14.55 GB, and either success at some thread count or the typed refusal — never a unit-level kill).

Files you may edit: crates/himmelcad-sidecar/src/colmap_runtime.rs, crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/main.rs (stage record/refusal plumbing only), crates/himmelcad-core/src/photolab_jobs.rs (the new degradation variant; additive; then `pnpm bindings:generate` and include regenerated files under packages/@himmelcad/data/src/generated/ if any change) and packages/@himmelcad/data/src/index.ts (ONLY the mirrored `matchingThreadsHalved` union member — announced to the Builder lane), apps/photolab/renderer/src/jobSurfaceItems.ts (one line rendering the new degradation, English sentence case), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-A7f" status paragraph). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/a7f/` and delete it before finishing; do not run any alignment/extraction/matching job on real images — unit tests only. Final summary: the probe fix, the retry ladder semantics, what partial output the retry clears, test counts, and anything you had to decide yourself.
