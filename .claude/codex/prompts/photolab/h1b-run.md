IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab-b5; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-H1b — Cancellable archive Save with granular progress (Size S; adoption-audit F01 residual; R1 gates 1/3). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md "### WP-H1 (Size S, gates 1/3)" and the F01 row; the H1 contract gap it left: "the sidecar route at crates/himmelcad-sidecar/src/main.rs (`photolab.project.save`, ~3381) accepts no operation parameters, while project_runtime.rs (~8090-8101) passes None for cancellation and progress; thread archiveOperationId and progressKey through those locations". Files you may edit: crates/himmelcad-sidecar/src/main.rs ONLY the `photolab.project.save` route and its params struct (announced to the Builder lane), crates/himmelcad-sidecar/src/project_runtime.rs (archive save path), apps/photolab/electron/main.ts + preload.ts + projectLifecycle.ts (the Electron `project:save` flow that H1 added), apps/photolab/renderer/src/App.tsx (status bar copy only if needed). No allowlist widening beyond passing params on the existing route. Cargo with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab-b5 only.

Design: `photolab.project.save` accepts `{ archiveOperationId?: string, progressKey?: string }`; the runtime registers the operation in `active_archives` with a CancellationToken (already the registry used by `drain_side_operations`), reports progress lines through the existing sidecar progress channel keyed by `progressKey` (phases: "Packing working copy", "Writing archive", "Verifying archive"), honours cancellation between phases and inside the archive writer loop (chunked copy), and on cancel leaves the previous archive untouched (existing test `cancelled_archive_save_preserves_existing_destination_and_live_manifest` must stay green and be extended to the new route). Electron: `project:save` passes a fresh operation id + key, forwards progress to the renderer's status bar ("Archive saving… <phase>"), exposes cancel via the existing `photolab.project.cancelArchive`-style route if one exists (grep; if none exists, name the exact line — do not add a route). The close-blocked drain (H1) keeps working unchanged.

Tests: Rust — cancellation mid-archive preserves the destination; progress phases are emitted in order; the route rejects an unknown extra param only if the existing params style is deny_unknown_fields (follow the file's convention). Electron TAP tests for the lifecycle (existing suite + 1). Final summary: the param shapes, phases, cancellation points, files changed, verification counts, anything not verified, contract gaps.
