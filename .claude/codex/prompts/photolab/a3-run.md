IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-A3 — Reachable true 3D mesh product, stage 1: mesh from the dense cloud (Size XL, investigation-bounded; R1 gates 1/4). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "### WP-A3 — Reachable true 3D mesh product" (design stage 1 only; stage 2 texturing/hole filling is NOT this package); docs/photolab-metashape-reference-2026-09.md row "Mesh / texture"; the anchors below. HEAD is e0da2a8. Cargo only with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab-b5 (never target/ or target/photolab).

Problem today: the only reachable mesh is the 2.5D terrain mesh draped from a DEM (`crates/himmelcad-sidecar/src/main.rs` `prepare_mesh_job` ~7703 / `run_mesh_job` ~7790, `mesh_tiler.rs`); COLMAP meshing exists only as dead configuration (`colmap_runtime.rs` `ColmapProductRequest { mesh: Option<ColmapMesher>, texture_mesh, .. }` ~269, default all off, sole caller in main.rs ~4743). Facade/close-range work cannot produce a mesh.

Files you may edit: crates/himmelcad-sidecar/src/colmap_runtime.rs (add the `poisson_mesher` invocation behind the existing kill-loop child supervision; reuse `ColmapMesher`), crates/himmelcad-sidecar/src/main.rs ONLY inside `prepare_mesh_job`/`run_mesh_job` and the mesh product-run configuration parsing (no new routes, no allowlist change), crates/himmelcad-sidecar/src/prepared_triangle_mesh_ply.rs (only if the tiler needs a vertex-colour option), crates/himmelcad-core/src/photolab_products.rs (mesh descriptor gains the frozen source: `meshSource: dem | dense` plus the dense-run id), apps/photolab/renderer/src/productConfiguration.ts and ProductPanel.tsx (the `meshSource` selector and its prerequisite copy), apps/photolab/renderer/src/productPrerequisites.ts (+ .test.ts: a dense mesh needs a published dense cloud; a DEM mesh keeps today's DEM prerequisite), scripts/photolab-e2e.mjs (add `--mesh-source dem|dense`, default dem, so the reviewer can run the smoke). Nothing else.

Design (stage 1, implement as specified):
1. Product configuration `kind: 'mesh'` gains `meshSource: 'dem' | 'dense'` (default `dem`, today's behaviour unchanged byte-for-byte for `dem`). For `dense`, the job takes the FROZEN published dense cloud (the same input freezing the DEM job uses: dense run id + artifact hash in the lineage), runs `colmap poisson_mesher --input_path <fused.ply> --output_path <mesh.ply>` with the vendored COLMAP binary (`vendor/colmap/linux-x64/bin/colmap`, `vendor/colmap/win32-x64/bin/colmap.exe`; reuse `colmap_runtime.rs`'s executable resolution) under the existing supervised-child/kill-loop cancellation (one child process, cancellable by kill, no mid-stage checkpoint — the cancellation matrix entry stays "restart stage"), with `--PoissonMeshing.depth` from an X6 tunable (default 11, rationale comment) and `--PoissonMeshing.trim` default 7 (removes low-support surface); then feeds the resulting PLY through the existing `build_prepared_triangle_mesh_from_ply` tiler for the viewer, honours `targetFaceCount` via the existing stride/decimation path, and publishes with the same 5-tuple lineage as the DEM mesh. Texture: vertex colours only in stage 1 (the ortho-drape texturer stays DEM-only; `buildTexture` is ignored for `dense` and the panel says so).
2. Descriptor/lineage: the published mesh record freezes `meshSource`, the dense run id and the fused-cloud SHA-256 (dense) or the DEM entity + hash (dem) so a later dense rebuild cannot silently change the mesh's provenance.
3. UI: in the Textured Mesh panel a `Source` select (`DEM · terrain drape` | `Dense cloud · true 3D`), inline prerequisite copy ("Needs a published dense cloud." / "Needs a published DEM."), and for `dense` the sentence "Stage 1 meshes with vertex colours; image texturing follows in a later release." (English sentence case; DESIGN-SYSTEM: the copy never claims unverified state).
4. Cancellation: the poisson child is killed within the existing bound; the candidate is discarded, the immutable dense input survives (E2).

Tests: Rust — a fake `poisson_mesher` executable in the test rig (like the brush/colmap fakes) proves invocation args, cancellation kill within the bound, and that the output PLY reaches the tiler; descriptor freezes source + hash; `dem` path unchanged (existing mesh tests green). Renderer — prerequisite test for both sources; configuration default/validation test. Do NOT run the e2e yourself; the reviewer runs the 24-image smoke with `--products depth,dense,mesh --mesh-source dense`.

Verify: cargo test -p himmelcad-sidecar and -p himmelcad-core (report counts); cargo fmt --all -- --check; pnpm --filter @himmelcad/photolab test:renderer; typecheck; node scripts/check-photolab-english-ui.mjs; prettier --check on touched files. Final summary: the poisson invocation (exact args), the lineage fields frozen, UI copy as implemented, files changed, verification counts, anything not verified, contract gaps (record id + sentence).
