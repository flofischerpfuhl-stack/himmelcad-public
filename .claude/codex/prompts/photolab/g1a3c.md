IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-G1a-3c — DTM lineage identity in the product import package (unblocks the G1c matrix "identity" cell for DEM-DTM; Builder G1b-check finding 2026-09-09 13:20). Read only: docs/builder-program/PHOTOLAB-G1C-MATRIX.md (checks table), docs/builder-program/evidence/G1a-3b-validity-mask-2026-09-09.md (what the republished packages look like), and the code named below.

Problem: the DTM package lineage (`.build/photolab-e2e/g1a3-dtm-smoke/photolab-e2e.hcad/.photolab/product-import-packages/product-3d43364e…/manifest.json`, read-only fixture) carries `dem_facts` but no identity of the ground classification that produced the DTM: no `ground_classification_sha256`, no SMRF algorithm/parameter identity, and no surface kind (dsm vs dtm). The raster artifact record already knows `ground_classification_sha256` (crates/himmelcad-sidecar/src/project_runtime.rs, `pub ground_classification_sha256: Option<ObjectHash>` on the raster artifact record, WP-A4) and the DTM job knows its SMRF parameters. A consumer cannot tell two DTMs with different SMRF parameters apart, and the G1c identity check ("lineage carries the frozen PhotoLab source … dem_facts where present") fails.

Change (additive, backwards compatible — old packages must still admit):
1. crates/himmelcad-core/src/product_import_package.rs: extend `PhotoLabDemFactsV1` with `surface: String` ("dsm" | "dtm"; required for new packages, default "dsm" when absent on read) and `ground_classification: Option<ProductLineageDemGroundClassificationV1>` = { `sha256: ObjectHash` (the content hash of `dense.classification.<hash16>.bin`), `algorithm_id: String` ("smrf@1"), `parameters_sha256: ObjectHash` (canonical JSON of the SMRF parameters, same canonicalisation as the frozen DEM config) }. Present iff surface == "dtm". Validation in the same file: dtm requires ground_classification, dsm forbids it; sha256 must be a well-formed ObjectHash. Keep the field names snake_case like the rest of the file.
2. crates/himmelcad-sidecar/src/project_runtime.rs: the DEM publisher (`freeze_product_lineage` caller for kind "dem") fills the new fields from the raster artifact record (`ground_classification_sha256`, surface, SMRF parameters of the job that built the raster) and adds a `ProductLineageIdentityV1` entry for the SMRF tool to `product_tools` (id "smrf", version from the algorithm id, parameters hash). Do not change the package hash canonicalisation order beyond appending the new fields where the serializer already places `dem_facts`.
3. Generated TS bindings: run `pnpm bindings:generate` and commit the regenerated files under packages/@himmelcad/data/src/generated/ that change (they are tracked and drift-checked). No other TS changes; the Builder reads the new fields later.
4. Tests (verbatim counts): core — dtm fixture with ground_classification round-trips and admits; dtm fixture without it is refused with the typed InvalidManifest message; dsm fixture with it is refused; a pre-change manifest (no `surface`) still admits as dsm. Sidecar — the DEM publication test for the dtm surface asserts the manifest carries sha256 == the artifact record's ground_classification_sha256 and the smrf tool entry. All existing tests keep passing: `cargo test -p himmelcad-core`, `cargo test -p himmelcad-sidecar --lib`, `pnpm --filter @himmelcad/photolab test:renderer`.
5. Evidence: republish only the DTM package on the finished project: `node scripts/photolab-e2e.mjs --source 'photolab/Agisoft Exampleprojects/260706_Sulzberg_SUMA_UrGel/01_Photos' --output .build/photolab-e2e/g1a3-dtm-smoke --reuse --max-images 24 --profile fast --products dem --dem-surface dtm --target-epsg 31468 --target-vertical-epsg 7837 --sidecar target/photolab/debug/himmelcad-sidecar` (≈13 min; run it only if `uptime` load < 4 and ≥ 15 GB free, otherwise end with READY FOR SMOKE naming that command) and quote the new manifest fields and the package id in the final summary.

Files you may edit: crates/himmelcad-core/src/product_import_package.rs, crates/himmelcad-sidecar/src/project_runtime.rs (DEM publisher + its tests only), packages/@himmelcad/data/src/generated/** (regenerated only), docs/implementation-plans/2026-09-photolab-release-polish.md (a "WP-G1a-3c" status paragraph under WP-G1a-3). Nothing else. Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/g1a3c/` and delete it before finishing. Final summary: the new manifest fields with an example from the republished package, test counts, and anything in the contract you had to decide yourself.
