IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-H2 — Side-operation drain coverage and a global jobs chip that rehydrates from the durable sidecar job list (adoption-audit F04; R1 gates 3/4). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "### WP-H2 (Size M, gates 3/4)" and the F04 row of "Adoption-audit disposition — 2026-09-02"; docs/builder-program/PHOTOLAB-ADOPTION-AUDIT-2026-09-02.md section F04; docs/builder-program/specs/ui-platform/ui-platform.md records UIP-D10 (registry of record in main; chip → jobs island → toast → console chain; reappear-after-reload; debounced chip) and UIP-D11 (cancellation first-class); docs/DESIGN-SYSTEM.md "Progress, cancellation, and feedback". Decision already taken for this package (do not re-open): NO PhotoLab-private registry and NO migration into the Builder-lane UIP-D10 registry yet (that substrate does not exist; COORDINATION.md) — this package is the ADAPTER: the durable sidecar job list plus the project runtime's side operations become the single truth the renderer rehydrates from after a reload, and cancel reaches the original owner.

Files you may edit: crates/himmelcad-sidecar/src/project_runtime.rs, crates/himmelcad-sidecar/src/job_runtime.rs (PhotoLab lane); NOT packages/@himmelcad/data/src/index.ts — the additive `PhotolabJob` change named below is ALREADY landed (dfd1bc3: kinds archiveSave/imageInspection/imageCommit/imageMask/gcpOperation and `origin?: 'job' | 'sideOperation'`; labels landed in f2c0914) and the Builder lane is editing that file now; apps/photolab/renderer/src/App.tsx, PhotolabBottomPanel.tsx, JobsView (wherever it lives under apps/photolab/renderer/src), a NEW apps/photolab/renderer/src/jobsChip.ts (+ .test.ts) pure helper, StatusBar usage in App.tsx; docs/photolab-automation-command-rows.md and apps/photolab/renderer/src/automationCommandRows.ts only if you change a row's meaning (you should not need to). NOT crates/himmelcad-sidecar/src/main.ts/main.rs, NOT apps/photolab/electron/*, NOT the RENDERER_SIDECAR_METHODS allowlist — if a route change seems unavoidable, stop and name the exact line in your summary instead. Cargo only with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; run `cargo test -p himmelcad-sidecar` for touched modules (use `-- <filter>` while iterating, full crate once at the end).

Part 1 — drain coverage (sidecar). `ProjectRuntime::drain_side_operations` (project_runtime.rs ~2452) requests cancel only for `active_archives` and `active_image_commits`; the runtime also tracks `active_image_inspections`, `active_image_masks`, and `active_gcp_operations`. Make the drain request cancel on all five registries, make `active_drain_operation_ids` (and the timed-out report) cover all five, keep the bounded deadline and the admission gate (`draining_side_operations` / `resume_side_operation_admission`) semantics unchanged. Verify every side-operation entry point checks the draining flag before admission (reject with the existing drain-in-progress error) — add the check where missing. Tests: drain cancels a slow inspection / mask / GCP operation and reports terminal state; a timed-out one appears in `timed_out` with its id; admission is refused while draining and resumes afterwards.

Part 2 — one durable list for the renderer (sidecar + shared type). `photolab.jobs.list` (JobManager) is the renderer's only rehydration source, but side operations are invisible to it. The TS type is already extended (see above); mirror the same five kinds and the `origin` member in the sidecar's serde `PhotolabJobKind`/job record (additive, serde rename camelCase as the existing members) so `photolab.jobs.list` can carry them. The sidecar's `photolab.jobs.list` result appends one synthetic `PhotolabJob` per ACTIVE side operation (origin `sideOperation`, id = the operation id, label from the operation, state running, progress from the operation's cancellation/progress handle if it has one, otherwise stage 1/1 with unknown units, `cancellable: true`); terminal side operations are not synthesized (they have no durable history — say so in a comment; the job history stays JobManager-only). `photolab.jobs.cancel` with a side-operation id requests cancel on that registry entry and returns the same shape as for a job; `photolab.jobs.status` for such an id returns the synthetic record or not-found after completion. Do not change the command rows' names or parameter shapes (P11 rows stay `photolab.jobs.list/status/cancel`). Tests in Rust for list/cancel/status of a synthetic side operation.

Part 3 — global jobs chip (renderer). Add a status-bar chip (right-aligned, after the existing items in `statusItems`, App.tsx ~3834) driven by a pure helper `jobsChipState(jobs, nowMs)` in jobsChip.ts → `{ label, tone, count }`: hidden when nothing is active and nothing finished within the last `JOBS_CHIP_LINGER_MS` (X6 tunable, 4000 ms, so a finished job does not flicker away before the user reads it — UIP-D10 debounce), "1 job running · Align photos 42%" for one active, "3 jobs running" for several, "Cancelling…" while a cancel is pending, "Job failed — <label>" for the most recent failure until acknowledged by opening the island. Clicking the chip opens the bottom panel on the Jobs tab (B2: clicking again while open closes it back to its previous tab); the chip is a real button with an aria-label and keyboard activation; it does NOT install any Escape handling (WP-H3 owns Escape). Rehydration: the existing 500 ms poll starts on mount, so after a renderer reload the list, chip, progress and Cancel reappear from the sidecar truth — make the first poll immediate (no 500 ms blank), and render synthetic side operations in JobsView with their Cancel button routed through `photolab.jobs.cancel`. Toasts: PhotoLab has no toast primitive today — do not build one; log job completion/failure to the console panel via the existing `logEvent` and leave a one-line "toast: pending Builder-lane primitive (UIP-D10 chain)" comment. Tests: jobsChip.test.ts (node --experimental-strip-types --test) for hidden / one / many / cancelling / failed / linger expiry.

Verify: CARGO_TARGET_DIR=…/target/photolab cargo test -p himmelcad-sidecar (report counts); pnpm --filter @himmelcad/data typecheck; pnpm --filter @himmelcad/builder typecheck (the workspace typecheck is one `tsc -b` graph now; `pnpm typecheck` at the root is the gate); pnpm --filter @himmelcad/photolab test:renderer; node scripts/check-photolab-english-ui.mjs; prettier --check on touched files. Final summary: the five-registry drain matrix, the synthetic job mapping table (registry → kind → label → progress source), shared-file change for the landed announcement (data/src/index.ts lines), files changed, verification counts, anything not verified, contract gaps.
