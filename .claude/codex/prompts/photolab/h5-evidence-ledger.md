IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-H5 tooling — release evidence ledger generator (mechanical; machine-checkable DoD). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "Adoption-audit disposition — 2026-09-02" (F11/F12/WP-H5) and "Integration evidence — 2026-09-02"; docs/builder-program/PHOTOLAB-ADOPTION-AUDIT-2026-09-02.md section "F11" (the per-gate evidence table and its "Status and required disposition" column); docs/ROADMAP.md lines 7-20 (the eight R1 gates); docs/TEST-TIERS.md.

Files you create: scripts/photolab-evidence-ledger.mjs, scripts/lib/photolab-evidence.mjs (pure helpers), scripts/test-photolab-evidence-ledger.mjs (node --test), and a root package.json script "photolab:evidence:ledger" (root package.json is a shared root config: add ONLY that one script line, nothing else). Do not edit any other file. No dependencies. Do not run cargo, pnpm build, Chrome, or any e2e; the ledger only READS existing artifacts.

What the tool does: `node scripts/photolab-evidence-ledger.mjs --out docs/photolab-release-evidence-<YYYY-MM-DD>.md [--candidate <git-rev>] [--e2e <dir>...] [--a11y .build/visual-regression] [--baselines apps/photolab/test/visual-baselines] [--cargo-log <file>...] [--node-log <file>...]` writes ONE deterministic Markdown ledger for the eight R1 gates. Per gate it emits a row: gate id/name (from ROADMAP), evidence items found (each with: source path, commit/candidate rev, machine (os-release + CPU model + core count + RAM from /proc), exact command if recorded in the artifact, input identity (e.g. e2e result.json `source`, `imageCount`, `profile`), output identity (result.json `products[].entityId`, `candidateMetrics` summary, a11y `critical/serious` counts, baseline manifest chromium version + count), result verdict as recorded by the artifact — NOT interpreted by the tool), and an explicit status `executed | executed-failed | not-executed` derived only from artifact presence and the artifact's own success/failure fields. Missing artifacts → `not-executed` with the exact command the operator must run (take these from docs/photolab-cancellation-matrix.md, docs/photolab-agisoft-golden-dataset.md "Real matrix"/protocol commands, TEST-TIERS release tier, apps/photolab package.json smoke scripts). Parse e2e result.json files (schemaVersion 1: stages[], success, error, imageCount, alignedCameraCount, products[], jobs[] with lastCheckpointSequence, candidateMetrics), a11y-report.json / a11y-summary.md, visual-baselines manifest.json, cargo/node test logs (count "test result: ok. N passed" lines and "# pass N" lines). Never fabricate: if a field is absent, print "absent". Deterministic output: sorted keys, no timestamps other than those read from artifacts; a fixed header stating that the ledger is generated and read-only.

Tests (node --test, pure helpers): parsing fixtures for each artifact kind (write tiny fixture JSON inline in the test), gate status derivation (present+success → executed; present+failure → executed-failed; absent → not-executed), determinism (same inputs → byte-identical Markdown), and CLI argument handling. Definition of done: `node --test scripts/test-photolab-evidence-ledger.mjs` all pass; `node scripts/photolab-evidence-ledger.mjs --out /tmp/ledger-smoke.md --e2e .build/photolab-e2e/wave1-4-integration --a11y .build/visual-regression --baselines apps/photolab/test/visual-baselines` runs and produces a file (paste its first 40 lines in your final answer); `pnpm exec prettier --check` on the three scripts and the generated markdown passes (run --write first); `node scripts/verification/planner.test.mjs` still passes. Final answer: files created, test counts, the ledger head, anything not verified.
