IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-G1a-2 — the `complete` publication path (R1 gate 8). Read only: docs/adr/0030-photolab-product-import-package-and-provenance.md (revision 4; Decisions 2, 5, 6, 8, 10 and the IF-D19 constraints section carry the new rules verbatim) and, in docs/builder-program/specs/import-formats/import-formats.md section "PhotoLab product datasets — 2026-09-02", the records IF-D26–IF-D34 plus the shape tables they cite ("ProductLineageV1", "PhotoLabProductPublicationRecordV1", "PhotoLabDemFactsV1", the reason-code table, the resource/artifact role enumerations, the Decimal64 rule). WP-G1a (commit e5fc50a) implemented the shapes with every site `partial`; your job is to make sparse/dense potree@2 and complete tiled-mesh publications `complete` and to add merged-alignment and lineage-residency behaviour. Zero design freedom: implement the records exactly; list any remaining gap (record id + sentence) in the final summary.

Files you may edit: crates/himmelcad-core/src/product_import_package.rs, canonical_json.rs, photolab_project.rs; crates/himmelcad-sidecar/src/project_runtime.rs, colmap_runtime.rs, mvs_runtime.rs, raster_runtime.rs, alignment_merge_runtime.rs; crates/himmelcad-sidecar/src/main.rs and packages/@himmelcad/data/src/index.ts only for the listing/wire fields these records change (announced). Do not touch anything else.

Implement: (1) IF-D26 exact serialization incl. always-present ordered `algorithms`/`configurations`/`tools` identity arrays `{id, sha256}` built from the frozen job configuration (profile/preset id + config hash), the alignment artifact's tool manifest (COLMAP/DeDoDe/MVS/GDAL/Brush ids + versions + binary hashes already recorded), omitted optionals never null; (2) IF-D34 Decimal64 strings for every logical f64 (add the canonical rule to canonical_json.rs: shortest round-trip, ties-to-even, no exponent, normalized per the spec; tests with the spec's examples) — never rewrite the model epoch; (3) IF-D29 identities: publication_generation = checked next journal command sequence, identical everywhere; publication_id and manifest_id = "product-" + sha256(canonical_json([source_project_id, product_entity_id, product_entity_version_hash, publication_generation])); ready record schema id `hcad.product-import-package-ready@1`; dataset ids from admitted prepared datasets; resource ids = their SHA-256; closed role enums; (4) IF-D27 missing_field_ids vocabulary (dot paths, bracket paths, sorted, de-duplicated) and IF-D28 closed reason codes with precedence and base copy in the listing; (5) IF-D33 lineage residency: PhotoLabProductPublicationRecordV1 with `package: null` when no package is written (DEM until IF-D30 facts are frozen, orthomosaic, splat pre-admission, binaryPly, mvsDepth) — complete lineage still frozen; (6) IF-D32 merged-alignment publications through the ordinary manifest with `source_alignment_inputs` ordered {id, sha256} (≥ 2) and the merge entity identity; (7) IF-D30: do NOT relax DEM — leave `package: null` unless RasterBuildSummary already freezes every required fact; if it does not, list exactly which facts are missing as a gap. Tests: canonical Decimal64 examples; identity derivation golden; a complete sparse publication yields provenance_status complete with empty missing ids and a ready record that agrees with the publication record; legacy record → partial with the exact sorted member-id list; reason-code precedence; merged publication with ordered inputs; serde back-compat for records without the new members. Keep core + sidecar suites green.

Verify (CARGO_TARGET_DIR=target/photolab): cargo test -p himmelcad-core -p himmelcad-sidecar; cargo fmt --all -- --check; pnpm --filter @himmelcad/photolab typecheck; pnpm --filter @himmelcad/photolab test; prettier --check on touched TS. Final summary: which publication sites are now `complete` and why, remaining gaps by record id, the identity derivation as implemented, files changed, verification counts.
