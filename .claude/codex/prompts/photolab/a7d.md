IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-A7d — the memory envelope must bound the matcher (owner D12/S23; blocks every ALIKED smoke on the 32 GB machine). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7", "WP-A7b", "WP-A7c" (status paragraphs), and the code named below.

Incident (kernel log, 2026-09-09): the A7c extraction smoke (24 Sulzberg images, fast profile, forced 2×1 ALIKED tiling, HEAD sidecar) was OOM-killed twice during "Match ALIKED with LightGlue": `colmap` anon-rss 28.8 GB (19:07) and 26.4 GB (20:42), total-vm 37–63 GB, on a 32 GB machine; the global OOM took the whole desktop session with it. The plan (crates/himmelcad-sidecar/src/job_runtime.rs `plan_alignment_memory`) predicted matching from `neural_matching_bytes_per_worker(request.keypoints)` = keypoints² × 4 B × 3 layers + 256 MiB ≈ 6.9 GB at the 24,000 cap and sized workers from half the usable envelope — but after tiled extraction the merged feature set per image is tiles × cap (≈ 48,000), and 48,000² × 12 B ≈ 27.6 GB per worker: exactly the observed RSS. The envelope bounded extraction, not what extraction produced.

Change (all in the sidecar; no core/io edits):
1. Post-merge keypoint cap (crates/himmelcad-sidecar/src/colmap_runtime.rs, the A7c deterministic merge kernel): after merging tile features, keep at most the planned per-image cap (`request.keypoints` / the plan's keypoint cap), selecting deterministically by the ALIKED score proxy the A7c implementer documented (row order/size proxy — read `.claude/codex/out/impl-A7c.last.md` §contract gaps); ties broken by (x, y). Record the pre- and post-cap counts in the stage memory record parameters (`mergedKeypointsBeforeCap`, `keypointCap`).
2. Re-plan matching from actual counts (job_runtime.rs + colmap_runtime.rs): after extraction the sidecar reads the real maximum keypoints per image from the COLMAP feature DB (A7c's `colmap_feature_db.rs` reader) and recomputes `matching_unit = neural_matching_bytes_per_worker(actual_max_keypoints)` and `matching_workers = min(logical, floor(matching_budget / matching_unit))` before the matching command is built; the `AlignmentMemoryPlan` gains `matching_replanned: Option<{ actual_max_keypoints, matching_workers, matching_unit_bytes }>` written to the job's memory record. If even one worker exceeds the usable envelope: degrade by re-capping keypoints (existing `largest_keypoint_cap_that_fits`, applied by trimming the feature DB deterministically as in item 1) and record `MatchingKeypointsCapped`; if that is impossible, fail the job with the typed refusal `alignment needs more memory than this machine has for matching` (reuse the existing refusal path in main.rs that A7b introduced for tiled extraction) — never start the matcher.
3. Hard cap on every COLMAP child (colmap_runtime.rs `spawn_colmap_child`): set RLIMIT_AS on the child (pre_exec, unix only) to `matching_unit × workers + 25 %` for the matching stage and `extraction_unit × workers + 25 %` for extraction, minimum 2 GiB; a child that dies from the limit yields the job error `worker exceeded its memory envelope (limit N GB)` with the stage name, and the memory record marks `degradations: [WorkerMemoryLimitHit { stage, limit_bytes }]`. Add an env override `HIMMELCAD_PHOTOLAB_WORKER_RLIMIT_DISABLE=1` for diagnostics only.
4. Calibration constant check: keep `NEURAL_MATCHING_ATTENTION_LAYERS = 3` unless the incident numbers contradict it — verify with the incident: 48,000² × 12 B + 256 MiB = 27.9 GB vs measured 26.4–28.8 GB (fits; document this in the plan status paragraph as the second calibration point).
Tests (verbatim counts): job_runtime — replanning with actual 48,000 keypoints on the 32 GB/29 GB-usable machine yields matching_workers 1 and a `MatchingKeypointsCapped` degradation to the largest cap that fits, and a 16 GB machine with 48,000 actual yields the typed refusal; colmap_runtime — post-merge cap keeps exactly `cap` features deterministically across two runs (hash equality of the trimmed feature set), and the RLIMIT pre_exec path is exercised with a tiny limit against the fake COLMAP rig (expect the typed worker error). All existing tests keep passing: `cargo test -p himmelcad-sidecar --lib`, `cargo test -p himmelcad-sidecar --bin himmelcad-sidecar`.
No smoke in this package: end with a "READY FOR SMOKE" section giving the exact command for the A7c smoke (as in `.claude/codex/prompts/full/run-A7c.md`) wrapped in `systemd-run --user --scope -p MemoryMax=18G -p MemorySwapMax=0 --` and the expected memory record fields.

Files you may edit: crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/colmap_runtime.rs, crates/himmelcad-sidecar/src/colmap_feature_db.rs, crates/himmelcad-sidecar/src/main.rs (refusal path + stage record fields only), crates/himmelcad-core/src/photolab_jobs.rs ONLY if the memory-record type needs the new optional fields (additive, then run `pnpm bindings:generate` and include the regenerated files under packages/@himmelcad/data/src/generated/), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-A7d" status paragraph after WP-A7c). Nothing else. Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/a7d/` and delete it before finishing; do not run any alignment/extraction/matching job on real images — unit tests only. Final summary: the replanning formula with the incident numbers, the RLIMIT values chosen, test counts, and anything you had to decide yourself.
