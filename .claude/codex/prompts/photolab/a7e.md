IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-A7e — recalibrate the neural matcher model and cap workers by cgroup, not address space (owner D12/S23; follows WP-A7d, blocks every ALIKED smoke). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7" (model paragraph), "WP-A7d", and the code named below.

Evidence (2026-09-09, 24 Sulzberg images, fast profile, forced 2×1 tiling, 32 GB machine, 29.1 GB usable):
- Actual keypoints per image after the tiled merge: 8,122 (fast-profile ALIKED cap 8,192; never 24,000). Both kernel OOM kills earlier that day (colmap anon-rss 28.8 GB and 26.4 GB) happened in "Match ALIKED with LightGlue" with 8 matcher threads at these 8,122 keypoints. So the real LightGlue cost is ≈ 3.3–3.6 GB per thread at 8k keypoints, while the current model `neural_matching_bytes_per_worker(k) = k² × 4 B × NEURAL_MATCHING_ATTENTION_LAYERS(3) + 256 MiB` gives 1.06 GB. A fit that reproduces the incidents: k² × 4 B × 12 + 256 MiB → 8,122² × 48 B + 256 MiB = 3.43 GB per thread, 8 threads = 27.5 GB (observed 26.4–28.8 GB). Treat 12 as the calibrated layer constant; keep the constant in one place with the two incident data points in the doc comment. Consequence on this machine: matching budget (half of 29.1 GB) / 3.43 GB → 4 threads.
- The A7d smoke (unit MemoryMax=18G, job memory record in `.build/photolab-e2e/a7c-tiled-smoke/photolab-e2e.hcad/.photolab/jobs/records/`) re-planned 8 workers at 1.06 GB and applied RLIMIT_AS = 10.6 GB; the matcher died at virtual 10.6 GB while its sampled RSS was 6.31 GB. RLIMIT_AS limits virtual size (thread arenas, mmapped weights), the model predicts resident bytes — wrong resource.

Change (sidecar only):
1. Calibration (crates/himmelcad-sidecar/src/job_runtime.rs): `NEURAL_MATCHING_ATTENTION_LAYERS` 3 → 12 with the doc comment above; adjust every unit test expectation that encodes the old constant (the plan tests at 24,000 keypoints will now show 27.6 GB per worker → on the 29.1 GB machine one worker at 24k, and `largest_keypoint_cap_that_fits` behaviour on 16 GB machines: recompute the expected caps and assert them explicitly; do not weaken the tests' intent).
2. Worker cap by cgroup (crates/himmelcad-sidecar/src/colmap_runtime.rs `worker_command`): on Linux, when `systemd-run` exists and a user manager is reachable (`systemd-run --user --scope -p MemoryMax=1 -- /bin/true` probe once per sidecar process, cached), spawn every COLMAP worker as `systemd-run --user --scope --quiet -p MemoryMax=<limit> -p MemorySwapMax=0 --collect -- <executable> …` with limit = the A7d `worker_memory_limit_bytes` (unit × workers × 1.25, min 2 GiB) — memory.max has resident semantics and matches the model; a kill is SIGKILL confined to the worker and must map to the existing `workerMemoryLimit` typed error (the scope's exit status/stderr "Killed" line — extend `outcome_indicates_memory_limit` accordingly). Fallback when the probe fails: keep prlimit RLIMIT_AS but with limit × 2 (address space runs about twice resident for this worker), recorded as `workerMemoryLimitMode: "rlimitAs"`; primary mode recorded as `"cgroupScope"` in the stage parameters. `HIMMELCAD_PHOTOLAB_WORKER_RLIMIT_DISABLE=1` disables both (diagnostics only).
3. Observed-vs-model in the memory record: each stage record already has `peakRssBytes`; add `modelBytes` (unit × workers as planned for that stage) to the stage parameters so the next smoke yields the calibration comparison without log archaeology; the Jobs card needs no change.
Tests (verbatim counts): job_runtime — model at 8,122 keypoints = 3.43 GB ± 1 %, 8 threads = 27.5 GB; plan on 29.1 GB usable / 8 logical CPUs at 8,192 keypoints → 4 matching workers; plan at 24,000 keypoints on the same machine → 1 worker, no cap degradation; 16 GB usable at 24,000 → `MatchingKeypointsCapped` to the largest quantum that fits (assert the number). colmap_runtime — the fake COLMAP rig runs under the cgroup mode when the probe passes (skip the test with a clear reason when systemd-run is unavailable) and the rlimit fallback path still produces the typed error with a tiny limit. All existing tests keep passing: `cargo test -p himmelcad-sidecar --lib`, `cargo test -p himmelcad-sidecar --bin himmelcad-sidecar`, `cargo test -p himmelcad-core`.
No smoke in this package: end with "READY FOR SMOKE" (same command as A7d's report, unit MemoryMax=18G) and the expected record fields (`matchingReplanned.matchingWorkers` 4, `workerMemoryLimitMode`, `modelBytes`).

Files you may edit: crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/colmap_runtime.rs, crates/himmelcad-sidecar/src/main.rs (only if the stage parameters are assembled there), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-A7e" status paragraph after WP-A7d + the model row in the constants table). Nothing else; no core/io/TS edits (the record fields go into the untyped `parameters` object). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/a7e/` and delete it before finishing; do not run any alignment/extraction/matching job on real images — unit tests only. Final summary: the calibration with both data points, the cgroup command line used, the fallback multiplier, test counts, and anything you had to decide yourself.
