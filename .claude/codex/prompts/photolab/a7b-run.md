IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-A7b — envelope follow-ups before the 16 GB gate run (owner D12/S23). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7" (landed b529641) and "WP-A7b" (items 1–3 verbatim), plus the code named below. Do not read the rest of the plan.

Owner rule (S23, verbatim intent): PhotoLab uses all memory the machine has; on smaller machines it gets slower, not worse. Degradation order: time first (fewer workers, sequential pair batches, tiling, streaming/spilling), quality only when a single unit of work cannot fit even sequentially.

What A7 landed (b529641): `plan_alignment_memory` in crates/himmelcad-sidecar/src/job_runtime.rs (usable bytes at admission; extraction model 750 B × actual resized pixels per worker; LightGlue model k² × 4 B × 3 layers + 256 MiB; matching share 50 % of usable; typed degradations `ExtractionEdgeReduced`, `MatchingKeypointsCapped`; memory preflight with `InsufficientMemory`), applied in main.rs `prepare_alignment_job` and passed to COLMAP via `--FeatureExtraction.max_image_size` and `--AlikedExtraction.max_num_features` (colmap_runtime.rs ~1958); peak-RSS sampling in process_group.rs; the harness mock in scripts/photolab-visual-regression.mjs carries a memory record.

Target host for the 16 GB gate run: 4 cores, 16 GB (14.3 GB available), no GPU. Today's model would (a) cap keypoints there because the 50 % matching share (≈ 5 GB) is below one 24k×24k pass (7.2 GB) although the pass fits the machine as the only worker, and (b) reduce the extraction edge because one full-resolution ALIKED pass (15.7 GB at 21 MP) cannot fit even alone. Both are quality degradations S23 forbids while a time-first path exists.

Files you may edit: crates/himmelcad-sidecar/src/{job_runtime.rs,colmap_runtime.rs,dedode_colmap_bridge.rs (only if the feature merge helper belongs there),process_group.rs}; crates/himmelcad-sidecar/src/main.rs ONLY inside `prepare_alignment_job` and the memory-plan call sites A7 added; crates/himmelcad-core/src/photolab_jobs.rs (additive record fields with serde defaults); apps/photolab/renderer/src/{jobSurfaceItems.ts,PhotolabBottomPanel.tsx} and apps/photolab/renderer/src/processingReport.ts (or wherever the report sections are built — find it) for item 3; packages/@himmelcad/data/src/index.ts only additively for new typed fields (announced). Do not run e2e, COLMAP, DeDoDe, the visual harness or vite; a DSM/DTM smoke and a harness run are active on this machine; unit tests only; `free -g` before tests allocating > 1 GB.

Implement:
1. Unit-of-work rule against usable memory: stage shares govern parallelism only. If one unit (one full-resolution extraction; one k×k attention pass) fits in the whole usable budget, run it with one worker — never degrade. Quality degradation applies only when the unit exceeds usable. Rewrite `plan_alignment_memory` accordingly; keep the A7 fixtures' outcomes for 27/32/56 GiB usable and change the 8 GiB fixture only where this rule changes it (state the new values). Add fixtures: 14.3 GB available on 4 logical CPUs (reserve → usable ≈ 10.3 GB): matching = 1 worker, 24,000 keypoints, NO cap; extraction: see item 2.
2. Tiled neural extraction as the time-first path when one full-resolution extraction exceeds usable: PhotoLab pre-tiles the resized image into N×M tiles with overlap ≥ 256 px (≥ 2 × the ALIKED descriptor support; make it an X6 tunable), runs COLMAP ALIKED extraction per tile set (tiles are written as temporary images into the scratch, extracted with the same parameters), merges keypoints per source image (shift tile coordinates to image coordinates; de-duplicate in overlaps by keeping the higher-score keypoint within 2 px; preserve the per-image `max_num_features` budget by global top-k on score), and imports through the existing `feature_importer` path used by the DeDoDe bridge (crates/himmelcad-sidecar/src/dedode_colmap_bridge.rs shows the text/binary formats). Tile count is the smallest that makes one tile's model bytes fit usable. Record `extractionTiled { tiles, overlapPx }` as a typed time-first choice (NOT a degradation) in the job record/lineage/status; `ExtractionEdgeReduced` remains only when even the smallest allowed tile (X6 floor, e.g. 1024 px) does not fit. Deterministic: same input → identical merged feature set (unit test with synthetic keypoints on a 2×2 tiling with overlaps). If the COLMAP importer path imposes a constraint you cannot meet (e.g., descriptor format), report it as a contract gap with the exact line rather than diverging.
3. Report rendering: the processing report gains a "Memory envelope" section (envelope, per-stage peak RSS and workers, time-first choices, degradations) from the job record; renderer test asserts the section text for a record with one tiled extraction and no degradation.

Tests (verbatim counts): job_runtime unit tests for the new rule and fixtures (27/32/56 GiB usable unchanged; 10.3 GB usable → 1 matching worker, no cap, tiled extraction with the computed tile count; 8 GiB → values you state); tiling/merge unit tests (coordinate shift, overlap de-duplication, top-k budget, determinism); process-group/import tests as needed; sidecar `cargo test -p himmelcad-sidecar` lib + bins (the two Builder-lane failures `automation_runtime::…destructive_approval…` and `canonical_project_store::…group_commit_machine_gate…` are not yours; cancellation-timing tests may flake under load — rerun serially and report), `cargo test -p himmelcad-core`, `pnpm --filter @himmelcad/photolab test`, `pnpm typecheck`, `cargo fmt --all -- --check`, prettier on touched TS, `node scripts/check-photolab-english-ui.mjs`.

Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only. Final summary: the revised plan rule with the fixture table (usable → workers, tiles, cap), the tiling/merge algorithm and its X6 tunables, where the tiled path enters COLMAP, files changed with line references, verification counts, what was NOT verified (no real COLMAP run — the PhotoLab session runs the 24-image smoke with forced tiling afterwards), contract gaps by record id.
