IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-B4 — Same-target admission + disk preflight (Size M; R1 gates 3/4). Scope note: the side-channel drain/mirror part of the plan's WP-B4 design is delivered by WP-H2 (adapters); this package is ONLY (1) same-target admission and (2) disk preflight. Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "### WP-B4 — Same-target admission + unify side-channel operations" (design items 1 and 2 and the acceptance criteria); docs/DECISION-DOCTRINE.md P5, X6. Files you may edit: crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/project_runtime.rs (only where jobs are admitted with their publication target), crates/himmelcad-sidecar/src/main.rs ONLY inside existing job-start handlers to pass the target key and the disk estimate (no new routes, no allowlist change), apps/photolab/renderer/src/ProductPanel.tsx + productPrerequisites.ts for the inline error copy. Nothing else. Cargo with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only.

Design:
1. Target key. `NewPhotolabJob` gains `publication_target: Option<PublicationTarget>` — a small serde struct `{ kind: ProductKind-or-'alignment'-or-'optimization', target_entity_id: EntityId, lineage_target: Option<EntityId> }` derived from the FROZEN request at admission (never from live selection). `JobManager::start` (job_runtime.rs ~524) rejects with a new `JobManagerError::ConflictingTarget { running_job_id, target }` when a queued or running job has an equal key; terminal jobs never conflict. Batches: a batch job carries the union of its steps' targets and conflicts if any collides. The error reaches the renderer as the existing RPC error shape with code `conflictingTarget` and message "A <kind> for this <target> is already <queued|running> (job <id>). Wait for it or cancel it." (English sentence case; the renderer shows it inline beside Start, not as a dialog).
2. Disk preflight. Before admission, estimate the job's scratch + output bytes per kind (X6 tunables in one table with rationale comments: alignPhotos 2 GB + 8 MB/image, buildDepthMaps 40 MB/image, buildDensePointCloud 1.5× depth-map bytes, buildDem/orthomosaic 4× raster pyramid estimate from GSD and extent, buildMesh 2 GB, buildGaussianSplat 6 GB) and compare with free space on the working-copy volume (`fs2`-free approach: use `statvfs` through std? — no new crates: on unix call `libc`-free `nix`? Not available either; use `std::fs` where possible, otherwise shell out to `df -k --output=avail <path>` on unix and `fsutil volume diskfree` on windows through the existing external-tool argument helpers, parse defensively). Reject with `JobManagerError::InsufficientDisk { required_bytes, available_bytes, path }` and the message "Not enough free space on <volume>: about <required> needed, <available> free." Estimates are conservative and stated as such in the message ("about").

Tests: target-key collision unit tests (same target queued/running → rejected; different lineage target → admitted; terminal job → admitted; batch union); two DEM builds on the same alignment → the second is rejected with the exact message; disk preflight rejects when the estimate exceeds a mocked available value (inject the availability function); existing job_runtime and project_runtime tests green (report counts). Renderer: a pure helper test for the inline error mapping (code → copy). Final summary: the target-key derivation per job kind (table), the estimate table, files changed, verification counts, anything not verified, contract gaps.
