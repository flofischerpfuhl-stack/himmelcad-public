IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-H3 — Escape ladder (UIP-D14 dispatcher, delegated to this lane by the Builder lane under four conditions), close symmetry, and selection lifecycle (adoption-audit F07 + F08; R1 gate 7). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md rows F07/F08 in "Adoption-audit disposition — 2026-09-02"; docs/builder-program/PHOTOLAB-ADOPTION-AUDIT-2026-09-02.md sections F07 and F08 (file:line evidence); docs/builder-program/specs/ui-platform/ui-platform.md records UIP-D14 (Escape ladder), UIP-D7 (function tabs close with x, Properties is the closeless fallback, ribbon toggle stays), UIP-D10 (job registry; cancel is first-class and must keep working), UIP-D16 (project-local selection persistence and revalidation), UIP-D18 (hide survives selection; deletion prunes only deleted ids); docs/DESIGN-SYSTEM.md "Input consistency" ("Escape in a text input reverts the field … the revert must not be committed by the resulting blur"). HEAD is c8ebec3.

DELEGATION CONDITIONS (binding; the Builder lane runs a conformance check of the dispatcher against UIP-D14 after landing):
(1) ONE dispatcher, implemented in the SHARED package: NEW packages/@himmelcad/ui/src/escapeLadder.ts (+ escapeLadder.test.ts next to the package's existing tests, exported from the package's public index). Rung order and semantics are taken VERBATIM from UIP-D14 — reproduce the rung list in a doc comment: (1) a focused commit/revert field reverts to its committed value (DESIGN-SYSTEM "Input consistency"); free-text surfaces (agent chat input, console input) are exempt: Escape never discards their content, at most releases focus, and is consumed; (2) an active drag reverts; (3) an open menu/quick-surface closes; (4) an armed tool/placement cancels; (5) a modal island traps Escape for its own close; (6) the topmost detached function island closes — persistent workspace islands are never Escape rungs; (7) the active function tab closes, the panel falling back to Properties (UIP-D7) — Properties itself is never a rung; (8) the selection clears. Innermost first, exactly ONE rung handles a press (the first registered rung, in ladder order, whose handler reports it acted; a rung that has nothing to do yields to the next). API shape (generic, product-neutral): `registerEscapeRung(kind, handler, { order?: number })` returning an unregister function, `dispatchEscape(event)` / `installEscapeLadder(target: Window | Document)` installing exactly one keydown listener, `EscapeRungKind = 'fieldRevert' | 'drag' | 'menu' | 'tool' | 'modal' | 'detachedFunction' | 'functionTab' | 'selection'`; a `freeText` opt-out marker (e.g. `data-escape-free-text` attribute or a helper) for exempt inputs. NO PhotoLab-specific rungs, flags, names, or copy in the shared code; nothing in packages/@himmelcad/ui may import from apps/*.
(2) FunctionPanel close action follows UIP-D7 wording: function tabs (not Properties) get a visible close affordance ("x", aria-label "Close <tab label>"); clicking a tab only activates; closing the last function tab falls back to Properties; the ribbon toggle stays in sync (additive props with defaults so Builder keeps its current behaviour). ImportChat: while `aria-busy`, clicking the close/cancel button issues the cancellation, the button becomes disabled and reads "Cancelling…" until cancellation is acknowledged, then the surface closes — UIP-D10/UIP-D11 wording (cancel is first-class; never a silent immediate close while a job runs).
(3) Before you finish: `pnpm --filter @himmelcad/ui typecheck` and the ui package's test script (check its package.json; run whatever test/lint scripts exist there), `pnpm --filter @himmelcad/builder typecheck`, `pnpm --filter @himmelcad/photolab typecheck`, `pnpm --filter @himmelcad/photolab test:renderer` (keep green; currently 70), node scripts/check-photolab-english-ui.mjs, prettier --check on touched files. Builder behaviour must not change (additive props with defaults; Builder does not yet consume the dispatcher — its own island wires it later).
(4) Your final summary lists every shared-file change (file, what changed, why additive) so the reviewer can announce "landed" and the Builder lane can run its conformance check.

Files you may edit: NEW packages/@himmelcad/ui/src/escapeLadder.ts + test + the package index export; packages/@himmelcad/ui/src/EntityTree.tsx, FunctionPanel.tsx, ImportChat.tsx (+ their module CSS) — announced; PhotoLab-local: apps/photolab/renderer/src/FloatingTaskIsland.tsx, ImageWorkspace.tsx, App.tsx, GcpImageMarkerOverlay.tsx, a NEW apps/photolab/renderer/src/selectionLifecycle.ts (+ .test.ts). Nothing else. No cargo.

F07 — consumers of the one dispatcher: remove every component-global window keydown listener (FloatingTaskIsland.tsx ~50-85 installs one per island so one Escape can close every mounted non-modal island and ignores focused inputs; ImageWorkspace.tsx ~964-974 capture-phase handler with stopImmediatePropagation). App root installs the ladder once (installEscapeLadder(window) in an effect). FloatingTaskIsland (PhotoLab-local) registers rung 5 when modal, rung 6 when a detached function island (topmost = most recently focused/opened first). ImageWorkspace registers rung 2 for an active drag (revert to pre-drag state) and rung 4 for an armed marker placement (GcpImageMarkerOverlay). Text inputs: rung 1 — Escape reverts the field to its last committed value and suppresses the blur commit (EntityTree rename today commits on blur while Escape only clears edit state — fix so the revert wins; implement with the shared rung so Builder's tree gets the same fix). App.tsx registers rung 8 (clear selection). FunctionPanel registers rung 7 for the active function tab. Menus/quick-surfaces present in PhotoLab register rung 3 where they exist.

F08 — selection lifecycle (PhotoLab-local selectionLifecycle.ts): App.tsx acceptProject (~840-913) clears selection unless a caller opts out, and rename/visibility/move/remove refreshes (~3092-3105) drop it. Change: selection is preserved and revalidated after every non-replacement snapshot (hide/rename/move keep it; deletion prunes only removed ids; stale ids are rejected); project-stream replacement (open another project) switches to that project's stored selection (store the selection per project id in the existing preferences/session store if one exists in the renderer — otherwise in-memory keyed by project id, documented in a comment). Viewport cloud/splat click selection stays not applicable (UIP-D15); D17 write-to-all not applicable.

Tests: packages/@himmelcad/ui escapeLadder.test.ts (same runner as the package's existing tests): rung precedence (fieldRevert beats drag beats menu beats tool beats modal beats detachedFunction beats functionTab beats selection), one-rung-per-press, a rung that yields passes to the next, register/unregister, free-text exemption (consumed, content untouched), field revert suppresses the blur commit. apps/photolab selectionLifecycle.test.ts (node --experimental-strip-types --test): revalidate(selection, snapshot) → kept/pruned for hide/rename/delete/replace.

Final summary: the rung table as implemented (who registers which rung, PhotoLab and shared), the selection rules, every shared-file change (for the landed announcement), files changed, verification counts, anything not verified, contract gaps (record id + sentence).
