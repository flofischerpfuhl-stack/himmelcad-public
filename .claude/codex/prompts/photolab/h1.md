IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-H1 (adoption-audit findings F01 + F05; R1 gates 1/3). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "Adoption-audit disposition — 2026-09-02" rows F01/F05 and the WP-H1 bullet; docs/builder-program/PHOTOLAB-ADOPTION-AUDIT-2026-09-02.md sections F01 and F05 (evidence with file:line). HEAD is 1caefed. A previous attempt died on a full disk; the tree is clean for your files.

Files you may edit: apps/photolab/electron/main.ts, apps/photolab/electron/preload.ts, apps/photolab/electron/projectLifecycle.ts, apps/photolab/electron/projectLifecycle.test.ts, apps/photolab/renderer/src/App.tsx, App.module.css, global.d.ts, and NEW apps/photolab/renderer/src/CloseBlockedDialog.tsx + .module.css. Nothing else (ConfirmationDialog.tsx is in flux — do not touch). No cargo.

F01 — for a project whose session.sourcePath ends with .hcadx, Electron `project:save` (main.ts ~1286-1298) calls only `photolab.project.autosave`; the sidecar's real archive save `photolab.project.save` (atomic manifest + .hcadx repack + candidate publication) is never called. Fix: Save/Ctrl+S on an archive-backed project calls `photolab.project.save` with progress through the existing project-file-operation progress island, reports success ONLY after the sidecar acknowledges archive publication; status bar distinguishes "Working copy stored · <time>" from "Archive saved · <time>" (archive changes-since counter resets on success; failure → loud "Archive save failed — <reason>" with Retry). Untitled folder projects: first Save is Save As (unchanged). Save As keeps its "switch active source" semantics (unchanged). Periodic working-copy autosave unchanged.

F05 — main.ts ~2505-2535 races drainAndFlush() against 25 s and both timeout and error reach `finally`, which stops the sidecar and quits although the sidecar refused to mark the session clean. Fix: on timeout/error do NOT quit; keep window + sidecar alive; send `window:close-blocked` with the drain report (still-active jobs/side operations, what is already durable); show CloseBlockedDialog (modal, English sentence case, tokens, initial focus on Retry, Tab trap, Escape = Cancel close) with Retry / Cancel close / Force quit — Force quit copy states running work is interrupted, the project reopens with recovery from the last durable state, clean shutdown is NOT recorded; it stops the sidecar without marking clean. Only a completed drain proceeds to close.

Tests: pure helpers in projectLifecycle.ts with tests in projectLifecycle.test.ts (runner: node scripts/test-photolab-preferences.mjs): saveRouteFor(session) → 'archiveSave' | 'saveAs' | 'workingCopyOnly'; closeDecisionFor(drainReport) → 'close' | 'blocked'. Keep node scripts/test-photolab-dialog-policy.mjs green.

Verify: pnpm --filter @himmelcad/photolab typecheck; pnpm --filter @himmelcad/photolab test; node scripts/test-photolab-dialog-policy.mjs; node scripts/check-photolab-english-ui.mjs; prettier --check on touched files (fix with --write). Final summary: Save routing table, close-blocked flow, files changed, verification counts, anything not verified.
