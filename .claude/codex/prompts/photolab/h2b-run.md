IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-H2b — Adopt the shared jobs status chip (Size S; obligation recorded in WP-H2; UIP-D10 revision). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "### WP-H2 (Size M, gates 3/4)" (the landed note and the obligation), packages/@himmelcad/ui/src/JobsSurfaces.tsx (`JobSurfaceItem`, `JobsStatusChip` props: jobs, now, debounceMs, lingerMs, onClick), packages/@himmelcad/ui/gallery/shots/{dark,light}/jobs-surfaces.png (the accepted look), apps/photolab/renderer/src/jobsChip.ts (the local chip you are replacing) and its use in App.tsx (`statusItems`, id 'jobs'). HEAD is a540935 or later (S-05 committed as a540935; build against the committed JobsSurfaces.tsx).

Files you may edit: apps/photolab/renderer/src/App.tsx (only the status-bar jobs item and its imports), a NEW apps/photolab/renderer/src/jobSurfaceItems.ts (+ .test.ts) replacing jobsChip.ts (delete jobsChip.ts and jobsChip.test.ts), apps/photolab/renderer/src/App.module.css (remove the `.jobsChip` rules). Nothing else — no sidecar, no shared package edits, no Jobs-tab/island change in this package (the island moves later).

Design:
1. Pure mapping `jobSurfaceItems(jobs: readonly PhotolabJob[]): JobSurfaceItem[]` — state: queued → 'pending-registration'; running/paused/pauseRequested → 'running' (phase carries "Paused" / "Pausing…" where applicable); cancelRequested → 'cancelling'; completed/failed/cancelled → the same names. `label` = the existing job label grammar (reuse the label table from jobsChip.ts, keep sentence case); `phase` = `progress.stage.label`; `fraction` = the existing overall fraction when `totalUnits` is known, else `null` (the shared chip renders "in progress" + indeterminate bar); `registeredAtUnixMs` = createdAtUnixMs; `finishedAtUnixMs` = finishedAtUnixMs ?? null; `suppressChip` = false; `cancellation` = { cancellable: state is queued/running/paused, atNextSafeBoundary: true for side operations (`origin === 'sideOperation'`) }. Side operations are ordinary items with their labels (Save archive, Inspect images, …).
2. App.tsx: replace the local button with `<JobsStatusChip jobs={jobSurfaceItems(visibleJobs)} onClick={toggleJobsTab} />` keeping the exact H2 click behaviour (open the Jobs tab; when it is already the visible bottom tab, restore the previous bottom tab and collapse) and the failure acknowledgement (a failed job stops feeding the chip once the Jobs tab has been opened — keep `acknowledgedFailedJobIds`). Terminal console logging stays as is. Keep the 4 s linger via the chip's `lingerMs` default.
3. Delete jobsChip.ts / jobsChip.test.ts and the `.jobsChip` CSS; the shared chip owns its look. No other visual change.

Tests: jobSurfaceItems.test.ts (node --experimental-strip-types --test): every PhotolabJobState kind maps to the right surface state; fraction null when units unknown; side operation → atNextSafeBoundary; labels for the five side-operation kinds. Keep `pnpm --filter @himmelcad/photolab test:renderer` green (report the count).

Verify: renderer tests; `pnpm --filter @himmelcad/photolab typecheck`; `pnpm --filter @himmelcad/builder typecheck` (unchanged shared package, but confirm); node scripts/check-photolab-english-ui.mjs; prettier --check on touched files. Final summary: the state mapping table, files changed/deleted, verification counts, anything not verified, contract gaps.
