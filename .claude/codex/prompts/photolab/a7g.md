IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-A7g — make the per-worker cgroup scope actually engage, prove it inside a systemd user unit, then run the A7c smoke yourself (owner D12/S23; follows WP-A7f). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md sections "WP-A7e", "WP-A7f", and the code and logs named below.

Evidence: A7c smoke #4 (2026-09-10 01:41–02:50, unit `hc-a7c-smoke` with MemoryMax=18G, binary built from main with WP-A7f) still recorded `workerMemoryLimitMode: rlimitAs` (limit 29 GB) for both stages, so the matcher ran without the cgroup cap, grew to ≥ 19.2 GB at 4 threads and the UNIT's OOM killer stopped it; the A7f retry ladder (`attempts`) never got a `workerMemoryLimit` outcome, only `cancelled`. The probe (`systemd_user_scope()` in crates/himmelcad-sidecar/src/colmap_runtime.rs, now `MemoryMax=64M`, `MemorySwapMax=0`, `LimitCORE=0`, `--collect`, `--quiet`, `/bin/true`) therefore still fails when the sidecar runs inside a user unit — although `systemd-run --user --wait --pipe -q /bin/sh -c 'systemd-run --user --scope -p MemoryMax=64M -- /bin/true'` succeeds from the same machine (verified by the architect). Logs: `.build/logs/a7c-tiled-smoke-2.log` (e2e + sidecar stderr; look for the once-per-process probe log line A7f added), the job record under `.build/photolab-e2e/a7c-tiled-smoke/photolab-e2e.hcad/.photolab/jobs/records/` (read-only).

Tasks:
1. Diagnose for real, not by guessing: add a small test binary or `cargo test -- --ignored` case that runs the exact probe command and prints its status, stdout and stderr; execute it (a) from your shell and (b) inside `systemd-run --user --wait --pipe --collect -q -p MemoryMax=2G <that command>` and (c) the way the smoke does it — the sidecar started by `node scripts/photolab-e2e.mjs` inside a unit (you may start the sidecar alone with `--help`-like no-op if it has one, otherwise a 30-second e2e `--import-only` run is acceptable, it needs no COLMAP). Record the three outcomes verbatim in your summary. Likely candidates to check explicitly: the probe's `find_in_path` when PATH is the unit's minimal PATH; `DBUS_SESSION_BUS_ADDRESS`/`XDG_RUNTIME_DIR` not reaching the sidecar (node may sanitise env); the probe's own `--collect` on a scope (scopes do not support `--collect` on some systemd versions → check `systemd-run --version` and the exit status); `--quiet` position; and the probe being evaluated at a moment where the systemd user bus is not yet available. Fix the real cause in the sidecar (fallback discovery of the bus via `/run/user/<uid>/bus` when the env var is missing is acceptable; dropping `--collect` for scopes if unsupported is acceptable), keep the once-per-process log line and make it say WHY it fell back (stderr excerpt).
2. Regression test that runs inside a unit: `cargo test -p himmelcad-sidecar --lib -- worker_scope_inside_unit --ignored` that spawns itself via `systemd-run --user --wait --pipe` and asserts the cgroup mode engages (skip with a clear reason only when `systemd-run` is absent, never when it merely failed).
3. Then run the smoke yourself: `cargo build -p himmelcad-sidecar --bins` (CARGO_TARGET_DIR below), then exactly `.build/photolab-runtime/run-a7c-smoke.sh` under `systemd-run --user --collect --quiet -p MemoryMax=18G -p MemorySwapMax=0 -p LimitCORE=0 --unit=hc-a7c-smoke-5 --working-directory=/home/oem/Dokumente/003_Projekte/10_himmelcad` (the script rebuilds nothing else, streams to `.build/logs/a7c-tiled-smoke-2.log`, ~1–2 h; wait with `systemctl --user is-active` polling every 5 min; do not run anything else heavy meanwhile). Expected: job record mode `cgroupScope`, matching limit ≈ 14.55 GB, and either a completed alignment at some thread count (with `MatchingThreadsHalved` degradations and `attempts`) or the typed refusal — never a unit-level OOM kill. Report the job memory record (stages, attempts, peak RSS per attempt, observedBytesPerThread) and `journalctl --user -u hc-a7c-smoke-5` tail verbatim. If the unit is OOM-killed again, report that as the result — do not loop.
Files you may edit: crates/himmelcad-sidecar/src/colmap_runtime.rs, crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/main.rs (probe log plumbing only), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-A7g" status paragraph). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/a7g/` and delete it before finishing; keep ≥ 15 GB free (check `df -h /` before the build and before the smoke; stop and report if below). Final summary: the three probe outcomes verbatim, the root cause, the fix, test counts, the smoke result with the memory record, and anything you had to decide yourself.
