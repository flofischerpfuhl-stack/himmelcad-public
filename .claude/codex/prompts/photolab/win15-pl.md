IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-WIN-15-PL — durable file/directory sync must be Windows-safe everywhere in the PhotoLab sidecar paths (Size S; PhotoLab lane; the real cause of the 16 GB gate publication failure, WIN-10/WIN-14). Read only: `.build/photolab-evidence/win14-report.md` (lines 130–155), crates/himmelcad-sidecar/src/project_runtime.rs (`sync_package_artifacts` ~2736–2760, `sync_parent_directory` ~13540–13550 incl. its `#[cfg(windows)]` twin), and the WIN-01b evidence `docs/builder-program/evidence/WIN-01b-*.md`.

Defect: `sync_package_artifacts` does `File::open(path)?.sync_all()?` for every package artifact and `File::open(root.join("manifest.json"))?.sync_all()?` right before the ready publication. On Windows `FlushFileBuffers` requires a handle with write access, so a read-only open + `sync_all` fails with `ERROR_ACCESS_DENIED` (os error 5) — the untyped `projectPublish: Zugriff verweigert (os error 5)` seen after successful compute in WIN-10 and WIN-14; on unix it works, which is why no test caught it. The same pattern exists 14 times in the sidecar; PhotoLab-owned sites: project_runtime.rs:2754, :2756, :13543 (directory), raster_runtime.rs:2694 and :2798 (directories), colmap_runtime.rs:5601 and :6363 (directories), viewer_raster_surface_manifest.rs:637 (directory), camera_export.rs:319 (directory), main.rs:8249 (directory; check that the enclosing handler is a PhotoLab one before touching it — if it is a Builder handler, leave it and list it). Builder-owned sites to leave untouched and list in the plan paragraph: canonical_project_store.rs:574, :585, :2375 and brush_runtime.rs:1191.

Change (sidecar only):
1. New module `crates/himmelcad-sidecar/src/durable_fs.rs` (+ `mod` in lib.rs): `sync_file(path)` — opens with `OpenOptions::new().read(true).write(true)` on Windows (read-only on unix), calls `sync_all`, errors carry `operation = "sync file"` and the path; `sync_dir(path)` — unix: open the directory and `sync_all`; Windows: logged no-op per the WIN-01b rule (directory handles cannot be opened with `File::open` there); both return `anyhow::Result` with context. Replace every PhotoLab-owned site above with the helper (file sites → `sync_file`, directory sites → `sync_dir`); make `sync_parent_directory` delegate to `sync_dir` and delete the duplicated cfg twin if it becomes redundant.
2. Context on the publication steps: in the product-import-package publication and `sync_package_artifacts`, wrap each step with `.with_context(|| format!("<step> {}", path.display()))` so a future failure names the artifact or manifest and the step (`hash`, `sync file`, `publish`) — the gate report showed an error without any step name.
3. Tests (verbatim counts): `durable_fs` unit tests on unix (sync_file on a temp file and a read-only file — on unix read-only must still succeed — and sync_dir on a temp dir; the Windows branch is compiled by construction: keep it `cfg(windows)`-only with `std` APIs, no new crates); a test that `sync_package_artifacts` on a temp package with one artifact succeeds and that a mismatching artifact fails with a message naming the artifact path. All existing suites keep passing (`cargo test -p himmelcad-sidecar --lib`, `--bin himmelcad-sidecar`).
Files you may edit: crates/himmelcad-sidecar/src/durable_fs.rs (new), crates/himmelcad-sidecar/src/lib.rs (mod line), crates/himmelcad-sidecar/src/project_runtime.rs, crates/himmelcad-sidecar/src/raster_runtime.rs, crates/himmelcad-sidecar/src/colmap_runtime.rs, crates/himmelcad-sidecar/src/viewer_raster_surface_manifest.rs, crates/himmelcad-sidecar/src/camera_export.rs, crates/himmelcad-sidecar/src/main.rs ONLY the single sync site at ~8249 if PhotoLab-owned (the Builder lane has uncommitted hunks in main.rs: touch nothing else), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-WIN-15-PL" paragraph listing the replaced sites and the Builder-owned ones left). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/win15/` and delete it before finishing; unit tests only. Final summary: the site list (replaced / left), the helper semantics per platform, test counts, and anything you had to decide yourself.
