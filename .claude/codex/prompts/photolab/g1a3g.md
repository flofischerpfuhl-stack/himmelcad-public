IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-G1a-3g — the DEM/orthomosaic admission must estimate the raster-input scratch and refuse on insufficient disk before compute (Size S; PhotoLab lane; measured 2026-09-10). Read only: crates/himmelcad-sidecar/src/job_runtime.rs (`DiskEstimateTuning`, `DISK_ESTIMATE_TUNING`, the `InsufficientDisk` path), crates/himmelcad-sidecar/src/dense_raster_prep.rs (the CSV → FlatGeobuf → raster pipeline), and `.build/logs/g1a3f-dsm-probe.sampler`.

Measured cause of the "intermittent" DSM failure: a DEM job on the 45.8 M-point dense cloud (PLY 2.0 GB) writes `.photolab/raster-inputs/<job>/dense.csv` (3.3 GB) and converts it with `ogr2ogr -f FlatGeobuf` (4.0 GB and growing) while GDAL keeps ~2.9 GB of temp under /tmp (root filesystem) — free space went 14 GB → 0 and ogr2ogr died with `ERROR 1: Unexpected I/O failure: writing feature`. The admission-time disk estimate (`DISK_ESTIMATE_TUNING` formulas keyed on images) does not model this scratch at all, so the job starts and fails after minutes; DTM runs earlier succeeded only because 35–40 GB were free.

Change (sidecar only):
1. Disk estimate for `buildDem` and `buildOrthomosaic` (and any product that goes through dense_raster_prep): required_bytes = scratch (CSV ≈ 72 B × points, FlatGeobuf ≈ 88 B × points, GDAL temp ≈ 64 B × points — derive the constants from the measured 45.8 M-point run: 3.3 GB / 4.0 GB / 2.9 GB, round up) + the raster outputs (existing estimate) + 10 % headroom, using the actual point count of the dense cloud the job will read (available from the dense PLY header / dense record). The typed `InsufficientDisk` refusal (WIN-06-PL carries available/required bytes) then fires at admission with the numbers; message: "Not enough disk for the DEM: needs about N GB of scratch, M GB free".
2. The scratch location is part of the estimate: check the free space of the volume that holds the project's `.photolab/raster-inputs` AND of the GDAL temp dir (set `CPL_TMPDIR`/`GDAL_TMPDIR` explicitly to the job's raster-inputs dir so the temp lands next to the scratch on the same volume and is removed with it — verify GDAL honours it in the ogr2ogr invocation) — one volume, one check.
3. Record the estimate in the job record (`diskEstimate { scratch_bytes, output_bytes, available_bytes, volume }`, additive) and mark the scratch as removed on completion/failure (it already is; assert in a test).
4. Plan doc: a "WP-G1a-3g" paragraph with the measured numbers and a follow-up note "WP-A4b: write FlatGeobuf/LAS directly from the PLY instead of CSV → ogr2ogr (3× scratch, minutes of I/O)".
Tests (verbatim counts): estimate for 45.8 M points ≈ 10.3 GB scratch + outputs; admission with `available_bytes` below the estimate returns the typed refusal with both numbers and starts no worker; above it proceeds; the GDAL temp env is present in the ogr2ogr command (fake-tool rig). All existing tests keep passing (`cargo test -p himmelcad-sidecar --lib`, `--bin himmelcad-sidecar`).
Files you may edit: crates/himmelcad-sidecar/src/job_runtime.rs, crates/himmelcad-sidecar/src/dense_raster_prep.rs, crates/himmelcad-sidecar/src/project_runtime.rs (DEM/ortho admission only), crates/himmelcad-sidecar/src/main.rs ONLY the DEM/ortho admission site (the Builder lane has uncommitted hunks in main.rs: touch nothing else), crates/himmelcad-core/src/photolab_jobs.rs (additive record field; then `pnpm bindings:generate`, include regenerated files), docs/implementation-plans/2026-09-photolab-release-polish.md. Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/g1a3g/` and delete it before finishing; unit tests only — no dataset runs (the architect reruns the DSM republish afterwards). Final summary: the constants and their derivation, the refusal wiring, the GDAL temp handling, test counts, and anything you had to decide yourself.
