IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-E1 — camera-calibration inspector (R1 gate 2: accuracy evidence must be inspectable; "finish, no extension"). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section WP-E1 and the "R1-gate triage" row for E1; docs/photolab-intrinsics-policy.md; the solver crates/himmelcad-core/src/photolab_gcp_optimization.rs (GcpOptimizationResult ~619; symmetric_condition_number ~1970; intrinsic_condition_number ~1922; propagate_point_covariance ~3743). HEAD is 1caefed. A previous attempt died on a full disk; the tree is clean for your files.

Files you may edit: crates/himmelcad-core/src/photolab_gcp_optimization.rs, photolab_gcp.rs; crates/himmelcad-sidecar/src/gcp_optimization_runtime.rs, gcp_runtime.rs, project_runtime.rs; crates/himmelcad-sidecar/src/main.rs and packages/@himmelcad/data/src/index.ts (announced to the Builder lane: new read-only query photolab.gcp.calibrationReport + wire types); apps/photolab/renderer/src/CaptureGroupsPanel.tsx (+.module.css), processingReport.ts. Nothing else; if the electron allowlist needs the new query, name the exact line.

Implement: (1) at convergence invert the reduced normal matrix for the intrinsic parameters per calibration group (reuse the reduced system the solver already forms; no dependency), emitting per group: refined parameter names in model order, values, 1-σ from the a-posteriori variance factor σ0² = vᵀPv/(n−u) (formula in a comment), the correlation matrix (symmetric, unit diagonal), seed values for before/after deltas; singular or condition-gated → `uncertainty: null` with a reason string. Add to GcpOptimizationResult and the published snapshot/record (serde default). (2) Radial residual profile per group before (seed intrinsics, input poses) and after optimization: 8 bins center→corner, mean |reprojection residual| px and count per bin. (3) Query photolab.gcp.calibrationReport { optimizationEntityId? } for the latest or given converged optimization. (4) CaptureGroupsPanel: a "Calibration" section per group — parameter table (seed → solved, Δ, σ), correlation heatmap (CSS grid cells shaded by |r| with tokens, value in title), inline SVG radial profile before/after, provenance line (optimization id · hash8), honest "sigmas need a converged GCP optimization" state; processingReport.ts replaces its null uncertainty placeholders with the same data (deterministic). (5) Tests: Rust — synthetic block where sigmas shrink with more GCPs, f–k1 correlation strongly negative, singular → null with reason, profile bin counts sum to observations, serde back-compat; renderer — pure helpers for heatmap shading and bins (node --experimental-strip-types --test).

Verify (CARGO_TARGET_DIR=target/photolab): cargo test -p himmelcad-core -p himmelcad-sidecar; cargo fmt --all -- --check; pnpm --filter @himmelcad/photolab typecheck; pnpm --filter @himmelcad/photolab test; node scripts/check-photolab-english-ui.mjs; prettier --check on touched TS (fix with --write). Final summary: covariance derivation (matrix, σ0²), what the tests prove, the exact allowlist line, files changed, verification counts, anything not verified.
