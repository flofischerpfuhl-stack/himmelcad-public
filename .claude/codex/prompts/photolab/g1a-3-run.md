IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-G1a-3 — DEM facts (IF-D30) so DEM publications become `complete` with a package (R1 gate 8). Read only: docs/adr/0030-photolab-product-import-package-and-provenance.md Decision 10 and the IF-D30 blockquote ("`product_kind: "dem"` requires `PhotoLabDemFactsV1` with `elevationZ`, exact `RasterInterpolation`, exact `RasterConnectivity`, explicit source NoData semantics, the mandatory validity resource, and a connectivity resource for mask connectivity. The canonical Grid and prepared Raster root bind the same facts and resources. Until all are frozen, the publication has `package: null` and is not Available."), the `PhotoLabDemFactsV1` wire shape already in packages/@himmelcad/data/src/index.ts and crates/himmelcad-core/src/product_import_package.rs (do not change the shape), and the code named below. WP-G1a-2 landed in 3c6f4d0 (git show it for the publication path).

Problem today: `publish_raster_product` in crates/himmelcad-sidecar/src/project_runtime.rs (search `vec!["dem_facts".to_owned()]`) publishes DEMs through `write_lineage_only_product_publication` with reason `NoPackage` and `missing_field_ids = ["dem_facts"]`; `lineage.dem_facts` is always `None` (search `dem_facts: None,` in project_runtime.rs). The prepared elevation hierarchy written by `raster_runtime.rs` (`publish_prepared_elevation_hierarchy` at raster_runtime.rs:777, implemented in crates/himmelcad-sidecar/src/viewer_raster_manifest.rs) already freezes `topology { kind: "continuous", maximumHeightJump, diagonal }` and `noData` in the raster root manifest but writes `"validityReference": null` and no validity resource. Height tiles are 512×512 Float32 raw files `view/height/L{level:02}/{column}/{row}.f32` (little- or big-endian per `RasterViewTileFormat`); NoData is `RasterNoDataValue::Numeric(finite)` or `Nan` (raster_runtime.rs). `RasterGrid { bounds, width_pixels, height_pixels, gsd, no_data }` describes the base level.

Files you may edit: crates/himmelcad-sidecar/src/raster_runtime.rs, crates/himmelcad-sidecar/src/project_runtime.rs, crates/himmelcad-core/src/product_import_package.rs, and crates/himmelcad-sidecar/src/viewer_raster_manifest.rs ONLY for the validity resource plumbing (`validityReference`, an option/return field carrying the resource identity) — this file is shared with the Builder lane, keep the diff minimal and additive, no reformatting of untouched code. Do not touch main.rs, packages/**, apps/**, scripts/**, golden-bin, `.build/**`, or the compute lease (`/tmp/himmelcad-photolab-compute.lock`); a 40-image diagnostic and a visual audit are running on this machine — do not run e2e, vite builds, the visual harness, or anything that needs GDAL/COLMAP; unit tests only.

Design (implement as specified; report contract gaps rather than diverging):
1. Validity resource: when the prepared elevation hierarchy is published, derive one bitset over the BASE level grid (level 0, `width_pixels × height_pixels`, row-major from the top-left cell, bit i = cell i, LSB-first within each byte = `bitsetLsb0`; trailing bits of the last byte zero) from the base-level height tiles: a cell is valid iff its Float32 value is finite and, for `Numeric(v)` NoData, `!= v` (bit-exact compare on the decoded f32; document that -9999.0 style sentinels compare exactly). Write it to `<product_root>/view/validity.bin` (path relative to the dataset root; byte length `ceil(w*h/8)`), hash it (SHA-256 = resource id), and set the raster root manifest's `"validityReference": { "uri": <relative uri like elevationReference>, "byteOffset": null, "byteLength": <n>, "contentHash": <sha256> }`. Cancellable, streaming (never load the whole grid at once — walk tiles; tiles outside the grid extent are padding and must not produce cells). Return `{ path, sha256, byte_length }` from `publish_prepared_elevation_hierarchy` via `PreparedElevationHierarchyArtifact`, and persist it in `RasterBuildSummary` or the raster artifact record (whichever the publication reads — keep the record schema additive with `#[serde(default)]`).
2. DEM facts: in `publish_raster_product`, for `PublishedRasterKind::Dem` build `PhotoLabDemFactsV1 { semantics: "elevationZ", interpolation: "bilinear" (the value the prepared raster root binds; if the root manifest carries no sampling/interpolation field, add `"interpolation": "bilinear"` next to `topology` so both bind the same fact — additive), connectivity: Continuous { diagonal: the exact root value, maximum_height_jump: the root value as a Decimal64 string when present, omitted otherwise }, source_no_data: Numeric { value: Decimal64 string of the sentinel } | Nan, validity: { resource: { resource_id: <sha256>, sha256, byte_length, media_type: "application/octet-stream" }, encoding: "bitsetLsb0" } }`. Mask connectivity is NOT produced (continuous only); do not invent a connectivity resource.
3. Publication: switch DEM from `write_lineage_only_product_publication` to `write_product_import_package` (the tiled-mesh path is the model) with `normalized_format_id = "himmelcad-prepared-hierarchy@1"`, the canonical Grid contract, `lineage.dem_facts = Some(...)`, empty `missing_field_ids`, reason `Available`, provenance `complete`; the validity resource is listed in the package `resources` (same id/sha256/byte_length/media_type) so the canonical Grid and the prepared raster root bind the same resource. Orthomosaic stays as it is (UnsupportedFormat, no dem_facts). DTM and DSM behave identically (both `product_kind: "dem"`; the DTM's `ground_classification_sha256` already travels in lineage/algorithms — keep it).
4. Core validation (product_import_package.rs): for `product_kind == "dem"`, `dem_facts` is required and its `validity.resource.resource_id` (and a mask-connectivity resource, if that kind is ever present) must appear in the manifest `resources` with identical sha256/byte_length; `missing_field_ids` for pre-G1a-3 DEM records remains `["dem_facts"]`. Reading an older DEM ready record without dem_facts must not panic (partial).

Tests (verbatim counts in the summary): (a) viewer_raster_manifest/raster_runtime unit test: a synthetic 3×2-cell base level with one NaN and one sentinel cell produces exactly the expected `validity.bin` bytes (write the expected bytes out in the assertion), the root manifest's `validityReference` matches its hash/length, and a big-endian tile variant decodes the same; (b) project_runtime test: a DEM publication fixture (extend the existing raster publication test) yields `provenance_status == Complete`, `reason_code == Available`, a package whose `resources` contain the validity resource, `dem_facts` with `source_no_data` = `{kind: numeric, value: "-9999"}` for a -9999.0 sentinel and `{kind: nan}` for NaN, and `package_sha256` recomputes; (c) core tests: dem_facts required for dem, resource binding mismatch rejected, wire shape golden unchanged (the existing `dem_facts_use_the_exact_mixed_case_wire_shape` must keep passing).

Verify (CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab): cargo test -p himmelcad-core; cargo test -p himmelcad-sidecar (lib + bins; the two known Builder-lane failures `automation_runtime::…destructive_approval…` and `canonical_project_store::…group_commit_machine_gate…` are not yours — report them verbatim, do not touch those files); cargo fmt --all -- --check (only your files may change). Final summary: the exact validity.bin layout, which publication sites are now complete, the dem_facts values as implemented, files changed with line references, verification counts, what was NOT verified (real GDAL DEM runs — the PhotoLab session runs the DSM/DTM smoke afterwards), and any contract gap by record id.
