IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

PACKAGE: WP-B5 — Journal/manifest ordering + orphan-dataset GC (Size M; R1 gates 3/5). Read only: docs/implementation-plans/2026-09-photolab-release-polish.md section "### WP-B5 — Journal/manifest ordering + orphan-dataset GC"; docs/PROJECT-FORMAT.md sections "Transactional publication" and "Product data"; docs/DECISION-DOCTRINE.md P5. Files you may edit: crates/himmelcad-sidecar/src/project_runtime.rs, crates/himmelcad-sidecar/src/canonical_project_store.rs (only `sync_dir` ~1800 and any helper it needs). Nothing else; no RPC or renderer change. Cargo with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; `cargo test -p himmelcad-sidecar` full crate at the end (project_runtime currently has ~84 tests in the bin target and the lib suite is 260; report exact counts).

Anchors: every mutating command does `write_journal_entry(&session.working_path, &journal)?` (twelve sites: project_runtime.rs ~3182, 3365, 4970, 5095, 6711, 6745, 7087, 7197, 7380, 7479, 7576, 7748) and then writes the manifest (see the atomic manifest writer covered by test `atomic_manifest_write_never_leaves_temporary_file`). Publication-existence heuristics use directory existence: ~8611 (`datasets/raster/<job>`), ~8640 (`datasets/splats/<job>`), ~8658 and ~8746 (`datasets/mvs/<job>`), plus the dataset root at ~6201. Journal entries carry `sequence` = manifest `command_sequence`.

Design (implement as specified; report gaps instead of diverging):
1. Manifest commits, journal follows. Introduce one helper `commit_manifest_then_journal(session, candidate_manifest, journal_entry)` used by all twelve sites: write the manifest atomically (existing writer), then `write_journal_entry`. If the journal write fails after the manifest succeeded, the command still succeeded (the manifest is truth); log the failure with the sequence and mark the session so the next open repairs it. Keep every existing invariant test green.
2. Open-time reconciliation. On open (and on recovery open), compare the last journal entry's `sequence` with `manifest.command_sequence`: if the journal is behind, re-emit the missing entry from manifest state with `recovered: true` (additive optional boolean on `PhotolabJournalEntry`, serde default false — the journal is evidence, so re-emission must be marked); if the journal is ahead (journal written, manifest write crashed — only possible for pre-B5 stores), keep the manifest as truth and mark that journal entry `orphaned: true` (additive optional boolean) instead of deleting evidence. Both cases produce one diagnostics line reachable through the existing project diagnostics/log path (no new RPC).
3. Orphan-dataset quarantine. On open, any `datasets/<family>/<id>` directory not referenced by the manifest (products, mvs runs, raster runs, splats — use the manifest's own dataset references, not job history) is moved to `<working>/tmp/orphaned/<family>/<id>-<unix ms>` with a log line; never deleted. Replace the four existence heuristics with manifest lookups (a dataset is published iff the manifest references it); keep a helper `dataset_dir_exists` only where a physical check is genuinely required (e.g. before a move) and say where.
4. Windows `sync_dir`: on non-unix, open the directory handle with `FILE_FLAG_BACKUP_SEMANTICS` via `std::os::windows::fs::OpenOptionsExt::custom_flags` and call `sync_all`; if opening fails, return Ok(()) with a debug log (best effort, documented in a comment). No new crates.

Tests (fault injection, deterministic): (a) journal written, crash before manifest (simulate by calling the pre-B5 order through a test seam or by hand-writing the extra journal entry) → open marks the entry orphaned and the manifest wins; (b) manifest written, crash before journal → open re-emits the entry with `recovered: true` and identical sequence/command payload derived from manifest state; (c) dataset dir renamed into place, crash before manifest → open quarantines it under tmp/orphaned and the product is not listed as published; (d) a referenced dataset is never quarantined; (e) all existing project_runtime tests green. Final summary: the twelve converted sites (line list), reconciliation rules as implemented, files changed, verification counts (before/after test totals), anything not verified, contract gaps (record id + sentence).
