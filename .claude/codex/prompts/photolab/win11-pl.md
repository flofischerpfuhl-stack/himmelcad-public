IMPLEMENTER BRIEF — PhotoLab lane (replaces the long reading list; read ONLY what is named in your package). Repo: /home/oem/Dokumente/003_Projekte/10_himmelcad. English sentence-case UI copy (run node scripts/check-photolab-english-ui.mjs). Build and test ONLY with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem and a 47 GB copy filled the disk on 2026-09-08); work in place in the repo; scratch files only under `.build/codex-scratch/<package>/` and delete them before finishing. do not reformat unrelated code; no new dependencies.

Rules that bind every package: X1 correctness > performance > UX > aesthetics; P5 nothing on the interaction path persists or blocks (ready record last, atomic visibility, cancellable jobs); P6 universal affordances keep an honest effect; P11 automation/console rows come from the one generated command table (never widen the raw RPC allowlist); design-system copy never claims unverified state; numeric thresholds are X6 tunables — name the constant and its rationale in a comment; doctrine rule 2: if a spec is wrong or ambiguous in a specific place, implement what is unambiguous and list the gap (record id + sentence) in your final summary — never diverge silently.

Ownership (docs/builder-program/COORDINATION.md): you may edit only the files named in your package. PhotoLab lane owns apps/photolab/**, docs/photolab-*, scripts/photolab-*, and the sidecar/core modules project_runtime, mvs/brush/raster/job runtimes, product_export, colmap/alignment_merge/gcp*/dedode/capture/image_commit/image_mask runtimes, pointcloud_export, camera_export, process_group, the portable MVS bin, core photolab_*, product_import_package, canonical_json. Shared substrate (packages/@himmelcad/*, crates/himmelcad-render, core entity_model/canonical_document/property_schema/app_protocol, himmelcad-io, sidecar main.rs, crs_runtime/crs_service, schemas, sdk, scripts/verification, root configs, apps/photolab/electron/main.ts allowlist) is edited only when your package names the file as announced; otherwise name the exact line the reviewer must add.

Definition of done: the named user flow works end to end (discovery, entry, cancel, close, recovery); tests named in the package pass with verbatim counts; TEST-TIERS changed-tier gates pass (typecheck, the package's node tests, cargo test for touched crates, prettier --check on touched files); your final summary states files changed, what was verified with counts, what was NOT verified, and any contract gaps. Codex prints "tokens used" at the end — leave it in place; the reviewer logs it.

Your package's owning records, gates, and files are listed below this brief.

Starting point: a previous run of this package died mid-way; its partial edits are saved as .build/codex-partials/a7c/tracked.patch (Cargo.toml/Cargo.lock/colmap_runtime.rs/job_runtime.rs/lib.rs) plus .build/codex-partials/a7c/colmap_feature_db.rs. Apply the patches in this order (`git apply .build/codex-partials/a7c/tracked.patch`, then `main.rs.a7c.patch`, then `project_runtime.rs.a7c.patch`, then `dedode_colmap_bridge.rs.a7c.patch` from the same directory) and copy `colmap_feature_db.rs` in first, review what exists, and continue rather than restart. Keep the tree compiling (`cargo check -p himmelcad-sidecar --tests`) at every step — the shared tree is built by another lane while you work.

PACKAGE: WP-WIN-11-PL — Windows-safe atomic publication with typed step context (Size S/M; PhotoLab lane; found by the 16 GB gate run WIN-10 on 2026-09-10). Read only: `.build/photolab-evidence/win10-report.md` (sections "Result" and the job-record excerpt around line 100–125), crates/himmelcad-sidecar/src/project_runtime.rs (`atomic_write_bytes`, `sync_parent_directory`, `commit_manifest_then_journal`, every `fs::rename(` in the publication paths: dataset scratch → dataset path, product-import-package publication (ready-last), object writes), crates/himmelcad-sidecar/src/canonical_project_store.rs (its `#[cfg(windows)]` code), and the WIN-01b evidence `docs/builder-program/evidence/WIN-01b-*.md` (Windows fsync/durability rules already adopted).

Defect: on Windows the alignment completed (24/24) and the sparse Potree 2.0 output was fully materialised, then the job failed with `projectPublish: Zugriff verweigert (os error 5)` — after the dataset rename and object writes, before `commit_manifest_then_journal`. The error names neither the step nor the path. On Windows `ERROR_ACCESS_DENIED` (5) / `ERROR_SHARING_VIOLATION` (32) on rename/replace typically means a freshly written file or directory is momentarily held (Defender real-time scan, the search indexer, or our own still-open handle); the publication must tolerate that without giving up atomicity.

Change (sidecar only):
1. One publication primitive module (`crates/himmelcad-sidecar/src/publish_fs.rs`, new; `mod` in lib.rs): `replace_file_atomically(temp, dest)`, `publish_directory(scratch, dest)` and `write_object_if_absent(path, bytes)` (content-addressed objects: if `dest` exists with the same length+hash, do NOT rename over it — skip; that removes one whole class of replace-over-open-file failures). On Windows (`cfg(windows)`) every rename/replace retries on os errors 5 and 32 with bounded backoff (e.g., 12 attempts, 50 ms → 1 s, total ≤ 6 s) and then fails typed; on unix no retry. Directory publishes close every handle the sidecar holds under `scratch` before the rename (audit: files kept open in `PreparedPotreeCloud`/dataset writers). Every error from these primitives carries `operation`, `source`, `destination` and the os error — surfaced as `projectPublish: <operation> <dest>: <os error>` so the next Windows run names the step.
2. Route all publication renames in project_runtime.rs through the module (dataset scratch → dataset path at the sites near lines 6069, 6204, 7947, 8284, 8434, 8971, 10089, 13200, 13399; the product-import-package ready-last publish; `atomic_write_bytes` for manifest/journal/records). Keep ready-last ordering and the WIN-01b durability flushes exactly as they are.
3. `sync_parent_directory` on Windows: verify it cannot itself raise access denied (directory handles need `FILE_FLAG_BACKUP_SEMANTICS`); if it can, make it a logged no-op on Windows per WIN-01b's rule, never a publication failure.
Tests (verbatim counts): retry logic with an injected error sequence (5, 32, then Ok) succeeds on the Windows path and fails typed after the budget; unix path does not retry; `write_object_if_absent` skips an identical existing object and replaces a differing one; the error message format names operation + destination; existing suites green (`cargo test -p himmelcad-sidecar --lib`, `--bin himmelcad-sidecar`); `cargo check --target x86_64-pc-windows-gnu` is NOT available here — keep the Windows code compiling by construction (no new crates; `std::os::windows` only behind cfg) and state that the Windows build is verified by the follow-up remote run.
Files you may edit: crates/himmelcad-sidecar/src/publish_fs.rs (new), crates/himmelcad-sidecar/src/lib.rs (mod line), crates/himmelcad-sidecar/src/project_runtime.rs, crates/himmelcad-sidecar/src/canonical_project_store.rs (only if it owns one of the publish renames), crates/himmelcad-sidecar/src/main.rs ONLY the `projectPublish` error-mapping sites (the Builder lane has uncommitted hunks there: touch nothing else), docs/implementation-plans/2026-09-photolab-release-polish.md ("WP-WIN-11-PL" paragraph). Verify with CARGO_TARGET_DIR=/home/oem/Dokumente/003_Projekte/10_himmelcad/target/photolab only; never use target/ or target/builder; do not commit; NEVER copy the repository, datasets or target directories anywhere (no `cp -r`, no `git worktree add`, no clones into /tmp — /tmp is on the root filesystem); work in place; scratch only under `.build/codex-scratch/win11/` and delete it before finishing; unit tests only. Final summary: the list of routed rename sites, the retry policy, the open-handle audit result, test counts, and anything you had to decide yourself.
