Domain: **Plan editor** (File-launched dedicated window per OWNER-DECISIONS D2; D4 is your working direction: paper space stays plan-composer content, .hcplan moves into the canonical project store as project-owned data). Output file: docs/builder-program/specs/plan-editor/plan-editor.md (~450-700 lines). Also read docs/PLAN-EDITOR-EXPORT.md.

Ground thoroughly in the existing implementation — it is substantial: packages/@himmelcad/plan/src/ (paper.ts, document.ts v2, templates.ts with field bindings, library.ts localStorage, export.ts deterministic SVG/PDF, viewport.ts with MockPlanModelViewAdapter), packages/excalidraw-plan/ (vendored fork; HCAD_FORK.md is the design-intent doc), apps/builder/renderer/src/PlanIsland.tsx (~1247 lines). Evidence: dossiers/rib-civil.md (plan production 2.9 / W7 — Planerstellung is the reference workflow).

Scope: (1) REAL model viewports replacing the mock adapter — a viewport on a sheet references a canonical model view (camera + display state; reconcile with view-domain VD-D3 bookmarks and VD-D13 ViewState v2 — cite, don't re-disposition), renders live or cached vector/raster content from the actual project at a scale (1:250 etc.), layer filtering per viewport (the stubbed Layers tab), update semantics when the model changes (stale badge / refresh), export fidelity through the existing deterministic pipeline; (2) .hcplan into the canonical project store per D4 (journaled vs content-addressed — decide with derivation); library scopes user vs project — the localStorage library follows bim-specs BS-D1's migration pattern (cite); (3) consolidation: sheet management, template/title-block field bindings against real project metadata, PDF/SVG export gaps (ellipses/diamonds, images bounds-only per PLAN-EDITOR-EXPORT.md), print. Surface: FloatingTaskIsland today — decide per B3 (likely dedicated resizable window; reconcile with ui-platform island/Escape rules UIP-D14). Automation: agents create sheets, place viewports, export (X3).

Full workflow narratives required: creating a sheet with a live model viewport at scale; a layer-filtered viewport; deterministic export.
