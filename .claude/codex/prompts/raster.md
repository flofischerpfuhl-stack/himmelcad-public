Domain: **Raster ribbon tab**. Output file: docs/builder-program/specs/raster/raster.md (~300-500 lines). Smaller domain — depth over breadth.

Evidence: dossiers/realworks.md (ortho-image 2.7/W6), dossiers/rib-civil.md (plan backgrounds / HV-Planverwaltung 2.3, W2). Draping, color ramps, and hillshade have NO reference documentation in any dossier (checked dossier-wide) — specify them as stated additions grounded in ADR 0020 and existing code, not as reference claims. Existing implementation: GeoTIFF/COG import (crates/himmelcad-io/src/geotiff_provider.rs, geotiff_preparation.rs — RasterImage or ElevationSurface per docs/adr/0020, independent color/elevation grids), elevation raster rendering (crates/himmelcad-render/src/providers/elevation_raster.rs), hcad.raster-image@1 entity, RASTER_STYLE hardcoded in apps/builder/renderer/src/BuilderKernelViewport.tsx, GeoTIFF export passthrough-only with loss codes.

Scope: raster display properties (transparency, draping onto terrain, color ramps for elevation rasters — two-layer display model per view-domain VD-D8, cite it); ortho-image generation hand-off (pointcloud spec's ortho_image row names Raster as the artifact owner — cite their record, don't re-disposition); georeferencing/registration of non-georeferenced rasters (a scanned paper plan placed by control points — rib-civil background-plan workflow; never-invent-coordinates applies); raster as drafting backdrop (snapping to raster? decide with reason, reconcile with draw DR-D12 snap-source model); clipping/cropping a raster; elevation-raster-to-DGM conversion (boundary with mesh-terrain — mark it); export honesty (existing loss codes, file-project FP-D5 disclosure standard).

Full workflow narratives required: placing/georeferencing a plan scan by control points; draping an ortho onto terrain with transparency; ortho generation hand-off from a cloud.
