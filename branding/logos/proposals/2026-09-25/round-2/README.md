# Logo proposals 2026-09-25, round 2 (not approved)

Proposals only; the masters in `../../source/` are unchanged. Round 1 is kept in `round-1/`.

Every mark has a `-on-light.svg` twin with a dark rim (`#003F7A`) that follows the
silhouette, because the lightest edge facets vanish on white.

| File                               | Proposal                                                                            |
| ---------------------------------- | ----------------------------------------------------------------------------------- |
| `himmelcad.svg`                    | Current Builder cloud (polygons unchanged), proposed as the general Himmel:CAD icon |
| `himmelcad-builder.svg`            | Builder: low-poly hard hat, three-quarter view with front peak and centre rib       |
| `himmelcad-photolab-a-prism.svg`   | PhotoLab A: prism, the light only as a band inside the glass                        |
| `himmelcad-photolab-b-crystal.svg` | PhotoLab B: the current crystal idea as clean geometry                              |
| `himmelcad-cap.svg`                | Cap (on hold): prism-faceted camera aperture                                        |
| `himmelcad-weltview.svg`           | WeltView: low-poly globe, 30° graticule                                             |

`overview-round-2.png`: dark background, light background (on-light variant), app icon at 96/48/32/16 px.
`generator/generate.py <out-dir>` rebuilds all files, including the berg:work set (needs numpy).
