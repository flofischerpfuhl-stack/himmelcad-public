# Logo proposals 2026-09-25 (not approved)

Proposals only; the masters in `../../source/` are unchanged.

| File                                             | Proposal                                                                                                    |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| `himmelcad.svg`                                  | Current Builder cloud (polygons unchanged, only the title changed), proposed as the general Himmel:CAD icon |
| `himmelcad-builder-helmet.svg`                   | New Builder mark: low-poly hard hat in the Azure Tech palette                                               |
| `himmelcad-photolab-a-refraction(-spectrum).svg` | PhotoLab A: prism with an incoming ray and an outgoing fan (azure or spectrum)                              |
| `himmelcad-photolab-b-crystal.svg`               | PhotoLab B: the current crystal idea redrawn as clean geometry                                              |
| `himmelcad-photolab-c-aperture.svg`              | PhotoLab C: prism-faceted camera aperture                                                                   |

`overview-round-1.png` shows each mark on dark and light, and as an app icon at 96/48/32/16 px.
`generate.py` creates all files, including the berg:work set: `python3 generate.py <out-dir>`.
